# PREREG — run9: parent run8@45k, the ONE training swap = the DATA REGIME, the gate a REGRESSION GUARD (R364, filled 2026-09-21 from the R362 skeleton)

Written 2026-09-19 as a SKELETON at the R362 landing (the eval rows fixed, every row the 30k reading
owned left BLANK); FILLED 2026-09-21 at the R364 landing after the 45k cell read and the PARENT RULE
of §1c named its output. The ruling is `docs/governance/RULINGS.md` R364; its grounds are the 45k
receipt, the 45k ring audit, the r11–r17 gate table, `STRENGTH_RESEARCH_2026-09-18.md` §Q2, F-44 and
`PARAM_DISTANCE_2026-09-21.md`. `configs/run9.yaml` is MINTED (§1); the stamp is the box's, written
once, after the mint commit is on `dev`. Nothing here runs on the box from this file.

**R365(a), 2026-09-21 — run9 is NOT STARTED and this prereg is NOT ARMED.** The mint (`61bd2fd1`) and
the budget-carry fix (`1da8a324`, ratified by R365(d)) stand as RUN10'S BASE; §3's reading is not taken
(the E4 resolution statement below says why); §3a's `one_hot_share_full` band is STRUCK until PROBE-1's
decomposer re-derives it; §5's box order was executed as far as run8's STOP and PERF-3 step 3 and no
further. Every other row below is left as minted, for run10's mint to replay; nothing here is re-litigated.

## 1. The config — `configs/run9.yaml`, MINTED 2026-09-21

Minted by `tools/mint_config.py --template dev`, replaying `configs/run8.yaml`'s header (its 31 `--set`
and 6 `--mint-row` rows) with the rows below changed — ONE command, the header replays, and
`tools/config_diff.py configs/run8.yaml configs/run9.yaml --expect <each key below>` says **MATCH** on
exactly these eleven leaves and nothing else (`--from-header configs/run9.yaml` MATCH too):

`run_id` · `identity.warm_start.checkpoint` · `identity.warm_start.net_hash` · `train.replay_capacity` ·
`train.training_steps_per_game` · `train.max_train_burst` · `train.eval_interval` ·
`eval.gate.sequential.mu0` · `eval.gate.sequential.mu1` · `eval.gate.sequential.at_max_pairs` ·
`selfplay.mcts.dirichlet_enabled`.

The seed is byte-equal (`20260914`, run7's mint value; R364 §0(3) "byte-equal elsewhere"), so is every
other row: σ off (`selfplay.q_rescale: false`), `train.augment: true`, Gumbel 320/16 at p 0.25, the
deploy head PUCT-256, `n_workers` 32, the aborts as run8 armed them.

### 1a. The eval rows — R362(c) as moved by R364(c)

| key | run8 | run9 | why |
|---|---|---|---|
| `train.eval_interval` | `3000` | **`36000`** | R362(c)'s cadence "15 000" was in STEPS at one step per game; R364(b) reads at EQUAL GAMES, so the cadence is 15 000 GAMES in steps: 2.4 × 15 000 = 36 000, a multiple of `train.checkpoint_interval` 3 000 (the round's candidate and the strix cell's checkpoint stay the same weights). The rounding clause of R364 §0(3) is a no-op at 2.4 — §1b says why 2.4 |
| `eval.gate.sequential.mu0` / `mu1` | `0.52` / `0.62` | **`0.42` / `0.52`** | R364(c): the gate is a REGRESSION GUARD. run8's H1 0.62 exceeded the run's per-15k gain and froze the anchor at 15k (r6–r17 all rejected; the trainer net read 0.31–0.54 against it while strix rose). The zero-drift point moves from 0.57 to 0.47: a candidate at its anchor's strength (0.50) drifts to ACCEPT, one clearly worse drifts to REJECT |
| `eval.gate.sequential.at_max_pairs` | `sign` (v34, what run8 ran) | **`promote`** | R364(c) "promote on accept or cap": an undecided candidate at 104 pairs is promoted; a GSPRT reject is the ONE way the anchor stays. NEW LEAF (contract v34): two rules ran on the record, so the file must say which (R1) |
| `eval.gate.sequential.{alpha, beta, check_every_pairs, min_pairs, max_pairs}` | `0.05, 0.1, 8, 16, 104` | same | the cap stays 104 pairs |
| `eval.gate.deploy_sims`, `eval.concurrency`, `eval.max_plies`, `eval.round_timeout_sec`, `eval.strength_floor`, `eval.random_floor_games` | `256`, `8`, `256`, `14400`, `{4, 0.25, 0.0}`, `20` | same | none moved; gate sims 64 stays UNMEASURED (CARD-EVAL-REDESIGN) |

**The P6b pin** (`tests/train/test_gate_interval_decoupling.py`) pins `monitor.gate_interval ==
train.log_interval` (both 1000, unmoved) and its committed-config count is widened BY NAME to 6 ("run9
joined at R364"). `PRODUCTION_CONFIGS` and the by-name production-set pins (fused caps, draw-rate
arming, strength floor, arch kind, microbatch caps) name `run9.yaml`; the undeclared-plant stem of the
gate-12 process tests moves to `run10`.

### 1b. The parent and the ONE training swap — RULED by R364(b)

| key | run8 | run9 | why |
|---|---|---|---|
| `identity.warm_start.checkpoint` | `checkpoints/run7/run7_00042000_46fdb931.ckpt` | **`checkpoints/run8/run8_00045000_3bdedf76.ckpt`** | the §1c PARENT RULE's output: 15k 0.104 → 30k 0.135 → 45k 0.142 monotone non-decreasing on points. File sha256 `c4990d03b79d3e10d6aa04fb2ffc309c9bc2286e8f9506e1c2353ce81326b7af` (the mirror's copy; the box's is the original) |
| `identity.warm_start.net_hash` | `a9a46c55…` | **`3aef78834b042d333400f12f6afb15e41cb716712f84386297bfbb1dd4126f42`** | `state_dict_param_hash` of the 45k checkpoint's 50 tensors, read on `dev` through the LAW-12 loader (`PARAM_DISTANCE_2026-09-21.md` §1); `bc_warmstart_loaded` refuses any other net |
| `identity.warm_start.reinit` | `[]` | `[]` | the whole net, the step-0 hash-equality witness armed |
| `train.replay_capacity` | `100000` | **`500000`** | THE DATA REGIME (CARD-RUN9-QUEUE (i), first in R359(c)'s order): a 5× window. run8's 100 000-row buffer turned over every ≈ 1 250 steps, so successive 3k-step segments trained on disjoint data (`PARAM_DISTANCE` §3); the ring beside every checkpoint grows to ≈ 180 MB |
| `train.training_steps_per_game` | `1.0` | **`2.4`** — the ENVELOPE is [2.0, 3.0] | reuse held ≈ 8 (strix's setpoint; run8 read 3.2 at 1.0). Derivation: run8's 45k ring reads `replay_ratio` 3.19 at 1.0 step per game = 256 samples per 80.3 positions per game, so 2.4 predicts **7.7**; run8's base rate ranged 3.02 (shakedown) – 3.61 (30k), which maps 2.4 to **7.2–8.7**, inside the 7–9 witness band over the whole observed spread (2.5 predicts 8.0 but reads 7.6–9.0 over the same spread, and 2.5 × 15 000 = 37 500 is not a checkpoint step). The dispatcher's pick inside the envelope; the shakedown reads it (§3a) |
| `train.max_train_burst` | `2` | **`8`** | the ceiling of one burst, raised to match: 98 % of run8's bursts saw ONE new game and 2 % two (0.05 % three), so bursts of 2–3 (one game), 4–5 (two) and 7–8 (three) all fit; the cap never binds at run8's arrival mix and drops, never owes, if it does |

**The key's semantics at HEAD, verified 2026-09-21 (R364 §0(3) asked):** before this landing
`_steps_budget` was `min(max(1, round(new_games × ratio)), max_train_burst)` PER BURST with no remainder
carried — at one game per burst (98 % of run8's) it realised only integers, and `round(2.5)` is 2
(banker's), so 2.4 would have trained 2 steps per game (≈ 6.4 reuse) and 2.6 three (≈ 9.6): the
envelope had a hole where the target sat. `mantis.train.mixing._steps_budget` now CARRIES the fraction
(`floor(carry + games × ratio)`, the ceiling still drops its excess, integer ratios unchanged — run8's
1.0 realises exactly as before), pinned by `tests/train/test_steps_budget_carry.py` (one game per burst at
2.5 → 2, 3, 2, 3; run8's arrival mix holds every ratio in the envelope to ±1 step per 1 000 games). The
carry is in-memory; a resume restarts it at 0 (< 1 step). This is a correctness fix to the key run9's swap
turns, not a second swap.

**The dispatcher's rule on the shakedown (R364 §0(3)):** `ring_audit … --events` on the shakedown twin's
step-3 000 ring reads `replay_ratio`; **in [7, 9] → 2.4 stands and is reported**; outside → the value is
re-picked inside [2.0, 3.0] from the measured base rate (ratio ÷ 2.4 × target 8) — a re-mint on `dev`
(its own `config_diff --expect` line, this section's numbers updated) and its OWN preflight, because a
twin inherits a stamp only when it differs in `run_id` alone (R360(c)); a base rate that no value in the
envelope brings into [7, 9] → **HALT** to the architect with the numbers. `train.eval_interval` follows the
re-pick (ratio × 15 000, rounded to the nearest multiple of 3 000).

### 1c. The PARENT RULE — pre-stated by R363(b) on 2026-09-20, APPLIED 2026-09-21

The gate's H1 = 0.62 exceeded run8's per-15k gain and its anchor froze at 15k (r6–r17 all GSPRT rejects
while the strix series and the sealbot rung rose): a frozen gate is not a ranking, so the parent is NOT
read off `best_model` when a strix triple exists. The rule, verbatim from R363(b): *if the follower
series 15k → 30k → 45k is monotone non-decreasing on point estimates, the parent is 45k; otherwise the
highest of the three whose CI contains the other two's points; the gate's best_model is the parent only
if no strix triple exists.* The three points, equal-work 256/256 solver ON, CONTENDED: 15k **0.104
[0.069, 0.139]**, 30k **0.135 [0.097, 0.177]**, 45k **0.142 [0.104, 0.181]** (the parent 0.111 [0.073,
0.149]) — monotone, so **the parent is run8@45k** (`run8_00045000_3bdedf76.ckpt`, net `3aef7883…`).
Run9's gate H1 is re-set in the same ruling: not from the per-15k gain as a bar (+3.1 pp then +0.7 pp is
below any H1 the GSPRT can resolve at 104 pairs) but as a regression guard, R364(c).

### 1d. The `dirichlet_*` rows — DROPPED to `false` (R359(d)), with the pin

`selfplay.mcts.dirichlet_enabled: false` (`alpha` 0.3 and `epsilon` 0.25 stay as the schema's bounds
require; the flag is the row). Inert either way on the Gumbel arm: `run_mcts_search` mixes root noise
under `SearchKind::Puct` only. The pin landed with the mint:
`crates/mantis-selfplay/tests/dirichlet_inert_on_gumbel.rs` drives one worker with the rows ARMED and
reads the drive's own counter (`dirichlet_root_fires`, LAW-18, on `RunnerStatsSnapshot`): **0 under
Gumbel** over a fully served 64-sim search, **> 0 under PUCT** with the same rows — the second half is
what makes the first a reading. Minting `false` is the cosmetic R359(d) named; the pin is the fact.

## 2. What the stream carries that run8's did not

`eval_round_complete.gate` = `{rule, pairs_played, stopped, llr, wr_confirm, n_pooled, promoted,
wall_sec}` (`mantis.eval.rounds.GATE_STREAM_FIELDS`), `null` when no gate ran; `wall_sec` is the gate
BLOCK's own wall. Under `at_max_pairs: promote` a cap round reads `stopped: "max"` with `promoted:
true` — a reader tells "promoted at the cap" from "accepted" by the `stopped` field, never by the WR.
The `wr_sealbot*` fields, the rung events and `eval_ladder_state.json` are GONE (contract #7, event
manifest); the dashboard draws the rung as a stated gap and the strix points panel as the external scale.

## 3. The reading — the strix cells at EQUAL GAMES (R364 §0(4), pre-registered)

Instrument unchanged: the strix follower's equal-work cell — ours PUCT-256 vs strix 256 sims, 288
paired games on `book_v1_s20260625_p4`, both colours, CONTENDED labelled, pair-level CI over distinct
games (LAW-04) — fired at every `train.eval_interval` checkpoint (`--follow --no-promotions --cadence
36000`, R361(a)), which is every 15 000 GAMES at 2.4 steps per game, the same data budget run8's
15k/30k/45k points had. The gate round falls on the same checkpoint by construction (§1a).

The BASELINE is the parent, run8@45k, read once in the same unit: **0.142 [0.104, 0.181]**.

**The unit's RESOLUTION (E4, `STRENGTH_RESEARCH_2_2026-09-21.md` P6 rank 1, landed by R365 §0(6)):** the
288-paired-game cell's CI half-width is ≈ 3.9 pp at the parent's 0.142 (pair-level bootstrap over distinct
games, LAW-04); the run's last measured interval moved +0.7 pp per 15k games (30k → 45k). The cell therefore
resolves a move of ≥ ≈ 4 pp — the SUCCESS line's +5 pp, a size or target swap's claimed ≥ 0.161/0.18 — and
CANNOT resolve the slope run8 was on: reading +0.7 pp at this CI needs ≈ (3.9/0.7)² ≈ 30× the games
(≈ 8 700 pairs, 34–45 box-h of cells per point). Two consequences, stated once: (i) a non-inferiority read
("the same strength at less wall") inside ± 3.9 pp cannot exclude a real 2–3 pp regression, so a systems or
throughput lever is not read by this cell at all — it needs wall-tracked progress or a cheaper equivalence
instrument named before its cell runs; (ii) a run whose expected gain is the parent's slope returns
INCONCLUSIVE by construction at this resolution, which is R365(a)'s reason for not starting run9 in this
unit. Every cell in this file and in run10's prereg carries this resolution beside its decision line.

- **SUCCESS**: run9@30k-games (step 72 000) ≥ **0.192** (parent + 0.05).
- **FALSIFIED**: run9@15k-games (step 36 000) ≤ **0.142** AND run9@30k-games ≤ **0.142** → the reuse
  regime is dead, filed in `falsified.md`; run10 = run9's parent at run8's rows.
- otherwise **INCONCLUSIVE**: read 45k-games (step 108 000) **THEN STOP** — the stop is in the line
  (R364(e)), the omission R363(a) made is annotated (A1 under its foot) and not repeated.

**STOP LAW (R364(e)):** run9 stops at 30k games, or at 45k games on INCONCLUSIVE; no further step
without a ruling. The stop is ONE SIGTERM to the supervisor (save-then-exit) once the cell that decides
has been read; `train.max_train_steps` stays 1 000 000 by "byte-equal elsewhere", so nothing in the
config stops the run — the operator's act does, as it did for run7 and run8.

**Box-hours beside every point (R364(b)):** each reading records the wall from START and the steps/h
and games/h over the span, because the swap trains 2.4× the steps per game and the trainer's share of
the card is the thing the regime changes (run8: trainer idle 92 %, F-44; alone 1 000–1 070 steps/h ≈
1 026 games/h). An ESTIMATE before the shakedown, not a number: ≈ 16 h per 15 000 games if games/h holds,
so 30k ≈ 33 h and 45k ≈ 50 h; the shakedown's games/h replaces it.

### 3a. The shakedown WITNESS and the ring audit's bands for run9 (`mantis.diagnostics.ring_audit` reads this block)

The 4 h twin (`--inherit-preflight configs/run9.yaml`, R360(c); a twin of run9 reaches its 3k
checkpoint in ≈ 1.3 h — 3 000 steps is 1 250 games at 2.4 — and its 6k and 9k inside the budget) runs
NO gate round: `train.eval_interval` 36 000 is 15 h away. The regression-guard rule is exercised
before START by the PREFLIGHT's terminal round (the burst net vs its anchor under run9's own rows,
H0 0.42 / H1 0.52, `at_max_pairs: promote`) and by its pins; the shakedown's job is the data regime.
The witness, on the step-3 000 ring (or the 6k ring if 3k predates the buffer's fill — the audit says
which): **(a)** the QSigma pin GREEN on the box tree (R357(a); FAIL → HALT, wiring); **(b)** the bands
below, each strict, a banded row the ring cannot measure a MISS; **(c)** `replay_ratio` in [7, 9]
(§1b's rule: the lower half is banded, the upper half is the dispatcher's read — above 9 is a
re-pick, not a pass); **(d)** `sym_bin0_over_mean` ≈ 1.0 and the full-arm median H(explicit) READ beside
its band. Only the PRE-START audit HALTS (R359(b)); a miss on a live 36k ring is REPORTED.

```toml
[ring_audit.bands]
counter_threat_share = { lt = 0.005 }   # run7 pre-fix 2.5–4.4 %; run8 read 0.009–0.031 % on every ring
quiescence_residue = { le = 0 }         # the A-2 falsifier's oracle; run8 0 of 4–8 on every ring
h_full_median = { gt = 0.02 }           # nats, is_full_search rows; run8 0.20–0.29 (the 45k ring 0.286)
cap_rate = { lt = 0.05 }                # games with value_valid == 0; run8 0.16–2.2 %
replay_ratio = { ge = 7.0 }             # §1b; the upper half (≤ 9) is read, not banded (the tool bands one operator per row)
```

**`one_hot_share_full` — STRUCK from the block by R365(d), 2026-09-21.** It was banded `< 0.25` at run8's
mint (calibrated on ONE 3 h shakedown ring at step 3 000, 22.1 %, the value head in warm-up; no
outcome-linked measurement ever backed the threshold) and RE-STATED `< 0.30` at R364 because the parent's
own rings read 0.2705–0.2761 flat. The instrument itself conflates two populations: `explicit_entropy`
reads a TAIL-ONLY row (α = 1.0, no explicit mass, the softest target in the ring — the tail rebuilt over
the prior) as H = 0 exactly as a one-hot, so the share is an UPPER bound with an unread tail-only term
(`ring_reader.py`; `STRENGTH_RESEARCH_2_2026-09-21.md` B §3, P5 own find 1). The row stays REPORTED
(the audit prints it); a band is re-derived from PROBE-1's decomposer (the share with tail-only rows
excluded, bucketed by `moves_remaining`, over run8's 3k → 51k rings) and written here with its
derivation, never asserted. Until then the block has FIVE bands.

Unbanded, reported: the full-arm one-hot share (above), the full-arm mean, p75 and p90, the quick-arm
stats, block-mass rows, draw share, mean |z|, `sym_bin0_over_mean`; sample age is NOT MEASURED from a
ring. run8's five-band baseline for comparison: shakedown8 PASS on all five (one-hot 22.1 %), the live
15k/30k/45k rings PASS four and MISS `one_hot_share_full` at 0.2745 / 0.2761 / 0.2705 under the old 0.25.

## 4. Gate 12 at the mint — READ 2026-09-21

`tools/ci_gates/preflight_mint.py --audit-only` rc 0 with `configs/run9.yaml` in `PRODUCTION_CONFIGS`,
bound by name beside run6/run7/run8; THREE rows DEFERRED (`policy_loss_trough`, `ply_cap_attractor`,
`grad_norm_hard_abort`), each printing its surface and cadence, gating nothing. Gate 7: every config
validates (run7 and run8 re-minted through their own headers with the v34 leaf `at_max_pairs: sign`,
byte-identical elsewhere; the box's run8 stamp predates the leaf and the loader tolerates it).

## 5. Sequence (R364 §2)

`dev` (this landing): the parameter-distance record → the mint (`config_diff --expect` the eleven rows)
→ this prereg → the pins by name → gate 12 → 3a on the tip → commit → push.
Box (alias `vast`), in order, on the pushed mint commit's tree: **run8 STOP** (one SIGTERM to the
supervisor, `shutdown_save`, the bundle receipted by the puller; 45k is mirrored, its cell ran; the
follower stopped with no cell in flight) → **PERF-3 step 3** in the window before run9's stamp
(`tools/bench_server.py` B ∈ {16 … 256} alone, then the contended arm beside one cell; ≈ 40 min; numbers
before any design, nothing from it rides run9) → `make build.cuda`, `torch.cuda.is_available()`, the
parent `run8_00045000_3bdedf76.ckpt` copied and sha-verified (`c4990d03…`) into the run tree's
`checkpoints/run8/` → **run9 preflight** on the mint commit (its terminal round runs the regression
guard) → the stamp (LAW-12, once) → the 4 h twin `--inherit-preflight` → the witness (§3a: QSigma pin,
the bands, `replay_ratio` 7–9, sym bins, entropy; the dispatcher reports the ratio and the value
that stands) → **START** → `tools/strix_follower.py --follow --no-promotions --cadence 36000 --poll-sec
300`. Report: numbers, regime (CONTENDED/IDLE per cell), box-hours per point. **Executed 2026-09-21 as
far as the STOP and PERF-3 step 3 (R365(a)); the preflight, stamp, twin, witness, START and follower did
not run — they are run10's, replayed from its own prereg.**
