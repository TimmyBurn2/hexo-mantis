# PREREG SKELETON — run9: the eval rows FIXED by R362(c); the ONE training swap and the parent RULED AT 30k (R362(e))

Written 2026-09-19 at the R362 landing, with run8 at ≈ step 9k and its 15k and 30k strix points
UNREAD. This is a SKELETON by design: every row an instrument ruling could fix is fixed here,
and every row the 30k reading owns is BLANK and says so. Nothing in this file is armed; run9's
config is minted at 30k, the stamp is written then, and the box block is that session's. The
ruling is `docs/governance/RULINGS.md` R362; its grounds are `EVAL_COST_2026-09-19.md`.

## 1. The config — `configs/run9.yaml`, NOT YET MINTED

Minted at 30k by `tools/mint_config.py --template dev`, replaying `configs/run8.yaml`'s header
deltas (31 `--set`, 6 `--mint-row` after R362(c)'s re-mint stripped the six rung rows) with the
rows below changed. The mint is ONE command; the header replays; `tools/config_diff.py
configs/run8.yaml configs/run9.yaml --expect <each key below>` must say `MATCH`.

### 1a. The eval rows — FIXED by R362(c), instrument rows, minted as written

| key | run8 | run9 | why |
|---|---|---|---|
| `run_id` | `run8` | `run9` | the run's name; every path derives from it |
| `train.eval_interval` | `3000` | **`15000`** | the gate cadence: one round per 15 000 steps, ON the strix cell's checkpoint (`train.checkpoint_interval` stays 3 000, so 15k is a checkpoint step and the round's candidate snapshot and the cell's checkpoint are the same weights). run7 ran 25 rounds in 83k steps at 50 h of eval wall; at 15 000 it would have run 5 (`EVAL_COST` §"The pre-stated directions") |
| `eval.ladder.*`, `eval.sealbot_model_sims`, `eval.rung_concurrency`, `monitor.wr_*` | present | **ABSENT — the schema has no such rows (contract v33)** | the sealbot rung is DELETED with its five consumers (R362(c)); no mint action, the template has no rows to omit |
| `eval.gate.deploy_sims` | `256` | `256` | UNCHANGED: gate sims 64 is unmeasured (a bench, not a census) |
| `eval.gate.sequential` | `{mu0 0.52, mu1 0.62, α 0.05, β 0.1, 8-of-16–104}` | same | UNCHANGED: H0 0.50 withdrawn (R362(d)); the long end is the 0.57 midpoint and H0 is a −7 % lever on it |
| `eval.concurrency`, `eval.max_plies`, `eval.round_timeout_sec`, `eval.strength_floor`, `eval.random_floor_games` | `8`, `256`, `14400`, `{4, 0.25, 0.0}`, `20` | same | the round's remaining rows; none moved by R362 |

The `--set` for the one moving eval row: `--set train.eval_interval=15000`.

**The P6b pin** (`tests/train/test_gate_interval_decoupling.py::test_p6b_every_committed_config_mints_gate_interval_equal_to_its_log_interval`)
pins `monitor.gate_interval == train.log_interval` on every committed config — the hard-abort
family's ARMING cadence against the narration cadence, both 1000 in run8 and unchanged in run9.
It does NOT read `train.eval_interval`, so the 15 000 row moves nothing it asserts. What it does
carry is the committed-config COUNT (`== 5`, "run8 joined at R356"): when `configs/run9.yaml`
lands, that line is UPDATED BY NAME to 6 ("run9 joined at R362/R36x"), never loosened to a `>=`.
Same for gate 12's `PRODUCTION_CONFIGS` and the by-name production-set pins run8 joined at R356.

### 1b. The training swap and the parent — BLANK BY DESIGN, ruled at 30k (R362(e))

| key | run8 | run9 | why |
|---|---|---|---|
| `identity.warm_start.checkpoint` | `checkpoints/run7/run7_00042000_46fdb931.ckpt` | **BLANK — ruled at 30k** | the parent is the 30k reading's: R356(c)'s three outcomes (SUCCESS run8@30k ≥ 0.161; FALSIFIED 15k ≤ 0.111 AND 30k ≤ 0.111; else INCONCLUSIVE, read 45k) decide whether run8's best_model or run7's 42k parent is the base |
| `identity.warm_start.net_hash` | `a9a46c55…` | **BLANK — ruled at 30k** | `net_param_hash` of that checkpoint's tensors; the boot refuses any other net |
| the ONE training swap | — | **BLANK — ruled at 30k** | R356(c)/R358(f)/R359(c)'s queue, ONE swap per run; which row rides is the 30k ruling's, not this skeleton's |
| `seed` | `20260914` | BLANK | disclosed as a dispatcher's value at run7's mint; the 30k session states it |

Why the config is not minted now: a `configs/run9.yaml` carrying run8's warm start would be a
hand-varied identity row (R1/LAW-11 — identity keys have no defaults and are never placeholders),
and `tools/config_diff.py --from-header` would ratify a lie. The eval rows are instrument rows
and are recorded here as the deltas the 30k session replays; the config exists when the parent
does.

## 2. What the stream carries that run8's did not

`eval_round_complete.gate` = `{rule, pairs_played, stopped, llr, wr_confirm, n_pooled, promoted,
wall_sec}` (`mantis.eval.rounds.GATE_STREAM_FIELDS`), `null` when no gate ran; `wall_sec` is the
gate BLOCK's own wall beside the row's round `wall_sec`. The census that could not read how a
round stopped without the spool reads it off the stream on run9. The `wr_sealbot*` fields, the
`eval_channel_health` / `eval_rung_skipped` / `eval_rung_skip_class` / `sealbot_wr_*` events and
`eval_ladder_state.json` are GONE (contract #7, event manifest); the dashboard draws the rung as a
stated gap on a run9 record and reads the strix points panel as the external scale.

## 3. The reading — the strix cells, unchanged (R356(c), R359(b), R361(a))

The strix follower fires ONE equal-work cell (ours PUCT-256 vs strix 256, 288 paired games,
`book_v1_s20260625_p4`, both colours, CONTENDED labelled) at every 15 000-step checkpoint and
NOT on promotions (`--no-promotions`, R361(a)). The gate round and the cell now fall on the same
checkpoint by construction (§1a), so each 15k point carries the internal comparator's verdict
AND the external reading. run9's three-outcome line is stated at the 30k ruling against the
parent it picks, in the parent's own 256/256 unit, read once.

## 4. Gate 12 at the mint

At 30k: `tools/ci_gates/preflight_mint.py --audit-only` rc 0 with `configs/run9.yaml` in
`PRODUCTION_CONFIGS`, bound by name beside run6/run7/run8. THREE rows DEFERRED since R362(c)
retired the fourth: `policy_loss_trough`, `ply_cap_attractor`, `grad_norm_hard_abort` — each
prints its arming surface and cadence and gates nothing. Gate 7: every config validates.

## 5. Sequence, owed to the 30k session (no box hours from this skeleton)

30k cell READ → the R356(c) outcome named → the parent and the ONE training swap RULED (R36x) →
mint (`--set train.eval_interval=15000` + the parent rows + the swap row; header replays;
`config_diff --expect` MATCH) → P6b / gate 12 / production-set pins widened BY NAME → push →
box: preflight on the mint commit, `make build.cuda`, `torch.cuda.is_available()`, the parent
copied and sha-verified into the run tree, the stamp (LAW-12, written once) — the stamp loader
tolerates the run8 stamp's retired rows (`RETIRED_STAMP_PATHS`, pinned) — run8 STOP after the
stamp is vested (one SIGTERM), the 4 h shakedown twin (`--inherit-preflight`, R360(c)) with the
ring audit's bands re-stated for run9, START, the follower `--follow --no-promotions --cadence
15000` beside it.
