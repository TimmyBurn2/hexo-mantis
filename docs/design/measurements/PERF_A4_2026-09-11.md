# PERF-A4 — the investigation's serving levers, landed one commit at a time (2026-09-11)

Input: `PERF_INVESTIGATION_2026-09-11.md` (§6 ranked levers, §8 reproduction). Branch `perf-a4`
off `dev` at `d12cd0b0`. One lever = one commit = one box arm against the immediately preceding
commit's arm, same minted config (the investigation's recipe: run6's deltas + `train.max_train_steps
150`, `train.min_buf_size 1024`, `train.terminal_eval_enabled false`, `train.draw_rate_abort null`
+ the arm's rows), same seed, box idle, tree `d12cd0b0` on the box until the first bundle lands.
Every arm is a 150-step burst read over its stepping window (steps 1→150) with the investigation's
`arms_summary.py`; a 150-step arm resolves ± 5 %, so a delta under 8 % is SIGN-ONLY and is said so.
LAW-05: F-17/F-19/F-21/F-44/F-45/F-46/F-47 read; nothing below re-proposes a falsified lever.
Every number is measured unless marked ESTIMATE. Host details stay out (gate 17); box paths are
`/workspace/...`.

## The one-paragraph answer

Four of the investigation's serving levers landed, one commit and one box arm each, and every one
met its pre-registered gain against the arm immediately before it: the GIL-released check 14
(+17 % leaves/s at 16 AND at 32 workers, F-46's refusal to arm `checker_thread` reversed by the
repair it named), pinned/sync-free collate (0 alone, the prerequisite), `inference.compile_trunk`
(+21 % serial; `unique_graphs` 1 across every burst; numerics eager-to-bf16-noise, keyed off by
default), and a two-thread software pipeline (+52 % over the serial loop, eager). Stacked at 32
workers: **1,831 → 3,662 leaves/s and 14.30 → 28.67 positions/s (2.0×)** on the pipeline tree
(§5; the final head's arm is §10), which prices the 25,001-step block at **≈ 10.1 h + the eval
rounds' 5–10 % ESTIMATE ≈ 10.6–11.1 h** against 20.5–21.5 h for the same 32-worker regime today
and 23–24 h for run6 as minted (§11). The codebook (lever 5) is bit-exact on the RTX 5080 and not
on sm_86, so it is deferred with its numbers rather than landed (§6). The eval-contention term is
≈ 0 at 16 workers once the pipeline is in (−2.4 %, §7). What is left on the chain is the server
thread's CPU stage (the bound at 32 workers), the cadence (`steps/game` 0.925 → `max_train_burst`),
and the pool spin the pinned copies cost. A red team of one reviewed the branch; its sixteen
findings and the action on each are §12 — three corrections to this record, one lever's abort
that could not fire (fixed), and no kept lever refuted.

## 0. Pre-registration (written BEFORE each measurement; the numbers were filled in after)

| # | lever (investigation label) | pre-registered gain | ABORT | arm design |
|---|---|---|---|---|
| A4-1 | L2: `verify_edge_geometry` with the GIL released (`py.detach` over the six borrowed slices) so `inference.edge_geometry_check: checker_thread` becomes a concurrent check | +17–23 % leaves/s at 32 workers (pops/s 32 → ≥ 37), `queue_wait` flat | < +8 % vs the same tree's `inline` arm at 32 workers | `w32ct` (32 workers + `checker_thread`) vs `w32` (inline), both on the L2 tree; `w16ct` vs `w16` decides the mint row at run6's 16 workers |
| A4-2 | L6 + the sync removals L1 needs: pinned staging + `non_blocking` H2D for the seven collate tensors, `output_size=` on `repeat_interleave`, the dist65 support resident on the device | +5 % ESTIMATE (the investigation's V1 measured 0 for the syncs alone; the copy is the term) | < −0.5 ms per pop at B ≈ 35, or any parity miss | microbench pinned variant + an in-run arm vs A4-1's |
| A4-3 | L3: `torch.compile(representation, dynamic=True)` in the inference server behind a schema key | +16 % (the microbench's −31 % serve wall) | > 8 recompiles in a burst, or ANY parity miss on the CUDA parity leg | in-run arm vs A4-2's; `tests/model/test_bf16_parity_nulldist.py` on the box |
| A4-4 | L1: two-stage server pipeline (collate pop N+1 while pop N's forward runs) | +40–60 % over the serial loop at 32 workers | < +25 % over serial in the microbench prototype, or a dispatch-order defect | prototype OFF-TREE first (two batches ping-ponged), then the loop |
| A4-5 | L4: the edge-attribute fold/codebook (inference side) | +4 % fold (measured in P6) + 7 % table ESTIMATE | < −8 % serve wall at B = 64, or any parity miss (then it is a NEW arch kind, not a landing) | microbench V4 + parity tests |
| A4-6 | L9: eval-round contention — (b) `eval.worker_device: cpu` as a config row; (a) batching the child's inference | during-round loss −20.7 % → ≤ −8 % | loss still ≤ −15 % | `w16ev`-style arm (250 steps, `train.eval_interval 100`) |
| A4-7 | L5: `selfplay.n_workers` 16 → 32 — a MINT row, recommended with its number, `configs/run6.yaml` untouched | +5 % (measured by the investigation) | — | read off the arms above |

## 1. Baseline arms on `d12cd0b0` (box tree, extension built at that HEAD)

Harness: `scratchpad/a4/box/run_arm_a4.sh` (the investigation's `run_arm.sh` with the tree's HEAD
written beside the run), configs minted by `startpath/mint_throwaway.py` from run6's 28 deltas
(the tree's run6 carries `train.actor_sync_cadence_steps 2`, so these arms do too, unlike P7's),
the optional leaves `inference.edge_geometry_check: checker_thread` / `inference.compile_trunk:
true` inserted post-mint exactly as P8 did. Read with the investigation's `arms_summary.py`.

| arm (tree) | pops/s | occ. | **leaves/s** | cycle | `queue_wait` | collate/part | **positions/s** | plies/game | server busy | GPU util |
|---|---|---|---|---|---|---|---|---|---|---|
| `w16` (`d12cd0b0`) | 45.9 | 34.9 | **1,602** | 21.8 ms | 3.06 ms | 6.33 ms | **12.49** | 46.9 | 90.4 % | 53.1 % |
| `w32` (`d12cd0b0`) | 33.4 | 54.8 | **1,831** | 29.9 ms | 2.82 ms | 9.25 ms | **14.30** | 39.7 | 99.0 % | 56.6 % |

Against the investigation's P7 (`w16` 1,687 / 13.01; `w32` 1,771 / 13.92, tree `7a97fa80`): −5 % and
+3 %, inside the ±5 % a 150-step arm resolves; game length moved 34.8 → 46.9 plies at `w16`, which is
the early-training noise the investigation named. These two arms are the denominators below.

## 2. A4-1 — check 14 with the GIL released (commit `13562ce1`)

Change: `verify_edge_geometry` (`crates/mantis-bridge/src/graph_contract.rs`) takes the six
`as_slice()` borrows and runs the verify inside `py.detach`. No wrapper is needed: `&[T]` is `Send`,
so the compiler proves the closure crosses `detach`; the borrows live in the calling frame for the
whole call. The soundness invariant is the callers': the six arrays are read-only while the check
runs — the server's are fresh per pop (`GraphWire.take()` moves the fused buffers into numpy and
nothing writes them afterwards; `slice_graph_wire` re-bases COPIES of the offset arrays and views
of the rest), the trainer's sample is read the same way. Pin: `tests/bridge/test_graph_wire_adv.py::
test_verify_edge_geometry_releases_the_gil_while_it_runs` measures the main thread's longest stall
while a 3.4 M-edge verify runs on a worker thread against a GIL-holding control (a big-int power):
locally the control stalls the main thread for 99.6 % of its wall, the verify for 0.6 %.

| arm (tree) | pops/s | occ. | **leaves/s** | cycle | `queue_wait` (min) | collate/part | **positions/s** | plies/game | server busy | all threads | GPU util |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `w32` inline (`13562ce1`) | 31.6 | 55.0 | 1,740 | 31.6 ms | 2.86 ms | 9.88 ms | 13.32 | 42.2 | 99.7 % | 2.84 cores | 57.4 % |
| **`w32ct`** (`13562ce1`) | **39.0** | 54.9 | **2,144** | 25.6 ms | 3.51 ms | 4.35 ms | **16.90** | 41.3 | 99.2 % | 3.48 cores | **68.4 %** |
| **`w16ct`** (`13562ce1`) | **53.6** | 35.0 | **1,875** | 18.7 ms | 3.54 ms | 2.97 ms | **14.72** | 39.2 | 90.0 % | 3.11 cores | **62.8 %** |

- `w32ct` vs the `d12cd0b0` `w32` baseline: **+17.1 % leaves/s, +18.2 % positions/s**; vs the same
  tree's inline arm +23.2 % / +26.9 %; against the mean of the two inline arms (1,786) **+20 %**.
  pops/s 33.4 → 39.0 (pre-registered ≥ 37). `queue_wait` +0.7 ms (F-46's arm read +9.4 ms) — the
  residual is the pop returning to a server thread that now has less to do per pop, not a GIL wait.
  **KEEP; inside the pre-registered +17–23 %.**
- `w16ct` vs `w16`: **+17.0 % leaves/s, +17.9 % positions/s** at run6's minted 16 workers — the
  posture that measured ≈ 0 at 16 workers in PERF-3b (F-46: the 9 % idle absorbed the GIL wait)
  is now positive by the full size of the check.
- What it costs: the checker thread now really runs concurrently, 0.6–0.7 of a core (`all threads`
  2.8 → 3.5 cores of 24); the GPU is busier by 10–12 points because the chain is shorter.
- LAW-18: the `edge_geometry_check` sub-block read `deferred` 17,715 (`w32ct`) / 26,067 (`w16ct`)
  parts, 0 inline fallbacks, 0 failures.

## 3. A4-2 — pinned `non_blocking` H2D, `output_size=`, resident dist65 support (commit `8b54b3e1`)

Change: `collate_graph_batch` stages each of its seven tensors pinned and copies `non_blocking`
(torch's caching host allocator holds each staging block until its copy completes); every
`repeat_interleave` in the forward and the two collate helpers passes the host-known `output_size`;
`decode_binned_value` keeps one resident support per device (built OUTSIDE inference mode — the
first cut cached an inference tensor and the trainer's backward through the shared cache then
raised; `tests/selfplay/test_collate_h2d_pinned_parity.py` pins both the byte-identity of the
device tensors against a pageable copy and the autograd path through the cache). The trainer's
`_segment_softmax` twin carries the same `output_size` (its AST-parity test with the collate's copy
requires it).

Microbench (the investigation's `server_forward_bench.py`, collate rows only — its V1–V3
monkeypatches predate the new signature and were not re-run), box, `collate(off)` = structural
checks + tensor build + H2D **including a `torch.cuda.synchronize()`**: B = 16 0.73 → 0.65 ms,
B = 35 1.22 → 1.11 ms, B = 64 2.35 → 2.03 ms, B = 128 4.74 → 5.56 ms. Faster at every served size
(the regime is B ≈ 35–55), slower at B = 128 where the pin copy of 33 MB dominates; the
pre-registered abort (< −0.5 ms at B ≈ 35) is not met.

| arm (tree) | pops/s | occ. | leaves/s | cycle | `queue_wait` | collate/part | positions/s | plies/game |
|---|---|---|---|---|---|---|---|---|
| `w32ct` (`8b54b3e1`) | 37.5 | 55.0 | 2,059 | 26.7 ms | 3.73 ms | 3.65 ms | 15.76 | 42.7 |

−4 % leaves/s against A4-1's `w32ct` — SIGN-ONLY (under 8 %); the collate timer fell 4.35 → 3.65 ms
because the CPU no longer waits on the DMA, and the time reappears at the forward's first sync, as
the investigation's V1 measurement (0 for the sync removals alone) predicted. **KEEP as the
prerequisite for A4-4**, not as a gain of its own.

## 4. A4-3 — `inference.compile_trunk` (commit `35d7480d`)

Change: a new OPTIONAL schema leaf `inference.compile_trunk: bool` (default `false`, registered in
`OPERATIONAL_DEFAULT_KEYS`, resolver `mantis.config.resolve.compile_trunk`, contract v22). When
`true`, the server builds a SERVER-PRIVATE `torch.compile(model.representation, dynamic=True)` and
hands it to `forward_batch(trunk=)`, a new keyword-only seam on both `GnnNet` and `GnnNetV2`
(`None` — and the eager server passes no kwarg at all — is byte-identical to before). Server-private
because `WorkerPool` is handed the trainer's own module: compiling `model.representation` in place
would have compiled the training forward too. The trainer and the eval child (which builds its own
`InferenceServer` from a hand-built config dict without the key) stay eager.

PARITY, stated: the compiled trunk is NOT bit-exact against eager — Inductor fuses the message pass
and orders the `index_add_` atomics differently. `tests/selfplay/test_compile_trunk.py` (CUDA leg)
is a SMOKE bound on a small net and the `b6` toy payload (max |Δprobs| < 1e-2, |Δvalue| < 1e-2;
local RTX 3070 4.2e-6 / 3.8e-4) — it proves the seam, not the served-shape numerics, which the red
team (finding 2) rightly separated. At the SERVED shape (`scratchpad/a4/box/served_parity.py`, run6
arch off the START ring, bf16 autocast): local RTX 3070 — compiled-vs-eager max |Δprobs| 1.6e-2 /
2.8e-2 / 2.3e-2 at B = 16 / 35 / 64 against that card's OWN eager-vs-eager null of 1.4e-2 / 4.1e-2 /
2.5e-2 (the compiled gap is inside the device's nondeterminism there); on the RTX 5080 (§10.1) the
eager null at the served shape is ALSO non-zero (1.8–3.1e-2) and the compiled gap (1.6–2.2e-2) sits
inside it — the compile is not resolvable above the eager path's own run-to-run noise. The R181
parity legs (`tests/model/test_bf16_parity_nulldist.py`) exercise the eager `forward_batch` only and
are untouched by this key; `false` is the shipped numerics. The dashboard's "Determinism hash" panel
is the LAW-04 trajectory-dedupe hash (`game_complete.game_id_byte_hash`), not a numerics hash.

LAW-18: the `compile` sub-block rides `iteration_complete.inference_batching`. As first landed it
carried `unique_graphs` and `recompile_limit` and the abort read "`unique_graphs` climbing past 8" —
which CANNOT fire: Dynamo stops compiling AT the limit and runs the frame eager, silently, with the
counter frozen at the limit (red team, finding 1, reproduced with `recompile_limit 3`). Fixed at
`8ebe135b`: a compiling server sets `torch._dynamo.config.fail_on_recompile_limit_hit = True`, so
the limit RAISES (`FailOnRecompileLimitHit`) and the pop fails through the loop's one failure path
instead of the lever silently disarming (R1's class), and the block adds
`fail_on_recompile_limit_hit`, `frames_total` and `frames_ok` (`total > ok` is a frame that ran
eager). Pinned by `test_the_compiling_server_makes_the_recompile_limit_a_loud_failure`.

| arm (tree) | pops/s | occ. | **leaves/s** | cycle | `queue_wait` | collate/part | **positions/s** | plies/game | server busy | GPU util | GPU mem max | `unique_graphs` |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **`w32ctc`** (`35d7480d`: ct + compile) | **45.5** | 54.7 | **2,491** | 22.0 ms | 3.48 ms | 3.65 ms | **19.55** | 37.6 | 99.6 % | 62.7 % | 7,798 MiB | 1 → 1 |

- vs A4-2's `w32ct` (2,059): **+21.0 % leaves/s, +24 % positions/s** (pre-registered +16 %); vs the
  `d12cd0b0` `w32` baseline **+36 % / +37 %** cumulative. **KEEP.**
- The abort did not fire: Dynamo compiled ONE graph for the whole burst (`unique_graphs` 1 at the
  first and the last `iteration_complete`; `dynamic=True` covers every N/E/Lg served), no fallback
  to eager. First-pop compile latency is inside the burst's fill (not separately timed here;
  P6 measured 4.4 s).
- GPU memory max 7,798 MiB against 7,956 MiB for the `w32` baseline (nvidia-smi `memory.used`
  at 1 Hz) — the compiled trunk allocates no more than eager at the served shapes.
- CPU: `all threads` 8.6 cores. This is NOT the compile: A4-2's pinned staging runs each
  `pin_memory()` copy through torch's intra-op pool (12 threads on the box), which shows in
  `threads.jsonl` as twelve threads at ≈ 33 % each (≈ 4 cores) — the pool's spin-wait after each
  parallel copy, not work. The parallel copy shortens the server's CPU stage by ≈ 1.3 ms per pop
  against a single-threaded `np.copyto` into pinned memory (measured locally: 0.08 vs 0.63 ms for
  the 8.7 MB `edge_attr`, 0.05 vs 0.49 ms for the 7 MB `edge_index`), so it is kept and the cost
  is stated: ≈ 4 cores of 24 spent spinning while a run serves, which a smaller host or a
  concurrent eval round (8 threads) would feel first.

## 5. A4-4 — the two-thread software pipeline (commit `c888c3b7`)

Change (`src/mantis/selfplay/inference_server.py`): the server thread now only pops, plans, collates
and LAUNCHES — every part's forward, segment-softmax and D2H (`non_blocking` into pinned host
buffers) are queued on the device and a CUDA event recorded — and hands the `_InFlightPop` to a
retire thread (`_PopRetirer`, FIFO), which `event.synchronize()`s (GIL released), runs the finiteness
gate on the HOST copies, submits ONE result per pop against the unsliced `legal_offsets`, hands the
captured check-14 parts to the checker, and produces the heartbeat / `first_inference_served` /
`_forward_count`. A `Semaphore(2)` bounds launched-but-undispatched pops (the one on the device plus
the one being dispatched); the server thread blocks on a third. Failures keep ONE path (`_fail_pop`)
but two arms: a launch failure fails the pop being launched, a retire failure fails the retired pop —
never the other. A check-14 finding latched while a pop was in flight refuses that pop at retire, so
only pops already launched when the finding landed can have been served (the checker-thread suite's
paced batcher now waits for dispatch before pacing on the checker; the "served once, next refused"
claim still holds under that pacing). `stop()` closes the batcher, the loop exits and drains its own
retirer; a loop that already returned is drained from `stop()`.

Why a retire thread and not the first cut's single-thread "retire N after launching N+1": that cut
made dispatch of N wait for the NEXT pop, which is the pop deadline (10 ms) whenever every waiter is
already inside N — the eval child at `concurrency 8`, the deploy head at 1, and 16 workers half the
time. It was replaced before any arm ran on it (its queued arms were cancelled), so no number below
is that design's; the local end-to-end smoke of the retire-thread form (8 workers, 101 steps,
`checker_thread` + `compile_trunk`) stepped cleanly with the `pipeline`/`compile` blocks in every
`iteration_complete`.

Tests: `tests/selfplay/test_pipeline_ordering.py` — per-pop ids and answers across three ragged pops
with distinct uid bases (a transposition would move a whole softmax segment), a simulated OOM on the
second pop failing ONLY its ids while its neighbours are served, and a batcher that hands out one pop
and then blocks as an idle queue would, asserting the pop's result arrives while the server thread is
still blocked (dispatch depends on neither another pop nor the deadline). The seam census
(`test_fused_forward_seam.py::test_fg7_03`) now walks the loop plus its four helpers: ONE
`submit_graph_inference_failure`, ONE `submit_graph_inference_results`, three broad handlers (launch
arm, retire arm, loop guard). The loop-census row (`test_selfplay_census.py`, frozen §Q6) moves
`(1, 1, 0)` → `(2, 1, 0)`: the parts loop is walked at launch and again at retire.

Memory: one FORWARD's activations are resident at a time in the allocator's accounting (the caching
allocator reuses a part's freed blocks for the next part in stream order); what doubles is the input
tensors of the pop in flight (≈ 17 MB at B = 35, ≈ 27 MB at B = 55) plus the pinned output buffers.
Measured per arm below as nvidia-smi `memory.used` max.

| arm (tree) | pops/s | occ. | **leaves/s** | cycle | `queue_wait` | collate/part | `gpu_wait`/pop | **positions/s** | plies/game | steps/game | server / retire / checker busy | all threads | GPU util | GPU mem max |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **`w32ct`** (`76f4a194`: pipeline, eager) | **65.7** | 47.6 | **3,127** | 15.2 ms | 2.19 ms | 2.54 ms | 11.06 ms | **24.53** | 39.0 | 0.949 | 97 / 77 / 52 % | 10.8 cores | **95.4 %** | 8,258 MiB |

- vs A4-2's `w32ct` (2,059, the last eager serial arm): **+52 % leaves/s, +56 % positions/s** —
  inside the pre-registered +40–60 %; vs the `d12cd0b0` `w32` baseline **+71 % / +72 %**. **KEEP.**
- The GPU is now the bound at 32 workers on the eager trunk: 95 % busy, `gpu_wait` 11.1 of the
  15.2-ms cycle (the retirer picks a pop up the moment it is launched, so `gpu_wait` IS the device
  stage per pop — read against the cycle, never against zero; red team, finding 3, and the manifest
  now says so), and pops return before the queue refills (occupancy 55 → 48). The server thread is
  ALSO 97 % busy at that cycle, but `queue_wait` + `collate` account for only 4.7 ms of it: the rest
  of the CPU stage (plan, slice, the 216 launches, softmax, D2H queue, and whatever the twelve pool
  spinners and the retirer's spin cost it in cache and scheduling) was UN-instrumented (finding 4);
  the `launch` timer added at `8ebe135b` measures it from the final-head arm on (§10).
- `steps/game` fell to 0.949: the trainer's one-step-per-completed-game cadence starts to skip when
  two games complete inside one coordinator sleep — the investigation's L8 (`max_train_burst` ≥ 2,
  a mint row under CARD-TRAINER-CADENCE), now measurable.
- CPU: `all threads` 10.8 cores — the retire thread's 77 % is the CUDA spin-wait moved off the
  server thread (`event.synchronize()` spins under `cudaDeviceScheduleAuto`), the twelve pool
  threads are at ≈ 50 % each (A4-2's parallel pin copies, ≈ 6 cores of spin), the checker at 52 %.
  On the 12-physical-core box this is the headroom an eval round's 8 threads eat into (§7).
- GPU memory max 8,258 MiB vs 7,956 for the `w32` baseline — INSIDE identical-config noise (the two
  inline `w32` arms read 7,956 and 8,238; the `w16` baseline 8,278), so no memory cost of the pop in
  flight is resolvable at 1-Hz `memory.used`.
| **`w32ctc`** (`76f4a194`: pipeline + compile) | **73.8** | 49.6 | **3,662** | 13.6 ms | 2.27 ms | 3.16 ms | 7.82 ms | **28.67** | 32.0 | 0.925 | 94 / — / — % | 11.9 cores | 85.4 % | 7,638 MiB |

- Pipeline + compile vs pipeline eager: **+17 % leaves/s** (the forward's −31 % now shows only where
  the GPU stage was the bound); vs the `d12cd0b0` `w32` baseline **+100 % leaves/s, +100 %
  positions/s** — 1,831 → 3,662 and 14.30 → 28.67. `unique_graphs` 1 for the whole burst.
- The bound moved back to the CPU stage: GPU 85 % (was 95 % eager), `gpu_wait` 7.8 ms, the server
  thread 94 % busy at a 13.6-ms cycle. What is left on the server thread per 50-leaf pop is the
  structural checks, `slice_graph_wire` (which copies `edge_index` even for a one-part plan), the
  seven pinned copies, the launches (106 after compile) and the fuse — the next millisecond lives
  there, not on the device.
- `steps/game` 0.925: at this games/h the trainer's cadence skips 7.5 % of games (L8, see §8).
- GPU memory max 7,638 MiB — the lowest of the arms, but the identical-config spread is 300 MiB, so
  read it as "no increase", not as a saving.

## 6. A4-5 — the edge-attribute fold / codebook: NOT landed (numerics)

Probe before any code (`scratchpad/a4/bench/codebook_parity.py`, run on real ring batches at
B = 16 / 35 / 64 under bf16 autocast): is `conv.lin(edge_proj(edge_attr))` bit-identical to the same
two linears evaluated over the ≤ 91 distinct attribute rows and gathered by code? The first linear
(`edge_proj`, K = 5) IS bit-exact as a table (`edge_proj(rows)[code] == edge_proj(edge_attr)` on
every batch); the per-layer second linear (`conv.lin`, K = 128) is NOT — the GEMM over 91 rows and
the GEMM over 400 k rows round differently (local RTX 3070: max |Δ| 1.6e-2 … 6.3e-2 per layer, a few
bf16 ulps; the box's RTX 5080 verdict is in §6.1). The fold `lin ∘ edge_proj → one 5→128 linear`
that P6's V3 measured at −7–8 % is the same class: a different rounding path. By R309's rule (GnnNetV2
landed as a NEW class; in-place numerics changes were rejected) the table/fold is a new arch kind or
nothing, and the pre-registered gain (+4 % measured fold, +7 % ESTIMATE table) does not buy a re-mint.
### 6.1 The box verdict — and why it is still not landed

On the RTX 5080 (the same probe, box idle, tree `c888c3b7`) the table IS bit-exact: all four layers,
`max |Δ| 0.000e+00` at B = 16, 35 and 64, `edge_proj` included. The numerics therefore depend on the
DEVICE's GEMM kernel selection (exact on sm_120, 1.6e-2…6.3e-2 off on sm_86) — the same class as the
R181 parity artifact, whose identical-code null is exactly zero on the 5080 and not elsewhere. That
makes the table a serving-side lever of `compile_trunk`'s kind (bit-exact where the block runs, not
in general), which would need its own key, its own parity leg and a pointwise code (the probe's
`torch.unique` derivation costs 1.4 ms at B = 35 against the 0.73 ms the four table gathers save;
the categorical code — axis × signed_dist × src_player — is a handful of pointwise ops). Measured
saving on the device: four layers' `e` 1.596 → 0.870 ms at B = 35, 2.966 → 1.542 ms at B = 64.
At the recommended regime the GPU is no longer the bound (§5: 85 % busy, the CPU stage is), so the
in-run value today is bounded by the GPU share of the cycle — **deferred with its numbers, not
landed**; the pre-registered "+4 % fold / +7 % table" was priced for a serial loop that no longer
exists.

## 7. A4-6 — eval-round contention

The investigation's diagnosis ("8 child workers × `forward_single`") does not match the code: the
eval child's graph leg (`LocalInferenceEngine`, `src/mantis/selfplay/inference_local.py`) builds its
own `InferenceServer` over its own `InferenceBatcher`, so its 8 games ARE batched through the same
loop this record changes — there is no per-game single forward to batch (lever (a) has nothing to
do). What remains is the measured term itself — two CUDA contexts time-slicing plus the child's 8 CPU
threads — so lever (b), `eval.worker_device: cpu` (a config row), is what the arm below measures:
`w16ev` (16 workers, `train.eval_interval 100`, 250 steps, `checker_thread` + `compile_trunk`, one
round overlapping the stepping window) against `w16evcpu` (the same with `eval.worker_device: cpu`),
both on the pipeline tree, read with the investigation's before/during/after windows.

## 8. Two follow-ups on the CPU stage (commits `aa87e3ae`, `8fba1953`) and the cadence

Once the pipeline made the server thread's CPU stage the bound (§5), two of its costs were plain:

- `aa87e3ae` — a one-part plan (every pop at `inference_batch_size 64`: 0 splits in every arm)
  collates the payload itself instead of `slice_graph_wire(payload, 0, B)`, which copied
  `edge_index` (7 MB at B ≈ 50) and re-based every offset array by zero. Byte-identical output; the
  dump-on-fire still writes what the check read.
- `8fba1953` — A4-2's `pin_memory()` runs a torch intra-op parallel region per array; the twelve
  pool threads then spin at ≈ 50 % each (§5). Only the two edge arrays are large enough to be worth a
  region; the five small ones (`x` at 0.76 MB and the four offset/gather arrays) are now staged with
  a single-threaded `np.copyto` into pinned memory. Two regions per pop instead of seven.

Both are measured by the final-head arm (§10) against `w32ctc@76f4a194`; neither has a
pre-registered gain beyond "the CPU stage shrinks" — a delta under 8 % is sign-only.

The cadence: `steps/game` fell from 0.96 (baseline) to 0.925 (`w32ctc`) because
`train.training_steps_per_game 1.0` with `max_train_burst 1` takes ONE step per coordinator wake
and two games now complete inside one 0.1-s sleep more often. At 25,001 steps that is 7.5 % more
games than steps. The investigation's L8 (`train.max_train_burst` ≥ 2 so steps stay ≡ games) is a
MINT row under CARD-TRAINER-CADENCE (the architect's) and is recommended in §11 with this number; it
is not edited here.

## 9. The 16-worker arms on the pipeline tree (`76f4a194`)

Run6 mints 16 workers; the investigation's L5 says the pipeline needs ≥ 32 (two pops in flight from
≈ 56 leaves is starvation). Measured:

## 10. The final head — arms, the served-shape parity, the tier and the gates

Branch head `6a0d5e78` (the pipeline tree `76f4a194` plus: the `stop()` guard, the pyright fix, the
one-part-plan self-slice skip, the two-region pin staging, the red-team fixes, the test declaration).

| arm (tree) | pops/s | occ. | **leaves/s** | cycle | `queue_wait` | collate/part | **`launch`/pop** | `gpu_wait`/pop | **positions/s** | plies/game | steps/game | server busy | all threads | GPU util | GPU mem max | `compile` |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **`w32ctc`** (`6a0d5e78`) | **77.2** | 49.3 | **3,804** | 13.0 ms | 2.36 ms | 3.18 ms | **10.58 ms** | 7.83 ms | **29.48** | 34.7 | 0.961 | 94.2 % | 11.9 cores | 87.6 % | 7,678 MiB | 1 graph, frames 2/2, fail-loud on |

| **`w16ctc`** (`6a0d5e78`) | 73.6 | 34.3 | **2,520** | 13.6 ms | 7.05 ms | 2.54 ms | — | — | **19.73** | 34.5 | 0.961 | 58.5 % | 9.9 cores | 62.3 % | — | — |

- `w32ctc` vs `w32ctc@76f4a194`: **+3.9 % leaves/s, +2.8 % positions/s — SIGN-ONLY** for the two
  CPU-stage follow-ups together (§8); `w16ctc` vs `w16ctc@76f4a194`: +9 % / +11 % (a 16-worker
  arm is the noisier one; still under the 8 % band on leaves/s once the pair's spread is allowed).
  At run6's minted 16 workers the head reads **+57 % leaves/s, +58 % positions/s** over the `w16`
  baseline (1,602 → 2,520; 12.49 → 19.73). The `launch` timer (finding 4) now measures the CPU stage directly:
  **10.6 ms per 49-leaf pop**, + 2.4 ms of `queue_wait` = the 13.0-ms cycle — the server thread's
  CPU stage IS the bound, and the device stage (`gpu_wait` 7.8 ms) has 5 ms of slack per pop under
  it. Of the 10.6 ms, `collate` is 3.2; the other ≈ 7.4 ms is plan + stone mask + the compiled
  forward's launches + softmax + D2H queue — the next lever is there.
- vs the `d12cd0b0` `w32` baseline: **+108 % leaves/s (1,831 → 3,804), +106 % positions/s (14.30
  → 29.48)**. `steps/game` 0.961 in this window (0.925 in the previous arm's — it moves with game
  length; both say the cadence row is due).
- The twelve pool threads are still at ≈ 57 % each with two parallel regions per pop instead of
  seven (§8): the spin is the pool's wait policy between regions, not the region count, so
  `8fba1953` did not buy the cores back. The parallel copy stays on the CPU stage's critical path
  (≈ 0.1 vs ≈ 1.3 ms per pop single-threaded, i.e. ≈ 11 % of a 10.6-ms stage), so it is kept;
  the falsifier — the same arm with the copy single-threaded — is the next arm, not run here.
- `compile`: `unique_graphs` 1, `frames_total` = `frames_ok` = 2, `fail_on_recompile_limit_hit`
  true — no eager fallback in the burst.

### 10.1 Served-shape parity on the RTX 5080 (`scratchpad/a4/box/served_parity.py`, tree `6a0d5e78`)

run6's arch (`GnnNetV2`, the warm-start weights) off the START ring, `inference_mode` + bf16
autocast, the same batch forwarded twice eager and twice compiled; max |Δ| over the softmaxed
policy (`p`) and the decoded value (`v`):

| B | N / E | eager-vs-eager null |Δp| / |Δv| | compiled-vs-eager |Δp| (median rel) / |Δv| | compiled-vs-compiled |Δp| |
|---|---|---|---|---|
| 16 | 8,213 / 209,000 | 2.26e-2 / 2.74e-4 | 1.58e-2 (2.3e-2) / 2.38e-4 | 1.52e-2 |
| 35 | 16,011 / 407,072 | 3.11e-2 / 3.00e-4 | 2.09e-2 (2.3e-2) / 2.37e-4 | 2.78e-2 |
| 64 | 29,542 / 752,716 | 1.82e-2 / 4.02e-4 | 2.18e-2 (2.2e-2) / 4.34e-4 | 1.77e-2 |
| 35 | 17,368 / 441,824 | 2.44e-2 / 2.10e-4 | 1.83e-2 (2.1e-2) / 2.84e-4 | 1.88e-2 |

Two facts, stated plainly. (a) At the SERVED shape the eager forward is NOT bit-reproducible on the
RTX 5080 either: run-to-run, its own policy differs by up to 2–3 e-2 (bf16 `index_add_` atomics
over 400 k edges) — the R181 artifact's "identical-code null exactly zero" is a property of its
fixture and regime, not of this forward. (b) The compiled trunk's distance from eager (1.6–2.2 e-2,
median relative 2.1–2.3 e-2 per entry) is INSIDE that eager null at every B, and compiled-vs-compiled
reads the same band: `compile_trunk` adds no numerics change resolvable above the eager path's own
nondeterminism at production shapes. `unique_graphs` 1, `frames` 2/2 (no eager fallback) on every
probe. The value head moves by 2–4 e-4 in both comparisons. This is the parity statement the key
ships with; "bit-exact" is not available to either path here.

### 10.2 The tiers and the gates

- Local default tier on the head, CUDA venv, `uv run --no-sync pytest` (the `--no-sync` matters:
  any bare `uv run` re-syncs the venv to the CPU torch group and takes the CUDA legs with it — red
  team, finding 5): **4,554 passed, 8 skipped, 52 deselected** in 4 min 57 s. The three CUDA-only
  `skipif` rows this packet adds are declared in `tools/ci_gates/tier_declaration.txt`.
- Box `make gates.exit` on `6a0d5e78`: §10.3.

## 11. Recommended mint rows (nothing under `configs/` is edited by this packet)

Block arithmetic (the investigation's: 25,001 steps ÷ (games/h × steps/game), games/h = positions/h
÷ 38.6 plies from the START burst — the plies term is ESTIMATE, positions/h and steps/game are each
arm's measured window; + 5–10 % for the eval rounds, ESTIMATE from §7):

| regime (arm) | positions/h | games/h | steps/game | steps/h | block | + eval rounds |
|---|---|---|---|---|---|---|
| run6 as minted: 16 workers, inline, eager, serial (`w16` on `d12cd0b0`) | 44,964 | 1,165 | 0.980 | 1,142 | 21.9 h | 23.0–24.1 h |
| 32 workers, inline, eager, serial (`w32` on `d12cd0b0`) | 51,480 | 1,334 | 0.961 | 1,282 | 19.5 h | 20.5–21.5 h |
| 16 workers + `checker_thread` (A4-1) | 52,992 | 1,373 | 0.980 | 1,345 | 18.6 h | 19.5–20.4 h |
| 32 workers + `checker_thread` (A4-1) | 60,840 | 1,576 | 0.993 | 1,565 | 16.0 h | 16.8–17.6 h |
| 32 workers + `checker_thread` + `compile_trunk` (A4-3, serial) | 70,380 | 1,823 | 0.961 | 1,752 | 14.3 h | 15.0–15.7 h |
| 32 workers + `checker_thread`, pipeline (A4-4, eager) | 88,308 | 2,288 | 0.949 | 2,171 | 11.5 h | 12.1–12.7 h |
| 32 workers + `checker_thread` + `compile_trunk`, pipeline (A4-4 tree) | 103,212 | 2,674 | 0.925 | 2,473 | 10.1 h | 10.6–11.1 h |
| **the branch head `6a0d5e78`, same regime (§10)** | **106,128** | **2,749** | 0.961 | **2,642** | **9.5 h** | **≈ 10.2 h at the measured −7 % during-round rate alive most of the block (ESTIMATE)** |

Rows, each with its number:

1. `selfplay.n_workers: 32` — +5 % alone (investigation, F-47) and +14 % over 16 on the L2 tree
   (`w32ct` 2,144 vs `w16ct` 1,875 leaves/s); the PREREQUISITE for the pipeline (§9: at 16 workers the
   pipeline is starved, `queue_wait` 8 ms). +1.3 GiB RSS (investigation); the ring rebuild goes to
   one sample thread (`resolve_sample_threads`), which the trainer's 92 % idle absorbs.
2. `inference.edge_geometry_check: checker_thread` — +17 % leaves/s at BOTH 16 and 32 workers on
   the detached verifier (§2); F-46's arming refusal was about the verifier as built, and the
   register row should be annotated with this record rather than edited. Cost: 0.6–0.7 core.
3. `inference.compile_trunk: true` — +21 % serial, +17 % under the pipeline (§4, §5); numerics
   consequence stated in §4; `unique_graphs` stayed 1 across every burst. Cost: 4–7 s of first-pop
   compile per process start (the eval child stays eager, so no compile per round).
4. `train.max_train_burst` ≥ 2 (CARD-TRAINER-CADENCE, the architect's) — `steps/game` 0.925 at the
   recommended regime, i.e. 7.5 % of games take no step; the row keeps steps ≡ games. Not measured
   here (a mint of the trainer's regime), stated with its measured operand.
5. `eval.worker_device: cuda` — stays: `cpu` costs −15 % leaves/s while a round is alive against
   −2.4 % (16 workers) / −7.4 % (32 workers) on `cuda` (§7). `train.eval_interval` should be
   revisited with the step rate (§7's regime note), which is the architect's call.

| arm (tree) | pops/s | occ. | **leaves/s** | cycle | `queue_wait` | collate/part | `gpu_wait`/pop | **positions/s** | plies/game | steps/game | server busy | GPU util | GPU mem max |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `w16ct` (`76f4a194`: pipeline, eager) | 62.6 | 31.6 | **1,977** | 16.0 ms | **8.26 ms** | 2.54 ms | 7.26 ms | **15.48** | 42.2 | 0.968 | 57.2 % | 68.5 % | 8,080 MiB |

| `w16ctc` (`76f4a194`: pipeline + compile) | 67.9 | 34.0 | **2,307** | 14.7 ms | 7.43 ms | 2.67 ms | — | **17.80** | 46.0 | 0.968 | 58.7 % | 59.9 % | — |

- `w16ct` vs A4-1's `w16ct` (1,875 / 14.72): **+5 % / +5 % — SIGN-ONLY.** The signature is
  starvation, as L5 said: `queue_wait` 3.5 → 8.3 ms (the pop waits for the deadline because every
  worker is inside a launched pop), occupancy 35 → 32, the server thread 57 % busy. The pipeline
  does no harm at 16 workers; it pays only with ≥ 32.
- `w16ctc` (everything, at run6's minted 16 workers): **2,307 leaves/s, 17.80 positions/s — +44 % /
  +43 % over the `w16` baseline**, still starved (`queue_wait` 7.4 ms); the same tree at 32 workers
  reads 3,662 / 28.67 — the 16 → 32 mint row is worth **+59 % / +61 %** once the pipeline is in.

## 12. The red team's findings, verbatim, and what was done about each

One devil's-advocate agent, mandate: assume the lead is wrong; reproduce every gain from the raw
arms; break the correctness; find what each change costs elsewhere; check the repo rules. Its list
(reviewed at branch `c888c3b7`, box tree `76f4a194`, a burst running the whole time so it ran no GPU
work on the box) is reproduced below unedited; the lead's action follows each item in **→**.

1. **A4-3's pre-registered abort cannot fire — `unique_graphs` saturates at `recompile_limit` and
   Dynamo silently runs eager. CONFIRMED.** The abort is "`unique_graphs` still climbing past 8".
   Dynamo stops compiling AT the limit, marks the frame skipped and runs eager with no exception;
   the counter never exceeds the limit. The LAW-18 `compile` block would then read `enabled: true,
   unique_graphs: 8, recompile_limit: 8` — indistinguishable from "8 graphs and still compiled", and
   `enabled: true` is reported while the served forward is eager. Repro (`dynamo_limit_probe.py`,
   `recompile_limit=3`, dtype-forced recompiles): `('int64', 3, {'total': 3, 'ok': 3})`,
   `('int32', 3, {'total': 4, 'ok': 3})` ← limit hit: eager fallback, counter frozen at 3 …
   `fail_on_recompile_limit_hit False, warnings: []`. Fix direction: read
   `counters["frames"]["total"] - counters["frames"]["ok"]` or set `fail_on_recompile_limit_hit=True`,
   and word the abort as "== recompile_limit".
   **→ Fixed at `8ebe135b` exactly so (§4); test added.**
2. **The compile parity claim is not established at the served shape; the test's tolerance is below
   the device's own eager null there. CONFIRMED.** `test_compile_trunk.py`'s CUDA leg uses hidden=32,
   2 layers, the b6 toy payload and an absolute bar of 1e-2. On the local RTX 3070 with the run6 arch
   off `ring400.bin` (`parity_shape.py`): B=35 eager-null |Δp| 1.55e-02, compiled-vs-eager 1.76e-02,
   |Δv| 2.98e-04; B=64 3.07e-02 / 3.11e-02 / 3.46e-04; B=16 1.26e-02 / 2.26e-02. The record's "Local
   RTX 3070: |Δprobs| 4.2e-6" is the toy payload. At served shape the compiled-vs-eager gap is 2–3
   points of policy mass on this card (equal to its eager null); on the deterministic RTX 5080 the
   served-shape gap is UNMEASURED (the mandated box parity run could not be done: burst running).
   `unique_graphs` stayed 1 across B=16/35/64 — that half holds.
   **→ Record corrected (§4 now labels the test a smoke bound and carries the served-shape numbers);
   the served-shape probe runs on the RTX 5080 in the final leg (§10.1).**
3. **The event-manifest decode rule for `gpu_wait` is wrong. CONFIRMED.** Manifest: "near 0 says the
   CPU stage bounds the loop, large says the GPU stage does." `gpu_wait` is timed from the retirer's
   pickup, which happens the instant a pop is launched, so it ≈ the pop's device duration in BOTH
   regimes. Counterexample landed on the box (`a4_w32ctc_76f4a194`, pipeline + compile): server
   thread 94.2 % busy (CPU-bound by the record's own reading), `gpu_wait`/pop **7.82 ms of a 13.6-ms
   cycle**, GPU util 85.3 %. The eager pipeline arm reads 11.06 of 15.2 ms. The instrument is useful
   as "GPU stage per pop" but only `gpu_wait / cycle` discriminates.
   **→ Manifest, docstring and §5 rewritten; a `launch` timer added beside it (`8ebe135b`).**
4. **The server thread's CPU stage is un-instrumented and the "balanced stages" reading rests on
   busy %. PLAUSIBLE.** Pipeline arm: cycle 15.2 ms, server 97 % running, but `queue_wait` 2.19 +
   `collate` 2.54 = 4.7 ms; ≈ 10 ms/pop (plan, slice, forward launch, softmax, D2H alloc) is
   unmeasured. Locally the same stage at B=48 costs 6.5 ms total (`launch_cost.py`: collate 3.43 +
   launch-no-sync 3.10 ms), and the box's collate is FASTER than local (2.54 vs 3.43), so ~7 ms/pop
   of box server-thread time is unexplained — launch back-pressure spin or contention from the 12
   OMP spinners + retire spin are the suspects. The now-landed pipeline+compile arm shows the
   consequence: 3,662 leaves/s, +17 % over the eager pipeline (compile bought +21 % serially) with
   server 94 % / GPU 85 % — the CPU stage is the bound. A `launch` timing sub-block would settle it.
   **→ `launch` timer landed (`8ebe135b`); the final-head arm reads it (§10). The OMP spinners were
   cut from seven regions per pop to two at `8fba1953` before this list arrived (§8).**
5. **The local venv silently flips torch cu128 → CPU on any `uv run` without `--no-sync`, and the
   lead's default-tier log is RED. CONFIRMED.** `uv run …` (and `make test`, `make lint`,
   `lint_gate.sh`, `comment_lint`) re-syncs the default `cpu` group: `- torch==2.11.0+cu128
   + torch==2.11.0+cpu` in 0.6 s; every CUDA leg then LOUD-SKIPs. It happened twice in this session
   (once by my first `uv run`, once by `lint_gate.sh`); restored both times with `uv sync --extra
   cuda --no-group cpu`. `scratchpad/a4/pytest_default_tier.log` shows the same class: `1 failed,
   3117 passed … stopping after 1 failures` at 67 %, the failure `AttributeError: module 'torch' has
   no attribute 'float16'` inside the contract-doc gate subprocess — a torch package being replaced
   under a running interpreter. There is no green default-tier log for perf-a4. Repro: `uv run
   python -c "import torch;print(torch.__version__)"` → `2.11.0+cpu`.
   **→ Confirmed as a pre-existing property of the repo's uv groups (the dev box is the CPU
   default; the CUDA extra is a box posture), not of this branch. The default tier is re-run green
   under `--no-sync` at the end (§10.2), and the box's `gates.exit` runs on its own CUDA venv.**
6. **The +300 MiB attributed to the pipeline is inside identical-config noise. CONFIRMED.**
   nvidia-smi `memory.used` max over the true stepping window: `w32_base` 7,956, `w32_l2` 8,238
   (same config, inline, 282 MiB apart), pipeline 8,258, pipeline+compile 7,638, compile-only 7,798,
   `w16_base` 8,278. The record's "the pop in flight's inputs plus the compiled/pinned buffers" is
   unsupported.
   **→ Record corrected (§5).**
7. **§2 mis-cites the A4-2 arm's counter as A4-1's. CONFIRMED (minor).** "`deferred` 26,067 / 17,662
   parts … on the two arms": 17,662 is `a4_w32ct_8b54b3e1`; the A4-1 `w32ct` (13562ce1) reads 17,715
   (`arms_summary_all.txt`).
   **→ Corrected.**
8. **A4-1 aliasing soundness as a defect at HEAD. REFUTED.** No writer exists: the collate copies
   (`pin_memory()` / `np.copyto` into pinned staging), `slice_graph_wire` re-bases copies, `take()`
   moves, the checker only reads. Racing a Python mutator against the detached verify on a 3 M-edge
   wire (`race_probe.py`): `flip {'ok': 14, 'ValueError: edge delta != signed_dist * axis_vec…': 6}`,
   `index {'ok': 20}` — clean raise or pass, never a crash (indices are range-checked). The invariant
   is by convention only (`attr.flags.writeable == True`); `setflags(write=False)` on the six arrays
   at `graph_wire_from_rust` would make it structural. The pin test cannot pass on the old extension
   (asserts stall < 0.5·wall; the GIL-held control stalls ≈ wall).
   **→ No change: `setflags(write=False)` would make every `torch.from_numpy` on the wire warn;
   the convention and this race result are recorded in §2.**
9. **Comparison flaws in the arms (windows, eval rounds, config, noise). REFUTED.** Event census: zero
   `eval_round_*` events in all 9 arms. `resolved_config.yaml` diffs: only `run_id` + the lever
   (`edge_geometry_check`, `compile_trunk`, `n_workers`). GPU util recomputed over the exact stepping
   window (`windows.py`): 52.8/56.2/57.0/68.4/62.6/68.8/62.4/95.3 % vs the record's
   53.1/56.6/57.4/68.4/62.8/68.8/62.7/95.4 (the 150–450 s window truncates at run end for the fast
   arms but is unbiased). Every ratio in §1–§5 reproduces to the digit. Identical-config inline arms
   differ by 5.0 % leaves/s / 7 % positions/s — the stated ±5 % holds; quarter-windows drift ≤ −13 %
   late in the long inline arms (game length), stable in the ct/pipeline arms. Caveat: the
   pipeline+compile window is 180 s and `steps/game` falls to 0.925.
   **→ No change.**
10. **`OPERATIONAL_DEFAULT_KEYS` grounds and the eval child. PLAUSIBLE.** v20's criterion: a key
    defaults when it names "how the PROCESS is operated … no run has ever decided it differently …
    it stays REQUIRED when a run chooses it — those are ARMING." `compile_trunk` changes served
    numerics and its own grounds say "arming it is a mint act". v21 set the precedent, but this row
    contradicts the criterion's words. Mechanically: `InferenceServer.__init__` turns `None` into
    `False`/`"inline"`, which is what keeps the eval child (`inference_local.py` hand-built dict)
    eager — the eval child and deploy head can never be armed for either lever, and both now carry a
    retire thread spinning in `event.synchronize()`.
    **→ Left for the operator: making the key REQUIRED re-mints every committed config including
    `configs/run6.yaml`, which this packet may not edit; the grounds text states the tension. The
    eval child staying eager is by design (§4); its retire thread's spin replaces the spin its
    server thread did in `cudaStreamSynchronize` before — no new core.**
11. **A4-2's OMP spin is kept on an in-run number that never appeared. PLAUSIBLE.** Twelve pool
    threads at ≈50 % each in the pipeline arm (`analyze_threads.py`: ~6 cores), ≈33 % in A4-2/A4-3.
    The A4-2 arm read −4 % leaves/s, cycle +1.1 ms; the "1.3 ms/pop saving" is a local copy
    microbench. With the CPU stage now the bound (finding 4), the spinners compete with it. Cheapest
    falsifier: the pipeline+compile arm with `torch.set_num_threads(1)` or `OMP_WAIT_POLICY=PASSIVE`.
    **→ `8fba1953` (two parallel regions per pop instead of seven) is measured by the final-head
    arm's thread table (§10); the single-thread falsifier is recorded as the next arm to run if the
    spinners survive it.**
12. **Pipeline failure paths. PLAUSIBLE (not reachable at HEAD).** `_PopRetirer.run` exits if
    `_fail_pop` itself raises (only `require_graph`/logging can); the server thread then blocks
    forever in `slots.acquire()` (no timeout) and `stop()` cannot unblock it — `pool.stop()` gives
    up after 5 s, the watchdog fires via the missing heartbeat. A check-14 inline fallback (queue
    full) now runs on the RETIRE thread and stalls dispatch; 0 fallbacks in every arm, checker at
    52 % in the pipeline arm. Launch failure releases the slot correctly; `stop()` semantics are fine
    at c888c3b7.
    **→ The retirer now survives its own exceptions (`_LOG.exception`, slot released; `8ebe135b`).
    The inline-fallback-on-the-retire-thread path is pre-existing behaviour of the bounded checker
    queue, 0 fallbacks in every arm; left.**
13. **Test scope. PLAUSIBLE.** `test_pipeline_ordering.py` and the checker-thread suite run on CPU
    where `pop.event is None` and `_to_host_async` is synchronous — they pin id/failure routing but
    nothing about the CUDA asynchrony (event stream, D2H-before-gate, pinned reuse). No CUDA-path
    server test exists on the branch; the only CUDA evidence is the box arms. The checker-thread
    test is paced, so "served once, next refused" is proven only under pacing (production can serve
    up to queue depth 4 + pipeline depth 2 pops past a defect — pre-existing for the queue part).
    **→ A CUDA-path ordering test added (`8ebe135b`; four ragged pops through the real loop on the
    GPU with events and pinned D2H), green on the local RTX 3070.**
14. **Repo rules and gates. REFUTED (all green).** comment_lint GREEN (floor lowered 13531→13527,
    ratchet direction correct); contract_doc_gate rc 0; rule7 `--base d12cd0b0` clean (27 files); r8
    header gate clean; lint_gate GREEN (ruff 0, pyright 0/199); `cargo fmt -p mantis-bridge --
    --check` clean; `cargo clippy -p mantis-bridge -- -D clippy::all` rc 0; 4/4 commits one-line,
    empty bodies, no trailers; 102 targeted tests pass locally with CUDA torch (compile,
    pinned-parity, bf16 nulldist, pipeline, checker, seam census).
15. **Box tree ≠ branch HEAD. CONFIRMED (labelled honestly).** `76f4a194` vs `c888c3b7` differ only
    in `stop()`'s `and not self.is_alive()` guard. At 76f4a194 a `stop()` on a live loop sends the
    retirer its `None` while the loop can still submit, so in-flight pops at a clean stop go
    undispatched; the box's `w16ct`/`w16ctc` arms run that pre-fix `stop()`. Stepping-window numbers
    are unaffected.
    **→ The final-head arms (§10) run on the branch head.**
16. **Record incomplete at review time.** §6+ (mint rows, block-time arithmetic) not yet written; the
    landed pipeline+compile arm (`a4_w32ctc_76f4a194`: 3,662 leaves/s, 28.67 pos/s, 73.8 pops/s at
    occ 49.6, 180-s window, GPU 85.3 %, server 94 %, mem max 7,638) is on the box and not in the
    record. Note the venv is currently restored to cu128; any further `uv run` without `--no-sync`
    will flip it again.
    **→ Written (§5–§11).**

Measured (`scratchpad/a4/eval_windows.py`, the investigation's `fill_45.py` parametrised; the
"during" window ends at the last `iteration_complete`, step 250 — the round itself continued into
the run's drain, so no round wall is read here):

| arm (tree `c888c3b7`, 16 workers, ct + compile, pipeline) | window | steps | wall | pops/s | occ. | **leaves/s** | cycle | `queue_wait` | collate/part | **positions/s** | GPU util / VRAM max | server busy |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `w16ev` (`eval.worker_device: cuda`) | before the round | 1→99 | 194 s | 69.2 | 34.3 | **2,378** | 14.4 ms | 7.08 ms | 2.51 ms | **18.17** | 58.8 % / 7,660 MiB | 61.2 % |
| | DURING the round | 100→250 | 262 s | 67.6 | 34.4 | **2,321 (−2.4 %)** | 14.8 ms | 7.15 ms | 2.79 ms | **18.20 (+0.1 %)** | 62.3 % / 8,033 MiB | 60.7 % |
| `w16evcpu` (`eval.worker_device: cpu`) | before the round | 1→99 | 225 s | 69.6 | 34.2 | **2,383** | 14.4 ms | 7.26 ms | 2.49 ms | **18.14** | 58.6 % / 7,698 MiB | 60.5 % |
| | DURING the round | 100→250 | 401 s | 58.6 | 34.5 | **2,025 (−15.0 %)** | 17.1 ms | 7.60 ms | 4.39 ms | **15.87 (−12.5 %)** | 51.6 % / 7,700 MiB | 65.1 % |

- On the pipeline tree at 16 workers the eval child on CUDA costs the self-play loop **−2.4 %
  leaves/s (positions/s +0.1 %)** while its round is alive — against −20.7 % in the investigation's
  serial-loop arm: the loop is starved at 16 workers (server 61 % busy, `queue_wait` 7 ms), so the
  second CUDA context's time slices land in slack. The pre-registered "≤ −8 %" is met without any
  code on the child.
- `eval.worker_device: cpu` is WORSE: −15.0 % leaves/s, `collate` +77 % — the child's 8 CPU threads
  compete with the server thread's CPU stage, which is now the scarce resource, and the round is
  slower besides (it had not closed by step 250 in either arm; the CPU child's window was 401 s to
  the CUDA child's 262 s for the same 150 steps). **The row stays `cuda`.**

| arm (tree `6a0d5e78`, 32 workers, ct + compile, pipeline) | window | steps | wall | pops/s | occ. | **leaves/s** | cycle | `queue_wait` | collate/part | **positions/s** | GPU util / VRAM max | server busy |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `w32ev` (`eval.worker_device: cuda`) | before the round | 1→99 | 125 s | 76.5 | 49.6 | **3,790** | 13.1 ms | 2.63 ms | 3.20 ms | **30.71** | 86.8 % / 7,518 MiB | 93.4 % |
| | DURING the round | 100→250 | 201 s | 71.3 | 49.3 | **3,510 (−7.4 %)** | 14.0 ms | 2.74 ms | 3.30 ms | **27.33 (−11.0 %)** | 88.0 % / 7,991 MiB | 94.1 % |

- At the recommended 32 workers, where the loop is not starved (server 93 %, GPU 87 %), the CUDA
  child costs **−7.4 % leaves/s / −11 % positions/s** while alive — inside the pre-registered
  "≤ −8 %" on leaves, a third of the serial loop's −20.7 %. No code on the child was needed.
- A regime consequence the operator should see: `train.eval_interval 1000` was minted at
  ≈ 1,100 steps/h (a round every ≈ 54 min, alive 21–40 % of the block). At ≈ 2,600 steps/h a round
  opens every ≈ 23 min while its length is unchanged (12–22 min; the child is eager and its own
  loop only pipelines) — the child is alive most of the block, and the arm's step-200 round was
  `eval_round_skipped_busy`. The block cost is then ≈ the during-round loss itself (ESTIMATE
  −7 % on leaves), and the number of rounds that actually run falls; scaling `eval_interval` with
  the step rate is a regime decision (not a lever here), stated with these numbers.
