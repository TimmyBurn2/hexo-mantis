# MEASUREMENT — the strix rung over run7's checkpoints at ~60k (2026-09-17, box, tree `ba51fd46`)

Instrument: the RUNG-2 cell A of `STRIX_RUNG_2026-09-14.md`, unchanged — `tools/strength_frontier.py
--config configs/run7.yaml --cells /workspace/oc7/cells_strix_60k.json` from the run's own tree
(`ba51fd46`, driver sha `30c4d5c6…`), ours at PUCT-512 on the card, strix (`vendor/pins.toml
[pins.hexo-strix]` `5a771e57`, `checkpoint_00237000.pt`) at 128 sims, m 16, noise off, on the box's CPU
through eight `tools/strix_driver.py` processes; book `book_v1_s20260625_p4`, 144 openings × both
colours = 288 paired games per cell, pair-level bootstrap CI over distinct games (LAW-04; eff_n 288 in
every cell). Sequential (`--parallel 1`) BESIDE the live run (trainer at 62 814 → 69 900 steps,
32 self-play workers, eval rounds r000020–r000021 in flight for parts of it); chain
`/workspace/oc7/chain_strix_60k.sh`, log `strix_60k.log`, results `strix_60k/<label>/cell.json`,
mirrored to the operator's `mantis-mirror/strix_60k/`. Operator direction 2026-09-17: run7 continued
past the 30k point R355 named; "let its strongest versions play against strix".

## A. The series (cell A; the three earlier points are on the same instrument)

| step | checkpoint | why this one | strix WR | 95 % CI (pairs) | W–L | seat-decided pairs | p1 wins | median plies | s/game | cell wall |
|---|---|---|---|---|---|---|---|---|---|---|
| 0 | BC net (`run6_00006500_ca1afb71`) | step-0 point (2026-09-14, idle card) | 0.083 | [0.052, 0.122] | 24–264 | 18 / 144 | 136/288 | 39 | 6.96 | 2 006 s |
| 9 000 | `ce6ff519` | first promoted anchor (2026-09-15) | 0.097 | [0.066, 0.132] | 28–260 | — | — | 41 | 13.18 | 3 795 s |
| 15 000 | `40e13c80` | cadence point (2026-09-15) | 0.045 | [0.021, 0.073] | 13–275 | — | — | 37 | 13.55 | 3 902 s |
| 24 000 | `efa82bb2` | promoted anchor at the resume | **0.139** | [0.101, 0.177] | 40–248 | 30 / 144 | 140/288 | 43 | 15.16 | 4 364 s |
| 30 000 | `d7f468db` | the R355 cadence point | **0.170** | [0.125, 0.219] | 49–239 | 40 / 144 | 139/288 | 42.5 | 15.96 | 4 597 s |
| 42 000 | `46fdb931` | `best_model`, promoted r12 | **0.142** | [0.104, 0.188] | 41–247 | 33 / 144 | 145/288 | 45 | 18.82 | 5 419 s |
| 45 000 | `b2716a84` | cadence point | 0.094 | [0.059, 0.132] | 27–261 | 23 / 144 | 143/288 | 43 | 17.31 | 4 984 s |
| 48 000 | `393d46b8` | sealbot-rung peak (0.710) | **0.163** | [0.122, 0.208] | 47–241 | 40 / 144 | 151/288 | 46 | 14.60 | 4 204 s |
| 60 000 | `5822b265` | latest at launch | 0.111 | [0.080, 0.146] | 32–256 | 29 / 144 | 144/288 | 43 | 14.22 | 4 096 s |

Fence findings (legal sets compared on every strix move, R257): 0 in 1 728 games today; forfeits 0;
draws 0. "Seat-decided pairs": both games of an opening won by the same SEAT (the only way two games of a
colour-swapped pair can have different winners); the remainder were won by the same PLAYER.

## B. What it says

1. **run7 is above its start against strix, by 6–9 pp at the plateau.** 24k, 30k, 42k and 48k read
   0.139–0.170 against the BC net's 0.083 and the 9k anchor's 0.097; every one of those four has a lower
   CI above the BC point's estimate. The 15k reading (0.045, the trough R354 read) is confirmed as the
   low point, not the trend: the net recovered and held a band through 48k.
2. **The four plateau points are one reading, not a ranking.** 0.139 / 0.170 / 0.142 / 0.163 sit inside each
   other's CIs (±4–5 pp at 288 games); nothing here orders 30k above 42k or 48k above 24k. The gate's
   internal ordering (24k promoted over 9k, 42k over 24k, 30k NOT promoted over 24k) is neither confirmed
   nor contradicted at this power — separating 0.14 from 0.17 needs ≈ 1 000 paired games per point.
3. **Two dips, both seen by both instruments.** 45k (0.094) and 60k (0.111) are the low points since 24k;
   the sealbot rung (256 unit) read 0.622 and 0.540 at the same checkpoints, its two lowest since the
   resume. Rank agreement over the six shared checkpoints (24k–60k): sealbot orders 48k > 42k > 30k > 24k >
   45k > 60k, strix 30k > 48k > 42k > 24k > 60k > 45k — the same top three and the same bottom two.
   The 60k dip is the latest checkpoint; whether it is a trough or a turn is what the next cadence point
   (75k) answers.
4. **Strix keeps its range where sealbot has lost it.** Seat-decided pairs are 16–28 % here (12–13 % at
   step 0) against 37–57 % in every sealbot cell on the same book; p1 wins 48–52 %. The sealbot rung sits
   at 0.54–0.71 under a ≈ 0.78 book ceiling; strix has ≈ 83 pp of headroom left.
   [2026-09-21, R365(e): the ≈ 0.78 is the sealbot RUNG population's ceiling (seat-decided 42–47 %), not the
   book's — this section's own sealbot cells read 37–57 % (0.72–0.82) and the strix cells 16–28 % (0.86–0.92);
   the annotation under `RUN7_EVAL_COST_2026-09-15.md` §B carries the per-population reading.]
5. **Cost, beside the run:** 14.2–18.8 s per game at 8 in flight (7.0–7.8 s on an idle card), 68–90 min
   per 288-game cell, 7.7 h for the six. The run paid little: median self-play rate during the cells
   1 048 games/h against 1 069 in the six hours before, steps/h 1 614 against 1 730 (the eval rounds beside
   them are the larger term). The equal-work cell at 15k (strix 256, ours 256) was faster than cell A, so
   the wall is our PUCT-512 side on the shared card, not strix's CPU search.

## C. What it does NOT say

- Nothing about strix's absolute level; the instrument is a fixed external reference (R352(e)).
- 288 games at WR ≈ 0.15 carry ± 4–5 pp; the discriminating pairs are the 27–49 wins per cell.
- The plateau is read on `book_v1` (uniformly random 4-ply); `book_v2` is a separate instrument.
- These are OFFLINE cells on frozen checkpoints; they are not in the run's event record or ladder
  file, so the dashboard's strength panel does not draw them (its series come from `eval_ladder_state.json`).

## D. run7's throughput, in units that do not depend on game length (LAW-03)

Derived from `events_run7_seg0001/2.jsonl` at trainer step 69 506 (2026-09-17 16:36 UTC). The runner's
`avg_game_length` and `positions_per_hour` are in compound TURNS (`(plies + 1) // 2`); `game_complete.moves`
is in PLIES (stones). Both are given.

| segment | wall | steps | games | plies | mean plies/game | games/h | plies/h | turns/h | steps/h | eval rounds in flight |
|---|---|---|---|---|---|---|---|---|---|---|
| seg0001 (2026-09-14 17:30 → 09-15 19:24) | 25.9 h | 23 827 | 23 944 | 1.62 M | 67.5 (median 57) | 924 | 62 400 | 31 200 | 920 | 5 rounds, 17.3 h = 67 % |
| seg0002 (09-15 20:03 → 09-17 16:36) | 44.6 h | 45 687 | 45 714 | 3.34 M | 73.0 (median 63) | 1 025 | 74 900 | 37 500 | 1 025 | 15 rounds, 23.1 h = 52 % |
| **total** | **70.5 h** | **69 514** | **69 658** | **4.96 M** | 71.1 | 988 | 70 300 | 35 200 | 986 | 40.4 h = 57 % |

The inference server's leaves/s is flat at 3 190–3 200 in every row of both segments (its capacity, not
demand): ≈ 0.81 G leaves over the run. The trainer is game-bound (steps ≈ games, one step per game).
Self-play rate with an eval round in flight vs none (seg0002 medians): 1 030 vs 1 046 games/h, 38.0k vs
38.6k turns/h — the eval child costs the trainer ≈ 2 % of throughput, not the 40 % its wall share suggests.
Game length rose 60 → 73 plies over the run (seg0001 quarters 60.0 / 69.8 / 69.4 / 70.8; seg0002 flat at 73),
so games/h understates the work by ≈ 20 % across the run; plies/h and leaves/s do not.

## Sources

`/workspace/oc7/strix_60k/{summary.jsonl,<label>/cell.json,progress.txt,games/}` (mirrored:
`mantis-mirror/strix_60k/`), `/workspace/oc7/strix_60k.log`, `/workspace/oc7/strix_step0/`,
`/workspace/oc7/strix_best9k/`, `/workspace/oc7/strix_15k/`, `/workspace/runs/run7/logs/
eval_ladder_state.json` and `events_run7_seg000{1,2}.jsonl` (the rate script in the session scratchpad,
`run7_rates.py` / `run7_inround.py`), `tools/strength_frontier.py` at `ba51fd46`.
