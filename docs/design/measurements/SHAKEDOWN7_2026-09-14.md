# MEASUREMENT — run7's 4 h shakedown twin (`shakedown7`, 2026-09-13 21:50 → 2026-09-14 01:50 UTC)

Instrument: `/workspace/run_shakedown.sh /workspace/oc7/shakedown7.yaml /workspace/runs/shakedown7 14400`
on the box (RTX 5080, 24 cores), tree `ccfaf699`, the config whose ONLY delta from
`configs/run7.yaml` is `run_id` (`tools/config_diff.py --expect run_id` MATCH), its own preflight
stamp (`preflight_shakedown7_20260913T205402Z.json`, pass, MIRRORED). Ended by `timeout` at
14 400 s, rc 124 — the success path; **the LAW-16 save DID fire under `timeout` this time**
(`shutdown_requested` → `resume_bundle_published` step 2 363 → `terminal_eval_skipped_on_signal_stop`,
exit in 3 s), which is the falsifier CARD-SHAKEDOWN-TIMEOUT-STOP asked for on the other side.
Record mirrored to the operator's `mantis-mirror/shakedown7/` (events, card.csv, launch log;
game shards left on the box). No F-816-37 dump, no stray process, no `training_alert`, no
`hard_abort`, disk 83 G free.

## A. The rate and the rounds

| | shakedown7 (PUCT 320/64 at p 0.25, τ 0.5) | run6 (Gumbel 320/64, block) |
|---|---|---|
| steps in 4 h | **2 363** (first at +223 s) | — |
| steps/h (stepping window) | **600** (games/h 606; 968 → 606 over the run) | 1 163 whole, 1 170 late |
| avg game length (cumulative) | 35 → **120 plies** | 23 → 42 plies |
| draw rate (cumulative, `iteration_complete`) | 0.00 → **0.31** | 0.002–0.005 |
| card peak | **10.98 GiB** (mean util 89 %) | 7.66 GB |
| `mcts_mean_depth` | 2.4–2.5 | 3.7–3.9 |
| eval rounds | **none** — step 3 000 was never reached | — |

The step-3 000 round — the reading R351's rows were minted around — never came: at 600 steps/h
the 3 000-step cadence is a 5 h interval, and the 25 001-step block is ≈ 42 h, not 21.5.

## B. The draw attractor, windowed (300 games each, `terminal_reason` off `game_complete`)

| games | wall | at the 256-ply cap | median moves |
|---|---|---|---|
| 0–300 | +0.4 h | 0.11 | 89 |
| 300–600 | +0.6 h | 0.01 | 65 |
| 600–900 | +0.9 h | 0.05 | 83 |
| 900–1 200 | +1.3 h | 0.16 | 133 |
| 1 200–1 500 | +1.9 h | 0.47 | 235 |
| 1 500–1 800 | +2.4 h | 0.34 | 185 |
| 1 800–2 100 | +3.1 h | 0.47 | 224 |
| 2 100–2 400 | +3.9 h | **0.84** | **256** |
| 2 400–2 421 | +4.0 h | 0.86 | 256 |

755 of 2 421 games ended by `ply_cap`, 1 666 by `six_in_a_row`. The drift begins during the value
warm-up (policy weight 0, the prior untouched — only the TARGETS and the value head move) and
accelerates once the policy trains (step 2 000 →): by the last hour five games in six run to the
cap. This is the F-02 / F-22 class (ply-cap draws climbing toward 90 %) — the attractor the
armed `train.draw_rate_abort {0.25, 25 000, 50, 3}` exists for, and at this trajectory it would
fire at step 25 000 on a run that had been junk for a day.

## C. The losses under the warm-up

| step | policy loss (measured; weight 0 until 2 000) | value loss |
|---|---|---|
| 1 | 1.77 | 0.64 |
| 296 | 3.02 | 0.63 |
| 886 | 2.52 | 0.51 |
| 1 771 | 2.88 | 0.26 |
| 2 066 (weight 1.0) | 2.31 | 0.29 |
| 2 361 | 1.75 | 0.27 |

The dashboard's trough input reads WARN on this (1.77 → 3.02 inside the first 300 rows) — the
prior's CE against targets that drift while the value head trains, the signature R351(d) demoted
to a warning; the halt would have killed this run at step ≈ 900 for a rise that reversed.

## D. What the shakedown says about the mint

1. **`eval.rung_concurrency: 8` did what it was minted for** — measured on the two preflights,
   not here: 392-game terminal rounds in 2 782 s and 2 834 s inside the 3 600 s timeout. This
   shakedown played no round, so the row's cost against training is still unmeasured.
2. **PUCT self-play at the minted regime is NOT a viable trainer for run7 as it stands.** R351(c)
   disclosed PUCT as "the measured reader, untested as a trainer at this regime"; 4 h of test say
   it drifts to the ply-cap attractor at 600 games/h with a 2.5-ply mean search depth. The Gumbel
   trainer of run6 — decisive 23–42-ply games at 0.5 % draws, 1 170 games/h, a net PUCT reads
   15 pp above its prior — was the alternative the ruling weighed and set aside because no σ
   pair was alive AS A DEPLOY HEAD; the frontier never measured Gumbel as a trainer, and this
   shakedown does not either. The self-play-kind row is back on the operator's desk with this
   evidence; START does not happen on this mint.
3. Under the warm-up the value head's early cost (0.27 / 0.40 vs `sealbot_d5` at step 101 on the
   two preflights, 0.225 vs the anchor) sits beside a rising draw rate; which of the two the
   warm-up causes is not separable on this record.

## E. The R352(c) halt, read on this record (added 2026-09-14 at the re-mint)

`tools/dashboard/health.py::ply_cap_windowed` over the 2 421 `game_complete` rows at the minted
terms {rate 0.5, window 600}: the window first exceeds 0.5 at game 2 229 (+3.43 h, train step
≈ 2 170, rate 0.502) and reads 0.68 at the end. `min_step` 3 000 was not reached in 4 h at 600
steps/h, so on this trajectory the halt's fire would have come at step 3 000 (≈ +5 h), not at
+1.9 h as the packet's note estimated from the 300-game table in §B (whose +1.9 h window reads
0.47). The dashboard's ninth health input reads this record `bad` at those terms.

## Sources

`mantis-mirror/shakedown7/logs/events_shakedown7_seg0001.jsonl` (13 381 rows), `card.csv` (2 880
samples), `shakedown7.launch.log`; run6's `events_run6_seg0001.jsonl` for the comparison column;
the two preflight records for the terminal-round walls.
