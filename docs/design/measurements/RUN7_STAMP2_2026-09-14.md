# MEASUREMENT — the re-minted run7's preflight stamp (`d13bc7c3`, 2026-09-14 05:27 UTC →)

Instrument: `/workspace/oc7/box_preflight_run7.sh configs/run7.yaml /workspace/runs/run7-preflight 101 900`
on the box (RTX 5080, 24 cores), tree `d13bc7c3` (R352's re-mint: Gumbel-320/64 self-play with
every head loaded, warm-up 0, the ply-cap halt armed; `deploy.search.kind: puct`, eval 512,
`eval.rung_concurrency 8`), the puller cycling from the operator's machine. The previous stamp of
this config family (`ccfaf699`, PUCT self-play) is the comparison column.

## A. The burst (101 steps)

| | this stamp (`d13bc7c3`, Gumbel self-play) | `ccfaf699` (PUCT self-play) |
|---|---|---|
| child wall to step 101 | ≈ 6 min (05:27 → 05:33) | ≈ 12 min |
| games at step 101 | 195 | — |
| avg game length | 23.8 plies | 35 → (drifting) |
| draw rate | 0.0 | 0.0 (then 0.31 over 4 h) |
| `alpha_full_row` events | 19 | 0 (PUCT has none) |

Run6's Gumbel character from the first minute: short decisive games, no draws.

## B. The terminal round — the reading the mint's rows are for

Round `r000001_101_terminal`, started 05:33:23 UTC, complete 07:09:45 (`wall_sec` 5 782), verdict
**pass**, stamp `preflight_run7_20260914T052717Z.json` (config identity `1676730c…`, MIRRORED,
`run7_00000101_299e6dd1.ckpt`). `eval.round_timeout_sec` is the schema default 3 600 and did NOT
bind this round — the terminal round runs under the drain caps (hard cap 14 400) — but it binds
every IN-RUN round, which is §C's point.

| phase | games | candidate wins | median plies | p90 | max | wall |
|---|---|---|---|---|---|---|
| floor probe | 4 | 4 | 15 | 17 | 17 | 0.1 min |
| gate screen vs the step-0 anchor (PUCT-512, 8 in flight) | 80 | 39 (**0.49**) | 37 | 85 | 141 | ≈ 15 min |
| gate CONFIRM (escalated: 0.49 ≥ `screen_confirm_lo` 0.44) | 128 | 68 (**0.53**, 1 draw; not promoted at 0.55) | 45 | 91 | 256 | ≈ 21 min (→ 06:21) |
| `sealbot_d5` rung, 288 paired games, PUCT-512, 8 in flight | 288 | **146 (0.507)** | 35 | 69 | 137 | ≈ 48 min (→ 07:09) |
| random floor | 20 | 20 | 15 | 17 | 19 | 0.1 min |

**101 Gumbel steps on every head read 0.507 against `sealbot_d5` at PUCT-512** — the `ccfaf699`
stamp's net (value-only warm-up) read 0.271 on the same construction and the BC net reads 0.413
at PUCT-150 (`STRENGTH_FRONTIER_1`, `bc_full`); run6's frozen 3k checkpoint read 0.63 at 512.
The gate reads the same net at parity with its own anchor (0.49 / 0.53), so the point is not a
gate artefact. The dispatcher's earlier reading of this round — "no rung game in an hour" — was
an artefact of the rung block's progress writer, which lands its rows in one batch at block
end; the rows are dated 07:09:42–45 and the rung's wall is the 06:21 → 07:09 span.

## C. What the round's wall means for the run

1. **Every in-run round at this cost would be KILLED.** The eval pipeline bounds a round by
   `eval.round_timeout_sec` (3 600, the schema default — an operational constant no run has
   minted). This round took 5 782 s; without the confirm escalation it is still ≈ 63 min
   (screen 15 + rung 48), over the bound, and under training contention longer. The `ccfaf699`
   stamp's 2 782 s fit only because its net lost fast (rung median 31 plies, 2 084 s). The row is
   re-minted at **10 800 s** (an operational ceiling, not a target; §D says what the round costs)
   and the twin shakedown measures the contended round.
2. **The vendored sealbot holds the GIL through its search**, so `rung_concurrency 8` overlaps
   only the candidate's side: `vendor/external/sealbot/current/minimax_bot.cpp` binds `get_move`
   with no `py::gil_scoped_release`. The extraction (`extract_game_state`, Python objects) and the
   search (`engine.get_move(gs)`, pure C++ over per-instance tables; the shared offset/zobrist
   tables are built in the constructor under the GIL) are already separate, so the release is a
   third hunk of `vendor/patches/sealbot.patch` that changes no move and no depth receipt. Sealbot
   d5's own move time is small — measured here on the round's longest positions (plies 10–90,
   436–757 legal cells) and on shakedown7's 200-ply positions (893 cells): 0.0–3.3 s per move —
   so ≈ 288 games × ~17 sealbot moves × ~0.5 s ≈ 40 min of the rung's 48 min is the serialised
   sealbot share. The hunk is the lever that makes the ratified row true (R351: "8 is the gate
   block's G"); it is R145-class (a patch change) and is proposed in §D with its verification.

## D. The rung's cost, measured

(filled when the cells complete)

## E. The third stamp — the GIL-patched tree `2177c926` (14:44 → 16:13 UTC, PASS, MIRRORED)

| phase | games | candidate wins | median plies | p90 | wall | per ply |
|---|---|---|---|---|---|---|
| floor probe | 4 | 4 | 15 | 15 | 5 s | — |
| gate screen | 80 | 40 (0.50) → escalates | 43 | 123 | **25 min** | 0.31 s |
| gate confirm | 128 | 51 (0.40, not promoted) | 47 | 113 | **36 min** | 0.29 s |
| `sealbot_d5` rung, 288, 8 in flight, GIL released | 288 | **183 (0.635)** | 31 | 49 | **21 min** | **0.12 s** (stamp 1: 0.23) |
| random floor | 20 | 20 | 15 | 17 | 21 s | — |

Round `wall_sec` 4 941. The rung's wall halved per ply as CARD-SEALBOT-GIL-SERIAL predicted; the
gate block's per-ply cost ROSE 0.20 → 0.30 s on an otherwise idle card (no other process, load
≈ 8.5 = the eight game threads) and is not explained here — recorded as the observation that
moved the timeout to 14 400 (prereg §8).
