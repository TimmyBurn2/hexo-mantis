# PREREG — run10: THE POLICY-CAPACITY RUN (R366, 2026-09-21): run9's base + the soft-policy head, ONE hypothesis (LR cosine 1e-3 → 1e-4), two conditionals settled in the preflight window, four in-run witnesses

Written 2026-09-21 at the R366 landing, from `RUN9_PREREG_2026-09-19.md` (run9 NOT STARTED, R365(a);
its mint is run10's BASE and is not re-litigated here). The ruling is `docs/governance/RULINGS.md` R366;
its grounds are `PROBE1_2026-09-21.md` rows 1–6, `PERF3_2026-09-18.md` step 3, the E1 receipt, F-53,
`PARAM_DISTANCE_2026-09-21.md` §2 and R365(c). `configs/run10.yaml` is MINTED (§1); the stamp is the
box's, written once, after the two conditionals (§2) are settled — a re-mint for a conditional is its
own preflight (R360(c): a twin inherits a stamp only when it differs in `run_id` alone). Nothing here
runs on the box from this file.

## 1. The config — `configs/run10.yaml`, MINTED 2026-09-21 over run9's header

`tools/mint_config.py --template dev`, replaying `configs/run9.yaml`'s header (its 34 `--set` and 6
`--mint-row` rows) with the rows below changed or added — ONE command, the header replays
(`config_diff --from-header configs/run10.yaml` MATCH), and `tools/config_diff.py configs/run9.yaml
configs/run10.yaml --expect <each leaf below>` says **MATCH** on exactly these TWELVE leaves and nothing
else:

`run_id` · `identity.arch_kind` · `identity.warm_start.reinit` · `model.aux_soft_policy.target_temperature`
· `model.aux_soft_policy.weight` · `train.scheduler_t_max` · `train.eta_min` · `train.heldout_gap.ring` ·
`train.heldout_gap.ring_sha256` · `train.heldout_gap.batches` · `train.heldout_gap.seed` ·
`train.heldout_gap.interval`.

Byte-equal elsewhere, by construction: the parent run8@45k (`identity.warm_start.checkpoint`
`checkpoints/run8/run8_00045000_3bdedf76.ckpt`, file sha256 `c4990d03…`, net `3aef7883…`, unchanged),
σ raw (`selfplay.q_rescale: false`), `train.augment: true`, the DATA REGIME (`train.replay_capacity`
500 000, `train.training_steps_per_game` 2.4 inside the envelope [2.0, 3.0], `train.max_train_burst` 8,
the budget's fractional carry), `train.eval_interval` 36 000 = 15 000 games, the gate as REGRESSION
GUARD (`eval.gate.sequential` H0 0.42 / H1 0.52, cap 104, `at_max_pairs: promote`), `dirichlet_enabled:
false` with its pin, Gumbel 320/16 at p 0.25, the deploy head PUCT-256, `n_workers` 32, the seed
`20260914`, `model.gnn: {hidden: 128, num_layers: 4}` (v35: the trunk's shape is a mint fact now; run10
mints the shape every stamp on record was built at), `train.ema.enabled: false`, the aborts as run8
armed them (gate 12 rc 0 with `actor_lag` RETURNED to the REQUIRED set — CARD-SERVER-OWNED-COPY landed).

### 1a. The rows that changed, each with its why

| key | run9 | run10 | why |
|---|---|---|---|
| `identity.arch_kind` | `GnnArchV2` | **`GnnArchV2SoftPolicy`** | THE SOFT-POLICY HEAD (R366(b), probe-admitted: PROBE-1 reading 5, median KL(prior‖target) 6.58 nats ≥ 0.1 by 65×; CARD-RUN9-QUEUE (vii)). V2's trunk and heads plus `aux_policy_head`, a second `PolicyHead` over the same legal embeddings; `forward_batch` / `forward_single` serve V2's outputs EXACTLY (`tests/model/test_gnn_v2_soft_policy.py`), so the server, the gate, the follower and every deploy reader see the same function they saw for V2 — only the trainer's `forward_batch_heads` evaluates the second head |
| `identity.warm_start.reinit` | `[]` | **`[aux_policy_head]`** | the parent never had the head: `load_from_bc` admits a `reinit` head ABSENT from the source (every other tensor strict both ways, byte-equal on landing), the head starts at its fresh init. The step-0 hash-equality witness is therefore OFF (a non-empty `reinit` — as run7's value-head reinit was); `bc_warmstart_loaded` still refuses any source but `3aef7883…` |
| `model.aux_soft_policy.target_temperature` | `null` (v36: the key did not exist) | **`4.0`** | KataGo's T = 4. The TARGET (`mantis.train.losses.soft_policy_target`): the searched target's EXPLICIT entries raised to 1/T and renormalised over the explicit mass 1 − α, the tail carried as the hard target rebuilds it (α over the detached prior). NOT the whole-set `target^(1/T)`: measured on run8@45k's ring (267 full-arm rows, the production sample), α reads median 0.000 / p90 0.020, and `target^(1/4)` over ~500 legal nodes put a median **40 %** (p90 60 %) of the soft mass on a tail of ~1e-4 per node — an artefact of the sparse row (KataGo's unvisited moves are exactly 0, as the tail's α is here). Under the explicit-only construction KL(t‖soft) reads a median **0.27 nats, 76 % of rows > 0.1** (the whole-set form 0.79, PROBE-1's 0.76) — the head still learns a target the main head does not, and the admission line holds |
| `model.aux_soft_policy.weight` | `null` | **`4.0`** — the ENVELOPE is [2, 8] | the dispatcher's pick at the envelope's geometric mean; KataGo's nominal 8 is their calibration, not ours (R366 §0(2)). The RULE: the weight is picked in the twin so the aux head's gradient norm ≈ the main head's — read off `trainer_step.aux_policy_head_grad_norm` / `policy_head_grad_norm` (the pre-clip L2 norm over each head's OWN parameters) over the twin's settled window; a ratio outside [0.5, 2] → re-pick inside [2, 8] by re-mint with its own preflight; no value in the envelope reaching it → HALT to the architect with the numbers. §1b carries the CPU pre-read that informed the pick |
| `train.scheduler_t_max` | `null` (→ `total_steps` 1 000 000, LR effectively flat: 9.97e-4 at 51k, `PARAM_DISTANCE` §2) | **`108000`** | THE ONE HYPOTHESIS (R366(b), un-probed): cosine 1e-3 → 1e-4 over 45k games × 2.4 steps per game. `train.total_steps` stays 1 000 000 by "byte-equal elsewhere"; the anneal rides `scheduler_t_max` (`Trainer._build_scheduler` prefers it). Pinned through the trainer's own scheduler: `tests/train/test_lr_schedule_run10.py` reads 1e-3 at 0, 5.5e-4 at 54 000, 1e-4 at 108 000. DISCLOSED: torch's `CosineAnnealingLR` is PERIODIC past T_max — over the ≈ 1 500 steps the 45k-games cell runs after 108 000 the LR climbs by < 1e-6 (pinned), and at 2 × T_max it is back at the peak; the STOP LAW (§4) is the guard, not the scheduler |
| `train.eta_min` | `0.0005` | **`0.0001`** | the anneal's floor, 1e-4 — a 10× decay (CARD-RUN9-QUEUE (ii) named 2e-4 → 2e-5; R366 keeps the 1e-3 peak run8 trained at so the drift reading (`PARAM_DISTANCE`: the 3k-step displacement flat at 19–24 under the flat LR) has a matched start) |
| `train.heldout_gap` | `null` (v37: the key did not exist) | **`{ring: checkpoints/run8/run8_00045000_3bdedf76.ckpt.ring.bin, ring_sha256: 2a3c4e57…, batches: 12, seed: 20260921, interval: 3000}`** | THE HELD-OUT WITNESS (R366(c); PROBE-1 reading 3 named it): a FROZEN slice of the parent's 45k ring (100 000 rows, 40 443 777 bytes, sha256 `2a3c4e57815b962a81773e170903153015f42f31d445ecb85563096e6468eba3` off the mirror; the box copies it beside the parent and the boot refuses any other file) that run10 never trains on, read forward-only every 3 000 steps through `run_declared_eval_step` (12 production samples of 256, the sampler re-seeded before every read so the rows are the same rows), reported as `heldout_gap` against the mean train loss of the taken steps since the last read. `interval` 3 000 = the checkpoint cadence, so every ring on the record has a reading beside it |

### 1b. The weight envelope's pre-read (CPU, dev, 0 box-h) — a fact for the twin's pick, not a decision

`aux_burst.py` (the landing session's scratch, kept OFF the tree with its JSON — R7; the numbers are §8): the 45k parent warm-started onto
`GnnArchV2SoftPolicy` through the production seam (`apply_bc_warm_start`, `reinit: [aux_policy_head]`),
trained on its own 45k ring on this workstation's CPU at run10's rows (batch 256, augment on, grad clip
1.0, T 4, weight 4) for 60 steps — §8 carries the numbers. What it establishes before the twin: the FRESH
head's CE starts at ≈ ln(legal) ≈ 6.2 nats and its own gradient norm starts an order of magnitude above
the trained main head's, so under `grad_clip` 1.0 the first steps' clipped update is mostly the aux
head's; the rate at which the ratio settles is what §8 reads and what the twin re-reads at scale. The
witness for the pick is the SETTLED ratio, never the first steps'.

### 1c. The parent and the regime — RULED by R364(b), carried by R365(a)

Unchanged from `RUN9_PREREG_2026-09-19.md` §1b–§1d: the parent rule's output (run8@45k, the monotone
triple 0.104 → 0.135 → 0.142), the data regime with its envelope and the dispatcher's rule on the shakedown
(`replay_ratio` in [7, 9] → 2.4 stands; outside → re-pick inside [2.0, 3.0] by re-mint with its own
preflight, `train.eval_interval` following; unreachable → HALT), the `dirichlet_*` rows and their pin.

## 2. The two CONDITIONALS — settled in the preflight window BEFORE the stamp (R366 §0(2))

**Net size, `model.gnn: {hidden: 192, num_layers: 6}` — rides ONLY if `tools/bench_server.py` in the
preflight window (the box, `--device cuda`, no `--checkpoint`: a random-init net of the config's shape,
200-s cells) reads leaves/s at B 64 ≥ 0.75 × the 4×128 net's at B 64 on the same box in the same session;
else it is run11's row.** The leaves exist (v35) and the bench builds the net from them
(`--config <a run10 copy with model.gnn set>`). **A PROBLEM THE PACKET DID NOT PRICE, the operator's
ruling before any re-mint arms it:** a 6×192 `GnnNetV2` shares NO tensor shape with the 4×128 parent
(`input_proj` 11→192, every conv 192→192, the JK-cat readout 6 × 192 = 1 152 wide into both heads against
512), and `load_from_bc` is strict both ways — the size conditional CANNOT ride on the run8@45k warm start
as §0(2) states it ("parent: run8@45k (unchanged)"). If the bench admits the size, the run needs its OWN
parent (a fresh net, or a BC pretrain at 6×192, or a net-growth transfer that is a design in itself) and
"equal games vs 0.142" is then a different comparison. Until that ruling the conditional's ARMING is
withheld whatever the bench reads; the bench is still run and recorded (it prices run11's row either way).

**EMA deploy, `train.ema.enabled: true` — rides ONLY if the EMA cell (`tools/strix_follower.py --once
<run8swa ckpt> --unit equal_work`, the SWA net `run8swa_00051000_ae478dee.ckpt` in the mirror root, net
`61b09ba8…`, 288 paired games at 256/256, 1.5 bh, the regime labelled) reads > 0.181 (the parent's CI upper
bound: a ≥ 4 pp move at this cell's resolution); ≤ 0.142 → dead; between → the LR floor stays run10's
hypothesis and EMA waits (PROBE-1 §7's line).** The prerequisite, CARD-SERVER-OWNED-COPY, is BUILT: the
inference server serves its own copy of the declared arch (`mantis.selfplay.pool.served_copy`), ActorSync
writes it from `Trainer.actor_state_dict` — the LEARNER's weights, EMA on or off — and the learner never
reads it (the hash witness, `tests/train/test_server_owned_copy.py`); with EMA on the DEPLOY net is the
shadow: `Trainer.inference_state_dict` / `deploy_module` for the gate's candidate and promotion,
`ema_state` on the envelope (contract `checkpoint_envelope.md`) for the follower's cell and the ladder bot
(`deploy_state(ck)`, the receipt says `weights: ema`), restored on resume. Arming it is `--set
train.ema.enabled=true` on a re-mint (decay 0.999, `update_every` 10 as minted) with its own preflight.

Either re-mint replays THIS file's §1 through run10's header with the conditional's row added, and its
`config_diff --expect` line joins §1's twelve.

## 3. The in-run WITNESSES (R366(c)) — producers, not bands alone; read on the twin and at every 36k point

| witness | producer (LAW-07/LAW-18) | the twin's band | what a miss means |
|---|---|---|---|
| held-out gap, value | `heldout_gap.gap_value` every 3 000 steps (§1a; `tests/train/test_heldout_gap.py`, the freeze's planted break) | **≤ 0.26** (2 × run8's 0.13 of 0.51, PROBE-1 §3) | the reuse regime MEMORISING its 500k window at 7.7 samples per row → `training_steps_per_game` re-picked inside [2.0, 3.0] by re-mint with its own preflight; ≤ 0.13 with the train loss falling → learning |
| held-out gap, policy | `heldout_gap.gap_policy`, the same row | reported (run8: 0.09 ± 0.035 on a flat 2.1-nat loss, three nets negative) | a policy gap rising with the value gap is the same memorising read in the head the run is meant to move |
| per-mr one-hot shares | `mantis.diagnostics.ring_audit` rows `one_hot_share_full_mr1` / `_mr2` and `tail_only_full` (PROBE-1's decomposer as audit rows; `tests/diagnostics/test_ring_audit.py`) | **pooled < 0.30, mr1 < 0.40, mr2 < 0.24** (§3a; run8's settled maxima 0.284 / 0.368 / 0.200 + ≈ 3 pp) | run7's 62–65 % collapse in either population; a pooled miss with both per-mr rows inside is an instrument question, not a run's |
| KL(prior‖target) median | `trainer_step.policy_kl_target_vs_prior` (the trainer's own direction, KL(t‖p) = CE − H, run8: 1.21 median) — the prior-direction median PROBE-1 read (6.58) has NO in-run producer at HEAD; the box reads it off the 36k rings with `tools/probe1.py netread` | reported | a KL that falls toward 0.1 while the soft head trains is the head's mechanism exhausting itself |
| aux-loss row | `trainer_step.aux_soft_policy_loss`, `aux_soft_policy_kl_hard_vs_soft`, `policy_head_grad_norm`, `aux_policy_head_grad_norm` (`tests/train/test_aux_soft_policy.py`, the planted break: a construction returning the hard target reads KL 0) | the head-norm ratio in [0.5, 2] over the twin's settled window (§1a's rule); `kl_hard_vs_soft` > 0.1 on the median step | a ratio outside → the weight re-picked; a KL at 0 → the construction is dead, HALT |
| the R357 witness | the QSigma pin on the box tree + the step-3 000 ring's full-arm median H(explicit) > 0.02 nats | unchanged | FAIL → HALT (wiring) |
| the A-2 bands | `counter_threat_share`, `quiescence_residue`, `cap_rate` | unchanged (§3a) | as run9's |
| replay ratio | `ring_audit --events`: `replay_ratio` | **[7, 9]** (§1c's rule) | outside → 2.4 re-picked |

### 3a. The ring audit's bands for run10 (`mantis.diagnostics.ring_audit <ring> --bands <this file> --events <jsonl>` reads this block)

The 4 h twin (`python -m mantis.run --inherit-preflight configs/run10.yaml`, R360(c); a twin reaches its
3k checkpoint in ≈ 1.3 h and its 6k and 9k inside the budget) runs NO gate round (`train.eval_interval`
36 000 is 15 h away); the regression-guard rule is exercised by the PREFLIGHT's terminal round. The
witness, on the step-3 000 ring (or the 6k ring if 3k predates the buffer's fill — the audit says
which): **(a)** the QSigma pin GREEN (FAIL → HALT, wiring); **(b)** the bands below, each strict, a
banded row the ring cannot measure a MISS; **(c)** `replay_ratio` in [7, 9]; **(d)** `sym_bin0_over_mean`
≈ 1.0 and the full-arm median H(explicit) READ beside its band; **(e)** the `heldout_gap` rows at 3k / 6k
/ 9k off the twin's events file, `gap_value` ≤ 0.26 on each; **(f)** the aux rows' settled ratio (§1a).
Only the PRE-START audit HALTS (R359(b)); a miss on a live 36k ring is REPORTED.

```toml
[ring_audit.bands]
counter_threat_share = { lt = 0.005 }     # run7 pre-fix 2.5–4.4 %; run8 read 0.009–0.031 % on every ring
quiescence_residue = { le = 0 }           # the A-2 falsifier's oracle; run8 0 of 4–8 on every ring
h_full_median = { gt = 0.02 }             # nats, is_full_search rows; run8 0.20–0.29 (the 45k ring 0.286)
cap_rate = { lt = 0.05 }                  # games with value_valid == 0; run8 0.16–2.2 %
replay_ratio = { ge = 7.0 }               # §1c; the upper half (≤ 9) is read, not banded (the tool bands one operator per row)
one_hot_share_full = { lt = 0.30 }        # RE-DERIVED (PROBE-1 §1): run8's settled envelope [0.257, 0.284] + 1.6 pp; refuses run7's 0.62–0.65 by 2×
one_hot_share_full_mr1 = { lt = 0.40 }    # the second stone: run8 0.297–0.368 on nineteen rings (the decomposer's row)
one_hot_share_full_mr2 = { lt = 0.24 }    # the first stone: run8 0.148–0.200
```

`tail_only_full` is REPORTED (run8: 0 on every ring; a non-zero count is what the pooled share's upper
bound is an upper bound BY). The band block PARSES and reads run8's 45k ring as expected: the five
carried bands PASS, the three one-hot bands PASS (0.2705 / 0.3502 / 0.1886), `replay_ratio` 3.03 MISS
(run8's regime at 1.0 step per game, read over the mirror's full events file — the miss the run10 regime
exists to move; rc 1 on the parent's ring, as run9's block read it at 3.06 over a shorter span).

## 4. The PRE-REGISTERED READING at EQUAL GAMES (R366 §0(4)), strix @ r8, 256/256

Instrument unchanged from run9's §3 (the follower's equal-work cell, 288 paired games on
`book_v1_s20260625_p4`, CONTENDED labelled, LAW-04's pair-level CI), fired at every `train.eval_interval`
checkpoint = 15 000 GAMES at 2.4 steps per game (36 000 / 72 000 / 108 000 steps), `--follow
--no-promotions --cadence 36000 --unit equal_work`. **THE UNIT IS strix @ r8** (R366(d), E1: the same
parent reads 0.080 at strix's trained radius 6 against 0.142 at the driver's default 8, the CIs disjoint —
every follower point on the record and every bar below is priced at r8; r6 is a QUALIFIER, read once
per run at block end if the box allows, 1.2 bh). The BASELINE is the parent, run8@45k: **0.142 [0.104,
0.181]**. E4's resolution statement (run9's §3) stands beside every cell: ± 3.9 pp at the baseline; the
cell resolves ≥ ≈ 4 pp and cannot resolve run8's +0.7 pp per 15k slope, which is why run10's
composition carries a head, a regime and an anneal together (R365(c)) rather than one knob.

- **SUCCESS**: run10@30k-games (step 72 000) ≥ **0.192** (parent + 0.05).
- **FALSIFIED**: run10@15k-games (step 36 000) ≤ **0.142** AND run10@30k-games ≤ **0.142** — the
  composition is dead at this resolution, filed in `falsified.md` by row (the head, the regime and the
  anneal are NOT separable by this reading; the witnesses of §3 say which mechanism did what).
- otherwise **INCONCLUSIVE**: read 45k-games (step 108 000) **THEN STOP** (R364(e), carried by
  CARD-RUN9-STOP-LAW to this line unspent).

**STOP LAW:** run10 stops at 30k games, or at 45k games on INCONCLUSIVE; no further step without a
ruling. One SIGTERM to the supervisor once the deciding cell is read; `train.max_train_steps` stays
1 000 000 by "byte-equal elsewhere". The LR's periodic tail (§1a) makes the 108 000-step point a hard
edge for the trainer too.

**Box-hours beside every point:** as run9's §3 — the wall from START, steps/h and games/h over the
span; ESTIMATE ≈ 16 h per 15 000 games if run8's games/h holds; the twin's games/h replaces it. The aux
head adds one MLP over the legal embeddings per training forward (the served forward is V2's) — the
twin's steps/h against shakedown8's ≈ 1 000 is the price, reported.

## 5. Gate 12 at the mint — READ 2026-09-21

`tools/ci_gates/preflight_mint.py --audit-only` rc 0 with `configs/run10.yaml` in `PRODUCTION_CONFIGS`,
bound by name beside run6–run9; the `actor_lag` row is BACK in the REQUIRED set (run10 arms it, as
every production config did) and THREE rows stay DEFERRED (`policy_loss_trough`, `ply_cap_attractor`,
`grad_norm_hard_abort`). Gate 7: every config validates (all seven re-minted through their own headers
at v35 → v36 → v37, byte-identical elsewhere; the box's run8 stamp predates the `model` section and
`train.heldout_gap` and the loader tolerates it — `RETIRED_STAMP_PATHS` unchanged, a grown leaf is
tolerated by `_validate_stamped_config`).

## 6. Sequence (R366 §2 and §0(6))

`dev` (this landing): the schema and the head → the held-out witness → the server-owned copy → the mint
and the pins → this prereg → R366 in the register, the cards, STATE → the exit sweep → push (the operator's).
Box (a FRESH instance; the old one is destroyed, R365): `make vendor` → `make vendor.strix` → `make
build.cuda` → the parent `run8_00045000_3bdedf76.ckpt` (sha `c4990d03…`) AND its ring
`run8_00045000_3bdedf76.ckpt.ring.bin` (sha `2a3c4e57…`) copied from the mirror into the run tree's
`checkpoints/run8/`, the SWA net `run8swa_00051000_ae478dee.ckpt` beside them → **run10 preflight** on
the pushed mint commit (its terminal round runs the regression guard; the boot opens the held-out slice
and logs `heldout_slice_opened`) → in the preflight window: the SIZE BENCH (`bench_server.py` at 4×128 and
at 6×192, B ∈ {16 … 256}, 200-s cells, `--device cuda`, IDLE) and the EMA CELL (1.5 bh) → the two
conditionals settled (§2: the size row waits on the parent ruling regardless; the EMA row re-mints on
> 0.181) → the stamp (LAW-12, once) → the 4 h twin `--inherit-preflight` → the witness (§3, §3a: the QSigma
pin, the eight bands, `replay_ratio` 7–9, the `heldout_gap` rows, the aux ratio, sym bins, entropy; the
dispatcher reports each and the value that stands) → **START** → `tools/strix_follower.py --follow
--no-promotions --cadence 36000 --unit equal_work --poll-sec 300`. Report: numbers, regime (CONTENDED/IDLE
per cell), box-hours per point.

## 7. What this prereg does NOT claim

PERF-4 (the CPU launch stage, 0.34–0.39 ms per leaf at every B, R366(e)) is its own packet; nothing perf
rides run10. Proof-as-target is DEAD (F-53) and the mid-turn value premise CLOSED (E3) — neither is a row.
ARCH-D6 stays parked. The soft head is a probe-supported change under R365(c), not a hypothesis: its
reading is the §3 rows, and the §4 reading cannot attribute a move to it.

## 8. The weight envelope's CPU pre-read (§1b) — numbers

60 real steps at run10's rows (batch 256, augment on, `grad_clip` 1.0, T 4, weight 4) on this workstation's
CPU (8 torch threads, ≈ 20 s per step), the parent's 45k ring as the buffer, seed 20260921; medians over
step windows off `trainer_step`:

| steps | aux CE | KL(hard‖soft) | policy CE | value | grad norm (pre-clip) | `policy_head_grad_norm` | `aux_policy_head_grad_norm` | ratio aux / main |
|---|---|---|---|---|---|---|---|---|
| 1–5 | 5.71 | 0.306 | 2.33 | 0.537 | 13.3 | 0.528 | 12.82 | 24 |
| 11–20 | 3.12 | 0.280 | 2.25 | 0.528 | 10.6 | 0.939 | 5.36 | 5.7 |
| 31–40 | 2.62 | 0.292 | 2.17 | 0.517 | 9.0 | 0.513 | 1.74 | 3.4 |
| 51–60 | 2.49 | 0.302 | 2.10 | 0.501 | 8.0 | 0.493 | 1.56 | 3.2 |

What it reads: the fresh head's CE falls 5.7 → 2.5 in 60 steps and its own gradient norm 12.8 → 1.6, the
ratio to the main head 24 → 3.2 and still falling at the window's end — NOT SETTLED at 60 steps, so the
twin's window is the reading and the pick stands as the envelope's; if the ratio plateaued at ≈ 3 the rule
of §1a would re-pick the weight at the envelope's bottom (2 → ≈ 1.6, inside [0.5, 2]). KL(hard‖soft) holds at
0.28–0.31 nats per step (the aux head's target is a distinct one, §1a). The pre-clip total norm 8–13 against
`grad_clip` 1.0 says the clipped update is dominated by the aux head's share for the first tens of steps
and by the trunk's after; the main head's own share of the pre-clip norm is ≈ 6 % at step 60. The
policy/value losses over the window read as the parent's own train loss on its ring (PROBE-1 §3: 2.14 /
0.52 at 45k) — the trunk is not moved off its parent by the head's first minute. A fact for the twin, not a
verdict.
