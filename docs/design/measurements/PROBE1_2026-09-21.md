# PROBE-1 — six readings on run8's mirrored checkpoints and rings, each against its pre-stated line (R365(b), 2026-09-21)

Ordered by R365(b) and the packet's §2: dev + mirror, 0 box-h, one record. Every row below carries its
instrument, n, number, the run10 change it admits or kills, and the line that was stated BEFORE the
number was read (copied from R365's entry; nothing here re-states a line after reading). The
instrument is `tools/probe1.py` (`tools/probe1/`, pinned by `tests/tools/test_probe1.py`): the ring
readings go through `mantis.diagnostics.ring_reader` and `ring_audit.reconstruct_moves` (the ONE cadence
search), the net readings through the PRODUCTION sample + collate (`HexgBuffer.sample_graph_batch`, no
augmentation, no recency, `_build_graph_parts`, the trainer's own target construction with the tail
rebuilt over the detached prior), the solver through the bridged `TacticalSolver`, the spread through
the analyzer's `sweep`. Inputs: the operator's mirror `mantis-mirror/run8/checkpoints/` — nineteen
stamped checkpoints 3 000 … 54 000 and the stop's 55 170, each with its ring (100 000 rows, the newest
≈ 1 250 steps of play), the 45k parent `3aef7883…`. Seed 20260921 throughout; JSON per reading in the
session's scratch, the numbers transcribed here. R366 composes run10 from this file under R365(c);
nothing here is a decision.

## 0. The lines, before the numbers (R365's entry, verbatim in substance)

| # | reading | pre-stated line | admits / kills |
|---|---|---|---|
| 1 | DECOMPOSER | the corrected one-hot share and its trend; a band is re-derived from it, not asserted | the `one_hot_share_full` band (struck by R365(d)) |
| 2 | E3 mr-CALIBRATION | error(mr = 1) ≥ 2× error(mr = 2) → mid-turn structure is a named target; within CI → premise closed | admits P-B4 λ / the proof lane, kills C2's scalar head — or closes |
| 3 | C3-1 GAP | gap < 0.1 nats and both high → UNDERFIT; gap > 0.3 → OVERFIT; between → report | UNDERFIT admits size 6×192 and reuse; OVERFIT kills reuse-8, admits window / augment / LR floor |
| 4 | P-B2 PROOF RATE | proof ≥ 3 % AND novelty ≥ 5 % AND ms/root ≤ 50 → admitted as run10's un-probed-hypothesis candidate; else dead, filed | proof-as-target |
| 5 | P-B1 KL | median KL(prior‖target) < 0.02 nats → dead; ≥ 0.1 → admitted (queue (vii)) | the auxiliary soft-policy head |
| 6 | SPREAD SERIES | spread falling while strix rises → ARCH-D6 stays parked; flat / rising → admitted to run11's design | CARD-ARCH-D6 |
| 7 | EMA CELL (box) | > 0.181 → EMA deploy rides run10; ≤ 0.142 → dead; between → the LR floor is run10's hypothesis | `train.ema` / the LR floor |

## 1. DECOMPOSER — the one-hot share with tail-only rows excluded, by `moves_remaining`

Instrument: `probe1.py decompose` over every mirrored ring; the audit's own statistic (`explicit_entropy`
< 1e-3 over `is_full_search` rows) beside the same statistic with α = 1.0 rows excluded, then split by
the row's `moves_remaining`. n = 24 755–25 309 full-arm rows per ring.

| ring (step) | full-arm n | share AS AUDITED | tail-only rows | share EXCL. tail-only | mr = 1 share | mr = 2 share | H(explicit) median (nats) |
|---|---|---|---|---|---|---|---|
| 3 000 | 25 100 | 0.2267 | 0 | 0.2267 | 0.3029 | 0.1475 | 0.344 |
| 6 000 | 24 755 | 0.2239 | 0 | 0.2239 | 0.2967 | 0.1495 | 0.426 |
| 9 000 | 24 944 | 0.2530 | 0 | 0.2530 | 0.3305 | 0.1719 | 0.286 |
| 12 000 | 24 847 | 0.2556 | 0 | 0.2556 | 0.3355 | 0.1743 | 0.253 |
| 15 000 | 24 844 | 0.2745 | 0 | 0.2745 | 0.3484 | 0.1982 | 0.209 |
| 18 000 | 25 010 | 0.2687 | 0 | 0.2687 | 0.3445 | 0.1906 | 0.235 |
| 21 000 | 24 869 | 0.2702 | 0 | 0.2702 | 0.3492 | 0.1885 | 0.246 |
| 24 000 | 24 972 | 0.2656 | 0 | 0.2656 | 0.3469 | 0.1831 | 0.290 |
| 27 000 | 24 816 | 0.2645 | 0 | 0.2645 | 0.3344 | 0.1941 | 0.287 |
| 30 000 | 25 062 | 0.2761 | 0 | 0.2761 | 0.3610 | 0.1891 | 0.200 |
| 33 000 | 24 957 | 0.2844 | 0 | 0.2844 | 0.3679 | 0.2000 | 0.188 |
| 36 000 | 25 028 | 0.2570 | 0 | 0.2570 | 0.3372 | 0.1751 | 0.289 |
| 39 000 | 25 000 | 0.2711 | 0 | 0.2711 | 0.3553 | 0.1864 | 0.270 |
| 42 000 | 24 949 | 0.2747 | 0 | 0.2747 | 0.3492 | 0.1990 | 0.225 |
| 45 000 | 25 309 | 0.2705 | 0 | 0.2705 | 0.3502 | 0.1886 | 0.286 |
| 48 000 | 25 195 | 0.2760 | 0 | 0.2760 | 0.3559 | 0.1933 | 0.242 |
| 51 000 | 24 816 | 0.2754 | 0 | 0.2754 | 0.3550 | 0.1938 | 0.217 |
| 54 000 | 24 907 | 0.2689 | 0 | 0.2689 | 0.3442 | 0.1924 | 0.270 |
| 55 170 (stop) | 25 021 | 0.2668 | 0 | 0.2668 | 0.3401 | 0.1932 | 0.273 |

**What it reads.** (i) **There is no tail-only row in any run8 ring** — 0 of ≈ 25 000 full-arm rows on
every one of nineteen rings — so the audited share IS the corrected share: the conflation the packet
expected the instrument to carry (F-45's α = 1.0 rows) does not occur in run8's regime (F-45's 25 rows
were run7's 23 829 ring, ≈ 1/1 000; the ring refuses an empty explicit set at push and run8's search
never wrote a zero-mass one). The `< 0.30` band R364 re-stated was therefore a band on the right number,
struck on a premise this reading does not support; what stays true is that no outcome-linked measurement
backs any threshold. (ii) The share ROSE from 22.4–22.7 % (3k–6k, the σ-off re-warm) to 27.5 % by 15k
and has been FLAT since: 25.7–28.4 % over fourteen rings from 15k to the stop (binomial SE ≈ 0.3 pp per
ring; the ring-to-ring spread is ≈ 4× that, real). (iii) **The structure is in the turn, not in the
tail: mr = 1 rows (the turn's SECOND stone) are one-hot 30–37 %, mr = 2 rows (the first stone) 15–20 %,
a ≈ 1.8× ratio on every ring** — the pooled 27 % is the average of two populations that never moved
together. The second stone of a compound turn is the completing stone of every forced pattern the
census reads (a block's completion, a win's second stone), so a one-hot there is the search reading a
decided position, which is the "decided-and-correct" reading B §3 named. (iv) H(explicit)'s median
fell 0.43 → 0.20 by 15k–33k and reads 0.22–0.29 since (the R357 witness statistic).

**The band, re-derived (a candidate for run10's prereg, the architect's to write):** on the pooled
share the settled span's envelope is [0.257, 0.284]; a band `one_hot_share_full < 0.30` sits 1.6 pp
above its maximum and refuses run7's 62–65 % collapse by 2×, and is the band R364 wrote. The
decomposer's own contribution is the split: `one_hot_share_full_mr1 < 0.40` and `_mr2 < 0.24`
(the settled maxima 0.368 / 0.200 plus ≈ 3 pp) would read the two populations separately, which the
pooled band cannot — a per-mr row is a new audit row (`ring_audit` prints the pooled share only) and
lands with its producer if adopted. Neither is asserted here.

**Admits / kills.** No run10 change rides on this reading alone; it corrects the instrument the others
read: E3's mr split (reading 2) is the same axis, and the 1.8× one-hot asymmetry is the first datum
that the compound turn's mid-state is a distinct population.

## 2. E3 mr-CALIBRATION — dist65 E[v] against the realised z on run8@45k's ring, by `moves_remaining`

Instrument: `probe1.py netread` — the 45k net (`3aef7883…`) on the 45k ring through the production
sample (80 batches × 256 = 20 480 rows drawn with replacement from 100 000; 20 385 value-valid — the
ply-capped and drawn games carry `value_valid` 0), E[v] = the dist65 head's expectation, z the row's
outcome in the mover's frame (±1 on every valid row), mr read off node feature 3. The batch losses on
the way: policy 2.116 ± 0.134, value 0.505 ± 0.024 (the in-run track at 45k reads 2.07 / 0.52).

| bucket | n | MAE |E[v] − z| | RMSE | bias E[v] − z | value CE mean · median | sign agreement | mean |E[v]| |
|---|---|---|---|---|---|---|---|
| pooled | 20 385 | 0.697 | 0.836 | −0.038 | 0.505 · 0.550 | 0.696 | 0.409 |
| mr = 1 (second stone) | 10 287 | 0.697 | 0.835 | −0.026 | 0.504 · 0.560 | 0.699 | 0.403 |
| mr = 2 (first stone) | 10 098 | 0.696 | 0.838 | −0.050 | 0.506 · 0.542 | 0.693 | 0.415 |
| full-arm rows only | 5 053 | 0.692 | — | — | — | — | mr 1 0.700 / mr 2 0.685 |

**MAE ratio mr = 1 / mr = 2: 1.002 [0.983, 1.019]; value-CE ratio 0.995 [0.973, 1.017]** (2 000 row
bootstraps). The reliability curve, E[v] decile → mean z, pooled / mr 1 / mr 2: [−1, −0.8) → −0.95 /
−0.95 / −0.95 (n 2 353); [−0.8, −0.6) → −0.65 / −0.67 / −0.63; [−0.6, −0.4) → −0.46 / −0.53 / −0.39;
[−0.4, −0.2) → −0.22 / −0.24 / −0.21; [−0.2, 0) → +0.01 / −0.01 / +0.02 (n 6 505); [0, 0.2) → +0.14 /
+0.14 / +0.14; [0.2, 0.4) → +0.34 / +0.35 / +0.33; [0.4, 0.6) → +0.50 / +0.51 / +0.50; [0.6, 0.8) →
+0.71 / +0.67 / +0.74; [0.8, 1] → +0.96 / +0.96 / +0.96 (n 2 698).

**What it reads.** The head is CALIBRATED, bin for bin, in both halves of the compound turn: a
predicted −0.95 realises −0.95, a predicted +0.14 realises +0.14, and the two buckets' curves lie on
each other within the deciles' noise (the [−0.6, −0.4) bin differs by 0.14 on ≈ 570 rows each, the
largest gap). The 0.70 MAE against a ±1 outcome is what an honest head reads when 32 % of rows sit in
[−0.2, 0.2) and realise z ≈ 0 on average — genuinely undecided positions — not a mis-calibration. The
bias is a 3–5 pp pessimism for the mover in both buckets. F-45's 25 α = 1.0 rows at mr = 1 were a
saturation artefact of run7's search, not a value blind spot at mid-turn.

**Against the line.** error(mr = 1) ≥ 2× error(mr = 2): **NO** — the ratio is 1.00 with a CI of ± 2 %.
**The premise CLOSES: one scalar (`moves_remaining / 2`) suffices for value calibration at this net;
mid-turn structure is NOT a named target.** P-B4's λ / proof lane gains nothing from this reading (its
own line stands unread — the ring stores no root values); C2's scalar head is not killed by the head's
calibration (the pooled AND the bucketed curves read clean, which is the "clean" branch C2's kill
condition needed and E3 supplied), so C2 stays where P6 ranked it, run11+, on its regime-confounded
evidence. What the reading does not reach: calibration at quick-arm roots was pooled in (the full-arm
rows read the same 0.69–0.70), and the value head's TRAJECTORY (PARAM_DISTANCE's +96 % norm) is a
different question from its calibration at 45k.

## 5. P-B1 KL — KL(prior‖target) over the full-arm rows of run8@45k's ring

Instrument: the same `netread` pass — per full-arm row (α < 1; 5 081 of the 20 480 sampled rows) the
45k net's prior p over the row's legal nodes and the trainer's target t (the explicit masses plus the
tail rebuilt over the detached prior, the ONE construction `ragged_policy_ce` trains on). Three KLs,
each in nats, each the median over rows with its quartiles: the packet's **KL(p‖t)**; KL(t‖p) = CE − H,
the trainer's own surprise; and the brief's own falsifier KL(t‖t^(1/4)) — t sharpened to KataGo's
T = 4 soft target — which is what a second head on target^(1/4) would learn that the first does not.
KL(p‖t) is unbounded where the target puts no mass under prior mass (α = 0 on 1.4 % of rows; a
tail mass spread over ≈ 340 legal nodes is ≈ 5e-5 per node), so it is reported at a 1e-12 clamp with
the share of rows carrying > 1e-3 of unsupported prior mass, and once more over the supported rows alone.

| statistic | rows | median | p25 · p75 · p90 | mr = 1 median | mr = 2 median |
|---|---|---|---|---|---|
| **KL(prior‖target)** | 5 081 | **6.58** | 0.94 · 15.7 · 21.1 | 4.97 | 8.72 |
| KL(prior‖target), supported rows only | 3 941 | 4.49 | 0.57 · 12.8 · 19.7 | — | — |
| KL(target‖prior) = CE − H | 5 081 | 1.21 | 0.39 · 2.29 · 3.25 | 0.88 | 1.51 |
| KL(target‖target^(1/4)) | 5 081 | 0.76 | 0.53 · 1.12 · 1.42 | 0.80 | 0.71 |
| policy CE (the loss term, per row) | 5 081 | 2.11 | 1.29 · 2.86 · 3.54 | 1.89 | 2.21 |

Unsupported prior mass > 1e-3 on 22.4 % of full-arm rows (mean 0.034); α = 0 exactly on 1.4 %.

**What it reads.** The prior and the searched target are FAR apart on the typical full-arm row: the
search moves 1.2 nats of surprise (median; 3.2 at p90) from prior to target, and the prior's mass on
the ≈ 340 unvisited legal moves — where the target's tail leaves ≈ 5e-5 per node — is what makes
KL(p‖t) read 4–9 nats (it is the "policy-surprise" statistic queue entry (vi) would weight by, and it
is large on nearly every row: p25 0.94). The soft target differs from the hard one by 0.76 nats on the
median row — a one-hot row contributes 0 to this KL, so the 0.76 is carried by the 73 % of rows with
mass to spread. mr = 2 rows (the first stone, 350 legal moves) read a larger surprise than mr = 1
(the second, often a forced completion) on every statistic but the soft one.

**Against the line.** median KL(prior‖target) < 0.02 → dead: **NO**; ≥ 0.1 → admitted: **YES, by a
factor of 65** (and by 45 on the supported rows, by 12 on the trainer's own direction, by 38 on the
brief's own falsifier). **The auxiliary soft-policy head (queue (vii)) is ADMITTED for run10 under
R365(c) — as a probe-supported change, so it may ride beside another.** What the line does NOT say and
this reading cannot: whether a second head on t^(1/4) moves strength; the 0.76 nats says the head would
learn a distinct target, not that the distinct target is a better one. Its in-run producer (LAW-18:
KL(hard‖soft) per step) rides with it if armed.

## 3. C3-1 GAP — train-vs-held-out loss on run8's eighteen checkpoints

Instrument: `probe1.py gap` — for every ring k (3k … 55 170), 12 production samples of 256 rows (no
augmentation, no recency; the batch loss exactly as `Trainer.eval_step_from_graph_batch` sums it, the
whole-batch denominators); on the SAME twelve batches two nets read: net k (its TRAIN loss — the ring
holds the rows generated in the ≈ 1 250 steps before k, which net k sampled ≈ 3.2× each) and net k − 3k
(its HELD-OUT loss — rows generated after it stopped training, never sampled by it). Per net the gap is
held-out(net k on ring k + 3k) − train(net k on ring k), paired by ring. Per-batch SD: policy 0.11–0.14
(the mean's SE ≈ 0.035), value 0.013–0.027 (SE ≈ 0.005).

| net | train policy | train value | held-out policy | held-out value | gap policy | gap value | gap total |
|---|---|---|---|---|---|---|---|
| 3 000 | 2.321 | 0.568 | 2.309 | 0.622 | −0.012 | 0.054 | 0.042 |
| 6 000 | 2.173 | 0.536 | 2.201 | 0.633 | 0.028 | 0.097 | 0.125 |
| 9 000 | 2.100 | 0.531 | 2.309 | 0.627 | 0.209 | 0.096 | 0.305 |
| 12 000 | 2.227 | 0.532 | 2.301 | 0.661 | 0.073 | 0.129 | 0.203 |
| 15 000 | 2.151 | 0.532 | 2.288 | 0.706 | 0.137 | 0.173 | 0.310 |
| 18 000 | 2.217 | 0.528 | 2.281 | 0.603 | 0.064 | 0.075 | 0.139 |
| 21 000 | 2.137 | 0.511 | 2.333 | 0.635 | 0.196 | 0.124 | 0.321 |
| 24 000 | 2.161 | 0.514 | 2.399 | 0.638 | 0.239 | 0.123 | 0.362 |
| 27 000 | 2.322 | 0.516 | 2.185 | 0.631 | −0.137 | 0.115 | −0.023 |
| 30 000 | 2.044 | 0.498 | 2.163 | 0.615 | 0.119 | 0.117 | 0.236 |
| 33 000 | 2.099 | 0.506 | 2.242 | 0.633 | 0.143 | 0.127 | 0.270 |
| 36 000 | 2.177 | 0.521 | 2.140 | 0.663 | −0.037 | 0.142 | 0.106 |
| 39 000 | 2.041 | 0.510 | 2.233 | 0.694 | 0.192 | 0.184 | 0.376 |
| 42 000 | 2.155 | 0.512 | 2.200 | 0.651 | 0.045 | 0.138 | 0.183 |
| 45 000 | 2.142 | 0.517 | 2.230 | 0.673 | 0.088 | 0.157 | 0.245 |
| 48 000 | 2.161 | 0.520 | 2.303 | 0.661 | 0.142 | 0.141 | 0.284 |
| 51 000 | 2.211 | 0.509 | 2.321 | 0.697 | 0.110 | 0.188 | 0.298 |
| 54 000 | 2.211 | 0.526 | 2.283 | 0.671 | 0.072 | 0.145 | 0.217 |
| 55 170 | 2.228 | 0.504 | — | — | — | — | — |

Over the eighteen paired nets: **policy gap mean 0.093 (median 0.099, range −0.14 … +0.24); value gap
mean 0.129 (median 0.128, range 0.054 … 0.188, POSITIVE on all eighteen); total 0.222.**

**What it reads.** (i) The two heads generalise differently. The POLICY head's gap is small and inside
its own noise (0.09 ± 0.035 per net, three nets negative) on a train loss that has been FLAT at
2.04–2.32 nats for 50k steps: it is not fitting its window and not improving on it — underfit-shaped.
The VALUE head's gap is precise and never zero: every net reads 0.05–0.19 nats (10–37 %, mean 25 % of
its train loss) worse on rows it never sampled, on a train loss flat at 0.50–0.57 — it fits the
100 000-row window it trains 3.2× over, and the fit does not transfer to the next 1 250 steps of play.
(ii) The held-out rows are ALSO newer play (nets up to 3 000 steps stronger generated them), so part of
each gap is distribution shift, not memorisation; the shift is the same for both heads, and only one
shows it. (iii) The gap does not grow over the run (value 0.05–0.10 at 3k–9k, 0.12–0.19 from 12k on,
flat after) — a property of the regime (window 100k, reuse 3.2), not of training time.

**Against the line.** gap < 0.1 with both high → UNDERFIT; > 0.3 → OVERFIT; between → report:
**BETWEEN (0.22) → REPORTED.** The split is the finding: the policy head sits on the UNDERFIT side of
the line (0.09, flat high loss — size and reuse would have a target there) and the value head on the
far side of it (0.13 of 0.51, consistent — a reuse regime that samples each row 7.7× instead of 3.2×
would deepen exactly this fit, which is KataGo's "larger numbers may cause overfitting" read in our
own numbers). Nothing is admitted or killed by the line; what the reading gives run10 is an IN-RUN
WITNESS for the data regime it inherits from the run9 mint: the trainer already owns a forward-only
held-out step (`run_declared_eval_step`, BC pretrain's), and the value gap on the next checkpoint's
ring — read on the shakedown twin and at every checkpoint — is the number that says whether reuse 7.7
over a 500k window fits or memorises. Pre-stated for that witness: a value gap ≥ 2× run8's 0.13 on the
twin is the regime memorising; ≤ 0.13 with the train loss falling is the regime learning.

## 4. P-B2 PROOF RATE — the tactics solver over run8@45k's ring roots

Instrument: `probe1.py proofs` — 5 000 full-arm rows drawn without replacement from the 45k ring
(seed 20260921), 4 877 rebuilt by the cadence search (123 refused: no legal cadence order reproduces
the stored (to move, k)), the bridged `TacticalSolver(window_half=None, cand_cap=40)` — net-free, the
board unbounded so no perception-window guard — at **depth 6 plies, 2 000 nodes** (the packet's
setting) and at 12 plies (F-38's turn unit) as a check. NOVELTY is read two ways: the proof's first
stone ≠ the row's target argmax (the packet's definition), and STRICT — the target's argmax played
from the proven-won root does NOT keep the win provable (a won root can hold several winning first
stones, so the first form over-counts).

| arm | roots | WIN | LOSS | UNKNOWN | proof rate | novelty (first stone ≠ argmax) | STRICT novelty | WIN's game won by the mover | LOSS's game lost | ms/root mean · p50 · p90 | nodes mean · budget hit |
|---|---|---|---|---|---|---|---|---|---|---|---|
| depth 6 | 4 877 | 112 | 5 | 4 760 | **0.0230 [0.0188, 0.0272]** | 55 / 112 = 0.491 | **0 / 112 = 0.000** | 112 / 112 | 5 / 5 | 4.5 · 2.3 · 7.8 | 13.5 · 0 % |
| depth 12 | 4 877 | 112 | 5 | 4 760 | 0.0230 | 0.491 | 0.000 | 112 / 112 | 5 / 5 | 5.3 · 2.8 · 8.6 | 14.4 · 0 % |

By `moves_remaining`: mr = 1 (2 475 roots) 0.0226, mr = 2 (2 402) 0.0233 — the same.

**What it reads.** (i) The solver proves 2.3 % of full-arm roots, every proof agreeing with the game's
realised outcome (112/112 wins, 5/5 losses — the solver is sound and the record is consistent with it),
in 4.5 ms per root on this CPU with a mean of 13.5 nodes: **the 2 000-node budget never binds and depth
12 finds the same 112 wins** — the proofs are short forced sequences, and no deeper threat-space search
exists to buy. (ii) **STRICT novelty is 0 of 112**: in every proven-won root the search's own target
argmax keeps the win — the 49 % "novelty" of the first form is won positions with several winning first
stones, not proofs the search missed. The searched target already contains every proof this solver can
produce. (iii) Per-game arithmetic, for scale: at 2.3 % of ≈ 40 full-arm turns a game holds ≈ 0.9
proven-won roots, and the target is already right at all of them.

**Against the line.** proof ≥ 3 %: **NO** (0.0230, the 95 % interval's upper end 0.0272); novelty ≥ 5 %:
YES on the packet's definition (49 %), **NO** on the strict one (0 %); ms/root ≤ 50: YES (4.5).
**Proof-as-target is DEAD by the line as stated (the proof-rate arm) and dead by mechanism (the strict
novelty arm): a proof target would teach the net nothing the searched target does not already say at
the roots it can prove.** Filed: `falsified.md` gains the row (the 2026-09-21 regime: run8@45k's ring,
Gumbel 320/16 at p 0.25, this solver at this budget); CARD-STRIX-NET-ONLY's A1 (the training-side
hypothesis "untested and unranked") is now READ — closing it is R366's. What the reading does
NOT reach: a solver stronger than this threat-space one (F-38 proved the 33 value-blind losses at depth
6–8 TURNS with a bounded minimax that is not this solver), or proofs at quick-arm roots.

## 6. SPREAD SERIES — the analyzer's symmetry spread over 24 positions × 19 checkpoints

Instrument: `probe1.py spread` — 24 full-arm `moves_remaining` 2 positions drawn round-robin over six
`ply_index` quantile bins of the 45k ring (plies 3–137; each rebuilt in a fence-legal cadence order so the
analyzer's `build_board` accepts it), then `analyzer.instruments.sweep` on every stamped checkpoint: the
net's raw value (quiescence-off tree, one leaf) under the 12 D6 maps about the first stone plus the
(1, −1) translation, the spread = max − min over the 12, the argmax agreement over the 12. Per
checkpoint the spread DISTRIBUTION over the 24 positions (B's caution: not one scalar).

| step | spread median | mean | p90 | max | argmax agreement (mean of 12) | 12/12 share | strix 256/256 |
|---|---|---|---|---|---|---|---|
| 3 000 | 0.149 | 0.228 | 0.522 | 0.959 | 0.785 | 0.333 | 0.056 |
| 6 000 | 0.158 | 0.223 | 0.531 | 0.671 | 0.823 | 0.542 | 0.0625 |
| 9 000 | 0.153 | 0.175 | 0.399 | 0.598 | 0.760 | 0.542 | |
| 12 000 | 0.096 | 0.151 | 0.356 | 0.504 | 0.847 | 0.500 | |
| 15 000 | 0.083 | 0.147 | 0.354 | 0.866 | 0.809 | 0.500 | 0.104 |
| 18 000 | 0.095 | 0.173 | 0.480 | 0.690 | 0.757 | 0.417 | |
| 21 000 | 0.120 | 0.165 | 0.374 | 0.529 | 0.778 | 0.542 | |
| 24 000 | 0.135 | 0.215 | 0.536 | 0.762 | 0.920 | 0.792 | |
| 27 000 | 0.125 | 0.181 | 0.440 | 0.942 | 0.719 | 0.333 | |
| 30 000 | 0.117 | 0.173 | 0.393 | 0.503 | 0.722 | 0.458 | 0.135 |
| 33 000 | 0.148 | 0.195 | 0.490 | 0.698 | 0.750 | 0.417 | |
| 36 000 | 0.123 | 0.188 | 0.490 | 0.743 | 0.865 | 0.667 | |
| 39 000 | 0.082 | 0.153 | 0.332 | 0.634 | 0.917 | 0.625 | |
| 42 000 | 0.060 | 0.150 | 0.473 | 0.697 | 0.847 | 0.667 | |
| 45 000 | 0.098 | 0.181 | 0.551 | 0.748 | 0.927 | 0.667 | 0.142 |
| 48 000 | 0.143 | 0.208 | 0.504 | 0.707 | 0.816 | 0.542 | |
| 51 000 | 0.108 | 0.187 | 0.446 | 0.685 | 0.799 | 0.500 | |
| 54 000 | 0.083 | 0.145 | 0.324 | 0.670 | 0.896 | 0.750 | |
| 55 170 | 0.092 | 0.140 | 0.366 | 0.490 | 0.844 | 0.542 | |

The translation check: |Δvalue| = 0.0 on all 304 in-window checks (translation-exact, as R363(d) read).
Least-squares slope of the median: −0.008 per 10 000 steps over the span, −0.0035 from 15k on; the
first six checkpoints' median mean 0.122, the last six 0.097. Four positions carry a mean spread of
0.41–0.47 across ALL checkpoints (rows 62418 at ply 97, 42600 at 81, 44631 at 23, 62568 at 67).

**What it reads.** (i) The median orbit spread FALLS, weakly and noisily: ≈ 20 % over 52k steps, with
checkpoint-to-checkpoint swings (0.060 at 42k, 0.143 at 48k) of the size of the whole trend — at n = 24
the median's own noise is most of the table. (ii) The mean and the p90 fall less (0.23 → 0.14; 0.52 →
0.37) and the max not at all (0.49–0.96 at every checkpoint): the non-invariance is CONCENTRATED in a
tail of positions the net reads differently under rotation at every step it has trained — it is not
washing out with augmentation. (iii) The argmax agreement over the orbit rises 0.79 → 0.84–0.93 (the
policy side is nearer invariant than the value side, R363(d)'s 12/12 reading on its one position was
the upper end of a 0.33–0.79 share). (iv) R363(d)'s 0.146 at 18k sits inside this distribution (18k's
median 0.095, p90 0.48).

**Against the line.** spread falling while strix rises → parked: **YES on the median** (−20 % while
strix 0.056 → 0.142); flat on the tail. **CARD-ARCH-D6 STAYS PARKED by the line as stated.** What R366
should weigh beside it: the fall is weak (a 3-pp-of-spread trend under noise of the same size) and the
tail positions do not move, so "augmentation is closing the gap" is not what this reads — it reads
"the median position's spread was never large and the large ones stay large". A D6-equivariant trunk
reads 0 by construction on every row of this table; whether that buys strength is the run C1 prices at
50–55 bh, and nothing here prices it lower.

## E1 — the ruler-r6 cell (the box's, R365 §0(3); not a PROBE-1 reading, recorded here as the leg's one cell)

Instrument: `tools/strix_follower.py --once run8_00045000_3bdedf76.ckpt --unit ruler_r6` on the box tree
`d4804e58` — the parent (`3aef7883…`) at PUCT-256 vs strix `checkpoint_00237000` at 256 sims, solver ON,
the driver's `placement_radius` **6** (its own trained radius; every reading on record rides the driver's
default 8), 288 paired games on `book_v1_s20260625_p4`, both colours, concurrency 8, **IDLE** by the
follower's heartbeat rule (run8 stopped; the PERF-3 contended bench ran beside its first 20 min — a
wall cost, not a WR one). Receipt `run8_00045000_3bdedf76.ckpt.strix256_r6.json`, mirrored.

| unit | WR | 95 % CI (pair-level, LAW-04) | W–L–D | eff_n | median plies | p90 · max | s/game | wall | regime | fence findings |
|---|---|---|---|---|---|---|---|---|---|---|
| strix @ r8 (the record, 2026-09-21 05:50 read) | 0.142 | [0.104, 0.181] | 41–247–0 | 288 | 43 | 85 · 157 | 20.1 | 5 776 s | CONTENDED | 0 |
| **strix @ r6 (E1)** | **0.080** | **[0.049, 0.115]** | 23–265–0 | 288 | 41 | 81 · 169 | 9.7 | 2 804 s | IDLE | 6 690 |

The findings are the instrument: at r6 every strix reply's legal set is a strict subset of our r8
fence ("only ours" = the outer cells), 6 690 of them over ≈ 7 150 strix stones, and NOT ONE out-of-fence
move (a strix move outside our fence would forfeit). The game-length distributions are the same shape
(no cap game in either cell).

**Against the line** (`STRENGTH_RESEARCH_2` E1: within ± 4 pp of the r8 cell's 0.142 → the ruler is
radius-stable; a move ≥ the CI half-width → "strix @ r8" is a stated unit qualifier on every follower
point). **The move is −6.2 pp, beyond either cell's half-width (± 3.9 / ± 3.3), the two CIs disjoint:
"strix @ r8" IS a unit qualifier.** Strix at its own trained radius is the STRONGER ruler against the
parent — the r8 default the rungs have always run hands strix ≈ 90 cells per position it never trained
on, and at 256 sims that dilutes its top-m draw. Every follower point on the record (15k 0.104, 30k
0.135, 45k 0.142, the run7 series) is a strix-@-r8 number; the parent rule, the gate's regression-guard
bounds and every bar in the research packet were priced in that unit and keep it. What this does NOT
change: the SERIES — all its points share the unit, so the monotone triple and the parent choice stand;
what it changes is any bar quoted as "vs strix" without the radius, and the deploy-matched reading of
"what strix is" — the honest ruler for a strength claim is strix at r6, where the parent reads 0.080.
The r6 cell is the second anchor E's brief named and did not price: two readings of the same net that
differ by more than the run's whole gain (+3.1 pp).

## 7. EMA CELL — the averaged net is BUILT; the cell is the box's, in run10's preflight window

Instrument: `probe1.py swa` — the uniform weight-average (SWA over stamped checkpoints; A-P2's step 1
names "EMA over run8's 18 checkpoints", the packet "30k–51k") of run8's EIGHT saves 30 000 … 51 000
(`f16bf6c9…`, `89f7915e…`, `b2a69df7…`, `f48206a5…`, `894dcc2e…`, `3aef7883…`, `7ff9ca02…`,
`aa25d3f3…`), the 50 floating tensors averaged in float64 and written back in float32, stamped through
the ONE writer as a weights-only v2 checkpoint under `run8swa` at step 51 000 with `configs/run8.yaml`
(HEAD, v34) as its config: **`run8swa_00051000_ae478dee.ckpt`, net hash `61b09ba879753ce3…`, file
sha256 `b8141494…`**, its `.derivation.json` beside it naming every source by file sha256 and net hash.
It loads through `load_checkpoint` (run_id `run8swa`, step 51 000, `gnn_axis_r8`, 50 tensors); the
LAW-12 stamp is a fresh one (a derived artifact is a new stamp, never a re-stamp), and the filename's
run id is the derivation's, so no reader can mistake it for a run8 save. Kept OFF the tree (R7) in the
operator's mirror root beside run8's checkpoints, to be carried to run10's box with the parent.

The cell (`tools/strix_follower.py --once <run8swa ckpt> --unit equal_work`, 288 paired games, 256/256,
`book_v1_s20260625_p4`, the regime labelled) is NOT run today: the packet places it in run10's preflight
window and the box budget it allotted today (1.6 bh) was the benches and E1. Its line stands as
pre-stated in §0 (> 0.181 → EMA deploy rides run10; ≤ 0.142 → dead; between → the LR floor is
run10's hypothesis), read in the same unit as the parent's 0.142 [0.104, 0.181] — with E4's
resolution beside it: the cell resolves ≥ 4 pp, and the line's "> 0.181" is the parent's CI upper
bound, a ≥ 4 pp move.

## 8. The lines against the numbers — what R366 has

| # | reading | number (n, regime) | the line's verdict | admits / kills for run10 |
|---|---|---|---|---|
| 1 | DECOMPOSER | 0 tail-only rows on all 19 rings; pooled share 25.7–28.4 % flat since 15k; **mr 1 30–37 % vs mr 2 15–20 %** (n ≈ 25k per ring) | the audited share is the corrected share; the band is re-derivable (pooled < 0.30; per-mr < 0.40 / < 0.24 as candidates) | no change on its own; the instrument the others read is CORRECT, the structure is per-mr |
| 2 | E3 mr-CALIBRATION | MAE ratio mr1/mr2 **1.002 [0.983, 1.019]**, value-CE ratio 0.995 [0.973, 1.017]; reliability curves lie on each other (n 20 385 valid rows, run8@45k) | within CI → **the premise CLOSES** | P-B4's λ / proof lane gains no target; C2's scalar head is not killed by calibration (both curves clean) and not admitted |
| 3 | C3-1 GAP | policy gap 0.09 ± 0.035 (flat 2.1-nat loss), **value gap 0.13 (0.05–0.19, positive on 18 of 18)**, total 0.22 (12 × 256 rows × 2 nets per ring, 18 nets) | BETWEEN → **REPORTED** | nothing by the line; the split (policy underfit-shaped, value memorising its window) names the value gap on the next ring as run10's in-run witness for its reuse regime |
| 4 | P-B2 PROOF RATE | proof **2.30 % [1.88, 2.72]**, strict novelty **0 / 112**, 4.5 ms/root, 112/112 z-agreement (4 877 roots, run8@45k) | proof < 3 % AND strict novelty < 5 % → **DEAD, filed F-53** | proof-as-target is OUT of the un-probed-hypothesis slot |
| 5 | P-B1 KL | median KL(prior‖target) **6.58** (4.49 supported rows), KL(target‖prior) 1.21, KL(target‖target^¼) 0.76 (5 081 full-arm rows) | ≥ 0.1 by 65× → **ADMITTED** | the auxiliary soft-policy head (queue (vii)) may ride run10 as a probe-supported change under R365(c) |
| 6 | SPREAD SERIES | median spread 0.122 → 0.097 (first six → last six checkpoints), −0.008 per 10k; tail positions 0.4–0.5 at every checkpoint; strix 0.056 → 0.142 (24 positions × 19 checkpoints) | falling while strix rises → **CARD-ARCH-D6 STAYS PARKED** | no run11 admission; the tail's persistence is noted beside the verdict |
| 7 | EMA CELL | the net is BUILT: `run8swa_00051000_ae478dee.ckpt` (net `61b09ba8…`, 8 sources 30k–51k, derivation beside it); the cell is run10's preflight window | unread | LR / EMA wait on the cell, per R365(d) |
| E1 | RULER-R6 (box) | **0.080 [0.049, 0.115]** vs r8's 0.142 [0.104, 0.181] (288 paired games, IDLE) | a move ≥ the CI half-width → **"strix @ r8" is a unit qualifier** | every bar priced "vs strix" carries the radius; the series stands (one unit throughout) |

**Box-hours (R365 §0(3)):** the instance ran 08:23:44 UTC (run8's STOP) → its release; measured work
0.33 h (the alone B-curve, 08:33–08:52) + 0.78 h (E1, 08:53–09:40, the contended B-curve inside its
first 20 min) = **1.11 bh** against the packet's 1.6; the build (`make build.cuda`, 08:24–08:31) and the
mirror verification are the rest. PROBE-1 itself: 0 box-h, ≈ 1 dev-day (the instrument, six readings
on a 16-core CPU: the decomposer 1 min, the 45k net-read 20 min, the proofs 1 min, the spread 11 min,
the gap 50 min, the SWA 1 min).

**What the readings say together, for R366 (no design here):** the compound turn's mid-state is a
distinct POLICY population (one-hot 1.8× as often at mr = 1) and NOT a distinct VALUE population (the
head is calibrated equally in both halves); the searched target already holds every proof this solver
can produce and sits 1.2 nats (6.6 in the other direction) from the prior; the value head memorises its
window while the policy head under-fits it; the net's non-invariance is a persistent tail, not a
shrinking median; and the ruler reads 6 pp differently at the radius strix trained at. Two of the
seven lines admit (the soft-policy head; E1's qualifier), two kill (proof-as-target; the mid-turn value
premise), one parks (ARCH-D6), two report (the gap; the band), one waits on the box (EMA).
