# PREREG — run7's re-mint rows, proposed under R350 (2026-09-13)

**Written before any run7 file is minted; the kind and sims rows filled from the frontier's record
when it completed (16:45 UTC).** Every row below is a PROPOSED value: R350 arms the (d) rows "at
run7's mint by the operator's forward", the search kind and sims rows carry what
STRENGTH-FRONTIER-1 measured, and the rest are the mechanisms this leg built, each with its off
value in every committed config today. The operator's forward of a filled row arms it (R337's fill rule); a row left blank halts
the mint. `configs/run6.yaml` is NOT edited for any of this — it is a finished run's record and
its two new members (`reinit: [value_head]`, the two `null` blocks) state what run6 ran.

## 1. Rows the ruling proposes (R350(d), (b), (e))

| key | run6 minted | run7 PROPOSED | envelope | clause | grounds |
|---|---|---|---|---|---|
| `train.eval_interval` | 1000 | **3000** | — | (d) | fewer, larger points at the same game budget; the cadence cost is 1.7 % of wall (block record §D) |
| `eval.ladder.rungs[sealbot_d5].games_max` | 32 | **288** (paired) | — | (d) | ± 5 pp per point; witness (ii) statable from WR ≥ 0.56 |
| `eval.random_floor_games` | 20 | **20** | — | (d) | unchanged |
| `eval.gate.stride` | 3 | **1** | — | (d) | the promotion gate rides every round on the 3 000 cadence (R345(c)'s 3 superseded) |
| `train.checkpoint_interval` | 1000 | **3000** | — | (d), R242 | RE-DERIVES as the relation `= eval_interval` (R242: stated as a relation, never re-authored); the puller cycle follows it (R349(b)) |
| `identity.warm_start.reinit` | `[value_head]` | **`[]`** | — | (b)(i) | every head copied; the step-0 witness is the live net's hash equal to `net_hash`, refused at boot otherwise |
| `identity.warm_start.checkpoint` / `net_hash` | the LAW-12 strip | **BC-3's checkpoint of record** | — | (b)(ii) | a re-run of BC on the corpus with the held-out VALUE loss read beside policy (`bc_heldout … value_loss=`); the strip's value head IS trained on the corpus outcomes already, so BC-3 is a re-run with the value line on the record, not a new recipe — the operator may instead pin the existing strip with `reinit: []` and skip the re-run |
| `train.policy_loss_weight_schedule.warmup_steps` | 0 | **2000** | [1000, 4000] | (b)(iii) | the value head sees the run's own positions before the policy prior moves; at ≈ 1 160 steps/h that is ≈ 1.7 h of the block |
| `train.policy_loss_trough_abort` | `null` | **`{delta_nats: 0.2, consec: 3, max_step: 5000}`** | — | (b)(iv) | the trough's signature (2.28 → 2.86 nats over 12k steps) halts live; at `gate_interval 1000` the earliest fire is boundary 4 = step 4 000 ≤ 5 000 (the manifest's bounded cadence audits it); the manifest row `policy_loss_trough` flips DEFERRED → REQUIRED at this mint |
| α = 1.0 rows | counted, trained | **excluded from the policy loss** | — | (e) | no key: `exclude_alpha_full_rows` is unconditional from this tree on; the count rides `trainer_step.policy_rows_excluded_alpha_full` (LAW-18) |
| `deploy.search.kind` (deploy head + eval; the key R351(c) split out) | gumbel | **`puct`** | — | (c), (f), (g) | the frontier: the Gumbel head reads the same net 24 pp below PUCT, inverts the 25k-vs-prior pair (0.438 vs 0.672) and reads worse with every doubling of sims (`STRENGTH_FRONTIER_1_2026-09-13.md` §E) |
| `selfplay.search.kind` (self-play) | gumbel | **`puct`** (by R351(c)'s rule: no σ pair alive in the four cells — `STRENGTH_FRONTIER_1_2026-09-13.md` §F, F-51) | — | R351(b)(c) | the block's Gumbel-320/64 self-play produced a net PUCT reads 15 pp above its warm start, on argmax targets from the first ring; both σ pairs the paper and Mctx ran read the 18k net at 0.06–0.07 at 512 sims against PUCT's 0.774 and fall from 128, so the rule's second arm fires: PUCT with playout-cap 320/64 at p 0.25, policy rows from the full arm only (`train.fast_policy_weight` 0.0), τ 0.5 (`temp_min`) — the measured reader, untested as a trainer at this regime, and that is disclosed |
| `eval.gate.deploy_sims`, `eval.sealbot_model_sims` | 160, 128 | **512, 512** | — | (f) | +6 to +13 pp per doubling under PUCT, not flat by 512; ≈ 35–49 s/game at 12-way load (≈ 25 min per 288-game point on an idle card) |
| `selfplay.mcts.n_simulations`, `playout_cap.*` | 320/64 at 0.25 | **unchanged unless the self-play kind changes** | — | (f) | the frontier measured the DEPLOY head's sims, not self-play's; a PUCT self-play arm needs its own sims row |
| `eval.ladder.rungs` | sealbot_d5 only | **sealbot_d5 only** | — | (g) | "sealbot-only rungs" |

## 2. Rows this leg found, proposed beside the ruling's

| key | run6 minted | run7 PROPOSED | grounds |
|---|---|---|---|
| `monitor.supervisor_kill_grace_sec` | 30 | **≥ 2 × `eval.worker_kill_grace_sec` + the pool's stop (measured ≤ 10 s) = 120** | CARD-STOP-DRAIN-VS-GRACE: run6's stop inside an eval round was SIGKILLed at +30 s while `close_out` waited on the round; the resumable stop now ABANDONS the round (bounded by 2 × 10 s) and closes the recorder, so the grace must cover that ladder — stated as a RELATION at the mint, not two numbers |
| `monitor.drain.*` | schema defaults (900 × 3, 14400) | **minted explicitly** | the block a run does not mint is the block nobody reads at the mint |
| witness (iii) | first-third Elo slope, ≥ 5 rounds | **OLS Elo slope over ALL points of the block (≥ 8 at 25k) with CI excluding 0** | (d) |
| witness (ii) | pooled WR over four rounds, CI excluding 0.5 | **statable at 288 games from WR ≥ 0.56** | (d) |

## 3. What the frontier returned (complete, 27 + 4 cells, 16:45 UTC)

`docs/design/measurements/STRENGTH_FRONTIER_1_2026-09-13.md` §E: the kind (the Gumbel head is the
failure), the head set (the BC value head is worth 0.413 vs 0.038), the net (not weak: 0.672
over its prior at PUCT-150), sims (a lever under PUCT, a liability under Gumbel). The rows in
§1 above carry those answers.

## 4. Sequence after the frontier (R350's ROUTE)

verdict on kind/sims → re-mint (`tools/mint_config.py` from the `dev` template with the rows above;
the header replays) → preflight stamp on the box (`sync_lag` tier from a ≈ 100-step burst) →
4 h shakedown twin → run7 START on the operator's word.

## 5. As minted (2026-09-13, after the four cells) — `configs/run7.yaml`

Every §1/§2 row above is minted as proposed, with R351(d)'s corrections and the four facts the
mint itself turned up:

- `selfplay.search.kind: puct`, `deploy.search.kind: puct`, `train.policy_target:
  raw_visit_distribution` (follows the self-play kind). `selfplay.c_scale` 1.0 / `q_rescale`
  true stay at the template's values, INERT under PUCT on both heads (F-51).
- `train.policy_loss_trough_abort: null` — DEMOTED to the dashboard's eighth health input
  (`tools/dashboard/health.py`, the prereg's {0.2, 3, 5000} signature read off
  `trainer_step.policy_loss`), never a halt (R351(d)); the manifest row stays DEFERRED.
- `identity.warm_start.reinit: []` on the existing strip (BC-3's re-run skipped, R351(d)).
- `eval.ladder.round_games: 288` beside `games_max: 288` — the ladder allocates
  `min(round_games, games_max)` to the one active rung, so the point is 288 only if both say so;
  and `calibration_games: 288`, `calibration_every_k_rounds: 1` — a sealbot-only rung that
  SATURATES (three rounds at lower-CI ≥ 0.75, which a 0.77 net reaches) would otherwise drop to
  8 calibration games every 4 rounds and end the watch; with these two relations a saturated rung
  still plays the full point every round.
- `monitor.supervisor_kill_grace_sec: 120.0` as the relation (≥ 2 × `eval.worker_kill_grace_sec`
  10 + the pool's stop ≤ 10 s, with margin).
- `monitor.drain.*` is NOT minted: the block at its schema defaults (900 × 3, 14 400, 14 400) is
  refused by `tools/config_diff.py --from-header` as a lying header (a minted row equal to the
  template is a claimed delta that is not one), and the boot's `resolved_config.yaml` records the
  four caps in full. The row's intent (the caps are READ at the mint) is met by this note.
- `seed: 20260914` — a dispatcher's value (the packet's date), disclosed as such.
- **`eval.rung_concurrency: 8` — a NEW row, the one the OPERATOR MUST RATIFY OR REVERSE before
  START.** The sealbot rung plays serially by R339(b)'s deliberate choice (then ~200 s of a
  ~7 000 s round); under these rows the rung IS the round — the frontier's PUCT-512 cells ran
  ≈ 40 s/game serially, so a 288-game point is ≈ 3 h against `eval.round_timeout_sec` 3 600 and a
  ≈ 2.6 h training interval, and every round would break by construction. The prereg's own
  "≈ 25 min per point" presupposed games in flight. The row mirrors `eval.concurrency` exactly
  (contract v28: default 1 = the serial arm byte-exact, one reader `_play_rung_block`, a fresh
  (candidate, opponent) pair per thread); 8 is the gate block's G. The shakedown measures the
  round's wall under it. Reversing it is a one-row re-mint to 1 — with a 3 h round.

## 6. As RE-MINTED under R352 (2026-09-14) — `configs/run7.yaml`, header replays (37 deltas)

The operator's forward of the run7-kind packet (R352) after the 4 h shakedown (`SHAKEDOWN7_2026-09-14.md`,
F-52). The mint is the §5 file replayed through its own header with EXACTLY four row changes,
`config_diff --from-header` MATCH; nothing else moved:

| key | §5 minted | §6 RE-MINTED | clause |
|---|---|---|---|
| `selfplay.search.kind` | `puct` | **`gumbel`** — run6's trainer (320/64 at p 0.25, σ = template's `c_scale` 1.0 / `q_rescale` true, `gumbel_m` 16, `fast_policy_weight` 0.0, α = 1.0 rows excluded) | R352(a) |
| `train.policy_target` | `raw_visit_distribution` | **`completed_improved_policy`** (follows the self-play kind, as run6) | R352(a) |
| `train.policy_loss_weight_schedule.warmup_steps` | 2000 | **0** — the delta is DROPPED (template value): "the value warm-up is dropped to keep the swap single" | R352(a) |
| `train.ply_cap_abort` | `null` | **`{rate: 0.5, window_games: 600, min_step: 3000}`** — the ply-cap halt, exit 50; read every training step over the pool's last 600 games, fires strictly above 0.5 at or past step 3 000 | R352(c) |

Unchanged and now RATIFIED by the forward: `deploy.search.kind: puct`, `eval.gate.deploy_sims` /
`eval.sealbot_model_sims` 512, `eval.rung_concurrency: 8`, the 3 000 / 288 cadence and calibration
relations, `reinit: []` on the existing strip, `supervisor_kill_grace_sec` 120, the demoted trough
(`policy_loss_trough_abort: null`, a dashboard warning), `draw_rate_abort {0.25, 25000, 50, 3}`,
`seed 20260914`. `configs/run6.yaml` gains only the template's new `ply_cap_abort: null` row (no
delta; the file stays the record of what run6 ran).

**The halt's terms read against the record that ordered it.** On shakedown7's 2 421 games the
600-game windowed cap rate first exceeds 0.5 at game 2 229 (+3.4 h, train step ≈ 2 170) and ends
at 0.68; `min_step` 3 000 was never reached there (600 steps/h), so on THAT trajectory the fire
would have waited for step 3 000. Under run7's Gumbel regime at run6's ≈ 1 160 steps/h, step 3 000
is ≈ 2.6 h and the window (600 games ≈ 30 min) is read every step from game 600 on — `min_step`
gates only the fire. run6's block never exceeded 0.005. The terms are the ruling's; this is what
they mean in hours.

The shakedown twin (`shakedown7g`, only delta `run_id`, `config_diff --expect run_id` MATCH) is
minted from the same command into the box's `/workspace/oc7/`; it needs its own stamp.

## 7. One more row after the stamp (2026-09-14) — `eval.round_timeout_sec: 10800`

The re-minted config's preflight PASSED (`RUN7_STAMP2_2026-09-14.md`: `sealbot_d5` 0.507 at step
101) and its terminal round took 5 782 s — the screen escalated to the confirm block and the
288-game rung alone took 48 min under `rung_concurrency 8`, because the vendored sealbot holds
the GIL through its search and only the candidate's side overlaps. The terminal round runs under
the drain caps, but every IN-RUN round is bound by `eval.round_timeout_sec`, an operational
constant (schema default 3 600, `OPERATIONAL_DEFAULT_KEYS`) no run had minted; at 3 600 every run7
round would have been killed. MINTED at **10 800 s** as a `--mint-row` (38 deltas, header
replays): a ceiling for the contended round the twin shakedown measures, not a target. A round
longer than the 3 000-step cadence (≈ 2.6 h) skips the next kick (`eval_skipped_busy`), which the
dashboard's rounds panel shows; the lever that shortens the round — the GIL release in the
sealbot patch, proposed in the stamp record — is R145-class and the operator's.

## 8. `eval.round_timeout_sec: 14400` after the GIL patch's stamp (2026-09-14 16:20 UTC)

The re-stamp on the GIL-patched tree (`2177c926`) read the rung at **21 min** for 288 games
(48 before) and `sealbot_d5` 0.635 at step 101 — but its gate block took 61 min (screen 25,
confirm 36; 36 on the first stamp: the games ran 10–26 % longer and the per-ply cost was ≈ 1.5×
higher, unexplained), so the escalated idle round is 82 min and, at the twin's measured 2.3×
contention, ≈ 3.1 h against a 10 800 s bound. A killed round loses its reading; a long one only
delays the next kick. MINTED at **14 400 s** — the drain's own hard cap — on the operator's
"lose no data" direction (2026-09-14); header replays, 38 deltas. The twin config follows.
