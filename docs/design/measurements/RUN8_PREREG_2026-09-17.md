# PREREG — run8: the gate's best_model + A-2 at HEAD + the σ swap (raw q, c_scale 1.0) + the 1-in-8 search-stats producer (R356(c), minted 2026-09-17)

Written at the mint, before the stamp, the shakedown and START. Every figure below was read from the
tree at the mint commit, from the box's run7 record over `ssh` (read-only) or from the operator's
mirror of it; nothing is carried from a register. The ruling is `docs/governance/RULINGS.md` R356;
the packet's §0 is the signature.

## 1. The config — `configs/run8.yaml`

Minted by `tools/mint_config.py --template dev` replaying `configs/run7.yaml`'s 41 header deltas
(`ce0a8ff6` lineage, contract v32) with THREE rows changed — the whole of what R356(c) arms:

| key | run7 (resume mint) | run8 | why |
|---|---|---|---|
| `run_id` | `run7` | `run8` | the run's name; every path derives from it |
| `selfplay.q_rescale` | `true` | **`false`** | THE swap: raw q ∈ [−1, 1] into σ, `c_scale` 1.0 unchanged — the paper's Go/chess pair (R355(d) grounds; F-50/F-51 name the pairs); inert on the PUCT deploy/eval heads, live on the Gumbel self-play target |
| `identity.warm_start.checkpoint` | `checkpoints/bc/run6_00006500_ca1afb71.ckpt` | **`checkpoints/run7/run7_00042000_46fdb931.ckpt`** | the gate's best_model at mint (§2) |
| `identity.warm_start.net_hash` | `2e72abd4…` | **`a9a46c55bd1ceadb38145fef6527254d77d37900aebea4332def55ca75f56bfc`** | `net_param_hash` of that checkpoint's 50 tensors; the boot refuses any other net |

`reinit: []` (every head copied; the step-0 witness is the live net's hash equal to `net_hash`).

Asserted by the repo's own instrument, not by eye: `tools/config_diff.py configs/run7.yaml
configs/run8.yaml --expect run_id --expect selfplay.q_rescale --expect identity.warm_start.checkpoint
--expect identity.warm_start.net_hash` → `MATCH` (exactly those four leaves differ), and
`tools/config_diff.py --from-header configs/run8.yaml` → `MATCH` (the header's 42 claimed deltas
are the real ones). The one non-value difference in a textual `diff`: the three rows run7's later
re-mints APPENDED to its header (`eval.gate.screen_confirm_lo`, `eval.max_plies`,
`eval.gate.sequential`) sit earlier in run8's header, because the tool writes every `--set` line
before every `--mint-row` line — the same three rows, the same values, a different line order; a
hand re-ordering would be a hand-varied config (R1), so it is reported here instead.

Confirmed present and byte-equal (the packet's list, each read off both files):
`selfplay.search_stats_every: 8` (the template's row, not re-added); `eval.gate.sequential`
{mu0 0.52, mu1 0.62, α 0.05, β 0.1, 8-of-16–104}; `train.ply_cap_abort` {0.5, 600, 3000};
`train.draw_rate_abort` {0.25, 25 000, 50, 3}; `train.eval_interval` = `checkpoint_interval` 3 000;
the 288-game point (`ladder.round_games`, `calibration_games`, `rungs[sealbot_d5].games_max`);
`eval.gate.deploy_sims` 256 and `eval.sealbot_model_sims` 256 (the rung unit); `opening_book`
`book_v1_s20260625_p4` on gate and rung; `eval.max_plies` 256; `eval.round_timeout_sec` 14 400;
`selfplay.search.kind: gumbel` 320/64 at p 0.25, `c_scale` 1.0, `gumbel_m` 16;
`train.policy_target: completed_improved_policy`, `fast_policy_weight` 0.0; `ema.enabled: false`;
`seed: 20260914` (byte-equal by the packet's rule; a different warm start draws different games from
the same seed, and the seed was disclosed as a dispatcher's value at run7's mint).

A-2 (the two-stone-turn quiescence unit) is CODE at HEAD — `2865cd3a` on `30198f05`'s primitive —
not a row: run8 gets it by being minted on this tree, run7 (tree `ba51fd46`) never had it. Its
falsifier record (`A2_QUIESCENCE_FALSIFIER_2026-09-16.md`): on the 23829 ring's 275 residue rows
the counter-threat child read ≥ the block child on **275 of 275** under the one-stone rule and
**0 of 275** under the corrected unit; leaf verdicts +1/−1 fire on 2.7 % each of 4 439 random rows,
the blend on 0.3 %. Its in-run fire count now rides `iteration_complete.mcts_quiescence_fires`
(cumulative since boot; landed with this mint so the shakedown can read it, LAW-18).

## 2. The parent — the gate's promotion record on the box, read 2026-09-17 19:45–19:56 UTC

| what | value |
|---|---|
| `/workspace/runs/run7/checkpoints/best_model.pt.provenance.json` | `{"step": 42000, "run_id": "run7", "encoding": "gnn_axis_r8", "promoted": true}` |
| `best_model.pt` sha256 (box) | `a3c282665bd9257f70e8cc327102f833ce56be7bc4dfb6e5916e881240d7ad35` (1 229 085 bytes, 2026-09-16 14:53 box time) |
| the checkpoint at that step | `run7_00042000_46fdb931.ckpt`, sha256 `5750cca4af2043a11daf769e9c3fbd35b0fe38a2e14ac4691b1401a4994eaea2` on the box AND on the mirror (receipt cycle 53) |
| identity | `best_model.pt`'s `model_state` is tensor-equal to the checkpoint's 50 tensors; both hash to `net_param_hash` `a9a46c55…` |
| the round | r000012 @42 000, `promoted: true`, `wr_sealbot` 0.681 (the 256 unit), wall 3 853 s; the only other promotion since the resume is r000006 @24 000 (0.660); r7–r11, r13–r21 not promoted |
| the dashboard / STRIX record | `STRIX_RUN7_60K_2026-09-17.md` names 42k "`best_model`, promoted r12" — record and dashboard AGREE, no halt |
| the parent's strix reading | 0.142 [0.104, 0.188] in the AS-SHIPPED unit (ours PUCT-512 vs strix 128); its 256/256 reading — R356(a)'s unit bridge and run8's baseline — is the follower's FIRST cell, owed |
| run7 at the mint | trainer step 72 025, r000022 @72k in flight; run7 CONTINUES until run8's stamp is vested (R356(b)) |

Not the 9k anchor (R355(d), superseded — ANNOTATION A2 under R355's foot): 9k read 0.097 when 15k
read 0.045; the 24k–60k plateau (0.14–0.17) is above it, and the gate is the promotion instrument.
NO hand re-ranking: 48k reads 0.163 and the rung's peak 0.710, and is not the parent, because the
gate did not promote it (R356(a): "no best is acted on before three cells agree").

Path on the box: the run tree resolves `checkpoints/run7/run7_00042000_46fdb931.ckpt` against its
own root, so the box block COPIES the checkpoint from `/workspace/runs/run7/checkpoints/` into
`<run tree>/checkpoints/run7/` and verifies the sha256 above before the stamp (the stamp then pins
it; LAW-12).

## 3. The shakedown WITNESS (R356(c)), pre-stated

R356(c): "mean policy-target entropy over the shakedown's full-arm rows must exceed run7's 0.002
nats by an order of magnitude — if it does not, the key did not reach the target and START is a
HALT." The figure verified at HEAD:

- run7's **0.002 nats is the forced-move census's H(explicit) MEDIAN** — the entropy of a ring row's
  explicit masses renormalised by 1 − α (`FORCED_MOVE_CENSUS_2026-09-15.md`, run7 table: median
  0.002–0.003, mean 0.18–0.20; "not the trough record's H(target), which the trainer rebuilt with
  the tail spread over the net's own prior; the two are not comparable to the decimal").
- Re-derived on the mirrored rings with `python -m mantis.diagnostics.ring_reader <ring>` (the
  `H(explicit)` line landed with this mint): 23829 ring pooled median 0.0016 / mean 0.190; 42k
  ring 0.0017 / 0.199; 69k ring 0.0021 / 0.195 — the census's numbers. FULL-ARM rows only (≈ 25 000
  of 100 000 per ring): **median 0.0000, mean 0.105–0.111, p75 0.027–0.034** on every ring read.
- run7's in-run `trainer_step.policy_target_entropy` (the trainer's H(target), tail included) reads
  a mean of 0.119 (seg0001) and 0.122 (seg0002, 47 964 steps).

So a MEAN over the full arm is 0.105–0.122 on run7 already and cannot be the line: "mean > 0.02"
is satisfied by the run the swap is meant to move away from. The witness is stated on the
statistic that read 0.002.

The witness as R357(a) states it (this line REPLACES the one first written here; R356(c)'s "mean"
sentence is annotated, A1 under R356's foot), three-part:

> **(a) PIN, dev, before the box** — `tests/selfplay/test_qsigma_rescale_reaches_target.py`: one
> fixed root with two candidates at Δq ≈ 0.01 and one at Δq ≈ 0.2, the real completed-Q target
> builder, rescale true vs false — the two targets differ; under false the logit gap between the
> two candidates equals (c_visit + max_n)·c_scale·Δq to 1e-6; under true the Δq ≈ 0.01 row is
> one-hot (H < 1e-3) and under false it is not. Symbol path `selfplay.q_rescale` →
> `SelfPlayHParams.q_rescale` → the runner's `q_rescale` ctor kwarg → `QSigma.rescale` at the
> workers' `configure_search` → `get_improved_policy_ls`, the completed-Q σ behind
> `completed_improved_policy`. **Pin FAIL → HALT, no START**, the report names the consumer chain.
> **(b) RING** — the shakedown ring beside the step-3 000 checkpoint, `is_full_search` rows,
> `python -m mantis.diagnostics.ring_reader <ring>` (`full … median`) and
> `python -m mantis.diagnostics.ring_audit <ring> --bands <this file>`: **PASS** = the MEDIAN of
> H(explicit) **> 0.02 nats** AND the one-hot share (rows with H < 1e-3) **< 25 %** (run7: 0.0000 /
> ≈ 48 %). **(c) Pin PASS + ring FAIL → HALT to the architect**, with the two Δq census rows re-read
> under the new key: "raw q does not soften targets on this game" is a finding, filed, not a fault.
> **Both PASS → START.**

Reported beside it, not gating: the full-arm mean (run7 0.105–0.111), p75 and p90; the pooled
median/mean; the quick-arm stats; and the mean of `trainer_step.policy_target_entropy` over the
shakedown's steps (run7 0.12). The clause's word "mean" is not edited (register text corrects by
annotation only); the derivation above is why the median carries the witness.

### 3a. The ring audit's bands for run8 (R357(b); `mantis.diagnostics.ring_audit` reads this block)

The audit runs on the shakedown ring before START and on every mirrored 15k ring; exit 1 on a miss.
Each key is a row the audit prints, each operator is strict as written; a banded row the ring
cannot measure is a miss, never a pass. R359(b), 2026-09-18: only the PRE-START audit HALTS; a miss on
a live-run (15k) ring is REPORTED — in STATE and beside the reading — and stops nothing by itself.

```toml
[ring_audit.bands]
counter_threat_share = { lt = 0.005 }   # run7 pre-fix 2.5–4.4 % (the census's mass(F) < 0.1 on forced-BLOCK rows); the A-2 falsifier 0/275
quiescence_residue = { le = 0 }         # the A-2 falsifier's oracle: counter-threat child ≥ block child through the real backup
h_full_median = { gt = 0.02 }           # §3(b), nats, is_full_search rows
one_hot_share_full = { lt = 0.25 }      # §3(b), rows with H(explicit) < 1e-3
cap_rate = { lt = 0.05 }                # games with value_valid == 0 (terminal_reason ply_cap); run6 0.2–0.5 %, F-52 0.84
```

Unbanded, reported: target mass on the block (median, share ≥ 0.5), quick-arm H stats, draw share
and mean |z| (the first measurement under A-2); sample age is NOT MEASURED from a ring — it carries
no step field — and the audit says so rather than printing a zero. Since R358(c) (2026-09-18) the
replay ratio and the augmentation bins are READ off the run's events with `--events <events jsonl>`:
`replay_ratio` (Δsamples_consumed_total ÷ Δpositions_produced_total over the ring's span, with the
span's wall hours; run7's derived 3.6, strix's setpoint 8.0) and `sym_bin0_over_mean` (1.0 uniform,
12 a draw stuck on the identity) — both reported, neither banded for run8.

The instrument on run7's mirrored rings (2026-09-18, the tool's landing commit, ≈ 55 s per ring),
the pre-fix baseline the bands cite: **23829 ring** — forced-block rows 23 427, mass on B median 1.0,
share ≥ 0.5 96.8 %, counter-threat share **2.89 %** (the census's 2.5–4.4 %; row 3664 is in the
named list), residue **0 of 275** through the backup (the falsifier's 275 candidates, its 0 under
A-2), full-arm H median 0.0000 / mean 0.109 / p75 0.032, one-hot share **64.8 %** in the full arm
(R357(a)'s "≈ 48 %" is the POOLED share: 42.9 % quick, 48.4 % pooled), cap rate 0.21 %, draw share
0, mean |z| 1.0. **81000 ring** — counter-threat 3.07 %, residue 0 of 184, full-arm median 0.0000 /
mean 0.153, one-hot share 61.9 %, cap rate 0.28 %. Both rings MISS the same three run8 bands
(counter-threat, `h_full_median`, `one_hot_share_full`) and pass the other two, which is what a
pre-fix ring should read; run8's shakedown ring is the first that can pass all five.

## 4. The PRE-REGISTERED READING (R356(c), verbatim), in the 256/256 unit

Instrument: the strix follower's equal-work cell — ours PUCT-256 vs strix 256 sims, 288 paired
games on `book_v1_s20260625_p4`, both colours, deterministic, pair-level CI over distinct games
(LAW-04) — written as a sidecar beside the checkpoint it reads. The BASELINE is the parent
(`run7_00042000_46fdb931.ckpt`) read once in the same unit; it is also R356(a)'s bridge from the
as-shipped series.

- **SUCCESS**: run8@30k ≥ parent + 0.05.
- **FALSIFIED**: run8@15k ≤ parent AND run8@30k ≤ parent — the σ swap is dead, filed in
  `falsified.md`; A-2 alone is run9's base.
- otherwise **INCONCLUSIVE**: read 45k; the three-cell rule (no "best" acted on before three
  cells agree).

The same three lines NUMERIC (R359(b), 2026-09-18 — a restatement of the bullets above, not a new
value): the baseline is the parent at 256/256 with strix's solver ON, **0.111 [0.073, 0.149]**
(read 2026-09-18, CONTENDED beside run8, 288 paired games; CARD-STRIX-NET-ONLY). **SUCCESS**:
run8@30k ≥ **0.161**. **FALSIFIED**: run8@15k ≤ 0.111 AND run8@30k ≤ 0.111. Otherwise
**INCONCLUSIVE**, read 45k. The solver-OFF cell (0.115) is the decomposition, not the baseline.

**READ (2026-09-20, R363(a)):** 15k **0.104 [0.069, 0.139]** (the FALSIFIED line's first half met by
0.7 pp), 30k **0.135 [0.097, 0.177]** — not ≥ 0.161, not ≤ 0.111 → **INCONCLUSIVE**; run8 continues to
45k and its cell decides under the three-cell rule, whose form for the run9 parent is PRE-STATED in
`RUN9_PREREG_2026-09-19.md` §1c (R363(b)) before that cell is read. One-hot share 27.4 % flat,
entropy holds — reported, stops nothing (R359(b)).

Every cell carries its regime (CONTENDED beside a live trainer, IDLE otherwise): the 60k record
measured 14–19 s/game contended against 7–8 idle, and the WR did not move with it, but the label
travels so a later reader does not have to assume that.

## 5. Gate 12 at the mint

`tools/ci_gates/preflight_mint.py --audit-only` rc 0 with `configs/run8.yaml` in
`PRODUCTION_CONFIGS` (bound by name beside run6 and run7, the by-name pins widened in the same
commit). The same FOUR rows DEFERRED as at run7's resume: `policy_loss_trough`,
`ply_cap_attractor`, `grad_norm_hard_abort`, `sealbot_wr_abort` — each prints its arming surface
and cadence on every run and gates nothing. Gate 7: every config validates.

## 6. The shakedown's reads (the packet's §5 step 10), each with its producer at HEAD

3 h twin of run8 in its own run dir, through one CONTENDED gate round at step 3 000; every number
stated with its regime. As run (2026-09-18): the 3 h cap CUT the 3k round 44 s after it started
(1 004 steps/h), so its wall was not read; R359(e) sets a twin at **4 h** so its 3k round is read.

| read | producer |
|---|---|
| steps/h, games/h | `iteration_complete.steps_per_hour`, `.games_per_hour` |
| plies | `game_complete.moves` (PLIES — one compound turn is two, LAW-03); `avg_game_length` and `positions_per_hour` are in TURNS |
| cap rate from step 0 | `monitor_gates.ply_cap_rate` over the halt's 600-game window; `game_complete.terminal_reason == ply_cap` |
| quiescence fire counters (LAW-18) | `iteration_complete.mcts_quiescence_fires` (cumulative; diff rows), new at this mint |
| search-stats shards 1-in-8 | `selfplay.search_stats_every: 8`: a sampled game's record carries `search_stats` (`docs/contracts/game_record.md`); read with `mantis.monitor.game_record.iter_run_games` / `tools/game_viewer.py` (the viewer's `stats` flag). `mantis.diagnostics.ring_reader` reads the RING, not the shards — it serves §3's witness |
| Gumbel self-play spent == budget | the suite's conformance section for every search kind (R355(b)) |
| gate round wall, GSPRT `pairs_played` / `stopped` | `eval_round_complete.wall_sec`, `.gate.rule` / `.gate.pairs_played` / `.gate.stopped` |
| the witness | §3 |

## 7. Sequence (R356's ROUTE)

mint (this commit) → operator push → box: preflight (disk/RAM/GPU recorded; `git fetch && git
checkout <mint commit>`; `make build.cuda`; `torch.cuda.is_available()` True — never a bare `uv
sync`) → copy + sha-verify the parent into `checkpoints/run7/` → stamp run8 (LAW-12, written once)
→ puller re-pointed at run8's dir (the OPERATOR's machine) → run7 STOP (one SIGTERM, wait for
`shutdown_save`, one puller cycle receipts the bundle, record the final step; if run7 has not
reached 75k, the 75k point is not read) → box quiet → 3 h shakedown → witness (§3) → START on the
vested stamp → the follower's first cell (the parent at 256/256) queued beside it → STATE repaired
in one line.

The twin's preflight (R359(e), CODE by R360(c)): a shakedown twin — the run's rows with only
`run_id` changed — INHERITS its run's VESTED preflight and is not preflighted again:
`python -m mantis.run --config <twin>.yaml --out-dir <twin dir> --inherit-preflight <run>.yaml`.
The trap (`require_preflight_stamp(inherit_from=)`) takes the run's stamp only if it is vested on
THIS tree (pass, MIRRORED, HEAD-bound), diffs the two configs leaf by leaf and refuses by name any
difference beyond `run_id` (`PreflightStampTwinMismatchError`; the pin plants a `seed`); the twin's
own stamp is then written with `inherited_from: <run sha>` and "preflight inherited from <sha>",
and a stamp of the twin's own always wins. shakedown8 was stamped on its own (≈ 1.5 h) before this.

The follower, as landed (`tools/strix_follower.py`, R356(a)), from the run tree on the box:

- the first cell, the parent at 256/256 (its sidecar lands BESIDE THE PARENT, in run7's dir):
  `uv run python tools/strix_follower.py --config configs/run8.yaml --run-id run8 --run-dir
  <run8 dir> --work-dir <work>/strix_follow --once <run7 dir>/checkpoints/run7_00042000_46fdb931.ckpt`
- the chain, detached beside run8 (`ssh -n`, PATH exported, one waiter): the same command with
  `--follow` in place of `--once` — every 15 000-step `periodic_checkpoint_save` and every
  `eval_round_complete.promoted` fires one cell; an existing sidecar is the receipt.
- the page: `make dashboard EVENTS=… LADDER=… EXTERNAL="<run8 dir>/checkpoints <run7 dir>/checkpoints"
  OUT=…` draws both series (run8's points and the parent's bridge cell, each on its run's step
  axis) with the gap to strix as a number.
