# REVIEW-W4 — SLIM-FIX wave 4 (47dd9a05..51eddda5)

Filed verbatim from the fresh read-only review agent (R367(b)); §5 is the dispatcher's disposition.

## §1 Scope and method

Range: 9 commits, 94 files (+533/−943; net −410). Read CLAUDE.md, RULINGS `### R367` and `### R368` (esp. (b) incl. "The same holds for … tools/ci_gates/**", (d), (e), (g), (i), (k)), PROGRESS (W3 exit incl. the KNOWN-RED record; W4 state), WAVE_BRIEF + W4_ADDENDUM, S-A-TOOLS-1/-2, S-L-STYLE (defect 23), REVIEW-W3 (format precedent; its finding 5/6 card candidates grepped — neither appears as code in this range). Every commit's diff read in full.

**A — R368(b) on tools/ci_gates/**: `git diff-tree` per commit over `tools/ci_gates tools/config_templates tools/mint_config.py` → the ONLY ci_gates files touched are `comment_lint.py` + `lint_gate.sh` + `comment_length_floor.txt` + `test_count_floor.txt` (5fc33aa1), `test_count_floor.txt` (60755d15/44678400/a9528a77), `encoding_io_gate.py` (51eddda5). `lint_gate.sh`'s edit is exactly the two ratchet-name lines (R346(f)→R368(g), echo text). `comment_length_floor.txt`: the two new rows land only in 5fc33aa1 (`ruling_cite_lines 1210`, `textfile_comment_excess_lines 466`) plus one down-fold in 60755d15 (docstring_excess 12268→12239). `test_count_floor.txt` moves only as folds. `tools/config_templates/**`, `tools/mint_config.py`, `.github/workflows/ci.yml`, `check_tracked_refs.py`, `tier_declaration.txt`: byte-untouched (empty diff). The addendum's permitted `check_tracked_refs.py` DISSOLVED_PATHS route was not needed (docs reworded instead — the preferred route). **No breach.**

**Planted/ratchet arms** (scratch worktree `.wt/review-w4` @ 51eddda5, removed after; main checkout tracked-clean at start and end):
- gate 14 self-test: `comment_lint.py --self-test` → "every measurement and verdict arm fires", rc 0. Producer tests `tests/tools/test_comment_lint.py` → **65 passed**, incl. the two new plant-and-move arms (`test_a_bare_ruling_cite_in_a_docstring_moves_ruling_cite_lines`, `test_a_hash_run_in_a_scoped_textfile_moves_textfile_comment_excess_lines`, `test_the_tree_measures_its_two_new_measures_on_real_files` over a scratch git tree).
- Ratchet arm (i): bare `R123` appended into a tracked docstring → `FAIL (grew): ruling_cite_lines is 1211, floor is 1210`, rc 1. Arm (ii): 4-line `#` run appended to tracked `tools/vendor_build_strix.sh` → `FAIL (grew): textfile_comment_excess_lines is 468, floor is 466`, rc 1. One-digit `R8` appended alone → both measures unmoved, GREEN (gate-15 interplay holds; also pinned by self-test and producer fixtures). Floor equality: full gate run → `3169/0/12239/1378/2486/1210/466` = floor on all seven, `GREEN`. `ruling_cite_comment_lines` gone from code/floor/tests (`git grep` → only CARDS.md history, finding 3). Lenient ref-floor: strict parse of a predating floor raises; lenient parse ratchets the measures it has (`test_a_reference_floor_that_predates…` → rc 1 "ratchet"); the live gate prints the expected "predates ruling_cite_lines, textfile_comment_excess_lines; … NOT armed" notice.
- gate 16: `encoding_io_gate.py` → "no encoding-less text I/O anywhere in the tree (728 tracked .py files; 1 registered exemption(s))", rc 0; EXEMPT row reworded and still matching; producer tests → **22 passed** (incl. the `_is_os_open` arm and green-on-tree). `os.open` skipped by receiver at scan level while `is_unsafe` still flags it (fail-loud preserved). No added `# encoding-gate: ok` hatch anywhere in the range (the only added mention is the gate's own docstring describing the mechanism).
- board-renderer parity (I): independent node harness, old inline draw body extracted from `47dd9a05:tools/viewer/html.py` vs committed `board.js` through the new `HexBoard.draw` composition — 6 scenes: midgame, win-at-final-ply, heat-with-visits, fast-arm stones, heat+win, empty board. Result: **byte-identical (viewBox and body) on every scene except the heat scene, where the only diff is the grid-cell class** `cell` → `cell win hot` on visit cells — exactly the leg's "one inert-class diff"; inertness verified against the viewer's CSS (no `.win`/`.hot` selector; the one-line marker at viewer/html.py:141 states it). (First harness pass was vacuous — I fed malformed visit triples and both sides NaN'd identically; fixed and re-run as above.) Both pinned witnesses green (test_analyzer_html, test_game_viewer).

**Run10 safety (F)**: `git diff 47dd9a05..51eddda5 -- configs/ tools/config_templates` → **0 bytes**; `run10_resolved.py configs/run10.yaml | cmp - run10_resolved_base.json` → **rc 0, silent (MATCH)**.

**Floors (G)**: collected at HEAD = **5035** = `test_count_floor.txt` (gate 3c: `collected=5035 floor=4862 ref=origin/dev tree_floor=5035`, tier census "76 deselected, all declared"). Every floor move rides its causing commit (`git log -p`): 60755d15 5041→5030 (test deletions; docstring floor −29), 44678400 +1 (producer test), a9528a77 +1 (pin test), 5fc33aa1 +3 (producer tests) + the two measure rows.

**Python/tests**: targeted — test_comment_lint 65, test_encoding_io_gate 22, the five loader-consumer files **67 passed 1 skipped** (the skip is the pre-existing R7 173 MB-record guard), test_ladder_client + test_game_record **30 passed** (endpoint pin green), test_vendor_pins_strix + test_meta_ci 10 passed. `ruff check .` → clean; pyright via the gate's own invocation (`UV_NO_SYNC=1 uv run pyright --outputjson`) → **0 errors** (a bare direct run without the venv env misresolves the interpreter and is not the gate's measurement). Gates green: 3c, 9 (171 modules, no cycles — loadpkg added), 10, 12 (`--audit-only` rc 0), 15 (166 justified, 0 stale; new files 30/38/151 lines, no headers), 16. **J (default tier, full)**: `4984 passed, 8 skipped, 43 deselected, 0 failed` in 378 s — no failure at all; on the KNOWN-RED posture see §4.

## §2 Findings

| # | sev | commit | file:symbol | finding | evidence (command → result) | suggested fix |
|---|---|---|---|---|---|---|
| 1 | NOTE | a9528a77 | tools/viewer/hexlogic.py::win_line | TOOLS-2-08 landed as an AMENDMENT to the addendum's spec, not as written: the addendum ordered the engine's `find_winning_line` adopted and "the 4 hexlogic tests and the helper/fixture go"; the commit keeps the local scan and all four tests, adds one engine-parity pin, and skips reader.py's replay→Board path. The ground is TRUE — I verified it in `crates/mantis-core/src/board/moves.rs:463`: `find_winning_line` fast-paths the last stone but its sorted-stones fallback returns an EARLIER stone's line when the last stone doesn't complete one (its own doc: "the first stone of a turn can complete a line the second misses"), which would mask exactly the completing-stone disagreement the viewer's function exists to catch (LAW-03's completing cell). Deviation is disclosed in the subject ("the local scan keeps the last-stone contract") and grounded in the docstring. Net +11 on a DUP row. | `sed -n 463,505p crates/mantis-core/src/board/moves.rs` (fallback loop over sorted stones); `git show a9528a77 -- tests/tools/test_game_viewer.py` (+8, `test_the_axes_and_win_length_are_the_engines_own`); `git diff --shortstat` → +17/−6 | record it in the W4 exit PROGRESS/LEDGER as AMENDED-with-ground (constants deduped, scan kept), so the row is not read as DONE-as-specified |
| 2 | NOTE | 5fc33aa1 | tools/ci_gates/comment_lint.py (module docstring) | The docstring line "bare cites are R10-and-up so gate 15's ``R8`` stays out" itself matches the new bare-cite regex (`R10` + word boundary at `-`), so the gate's own explanation contributes one line to the gated `ruling_cite_lines` baseline. Harmless — it is measured into the 1210 floor and the text is what the addendum ordered ("state this in the docstring") — but it is one floor line the W8 cite pass can reclaim. | `\bR\d{2,3}\b` vs the line; gate run shows `ruling_cite_lines=1210/1210` with the docstring in scope | optional W8 reword ("two-digit-and-up"); nothing now |
| 3 | NOTE | 5fc33aa1 | docs/governance/CARDS.md:506 (CARD-STYLE-BACKLOG) | The card still presents `ruling_cite_comment_lines 351 (measured, not gated)` as the live figure for the cite class; the counter was retired and replaced by the gated `ruling_cite_lines` in this range. Docs-only (the review contract's code/floor/tests check is clean), and the card is the W8 sanctioned pass's natural consumer — but as written it is a stale measurement a future reader could cite. | `git grep -n ruling_cite_comment_lines` → CARDS.md:506 + the floor file's own provenance row only | W6/W8: refresh or annotate the card's measure list when the style pass lands |
| 4 | NOTE | 51eddda5 | tools/ci_gates/encoding_io_gate.py (vs W4_ADDENDUM "Checks") | The addendum's per-commit check list names `encoding_io_gate.py --self-test`; no such flag ever existed (grep at 47dd9a05 and HEAD → no self-test entry point). The arms live where LAW-07 wants them — tests/tools/test_encoding_io_gate.py (green, incl. the `_is_os_open` arm) — so the gate is sound; only the brief's text was wrong. | `grep -n "self" tools/ci_gates/encoding_io_gate.py` → none; `git show 47dd9a05:…` → none | one-line correction when the brief is archived; no code change |
| 5 | NOTE | — (PROGRESS.md at HEAD) | docs/slim/PROGRESS.md "W4 state" | PROGRESS still reads "W4 state: … NOT yet integrated … gate-14 measure leg and gate-16 widening leg are next", and the Ledger section is still "(filled per wave)" — both stale against the landed 9 commits. Expected sequencing (the wave order is REVIEW-W4 → exit gates → PROGRESS), named here so the exit update carries: the per-row ledger, the re-lane table the addendum orders, finding 1's amendment, and the Δlines judgments of §3. | `grep -n "W4 state" docs/slim/PROGRESS.md`; `git log 47dd9a05..51eddda5 --oneline` (9 commits) | fold into the W4 exit PROGRESS rewrite |

No MUST-FIX, no SHOULD-FIX. No protection breach (A holds exactly), no witness narrowed, no run10-safety breach, no LAW-07 break (every new gate input has live producer arms), no census/by-name pin edit, no new `# noqa`, no new run-named symbol (the run6.yaml tokens in the gate-16 sweep are pre-existing lines that only gained `encoding=`), no added escape hatches, and the only added ruling tokens outside CLAUDE.md are the floor file's own provenance rows, lint_gate.sh's two authorized ratchet-name lines, the ordered "R10-and-up" docstring clause, and regex/test fixture strings (mechanism, not commentary).

## §3 Per-row verdicts

- **L15 TOOLS-2-15 (6adbeebf)** — HELD. `challenges`/`list_bots`/`cancel` deleted (−8 in client.py incl. separating blanks, matching the scout's amended probe); the one test line went with `cancel`; the endpoint pin `test_endpoint_strings_live_only_in_the_client_module` stays green (30 passed in the file); no caller anywhere (`/api/bots` residue is BUILD.md's by-hand procedure, pre-existing).
- **L16 TOOLS-2-12** — RECORD-ONLY as ordered (landed in W3, 84a4eef0, outside this range). Holds.
- **L17 TOOLS-1-01 (622893b6)** — HELD. Both stale counts dropped, nothing re-transcribed (R192(e) derive-or-delete); the surviving `(AUDIT-1 F-09)` cite are pre-existing context; Makefile outside ci_gates, so no R368(b) question.
- **L18 TOOLS-1-14 — KEEP by R368(k)**: verified as a disposition, i.e. `.github/workflows/ci.yml` byte-untouched in the range.
- **L19 TOOLS-2-07 (71903a8d)** — HELD. `mantis/util/loadpkg.py` (30 lines): `spec_from_file_location` + `submodule_search_locations`, zero `sys.path` writes (repo-wide grep: prose mentions only), one-line R5 ground in the docstring, `Raises: ImportError` named, `__all__`. All seven copies replaced (5 tools + conftest + test_probe1); the only remaining `submodule_search_locations` in the tree is the helper itself; callers use fully-qualified `from mantis.util.loadpkg import …` (the util `__init__` no-re-export contract respected; util stays torch-free). The `_REPO_ROOT`-from-installed-package semantics equal the old per-file resolution in every sanctioned route (uv sync is editable — `_editable_impl_mantis.pth` verified — and the `repo_root` kwarg covers embedding). Net −79; the +48 wiring is the sanctioned exception and is justified (five shims → one import each). Loading tests 67 passed.
- **L19 TOOLS-2-08 (a9528a77)** — HELD AS AMENDED (finding 1): the constants are now the engine's own exports (pinned value-and-order by a new test, floor +1); the win-line scan stays local on a ground I independently confirmed in the Rust. Sound deviation, disclosed; needs its LEDGER line.
- **L19 TOOLS-2-09 (e87e3686)** — HELD. board.js is the one renderer (header comment updated, `fast` scene field added); viewer/html.py inlines it at render time exactly as analyzer/html.py does (`_BOARD_JS.read_text(encoding="utf-8")`); my parity harness reproduces the leg's claim (§1); net 21/21.
- **L19 TOOLS-2-10 (44678400)** — HELD. `run_shard_paths` is the one shard enumeration (`_SHARD_RE` + `(seg, hour, name)` ordering now live once, in game_record.py); `iter_run_games` reads it; viewer/reader.py's twin deleted; LAW-07 producer test added (floor +1). Net +8 overall is the producer test — src nets −6; judged sanctioned.
- **Sealbot vendor side (60755d15, R368(e))** — HELD. Script, patch, pin block, Makefile target + `.PHONY`, both pin tests deleted; `vendor/patches/` gone; pins.toml carries only hexo-strix (no dangling patch reference); `make -n vendor.strix` resolves; meta_ci pin set == Makefile target set exactly; CLAUDE.md keeps `make vendor` and gains `make vendor.strix` per the addendum; eval_instrument v2→v3 bumped in the SAME commit with its history line and the pinning row re-pointed to `tests/tools/test_vendor_pins_strix.py` (exists, passes) — coherent with the doc's version convention. Residual `sealbot` tokens classify as allowed: RETIRED-stamp paths (`checkpoints.py`), R362(c)-era refusal/deletion notes in src, pre-R362 record rendering in tools/dashboard (lane C, untouched), regime-key test data, BUILD.md procedure prose. The one ADDED token is the v3 history line itself. Gate 10 green (no doc cites a deleted path).
- **Gate-14 leg (5fc33aa1, defect 23 / R368(g) / TOOLS-1-12)** — HELD. Both measures gated at their measured baselines with self-test and producer arms (§1); the old ungated counter deleted in the same commit (derive-or-delete); `_rust_doc_excess`'s loop folds into `_excess(cap=1)` (TOOLS-1-12) with its self-test arm intact ("rust doc run: want 2"); one-digit `R8` excluded with the interplay stated in the docstring; textfile scope is exactly the addendum's (tools/ `.sh/.yml/.yaml/.toml/.txt`, Makefile, workflows; tests/ excluded — pinned by the scope-predicate arm); CLAUDE.md's gate-14 text names both measures. The +103 growth is the ordered gate learning itself plus its LAW-07 arms — the sanctioned exception, judged justified.
- **Gate-16 leg (51eddda5, R368(g))** — HELD. Widened to every tracked `.py` (git ls-files, 728 files ≥ the 700 floor), function scope included, module-scope carve-out gone, `os.open` skipped by mechanism with the over-approximation still pinned in `is_unsafe`, EXEMPT row reworded (stale conftest grounds gone) and still matching, stale "639 tracked text files" message derived away, CLAUDE.md states the widened rule in both places. Site count honest: 207 added `encoding="utf-8"` occurrences − 4 tooling-side (the gate's own read + producer fixtures) = the subject's 203 tree sites (4 src + ~199 tests). The four src sites are consistent read/write pairs (pipeline↔worker spec/result JSON); ~30 tests/ sites spot-checked across 9 files — correct kwarg placement, nested expressions intact, no behavior change beyond locale-safety; E501 is dispositioned NEVER (documented) and ruff is clean; net −7 despite the sweep.
- **Re-lane dispositions verified as recorded (do-not-implement rows)**: TOOLS-1-02..06, 08, 09, 10, 11, 16 stay C on R368(b)'s "same holds for … tools/ci_gates/**" ground (and config_templates is the brief's do-not-edit) — confirmed untouched; the stale-text-inside-comment_lint/encoding_io_gate rides only the named edits (seen: gate docstrings/EXEMPT grounds/messages reworded inside the two authorized files). TOOLS-1-13 KEEP, TOOLS-1-15/+16 KEEP/stay C by R368(k) (workflow untouched). TOOLS-1-NEW-2 RESOLVED by R368(b) itself — record-only. TOOLS-2-01/02/03/04 KEEP (audit_bootstrap_corpus, probe1 `proofs`, dashboard pre-R362 lane, gen_mctx generator all untouched). TOOLS-2-13/-14/-16/-17 stay C on their PZ grounds (no site in this range; the probe1/swa site already died in W3).

## §4 Checked and holds

- **R368(i) run10 safety**: configs/ and tools/config_templates byte-unchanged; no schema key, minted row or default moved; no trainer/search/eval numerics touched (the eval edits are `encoding=` kwargs on JSON round-trips whose content is `json.dumps`-ASCII; the monitor edit is a pure refactor of shard enumeration with identical sort semantics).
- **R367(a)**: no run-named symbol/test/pin added; census untouched; no by-name pin set edited (the only target-set edit is the sealbot DELETION from both Makefile and meta_ci in one commit); no compatibility shim added (grep over added lines → none); added ruling cites are §2's classified set only.
- **R368(b) protection**: §1's A-check — the ci_gates edits are exactly the authorized five files, with lint_gate.sh's two lines and the floor folds as claimed; PZ-2/PZ-6 members outside the named orders untouched (`check_tracked_refs.py`, `tier_declaration.txt`, mint, templates, workflow).
- **LAW-07**: both new gate measures and the widened scan have producer tests that plant-and-move (§1); `run_shard_paths` has its enumeration producer test; `loadpkg`'s consumers are the loading tests themselves.
- **Commits**: all nine single-line subjects, empty bodies, no trailers (`git log --format=%h|%b` → no bodies; trailer grep → none). Δlines: net −410; positive only on 5fc33aa1 (+103, the ordered measure leg), a9528a77 (+11, amended row + pin test), 44678400 (+8, producer test with src at −6) — each judged in §3; 51eddda5 nets −7, e87e3686 and 622893b6 net 0, the rest negative.
- **Floors**: collected == floor == 5035 at HEAD; all seven comment measures exactly at floor; every floor move in its causing commit; direction never violated.
- **J — KNOWN-RED posture**: the full default tier at 51eddda5 came back **0 failed** — `test_the_stop_handlers_are_installed_by_main_and_never_at_import` PASSED in this run (and 12/12 standalone). Per the review contract this is not a W4 finding either way; it is one more data point for the operator's pending ruling — the recorded mechanism (timing-dependent inherited SIG_IGN) is consistent with a pass at a later tip, and nothing in this range touches signal handling or the supervisor.
- **Hygiene**: scratch worktree `.wt/review-w4` removed; `/tmp` harness files removed; main checkout tracked-clean at start and end (untracked dotfile noise pre-exists); no pushes, no builds, no uv sync.

## §5 Disposition (dispatcher)

- **N1 (TOOLS-2-08 AMENDED)** — RECORDED in the W4 exit PROGRESS: constants deduped to the engine exports (pinned value-and-order), the win-line scan kept local on the completing-stone ground the reviewer independently confirmed in `crates/mantis-core/src/board/moves.rs`; the row reads AMENDED-with-ground, not DONE-as-specified.
- **N2 (the gate's own docstring cite line)** — RECORDED as W7/W8 residue: one reclaimable `ruling_cite_lines` floor line ("R10-and-up" wording).
- **N3 (CARD-STYLE-BACKLOG's stale figure)** — W6 row: the card's measure list is refreshed when the style pass lands (recorded in PROGRESS W4 residue for W6).
- **N4 (the brief's `--self-test` slip)** — RECORDED; no code change (the arms live in the producer test, where LAW-07 wants them). docs/slim is archived at W8.
- **N5 (PROGRESS staleness)** — folded into this W4 exit rewrite, with the per-row ledger and the re-lane table.
- The KNOWN-RED passing in the review's own full tier is recorded as a further flake data point in the operator's pending row.
