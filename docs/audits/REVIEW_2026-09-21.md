# REVIEW-1 — the R366 leg (51f40a18..19e8351d) against R367(a) + correctness

> **Provenance.** Taken 2026-09-21 at `19e8351d` by a fresh read-only review agent under R367(b),
> in its own worktree, with no history of the leg. Three passes: (1) the seven commits read one at
> a time, `src/` + `tools/` hunks in full, then every new or changed test file in full; (2) every
> changed symbol followed into its file at HEAD (`checkpoints.py`, `trainer/core.py`, `losses.py`,
> `pool.py`, `run.py`, `coordinator/step.py`, `arch.py`, `schema/core.py`, `ema.py`, `anchor.py`,
> `warmstart.py`, the Rust `hexg/{mod,sample,persist}.rs` the witness leans on); (3) tree-wide
> greps at HEAD for run-keyed strings, by-name config pins, stub trainers, fixture copies, sha256
> helpers, deploy readers, docstring lengths. Nothing was run — no `uv`, `cargo`, `pytest`, `make`;
> every number below is `grep -c` / `wc -l` / `awk length`, labelled **measured**. Governance docs
> (RULINGS R366, RUN10_PREREG, CARDS, STATE) were read for the STOP LAW, the heldout ring's facts
> and the memory pre-read only. What was NOT read is stated in §8.

Range: 7 commits, 101 files, +2 612 / −184. No Rust file is in the range (**measured**: the stat
over `crates/` is empty), so the Rust rules (unwrap, rustfmt) have no subject here.

---

# 0. Verdict

**Not done under R367(a).** The leg's four mechanisms are sound and their LAW-07 breaks are
present (§5 holds on 9 rows), but the mint commit did, by its own message, the thing the standard
forbids — "every by-name production pin names run10" — at 11 edit sites plus a new run-named test
file; four things exist twice or three times in `src/` (the sparse-row tail reconstruction ×3,
the `model.gnn` parser ×2, the soft-policy kinds table ×2, a verbatim copy of
`util.hashing.sha256_file`); one compatibility shim admits a model-less mapping the schema no
longer allows; and one test affordance (`WorkerPool(arch=None)`) re-enables the exact B-1
self-serve hazard the card closed. The two correctness gaps that matter for box time: the STOP LAW
is prose — `max_train_steps` stays 1 000 000 while the LR is periodic past 108 000 and no schema
row or test binds the two — and the composition root's `actor_state_dict` wiring is unpinned (all
14 stub trainers return the same object for the actor and deploy paths, so a revert to
`inference_state_dict` stays green). Shortest path to done: six commits, one per class below, in
the order F5 → F1 → F4 → F2 → F6 → F3; F5.1 and F5.2 land before anything is started on a box.

| class | count | severity | one-line fix |
|---|---|---|---|
| F1 run-keyed artefacts | 6 | must-fix ×4, should-fix ×1, minor ×1 | `PRODUCTION_CONFIGS` = `discover_configs − EXEMPT`, the 8 by-name sets read it; the LR test parametrised over every cosine config; `_MINTED_ARCH_KIND_ROW` deleted (the file's own row is the expectation); the plant stem a non-run name |
| F2 duplication | 4 | should-fix ×3, minor ×1 | ONE stub trainer base in `tests/conftest.py` (14 copies); ONE `gnn_widths_block(arch)` helper (24 sites); the heldout test's `_Pool`/`_Trainer` shared |
| F3 comments | 5 | should-fix ×1, minor ×4 | 23 docstrings > 200 chars trimmed to the invariant (the measurement narrative is already in the prereg); 9 restating comments deleted; 2 stale `:NNN` line cites and 3 stale docstrings fixed |
| F4 extensibility / one-implementation | 7 | must-fix ×2, should-fix ×4, minor ×1 | `sha256_file` reused; `rebuild_sparse_target` + `segment_softmax` exported from `losses` and used by the CE, the soft target and the KL row; one widths parser; one kinds table (in `config`, imported by `model`); `deploy_state` on every deploy reader; the trainer calls one `forward_batch_heads` on every net |
| F5 correctness | 7 (+9 holds) | must-fix ×4, should-fix ×1, minor ×2 | a schema rule `scheduler_t_max ≥ max_train_steps` under cosine (or a floored schedule) with `max_train_steps` minted at the stop; a root-wiring pin with distinct actor/deploy sentinels; the from_config shim deleted; `arch=None` never serves the learner's own module |
| F6 bloat | 5 | should-fix ×2, minor ×3 | the deletions in §6, ≈ 190 lines with no loss |

Findings: **34** — must-fix 10, should-fix 12, minor 12. Checked-holds rows: 9.

---

# 1. Checklist item 1 — run-keyed artefacts

**Premise check.** The checklist says "seven by-name production-pin sets". **Measured**: 8 pin
lines in 7 files (`test_graph_microbatch_authority.py` carries two, `:153` and `:412`) plus one
per-mint COUNT (`test_gate_interval_decoupling.py:357`, `len(configs) == 7`) plus
`PRODUCTION_CONFIGS` itself. The mint commit `6da4e4c3` edited 11 sites in 9 files outside
`configs/` and added one file to land one config. `_MINTED_ARCH_KIND_ROW` is NOT already a census
— its loop is (`CONFIGS.glob`), its expectation table is by name.

**F1.1 — `tests/train/test_lr_schedule_run10.py` is run-keyed in name, path and literals** ·
`:1` docstring, `:15` `_RUN10 = _REPO / "configs" / "run10.yaml"`, `:16` `_HORIZON = 108_000`,
`:52` the `1_500` "cell" literal · R367(a) "no runN in a symbol, test, pin or tool"; the horizon
and the post-horizon window are typed, not read off the config · Fix: rename to
`test_lr_schedule.py`; parametrise over `[c for c in discover_configs() if
load_config(c).train.lr_schedule == "cosine"]`; read `T_max`/`eta_min`/`lr` off each config and
assert the three points from the closed form; the post-horizon assertion becomes F5.1's pin
(monotone non-increasing over `[0, max_train_steps]`) · **must-fix**.

**F1.2 — the by-name production-pin sets, edited per mint** · `src/mantis/config/armed_aborts.py:916`
`PRODUCTION_CONFIGS`; `tests/config/test_drawrate_schema_range.py:241`;
`tests/config/test_fused_graph_caps_authority.py:47` `_PRODUCTION`;
`tests/eval/test_eval_posture_inert.py:104` `_ARMED_STRENGTH_FLOOR`;
`tests/train/test_graph_microbatch_authority.py:153` `_PRODUCTION_CAPPED` and `:412`;
`tests/tools/test_preflight_mint_process.py:2404-2415` (two new asserts, one an equality against
the whole tuple); `tests/train/test_gate_interval_decoupling.py:357` · R367(a) "production pins are
a census of configs/, not a list edited per mint" · Fix: `PRODUCTION_CONFIGS` becomes derived —
`tuple(sorted(p for p in discover_configs(CONFIGS) if rel(p) not in {e for e, _ in
EXEMPT_CONFIGS}))` — so the small, stable side (two exemptions, each with grounds) is the only
by-name list; every test set above imports it (or a `production_configs()` fixture in
`tests/conftest.py`), and the vacuity guard the old comments defend ("a predicate over configs/
would go vacuous") is `assert production_configs()` non-empty plus the partition's remaining
half (an exemption naming a file not on disk is still rc 31). The `test_run5_is_bound_BY_NAME…`
row and the `#: Membership is audited BY NAME` comment state the OLD rule and are retired in the
same commit, citing R367(a) — this reverses F-P2B/N-1's argument and the ruling record must say
so, not the test · **must-fix**.

**F1.3 — `_MINTED_ARCH_KIND_ROW`** · `tests/model/conformance/test_arch_selector_makes_v2_selectable.py:157-158`
· R367(a) census clause; the table transcribes each file's own `identity.arch_kind` and must be
edited on every mint that moves the kind · Fix: delete the table; the expectation for each
discovered config is `ARCH_KINDS[config.identity.arch_kind or INCUMBENT_ARCH_KIND[rep]]` — the
"a move needs a ruling" guard is the mint header (`config_diff --from-header`), not a test literal
· **must-fix**.

**F1.4 — the plant stem `"run11"`** · `tests/tools/test_preflight_mint_process.py:1608`
`_F1_PLANT_STEM = "run11"` (edited from `"run10"` by the mint) · R367(a); the stem is re-typed on
every mint that lands the name it guessed · Fix: a stem that is not a run name and can never be
minted, e.g. `"undeclared_plant"`; the existing
`test_the_F1_plant_stem_is_declared_by_NEITHER_tuple` stays as the collision guard · **must-fix**.

**F1.5 — `configs/run9.yaml` loaded by name as a fixture in new tests** ·
`tests/config/test_gnn_widths_resolver.py:38,47,54,63`; `tests/train/test_aux_soft_policy.py:124`;
`tests/train/test_server_owned_copy.py:160` · the checklist's last clause; each wants "a
production graph config", not run9 · Fix: `smoke_run_config` / a `production_config` fixture
(first of the census); `test_gnn_widths_resolver.py:23` also re-implements discovery with
`(_REPO / "configs").glob("*.yaml")` where `mantis.config.loader.discover_configs` is the one
authority · **should-fix**.

**F1.6 — the standing inventory outside the range (for a card, not this fix leg)** ·
**measured** at HEAD: `RUN5 = REPO_ROOT / "configs" / "run6.yaml"` in 4 files
(`tests/config/test_cadence_clock_mutations.py:29`, `test_armed_abort_cadence.py:44`,
`test_config_discovery_authority.py:13`, `tests/tools/test_preflight_mint_process.py:51`) — a
name that lies about its file; 32 test functions carry a `runN` token in their name (16 files);
≈ 800 `run5`/`run6` identifier tokens across `src/tests/tools`; `tests/tools/test_analyzer_*`
use `"run9"` as a synthetic run_id (harmless but keyed). The in-range addition
`tests/config/test_armed_abort_manifest.py:133-136` renamed the message back to "run5 with the
arming flipped OFF" over a `run6.yaml` load — a regression in a message · Fix: card the sweep;
the fix leg fixes only the in-range line · **minor**.

---

# 2. Checklist item 2 — duplication

**Premise check.** "~30 test fixtures that now state GNN widths" — **measured** 9 verbatim
`"model": {"gnn": {...}, "aux_soft_policy": None}` dict entries in 9 files and 15
`["model"]["gnn"] = {...}` / `["model"]["aux_soft_policy"] = {...}` patch sites in 10 files
(24 sites, 19 files). "~14 stub trainers" — **measured** 14 `def actor_state_dict` + 14
`def deploy_module` in 14 files, plus a 15th coordinator-facing `_Trainer` in
`tests/train/test_heldout_gap.py:102`. Both classes PRE-EXIST the leg (each of those tests already
carried its own full config literal or its own drivable trainer); the leg propagated a new leaf
into every copy rather than hoisting. `tests/test_run_strict_composition.py:8`'s own R8 header
says "splitting … would fork a fourth copy of the drivable" fakes — the tree documents the copy
count itself.

**F2.1 — 14 stub trainers gained the same six lines** · `tests/config/test_coordinator_knobs_wiring.py`,
`test_drain_caps_wiring.py`, `tests/test_actor_sync_composition.py`, `test_run_composition.py`,
`test_run_disk_guard_abort_rc.py`, `test_run_eval_enabled_authority.py`,
`test_run_partial_composition.py`, `test_run_root_lifecycle.py`, `test_run_strict_composition.py`,
`tests/train/test_actor_lag_wiring_live.py`, `test_actor_sync_production_posture.py`,
`test_actor_sync_real_config.py`, `test_drawrate_abort_threading.py`, `test_terminal_eval_rc.py`
· R367(a) "one implementation per thing"; the composition seam's trainer protocol is now written
14 times · Fix: ONE `DrivableTrainerStub` in `tests/conftest.py` carrying the seam's method set
(`inference_state_dict`, `actor_state_dict`, `deploy_module`, `save_checkpoint`, `step`, …) with
DISTINCT sentinels for `actor_sd` and `inference_sd` (F5.2 needs that); the 14 subclass or
instantiate it. Same commit as F5.2 · **should-fix**.

**F2.2 — the `model.gnn` block typed 24 times** · the 9 dict copies
(`tests/config/test_actor_sync_schema.py`, `test_eval_schema_bounds.py`, `test_gnn_widths_resolver.py`,
`test_schema_strict.py`, `test_train_policy_value_target_consistency.py`, `tests/train/conftest.py`,
`test_checkpoint_conformance.py`, `test_launch_path_smoke.py`, `test_resume_wiring_integration.py`)
and the 15 patches (`tests/config/test_gnn_widths_resolver.py` ×4,
`tests/model/conformance/test_arch_selector_makes_v2_selectable.py`, `tests/tools/conftest.py`,
`tests/tools/test_ladder_backends.py`, `tests/train/_microbatch_harness.py` ×2,
`test_arch_stamp_authority.py`, `test_aux_soft_policy.py` ×2, `test_periodic_checkpoint.py`,
`test_resume_owned_paths.py`, `_warmstart_config.py`) · the "widths of this arch as the config
block" is `checkpoints._SYNTH_ARCH_SCOPED[("model","gnn")]`'s lambda in `src` and is re-typed in
tests · Fix: export `gnn_widths_block(arch) -> dict` from `mantis.model.arch` (the `_SYNTH` lambda
calls it); tests call it; the 9 full-config literals take their `model` block from the one
`make_run_config` in `tests/train/conftest.py:213` or `smoke_run_config` · **should-fix**.

**F2.3 — two `TrainHParams` base builders both grew `aux_soft_policy=None`** ·
`tests/train/_microbatch_harness.py:141` `graph_hparams` and `tests/train/conftest.py:236`
`make_full_train_hparams` are the same 12-key literal · pre-existing pair, both touched · Fix:
one of them imports the other · **should-fix**.

**F2.4 — `tests/train/test_heldout_gap.py:102-152` `_Trainer` and `_Pool`** · `_Pool`'s docstring
says it is "test_ply_cap_gate's stub" — a declared copy · Fix: share via the F2.1 base and a
`_Pool` in `tests/train/conftest.py` · **minor**.

---

# 3. Checklist item 3 — comments

**Ratchet: checked, holds.** `tools/ci_gates/comment_length_floor.txt` `comment_excess_lines`
3405 → 3403 across the leg (**measured**, two commits lowered it by one each); no added own-line
run exceeds the cap of 2; `banner_comment_lines` 0; `docstring_excess_lines` unchanged at 13105.
Every added file is under 300 lines or carries an R8 header (`losses.py` at 316 gained one that
states a reason, no count) — **measured** over all 84 Python files in the range.

**F3.1 — the ratchet satisfied by line length: 23 added `src/tools` lines over 200 characters,
the longest 669** · **measured** (`awk length`): `src/mantis/train/losses.py:204` (668 chars, the
`soft_policy_target` docstring carrying "run8@45k's alpha reads median 0.000 / p90 0.020 … 40 %
… ~500 tail nodes of ~1e-4"), `src/mantis/config/schema/train.py:169` (403),
`src/mantis/config/schema/model.py:15` (397), `src/mantis/run.py:256` (336),
`src/mantis/model/arch.py:67` (320), `src/mantis/train/heldout.py:40` (305), plus 17 more between
200 and 300; 10 in tests · CLAUDE.md R316(e): a needed comment "states its non-obvious fact in
ONE line" — these are paragraphs folded onto one physical line so `docstring_excess_lines` does
not move; the measurement narrative duplicates RUN10_PREREG §1a rows 42–45 verbatim (E501 is
ignored in pyproject, so no gate sees it) · Fix: each docstring trimmed to the invariant and the
`Raises:` clause; the numbers stay in the prereg the docstring cites · **should-fix**.

**F3.2 — comments that restate the code beside them** · `src/mantis/run.py:860` ("The gate's
candidate is the DEPLOY net" over `eval_model=trainer.deploy_module()`);
`src/mantis/train/checkpoints.py:349` ("Written only when EMA is on" over `if ema_state is not
None`); `src/mantis/train/coordinator/step.py:191` ("`None` is the config's explicit OFF");
`src/mantis/train/trainer/core.py:184` ("The head and its rows travel together" over a raise whose
message says it) and `:522` ("R366(b)'s LAW-18 rows" over `_aux_soft_policy_block`);
`src/mantis/train/losses.py:217,225` (section labels over variables named `soft_explicit` /
`soft_tail`); `src/mantis/selfplay/pool.py:110-111` (restates `served_copy`'s docstring);
`src/mantis/train/orchestrator.py:149-150` (restates `resolve_gnn_widths`'s docstring) · R316(e)
"a comment states what code cannot" · Fix: delete; the ratchet gains ≈ 0 (none of these runs
exceeds 2) so no floor edit is owed · **minor**.

**F3.3 — transcribed line numbers asserted as evidence, already wrong** ·
`tests/config/test_armed_abort_manifest.py:108` cites `dev_example.yaml … at :200` and `:119`
cites `run6.yaml … at :203`; **measured** at HEAD `actor_lag_abort_enabled` sits at
`dev_example.yaml:130` and `run6.yaml:175` (the leg's own `model:` insert moved them) · the R8 /
G-DFIX-4 derive-or-delete class · Fix: drop the `:NNN` · **minor**.

**F3.4 — docstrings the leg made stale** · `src/mantis/model/build.py:35` `Raises:` still says
"neither `GnnArch` nor `GnnArchV2`" (the body names three); `src/mantis/train/coordinator/dispatch.py:324`
"BC pretrain is the only consumer" of `run_declared_eval_step` (the witness is a second);
`tests/model/conformance/test_arch_selector_makes_v2_selectable.py:143` still says "`graph`
admits two" · Fix: reword · **minor**.

**F3.5 — `src/mantis/config/armed_aborts.py:500-507`, the `actor_lag` note** · 8 lines of
history ("Armed on configs/run5.yaml since the R59 flip … the run3 failure mode … RETIRED
2026-09-16 … RETURNED at R366") in a source literal; two run names in it · R367(a) (run names in a
tool string) and R316(e) · Fix: the mechanism in one line ("the actors serve a copy the sync
writes, so between syncs their weights are stale"); the history is in RULINGS · **minor**.

---

# 4. Checklist item 4 — extensibility and one-implementation

**Dispatch shape: checked, mostly holds.** The soft head IS a sibling dataclass
(`GnnArchV2SoftPolicy`, `arch.py:66`), most-derived-first `isinstance` in `build_net`
(`build.py:40`), a shared `_readout` in `GnnNetV2` (`gnn_v2.py:161`) that `forward_batch` and
`forward_batch_heads` both call — the pattern a third head would copy. Checkpoints, the selector,
the snapshot and the arch-scoped loop carry NO kind-specific branch (**measured**: the kind's name
appears in `src/` only in `arch.py` ×4 table rows, `build.py` ×3, `gnn_v2.py` ×2,
`model/__init__.py` ×4 exports, `schema/core.py` ×1). The trainer branches on
`self.soft_policy` at 4 sites (`trainer/core.py:185, 393, 460, 569`). `heldout_gap` is keyed to
NO ring and NO run: `HeldoutSlice.open(spec, encoding, visit_capacity, capacity, root)` takes any
HEXG ring at the run's geometry and the engine names a foreign one (`persist.rs:142-150`).
`served_copy` (`pool.py:77`) is the ONE place the actors' net is built. `deploy_state`
(`checkpoints.py:106`) is NOT the one place a deploy net's weights are read (F4.5).

**F4.1 — `SOFT_POLICY_ARCH_KINDS` defined twice** · `src/mantis/model/arch.py:107` and
`src/mantis/config/schema/core.py:35` ("a copy … this package imports no torch"), pinned equal by
`tests/model/test_gnn_v2_soft_policy.py:45` · R367(a) "one implementation per thing"; the stated
reason is insufficient — `mantis.model.arch` imports no torch either and the design's
`model → config` edge already exists (`model/amp.py:19` imports `config.resolve.amp`) · Fix: the
string set lives ONCE in `mantis.config` (beside `ARCH_SCOPED_KEYS`), `mantis.model.arch` imports
it; the equality test and the copy go · **should-fix**.

**F4.2 — `file_sha256` is a verbatim copy of "the one `sha256_file`"** ·
`src/mantis/train/heldout.py:18-24` vs `src/mantis/util/hashing.py:10` (whose module docstring
says "the one `sha256_file` the bundle, the receipts and the puller share"); same chunk size, same
loop · R367(a) · Fix: delete `file_sha256`, import `sha256_file`; `tests/train/test_heldout_gap.py:21,40`
and the LAW-08 registry row `train.heldout_gap.ring_sha256` follow · **must-fix** (trivial, but it
is exactly the class the standard names).

**F4.3 — the sparse row's tail is rebuilt three times** · `src/mantis/train/losses.py:157-168`
(inside `ragged_policy_ce_and_entropies`), `losses.py:225-231` (the tail half of
`soft_policy_target`), `src/mantis/train/trainer/core.py:682-694` (`_hard_target`, "restated for
the KL row"); and the segment-id vector `torch.repeat_interleave(arange(b), counts)` is built 5
times (`losses.py:25,155,212`; `core.py:535,688`) · R367(a); the R8 header the leg added to
`losses.py` justifies the file's size by "the hard target it rebuilds and the soft target that
shares its tail are ONE … contract — split, the two targets would disagree", yet the tail is three
implementations that CAN disagree · Fix: `losses.py` exports `segment_ids(legal_offsets)`,
`segment_softmax` (public; `core.py:675` `_segment_probs` lazily imports the private name today)
and `rebuild_sparse_target(policy_target, probs_detached, explicit_mask, tail_mass,
legal_offsets)`; the CE, the soft target and `_aux_soft_policy_terms` call them; `_hard_target`
and `_segment_probs` are deleted · **must-fix**.

**F4.4 — two parsers of `model.gnn`** · `src/mantis/config/resolve/gnn_widths.py:27`
`resolve_gnn_widths` ("the parse is restated here because this package imports no `mantis.model`")
and `src/mantis/model/arch.py:145` `declared_gnn_widths`; `orchestrator.py:151` calls the first and
`arch_from_spec_and_config` (line 156, two lines later) calls the second on the same mapping, so a
fresh boot parses the block twice and the first result feeds only a log line · R367(a); the same
DAG fact as F4.1 (`model → config` is allowed) · Fix: `declared_gnn_widths` becomes a two-line
wrapper over `resolve_gnn_widths` returning `{}` only when the mapping carries no `model` key (the
legacy-stamp case its docstring names); the `_MEMBERS` check exists once · **should-fix**.

**F4.5 — `deploy_state` is not the one deploy reader** · readers that rebuild a net from a stamp
and read `ck.model_state` directly at HEAD (**measured**): `src/mantis/train/anchor.py:288`
(`load_anchor` — a v2 `.ckpt` used as bootstrap/anchor would anchor the LEARNER of an EMA-on run),
`tools/analyzer/engines.py:164` (plays the net), `tools/bench_server.py:155`,
`tools/probe1/nets.py:46`, `tools/probe1/swa.py:37`; `src/mantis/train/warmstart.py:119,129`
(the warm start's identity hash and transfer read the learner — for an EMA-on parent the config
must say which net is the parent) · the card's own contract ("the shadow as the DEPLOY net …
for the follower and the ladder bot") stops at two tools · Fix: `load_anchor` and the analyzer go
through `deploy_state`; `bench_server`/`probe1` state in one line that they measure the learner;
`warm_start` gains a stated rule (the deploy net, i.e. `deploy_state`, with `net_hash` taken over
it) before any EMA-on run becomes a parent · **should-fix**.

**F4.6 — the trainer's forward is a two-way `if` on the kind** · `trainer/core.py:393-401`
(`forward_batch_heads` vs `forward_batch`), `:460` (`_policy_head_grad_norms` only under the
flag), `:555-563` (head names `("policy_head", "aux_policy_head")` hard-coded) · a third head adds
a third arm at each · Fix: every graph net exposes `forward_batch_heads` returning
`(policy_logits, value, bin_logits, aux_logits | None)` (V2's returns `None`); the trainer calls
ONE method and branches on `aux_logits is not None`; grad norms iterate the net's declared
`policy_heads()` · **should-fix**.

**F4.7 — `deploy_module` plants `.arch` on the EMA view from outside** ·
`trainer/core.py:594` `view.arch = self.arch  # type: ignore[attr-defined]`; `ema.py:83`
`_EmaModuleView` has no `arch` · the snapshot's "arch travels with the model" convention met by a
monkeypatch each call · Fix: `EmaModel(model, decay, arch=model.arch)` and the view carries it
from construction · **minor**.

---

# 5. Checklist item 5 — correctness

**Checked, holds (9 rows):**
- (i) shape lie — `tests/config/test_gnn_widths_resolver.py:75` mutates the config against the
  stamped arch and asserts `CheckpointStampError` by name; identity drift — `:88` asserts
  `ResumeIdentityMismatchError` "model.gnn.hidden: checkpoint=8, resume=192" and the unmoved
  resume lands. Both planted breaks present.
- (ii) `forward_batch` parity — `tests/model/test_gnn_v2_soft_policy.py:49` loads V2's weights
  into the soft net and asserts `torch.equal` on all three `forward_batch` outputs AND on
  `forward_single`; the aux logits differ from the policy logits (the head is its own function).
- (iii) EMA hash witness — `tests/train/test_server_owned_copy.py:86` after three real steps:
  learner hash ≠ EMA hash, the synced payload hashes to the LEARNER, `inference_state_dict` to the
  EMA, the snapshot of `deploy_module()` to the EMA; `:48` proves a write to the served copy never
  reaches the learner. See F5.2 for the half that is missing.
- (iv) heldout freeze — `tests/train/test_heldout_gap.py:74` two reads equal; an `_Unseeded`
  buffer sharing the real sampler drifts over six reads (the planted break); `:60` the sha pin,
  the foreign-geometry refusal ("slot-geometry mismatch", `persist.rs:143`) and the missing file.
  The witness's determinism holds in Rust: indices come from the seeded `StdRng`
  (`hexg/mod.rs:351`), threads only build/align in index order (`sample.rs:216,275-296`),
  `augment=False` is fixed in `run_declared_eval_step` (`dispatch.py:338`), and
  `eval_step_from_graph_batch` restores train mode in a `finally` (`core.py:282`). The slice is its
  OWN buffer, so `samples_consumed_total` and the replay-ratio row are untouched.
- The `heldout_gap` read sits INSIDE the per-step burst loop (`step.py:557`), so run10's
  `max_train_burst: 8` cannot step over an `interval` boundary; the train means count only
  finite-`grad_norm` steps (`:635`).
- LAW-18 aux rows with the planted break — `tests/train/test_aux_soft_policy.py:64` (aux CE,
  KL(hard‖soft) > 0, both heads' grad norms, the head moves, the loss sums as
  `policy + value + weight·aux`) and `:84` (a construction returning the hard target reads KL 0).
- Warm start admits the fresh head — `gnn.py:232` subtracts `reinit_keys` from `missing`;
  `test_aux_soft_policy.py:135` proves refusal without `reinit` and byte-equal transfer with it.
- LAW-08 — all nine new leaves have consumer rows in BOTH registries (**measured**: identical
  additions in `test_every_key_has_consumer.py` and `_p2.py`); the manifest gains `actor_lag` with
  its cadence rows and the mutation-battery row (`test_cadence_clock_mutations.py:91`); the two
  new events (`heldout_gap`, `ema_shadow_reseeded`) have manifest rows citing their tests.
- No encoding-less text IO, no broad `except`, no code-side default in the added resolvers (each
  refuses absence by name); `heldout.py` opens binary only.

**F5.1 — the STOP LAW is prose and the config contradicts it** · `configs/run10.yaml:110`
`scheduler_t_max: 108000`, `:120` `max_train_steps: 1000000`; no schema rule relates the two
(**measured**: `scheduler_t_max` appears in `schema/train.py:199` only); `Trainer._build_scheduler`
(`core.py:254-259`) is torch's periodic `CosineAnnealingLR`; RUN10_PREREG §4 "STOP LAW … One
SIGTERM to the supervisor … `train.max_train_steps` stays 1 000 000 by byte-equal elsewhere";
`test_lr_schedule_run10.py:49-58` pins the rise as `< 1e-6` over a typed `1_500` and states the
guard is the STOP LAW · checklist 5(v): not pinned against the config's step budget; a missed
SIGTERM trains the second half of the block at a RISING LR (back to 1e-3 at 216k) with nothing in
the tree refusing it · Fix (mechanism, not prose): EITHER a `RunConfig` validator — under
`lr_schedule: cosine` with `scheduler_t_max` set, `scheduler_t_max >= max_train_steps` ("the
periodic tail inside the budget is refused") — with `max_train_steps` re-minted at the block's end
(108 000; the terminal round at that step is the regression guard the prereg wants read, and
`test_terminal_eval_rc.py` is its pin) — OR a floored schedule (`LambdaLR`: cosine to `T_max`,
`eta_min` after) so the LR is monotone by construction and the STOP LAW governs games, not the LR.
Either way F1.1's test asserts monotone non-increasing LR over `[0, max_train_steps]` for every
cosine config · **must-fix** (before START).

**F5.2 — the composition root's actor wiring is unpinned** · `src/mantis/run.py:692`
`state_dict_fn=trainer.actor_state_dict` is the mechanism, but every one of the 14 stub trainers
returns the SAME object from `actor_state_dict` and `inference_state_dict` (**measured**: 9 return
`self.inference_sd`, 2 `{"w": "SENTINEL"}`, 4 `{}`/`self.model.state_dict()`), and
`tests/test_actor_sync_composition.py:161-163` still asserts "every push must carry
`trainer.inference_state_dict()`'s result (EMA-aware weights)" — the OLD contract's text over a
comparison that cannot tell the two apart; the hash witness (§5 holds, iii) drives `ActorSync`
with a stub target, not `compose_run` · LAW-07: a revert of `run.py:692` to
`inference_state_dict` leaves the whole tier green · Fix: with F2.1's base, `actor_sd` and
`inference_sd` are distinct sentinels; `test_actor_sync_composition.py:161` asserts pushes carry
`actor_sd` and never `inference_sd`, message reworded · **must-fix**.

**F5.3 — a compatibility shim for a mapping the schema no longer admits** ·
`src/mantis/train/trainer/core.py:117` `aux = resolve_aux_soft_policy(cfg) if
isinstance(cfg.get("model"), dict) else None` with the comment "No `model` section = a pre-v36
mapping with no head" · R367(a) "no compatibility shims for a state the tree no longer has";
`model` is REQUIRED on `RunConfig` (v35) and `aux_soft_policy` REQUIRED inside it (v36), so the
only mappings without it are un-validated test dicts; on a V2 arch the shim resolves OFF silently
(R1: a code-side default) · Fix: call `resolve_aux_soft_policy(cfg)` unconditionally (it refuses
absence by name); the test mappings get their `model` block from the F2.2 fixture · **must-fix**.

**F5.4 — `WorkerPool(arch=None)` serves the learner's own module** · `src/mantis/selfplay/pool.py:112`
`self.model = served_copy(model, arch) if arch is not None else model`; both production callers
pass `arch` (`run.py:407`, `worker_sweep.py:712`); **measured** 15 test sites pass `arch=None` ·
R367(a) shim clause; the `None` arm IS the B-1 hazard (ActorSync writing INTO the learner) kept
alive as a test affordance, and `test_the_pool_serves_its_own_copy…` cannot see it because it
passes `arch` · Fix: `served_copy(model, arch)` deep-copies the module when `arch is None`
(`copy.deepcopy(base)`), so a pool never serves its argument; or `arch` becomes required and the
15 sites pass one · **must-fix**.

**F5.5 — `run.py` now imports the engine three ways** · `run.py:257` a second lazy
`from mantis._engine import …` (in `_derived_visit_capacity`, no stated reason), `:293` the
original lazy one ("the one import this root must not make unconditional"), and `:86`
`from mantis.train.heldout import HeldoutSlice` whose module imports `HexgBuffer` at top
(`heldout.py:9`) · the stated DAG reason was already moot (`run.py:78` imports `selfplay.pool`,
which imports `_engine` at top), so both lazy sites are theatre and `repo_design.md:137`'s "the
ONE lazy import" row is false · Fix: one top-level import in `run.py`, the two lazy sites and
their comments deleted, the repo_design row amended in the same commit (R9) · **should-fix**.

**F5.6 — the frozen slice is allocated at the TRAINING ring's capacity** · `run.py:841`
`capacity=int(resolve_coordinator_knobs(config.train).capacity)` → `HexgBuffer(500 000, …)` for a
100 000-row file (`heldout.py:50`; `hexg/mod.rs:308-330` allocates every column eagerly at
`capacity × visit_capacity`); Rust refuses `size > capacity` (`persist.rs:163`) so a smaller
number needs the file's own row count · NOT measured (RSS depends on the allocator's zero-page
laziness; the prereg's memory pre-read was not re-derived here) · Fix: `HeldoutSlice.open` reads
the HEXG header's `size` (40 bytes: magic, version, max_stones, max_visits, capacity, size) and
allocates exactly that; `capacity` leaves the signature · **minor**, stated so the box session
knows the number is virtual, not that it is free.

**F5.7 — `_refuse_identity_drift` skips the width check on a stamp with no identity block** ·
`checkpoints.py:1129` returns early when either side lacks an `identity` dict, which also skips
the new comparison at `:1146` (it needs only `arch` and the effective mapping) · Fix: the width
comparison runs before that early return. (The EMA re-seed on a shadowless stamp — `:1271`,
`test_server_owned_copy.py:141` — holds and EMITS.) · **minor**.

---

# 6. Checklist item 6 — bloat

Deletable with no loss (≈ 190 lines), each replaced by something that already exists or by one
new helper:

**F6.1** · `src/mantis/train/heldout.py:18-24` → `mantis.util.hashing.sha256_file` (F4.2) ·
**should-fix**.

**F6.2** · `src/mantis/train/trainer/core.py:675-694` (`_segment_probs`, `_hard_target`) and
`:540-551` (the hand-rolled per-graph scatter in `_aux_soft_policy_terms`), `losses.py:225-231`
→ `rebuild_sparse_target` / `segment_ids` / `segment_softmax` (F4.3), ≈ 35 lines ·
**should-fix**.

**F6.3** · `src/mantis/config/schema/core.py:33-35` + `tests/model/test_gnn_v2_soft_policy.py:45-46`
→ one definition (F4.1); `src/mantis/model/arch.py:145-158` → a wrapper (F4.4);
`src/mantis/train/checkpoints.py:153-163` and `:1146-1149` are the same three-line width
comparison written twice → one `_width_drift(config, arch) -> list[str]`; `run.py:255-272` keeps
`_derived_visit_capacity` (a real de-dup the leg did right) but loses its lazy import (F5.5);
`src/mantis/model/arch.py:138-142` `_GRAPH_CONFIG_KEYS` reads three flat keys "no schema leaf
carries" — under `extra="forbid"` no validated config can carry them, so the loop at `:259` is
dead on every production path (pre-existing; one line to delete once the conformance suite's
diagnostic use is confirmed gone) · **minor**.

**F6.4** · tests: 14 × 6 stub lines (F2.1, ≈ 80 lines), 24 width sites (F2.2, ≈ 20 lines),
`tests/train/test_lr_schedule_run10.py` 58 lines → ≈ 30 parametrised (F1.1),
`tests/train/test_heldout_gap.py:102-152` 51 lines of stubs (F2.4) · **minor**.

**F6.5** · `src/mantis/config/armed_aborts.py:500-507` the note (F3.5); the 23 over-long
docstrings (F3.1) shrink by roughly half their characters without losing an invariant ·
**minor**.

Not bloat, kept: `resolve_{gnn_widths,aux_soft_policy,heldout_gap}` as three files follow the
package's one-resolver-per-block convention; the two consumer registries are a deliberate pair
(their own R8 header says why); `HeldoutSlice` and `served_copy` are the right size.

---

# 7. Strengths — what the leg did right (the fix leg must not undo these)

- The v35 partition is complete on both halves: `model.gnn` is in `ARCH_SCOPED_KEYS` with grounds
  (`schema/core.py:79`), the resolver refuses a foreign representation BEFORE reading
  (`gnn_widths.py:29`), `arch_from_spec_and_config` refuses a declared representation the spec
  lacks (`arch.py:166`), the arch-scoped strip loop became a table of callables so the synthetic
  widths are the stamped arch's own (`checkpoints.py:139-146`), and the writer refuses a shape
  lie at the stamp (`:153`). Both refusals have planted breaks.
- The soft target's form is measured, not assumed: temperature on the EXPLICIT entries with the
  tail carried (`losses.py:196`), `gt=1.0` on the schema so a dead head cannot read armed
  (`schema/model.py:17`), the head and its rows paired at the schema AND at the trainer
  (`schema/core.py:478`, `core.py:185`), the rows OMITTED on an arch without the head rather than
  zeroed (`core.py:565`), and `policy_weight == 0.0` leaves the aux term out during warm-up
  (`core.py:425`) — consistent with the main head's own rule.
- `GnnNetV2._readout` (`gnn_v2.py:161`) is a real extraction: one trunk pass, two entry points,
  served outputs byte-identical to V2's, pinned.
- The witness is honest about absence: `heldout_gap` is absent whole under `null`, the train
  means are `None` when no step fed the window, the event carries the sha and `read_index`
  (`events.py:387`), and `RunConfig` refuses an `interval` the run cannot reach (`core.py:535`).
- `served_copy` + `actor_state_dict` / `inference_state_dict` / `deploy_module` are three NAMED
  seams with distinct docstrings (`core.py:579-596`), `ema_state` is refused at load unless its
  key set is the model's (`checkpoints.py:615`), `EmaModel.load_state_dict` copies IN PLACE with a
  named `KeyError` (`ema.py:69`), and the re-seed on a shadowless stamp EMITS (`:1271`) — never
  silent. The retired `_ema_needs_a_server_owned_copy` validator and its test file were DELETED
  (`test_ema_refused_until_server_copy.py`), not left as a disabled arm.
- `_derived_visit_capacity` (`run.py:255`) hoisted the one derivation the slice needed instead of
  copying it; `per_mr_rows` reads `is_alpha_full` from `util.constants` rather than re-typing the
  α = 1.0 test.
- The comment ratchet was traded down, not held: 3405 → 3403.

---

# 8. What this review did not read

- Nothing was executed: no gate, no tier, no bench; the two integration reds STATE.md records
  (sandbox `XDG_STATE_HOME`) were not reproduced. The LR test's wall cost (F1.1) and the slice's
  resident memory (F5.6) are reasoned, not measured.
- `docs/governance/RULINGS.md` R366 beyond its header, `CARDS.md`, and `STATE.md` were skimmed for
  the STOP LAW, the heldout ring's facts and the pin list only; the prereg's §1b CPU pre-read and
  §3 witness bands were not re-derived.
- Rust: no file in range; `hexg/{mod,sample,persist}.rs` were read only for the witness's
  determinism and the load refusals, not audited.
- The mint's twelve-leaf `config_diff --expect MATCH` claim and the "byte-equal elsewhere"
  re-mint of the six other configs were taken from the commit message and the `# delta:` headers;
  the header round-trip test exists (`tests/config/test_config_diff_from_header.py`) and was not
  run.
- `tools/strix_follower.py` and the dashboard/analyzer readers of `ema_state`/`weights` were
  grepped for `model_state` reads (F4.5) but not read for how they present the `"weights"` field.
- The pre-existing run-keyed inventory (F1.6) was counted, not itemised; that is the card's job.
