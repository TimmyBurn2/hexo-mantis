# REVIEW-W6 — SLIM-FIX wave 6 (4e8c663a..8eec45a9)

Filed verbatim from the fresh read-only review agent (R367(b)); §5 is the dispatcher's disposition.

## §1 Scope and method

**Range:** 45 commits and 79 files (+970/−9 780; the bulk is the RULINGS_ACTIVE.md delete and the STATE and CARDS rewrites). No commit in the range has a body or a trailer: `git log --format='%h|%b' 4e8c663a..8eec45a9 | grep -v '^[0-9a-f]*|$'` prints nothing.

**Read first:** CLAUDE.md; RULINGS `### R368` (all clauses) and `### R367`; PROGRESS `## W6`; WAVE_BRIEF and W6_ADDENDUM; 00_MAP §3 (PZ-1..PZ-6) and §5; REVIEW-W5, whose format this report follows.

**Workspace:**
- Scratch worktree `.wt/review-w6`, detached at 8eec45a9, with its own venv (`uv sync`, run unsandboxed). Removed at the end with `git worktree remove --force`.
- Every plant was reverted with `git checkout` and the worktree was tracked-clean before removal.
- I made no edits to the main checkout. Its HEAD moved from 8eec45a9 to 2057c043 while this review ran: two docs-only dispatcher commits, 8e5eab3e (the W7 work order) and 2057c043 (PROGRESS). This review covers 4e8c663a..8eec45a9 only.

**Helpers:** three read-only helper agents took these slices, using `git show` and `git grep` only:
- the STATE rewrite's citations;
- the CARDS sweep and the RULINGS_ACTIVE retirement;
- the mechanical docs leg and the register annotations.

I re-verified each finding filed below before filing it. The re-checks were: the line-81 commit stats, the EVAL_COST run7 line, the CARDS marker set derived both ways, both `_SIZING_BUDGET` authorities, the R341(b) caveat diff, the OC7 host row, Cargo's `strip`, and the R336(e) text.

**Collection census:** `pytest --collect-only -q -m ''` at the base and at every commit in the range, using the tip-built `.so` (the extension surface does not change in the range).
- The collected count equals that commit's `test_count_floor.txt` at **every** commit. It runs 4927 → 4933 (c7e5cbba) → 4969 (9650fd10) → 4976 → 5067 → 5074 → 5083 → 5086 → **4928** (8b00b4dd).
- Node ids from base to tip: 118 removed and 119 added. Every removed id is one of three kinds:
  - a `[run7.yaml]` or `[run8.yaml]` parameter of a census-parametrized row that survives on the other members;
  - a single-config row now parametrized over the census (the same body, re-keyed);
  - a rename with the same body: `test_run6_is_not_one_of_them` → `test_no_production_config_is_one_of_them[*]`, `test_r328b_0{1,2}`, `test_n1_*`, `test_of2_{8,14}_*`, `test_run5_config_produces…` → `test_a_production_config_produces…[*]`, `test_run3_parity_values_pinned` → `test_the_gate_parity_values_are_pinned[*]`, and `test_run8_and_run7_configs…` → `test_rescale_on_and_off…`.
- I found no removed id without a named survivor.

**Removed `assert` and `def test` lines** (`git diff 4e8c663a..8eec45a9 -- tests | grep -E '^-\s*(assert|def test)'`, 57 lines): I read each one against its survivor at the tip. All are renames or re-keys with the same predicate, with the exceptions ruled on in §3:
- `screen_confirm_lo == 0.44` removed;
- `deploy_sims == 160` moved to its own row;
- `len(_MINTED) >= 3` and `"run6.yaml" in _MINTED` replaced;
- `== 1_000_000` became `== ceiling`;
- `10**9` became `ceiling + 1`;
- `"book_v1_s20260625_p4"` dropped;
- the fg5_02 `any(len(pairs) > 1)` vacuity guard replaced by a planted twin;
- the `run6` premise assert in eval_posture_inert dropped.

**Planted breaks** (scratch worktree only, each reverted):

| plant | suite | result |
|---|---|---|
| `configs/run6.yaml` `selfplay.mcts.c_puct` 1.5 → 99.0 | test_deploy_matched_hparam_coincidence.py | RED `test_every_production_config_carries_the_values_read_above[run6.yaml]` (1 failed, 7 passed) |
| `hparams.py` `q_rescale=bool(sp["q_rescale"])` → `q_rescale=True` | test_qsigma_rescale_reaches_target.py | RED ×5 (all rows) |
| `run.py::_resolve_stop_step` `bound > ceiling` → `bound > ceiling + 1` | tip `test_the_launcher_has_no_route_to_the_burst_bound` | RED. The **base** body of the same test stays GREEN on the same plant (its refusal probe was `10**9`), so the re-expression is STRONGER |
| `configs/run6.yaml` `eval.gate.screen_games` 80 → 81 | test_eval_config_remint.py | RED `test_the_gate_parity_values_are_pinned[run6.yaml]` |
| `configs/run6.yaml` `actor_lag_abort_enabled` → false | test_armed_abort_manifest.py + test_armed_abort_cadence.py | RED ×6 |
| `configs/run6.yaml` removed (simulating a HALT grant) | every W6-touched file + tests/config + the W5 census suites (1221 tests) | exactly one RED: `test_the_ruled_deploy_sims_are_pinned` (HALT 2). No other row reds or goes vacuous |

**Gates at the tip** (worktree, rc via `$?`, no pipe):
- 10 `check_tracked_refs`: rc 0 (17 files).
- 13 `contract_doc_gate`: rc 0.
- 15 `r8_header_gate`: rc 0 (150 justified, 0 stale).
- 17 `rule7_gate --base 4e8c663a`: rc 0 (75 text files). This was the tracked floor only; the local supplement is absent in a worktree.
- 7 `validate_configs`: rc 0 on dev_example, run10, run6, smoke_preflight_armed.
- 12 `preflight_mint --audit-only`: rc 0.
- 14's `comment_lint`: GREEN, every measure at its floor (3124, 0, 11908, 1283, 2486, 1163, 466).
- 3c: `collected 4928` == `test_count_floor.txt` 4928.
- `cargo metadata --locked --no-deps` parses the edited Cargo.toml.

**Targeted tests at the tip:**
- Every W6-touched test file, default tier: **773 passed, 4 skipped** (all loud skips: CUDA, and the untracked warm-start artifacts).
- The touched files' integration rows (`-m 'integration or slow'`): **2 passed, 3 skipped** (strix not vendored; CUDA-only legs 2 and 2b) in 596 s. The two real-boot launcher rows pass on the census config.
- The R310-frozen `test_preflight_mint.py` + `test_preflight_mint_process.py` + `test_run_eval_enabled_authority.py`, default tier: 188 passed.

## §2 Findings

| # | sev | commit | file:symbol | finding | evidence (command → result) | suggested fix |
|---|---|---|---|---|---|---|
| 1 | MUST-FIX | 82fadce4 (after b13e324d) | docs/governance/CARDS.md "CARD-* that exist only as in-source markers" | **Two OPEN cards dropped from the open-work register.** R368(e) decides the deletion of "CARDS.md's **closed** rows". The re-derivation of the markers section dropped two cards that are not closed. Both in-source markers had been stripped earlier by a style pass (2649e0b9), so the rule "grep src for CARD-" no longer finds them. CARD-BUDGET-AUTHORITY-CONSOLIDATION is debt FILED by R327(d): two `_SIZING_BUDGET_*` authorities, one ruled to "STAY UNMOVED" until a re-fit. Both authorities are still in the tree, with the test comment "Needs a RE-FIT". CARD-CONFIG-DISCOVERY-ROOT is a deliberately carded limitation (a loadable config outside `configs/` is launchable undiscovered) that nothing closed. Neither id now appears anywhere in CARDS, src or tests. | `git grep -n "_SIZING_BUDGET_BYTES =\|_SIZING_BUDGET_GIB =" 8eec45a9 -- tests` → test_graph_microbatch_authority.py:128 and test_graph_microbatch_bound.py:227. `git show 2649e0b9 \| grep -n "BUDGET-AUTHORITY\|CONFIG-DISCOVERY-ROOT"` → the stripped markers ("FILED AS DEBT", "carded as"). `git grep -n "BUDGET-AUTHORITY\|CONFIG-DISCOVERY-ROOT" 8eec45a9 -- src tests tools docs/governance/CARDS.md` → nothing | Restore both as open rows in CARDS (an ordinary card section, not the markers list, since their markers are gone), each naming its ground: R327(d) with the two test files, and the loader limitation |
| 2 | SHOULD-FIX | 14f86534, 57279306 | docs/governance/STATE.md pointer table (lines 77, 81, 82) | **Three rows of the R368(f) "dropped class → where it lives" table point wrong.** (a) Line 81: of the six "leg exit commits", four hold no exit facts. c31fd74e is the R353 ruling commit (RULINGS + CARDS). a693a0a4 is a one-line test import fix. ff5a43d8 is the RUN9_PREREG skeleton. 23003bb9 is a research-doc sanitise. None of them touches STATE.md. The R358, R359, R360, R361 and R363 exit facts have no row at all. (b) Line 77 sends run8's START time, witness 0.270 / 22.1 %, audit PASS and replay_ratio 3.02 to R358–R360 and three commits. None of those holds them; they live only in STATE history (86308bc7). (c) Line 82 says box specifics were "already sanitized forward in history". History at 029adc0a still names the service and unit names, the chain scripts and the box branches. That is fine under R368(f) ("history is not rewritten"), but the sentence is false. Line 63 (`git show 029adc0a:…STATE.md`) does hold every dropped fact, so nothing is lost; the rows as written misdirect. | `git show --stat --format= c31fd74e a693a0a4 ff5a43d8 23003bb9` → no STATE.md. `git show 4e8c663a:docs/governance/STATE.md \| grep -n "16:55\|0.270\|replay_ratio 3.02"` → old lines 226–232 only. `git grep -n "16:55\|0\.270" 8eec45a9 -- docs/governance/RULINGS.md` → none | Line 81: name the STATE commits that carry each leg's facts (7e0a424c R353, 86308bc7 R358, 7278476a R359, 1a153c79 R360, 671bf12c R361, 0c880a6e R357, ea67af89 R363, e5b01d88 R366, 69e15329 R367), or reword to "gated on …" and point at line 63. Line 77: add 86308bc7. Line 82: "left the tree; history is not rewritten" |
| 3 | SHOULD-FIX | 16e1a912 | src/mantis/config/armed_aborts.py MANIFEST rows `policy_loss_trough` (note), `ply_cap_attractor` (owner, note) | **PZ-6 manifest text edited, and one edit now states a false ground.** (a) The policy_loss_trough note now reads "a production config minted before this row mints null truthfully; the next mint proposes {…} and flips this row to REQUIRED". The row landed at 25073aed (2026-09-13). run7, run8 **and run10** were all minted after it, and all three mint `policy_loss_trough_abort: null`. So the DEFERRED ground for the only live config is not "minted before this row", and "the next mint proposes" has already failed three times. The old text ("run7's mint proposes") was stale, and the reword made that general instead of repairing it. The `ply_cap_attractor` rewording is accurate (run10 arms it; run6 is the only null). (b) Standard inconsistency: this wave edited MANIFEST row text without a ruling. Yet PROGRESS W5/W6 routes the `terminal_eval_broken` RESIDUAL text and this same row's `owner` ("run7's mint") to the operator as "needs a ruling-named re-point" (00_MAP PZ-6: "armed_aborts.py::MANIFEST … touch only with a ruling"). The addendum did order the reword, but the addendum is not a ruling. | `git show 4e8c663a:configs/run7.yaml \| grep -n policy_loss_trough_abort` → null (run8 the same); `grep -n policy_loss_trough_abort configs/run10.yaml` → `null`; `git log -S'name="policy_loss_trough"' -- src/mantis/config/armed_aborts.py` → 25073aed | Put all three MANIFEST text items (this note, this owner, terminal_eval_broken's RESIDUAL) in ONE operator ask. Until then, make the note true: "no production config has armed it yet; a mint that pre-registers {…} flips this row REQUIRED". Record in PROGRESS that the addendum's reword touched a PZ-6 row |
| 4 | SHOULD-FIX | b13e324d, 82fadce4 | CARDS.md markers section; the in-tree CARD- references | **The markers section is incomplete by its own derivation rule, and closed cards now dangle from live references.** The section says it is derived by `git grep -hoE "CARD-[A-Z0-9.-]+"` over src, tests, tools and crates, "less what this file already names". Applied at the tip, that rule yields six ids CARDS no longer names: A4-MINT, EVAL-GATE-FIELDS-IN-STREAM, SEALBOT-TT-SEAT, SELFPLAY-SEARCH-STATS, SERVER-OWNED-COPY, STAMP-FLOOR. b13e324d removed their cards before 82fadce4 re-derived the list. Each card was legitimately closed, but src, tools, tests and contracts still cite them. One is asserted: `tests/tools/test_run_dashboard.py:215` checks that the page note names CARD-SEALBOT-TT-SEAT. Also `docs/contracts/game_record.md:8` cites CARD-GAME-RECORD-SELFPLAY-STATS, and that contract still says the self-play channel has "no producer" for search stats, although `selfplay.search_stats_every` landed (R355(d)). | `comm -23 <(git grep -hoE "CARD-[A-Z0-9.-]+" -- src tests tools crates \| sed 's/[.-]*$//' \| sort -u) <(grep -oE "CARD-[A-Z0-9.-]+" docs/governance/CARDS.md \| sed 's/[.-]*$//' \| sort -u)` at the tip → the six ids (plus the truncated-wrap fragments EVAL-GATE and PREFLIGHT-CHILD, which were present at base too) | Add the six to the markers section as CLOSED, one line each with the closing ruling or commit, so the section's rule holds and every live cite resolves. Card the game_record.md drift (contract text, on contact) |
| 5 | SHOULD-FIX | b13e324d, 33e32a16, b5c32630 | CARDS.md rows R341(b)/R319(d), RUNG-2, CARD-OC7-OVERRUN | **Condensation changed meaning in three rows.** (a) R341(b)/R319(d) went from "DISCHARGED AT G=8 ONLY … still LIVE below G=8" to "DISCHARGED AT G=8". It dropped "discharged by the ARMED VALUE, not by the geometry becoming safe — lowering concurrency to 4 walks straight back into a timeout". That is live residue: run10 arms `eval.concurrency: 8`, and a re-mint below 8 is exactly the event the caveat names. CARDS' own reading note 2 warns about this: the discharge scope lives at the row's foot. (b) RUNG-2 now reads "strix cell on every checkpoint and promotion". R361(a) WITHDREW the per-promotion trigger "for run8 and all later runs", and base said "every 15 000-step checkpoint". (c) OC7: "OC-7 now PASSES in 178.4 s inside its ceiling on that host", where "that host" is the AVX2 dev box. The 178.4 s is the other box's row (Ryzen 9 9900X, AVX-512 BF16). | `git show 4e8c663a:docs/governance/CARDS.md \| sed -n 840,851p` against CARDS.md:549–556 at the tip; `grep -n 178.4 docs/design/measurements/MEASUREMENT_OC7_2026-09-11.md` → the 9900X row; RULINGS R361(a) | Restore the G=8 scope sentence; RUNG-2 "every 15 000-step checkpoint (the per-promotion trigger withdrawn, R361(a))"; OC7 "passes in 178.4 s on the AVX-512-BF16 box; on the AVX2 host the carve-out removes the emulated-bf16 hang" |
| 6 | SHOULD-FIX | 90c99fa6, cac1d6dc | docs/design/repo_design.md (lines 159–164, 299–301, 433–435) | **The replacement profiling recipe does not produce symbols, and the amendment notes cite ids that W8 deletes.** (a) It says a profiling build is "release + debug symbols via `CARGO_PROFILE_RELEASE_DEBUG=true`". `[profile.release]` sets `strip = "symbols"`, which in cargo strips debuginfo too. The deleted profile carried `strip = "none"` for exactly that reason, so the recipe needs `CARGO_PROFILE_RELEASE_STRIP=none` as well. (b) The two R9 amendments are inline parentheticals keyed "SLIM-FIX S-A-DOCS-3-04" and "S-A-DOCS-4-11". Those ids resolve only into docs/slim/, which W8 removes (R368 enactment (3)). Prior amendments in the same file read `AMENDMENT (R347 / CONFIG-1, 2026-09-10)`. | `sed -n 40,44p Cargo.toml` → `strip = "symbols"`; `git show 90c99fa6` → the deleted `strip = "none"` | Add `CARGO_PROFILE_RELEASE_STRIP=none` to the recipe; re-key both notes as `AMENDMENT (R368, 2026-09-25)` |
| 7 | SHOULD-FIX | 77c586f3 (filed), PROGRESS W6 HALT | docs/slim/handoff/halt_twin_heldout.diff | **The grant request does not disclose that the oracle edit NARROWS.** PROGRESS calls the diff "the measured fix". Its third hunk turns the frozen oracle's bounded-difference EQUALITY `differing == FORCED_TWIN_LEAVES` into `differing <= FORCED_TWIN_LEAVES and "train.device" in differing`. A twin that silently FAILED to replace the warm start, or any other forced leaf, would then pass. R368(c): a repair may extend a protected witness, never narrow one. | `sed -n 25,30p docs/slim/handoff/halt_twin_heldout.diff` | State the narrowing in the operator ask, or re-shape the diff to keep equality: derive the forced set from the base config (include the heldout_gap leaves only when the base carries the block), so `==` survives |
| 8 | SHOULD-FIX | 2b83e062 | tests/eval/test_deploy_matched_hparam_coincidence.py::test_every_production_config_carries_the_values_read_above | **A second, hand-kept copy of the D-15 key list.** The census row re-types `("c_puct", "fpu_reduction", "quiescence_enabled", "quiescence_blend_2")`, and the parametrize table of `test_deploy_head_mcts_default_equals_run5` holds the same keys. A fifth ctor key added to the table would be checked on `production_configs[0]` only, and the other members would go unwatched. The plant on the four current keys does red (§1), so the row is live today. | the file, lines 88–95 against 192–198 | Hoist the pairs into one module constant that both the parametrize and the census row read (the D-15 assertions stay byte-unchanged; only their parameter source moves) |
| 9 | NOTE | 7df8117d | tests/config/test_eval_config_remint.py | **The two removed or moved pins: sound.** `screen_confirm_lo == 0.44` has no ruling: `grep -n "screen_confirm_lo\|0\.44" docs/governance/RULINGS.md` → none. The archive register mentions it only as a measured referent of `configs/run5.yaml`. run7's re-mint moved it to 0.5 "since every screen escalated" (79d832cd), and run10 carries 0.5. It is a per-mint value, and R367(a) plus this file's own provenance doctrine forbid pinning it. `deploy_sims == 160` is ruling-named (R346(b)/R348(e)) and correctly stays on run6 as HALT 2. The rest of the gate recipe is extended from run6 to every production config. PROGRESS's "the inherited by-name run10 pin" does not appear in the base..tip diff; the only run10 by-name reader in the range (worker_sweep's `--config configs/run10.yaml`) was re-pointed, not removed. That is a record inaccuracy. | as stated | Correct the PROGRESS phrase |
| 10 | NOTE | 8eec45a9 (PROGRESS residue) | tests/diagnostics/test_ring_audit.py, tests/encoding/test_resolvers_nested_identity.py, tests/eval/test_deploy_matched_hparam_coincidence.py | **The run-named residue list is incomplete.** Missing from it: `test_a_clean_ring_passes_the_run8_bands`, `test_the_prereg_carries_the_five_run8_bands`, `test_nested_identity_resolves_the_graph_encoding_run5_declares`, `test_deploy_head_mcts_default_equals_run5` and `test_the_literal_covers_every_key_run5_declares`. The last two sit in a file the addendum held to re-point only; a rename is not an assertion edit. | `git grep -nE '^\s*def (test_)?[A-Za-z_]*run[0-9]+' -- tests` | Add them to the residue row |
| 11 | NOTE | 14f86534, 57279306 | docs/governance/STATE.md lines 18, 39, 42, 87 | Smaller STATE items. (a) "run7 stopped at step 83 482 on 2026-09-18": the date is sourced nowhere tracked outside STATE history (EVAL_COST has only "25 rounds in 83 482 steps"). RULINGS:835 (R358) cites "`STATE.md`" for that stop. (b) "After W6 the committed configs are …" contradicts line 11's "W6 … in progress", and it was false at both STATE commits (the delete is 8b00b4dd). (c) "worktree branch `w6-state`" names a host-local transient branch. (d) "the three display tools" is a transcribed count. | `grep -n 83\ 482 docs/design/measurements/EVAL_COST_2026-09-19.md` → :162 only | Repair on contact |
| 12 | NOTE | 8db24400 | CLAUDE.md Rust bullet; R368 Amends | **Register-level ambiguity.** R336(e) (archive register) is rustfmt-only: touched files, rustfmt.toml, no tree-wide pass. R368's Amends line nevertheless lists "R336(e)'s on-contact clause" among the three COMMENT rules it replaces. To reconcile the two, CLAUDE.md invents "its comment-application half", which R336(e) does not have. | `sed -n 8232,8236p docs/governance/archive/rulings_register.md` | Operator: annotate R368's Amends line (R336(e)'s on-contact clause is the rustfmt one), then drop the gloss |
| 13 | NOTE | 8db24400 (file touched) | CLAUDE.md:148–153 | Pre-existing drift on a touched file. The integration tier is "UNBOUNDED on an AVX2 host … see CARD-OC7-OVERRUN", but that card now reads DISCHARGED (the carve-out landed at 9491b4d0). Also: "THREE exceptions" is a transcribed count; the `grep -c` census is split across a line break inside a code span; and the analyzer is "loopback-bound", where its amendment says loopback by default with `--bind` documented. | `git grep -n 'OC7\|AVX2' 8eec45a9 -- CLAUDE.md` | On contact |
| 14 | NOTE | 33e32a16 | CARDS.md F-816-34/35 rows; docs/governance/archive/README.md | The F-816-34/35/36 figures are carried exactly, and so is the stale status "never adjudicated / last moved: none". R338(a)/(b)/(c) ruled on 34 and 35; the deleted index held that link. The frozen archive README (R346(e)) got a new 5-line tombstone paragraph beyond the minimal removal of the deleted file's mention; no ruling names that edit. | `git log -- docs/governance/archive/README.md` → 33e32a16, 7a90a997 | Link R338 on the two rows; operator's call on the README paragraph |
| 15 | NOTE | 89802fb6, 6db4f09a, 882b5e16 | .gitattributes:34–37; Cargo.toml:24; README.md:10 | .gitattributes still says "every" fixture is manifest.toml-pinned or golden-read (16 graph_parity raw files are manifest.tsv-pinned; two fixtures are in neither class). The new Cargo comment carries narrative ("one already went stale twice"). README's "where the current run stands" assumes a live run. | helper evidence, spot-checked | On contact |
| 16 | NOTE | 4f64a2c0, 23be1a21 | test_fused_graph_caps_authority.py::test_fg5_02_…SHARING_A_FIT…; test_run_strict_composition.py::test_the_axis_is_the_whole_minted_set_and_is_not_empty | **Two vacuity guards replaced: acceptable.** fg5_02's "some group has > 1 member" guard would red now that no two production configs share a fit. It became a planted-twin proof that `_disagreeing_fits` bites, while the real-config comparison runs over singletons today. The axis row's `len >= 3` count went (R367(a)). Its replacement `census ⊆ _MINTED` is near-tautological, since both come from `discover_configs`. Non-emptiness is now held by `production_configs` raising `ConfigCensusError` on an empty census. | reading | None |
| 17 | NOTE | 16e1a912 | tools/probe1/cli.py `gap --run-id` | Now required, with no caller in tests, tools or the Makefile (`gap --help` shows `--run-id RUN_ID`). PROBE1_2026-09-21.md's `probe1.py gap` reproduction now needs `--run-id run8` spelled out. | `git grep -n probe1 -- Makefile tools/ci_gates tests` | None |
| 18 | NOTE | — | tools/ci_gates/check_tracked_refs.py `DISSOLVED_PATHS["configs/run9.yaml"]` | Its reason, "STATE names it as history", is stale: the new STATE says "its config is deleted" and does not name the path. tools/ci_gates/** is off-limits to this wave, so this is noted only. | `grep -n run9 docs/governance/STATE.md` | Next ruling-named gate edit |

## §3 Per-row verdicts

**1. Protected witnesses (R368(b)/(c)): NOT NARROWED.**
- (i) The two removed pins: see #9. They are SOUND. The "inherited run10 pin" has no diff footprint.
- (ii) The PZ-1 renames:
  - `RUN5_PREREG` → `DRAW_RATE_PREREG` and `_RUN5_EVIDENCE_CEILING` → `_EVIDENCE_CEILING` are symbol renames with identical values (0.25 / 25000 / 50 / 3). Every assertion predicate is unchanged, and the vehicle moved from run6 to `production_configs[0]` (run10), with the ceiling still derived off the vehicle's own `n_workers`.
  - `test_run3_parity_values_pinned` → `test_the_gate_parity_values_are_pinned`: the source-text check in test_minted_values_are_provenance_not_expectation.py names the new def, and the "adjudication A-3" check still holds. The plant reds it (§1).
  - The deploy-matched coincidence file: the pre-existing assertion bodies are byte-unchanged. `git diff` shows only the docstring, the `_PRODUCTION_CONFIG` constant, `_run5()`'s path and the added census row. The census row EXTENDS the file, and its plant reds (§1). See #8.
  - `test_qsigma_rescale_reaches_target`: the old pair differed in `q_rescale` alone (run7 and run8 both `c_scale: 1.0`, `c_visit: 50.0`), and the new form flips exactly that key on one census config. This is R357 §0(4)'s wording ("rescale true vs false"). The src plant reds all five rows. EQUIVALENT.
  - `test_periodic_checkpoint`: the net is now `arch_from_spec_and_config(lookup(enc), d)` at the tiny widths, so each member builds its own arch kind (GnnArchV2SoftPolicy on run10). That is closer to "everything is the config's except the net's size". EXTENDED.
  - `test_run_strict_composition`: the stop-step bound is now derived and tighter (#P6 plant). `max_train_steps` is 1 000 000 on both members, so `None → ceiling` equals the old literal. The axis row: see #16.
- The R310-frozen files `test_preflight_mint.py` and `test_preflight_mint_process.py`: `git diff --stat 4e8c663a..8eec45a9` on them is empty, and both are green at the tip.

**2. The census rule per re-pointed reader: HOLDS.**
- The sweeps and the absence or pairing laws read `discovered_config_paths` or `discover_configs`. That is the case for actor_sync_schema (it was a hand list of three that included the two exempt configs, and now covers all four), allocator ap01, schema o16 round-trip, graph_visit's every-minted row (it was `run6, run6, smoke, dev`), book pairing's shipped-config sweep, and eval_posture's config sweep.
- The production-only laws read `production_configs`: arming, cadence, gate parity, deploy-matched, allocator posture on cuda, frontier and strix cells, f32 warm-start pin, microbatch arming, and fused caps.
- No sweep was narrowed from discovered to production. There are no hard config counts.
- Every `production_configs(...)[0]` "any member" use is a vehicle for mutations, path-only claims or refusal rows. It holds with a one-member census, and with run6 removed (simulated, §1: only HALT 2 reds). It cannot go vacuous, because `production_configs` raises on an empty census.
- `_cuda_config()` (preflight start-halts) asserts non-None rather than skipping.
- test_ladder_backends' `next(...)` raises StopIteration if no member carries the encoding. That is loud, if opaque.
- The configs-b commits c7e5cbba, 9650fd10, e97609be and 92156f4e: each reader was checked by the same rule and holds.

**3. R368(e) deletions:** mostly HELD, with one exception.
- 8b00b4dd deletes exactly `configs/run7.yaml` and `configs/run8.yaml`, plus the floor fold. `git grep 'run[78]\.yaml' -- src tools tests crates Makefile` → none. The contract doc's v34 history row still names them; that is history.
- RULINGS_ACTIVE.md is deleted. F-816-34/35/36 were carried with their figures (#14). No live instruction cites the file.
- Of the CARDS rows removed, every one was closed, landed, spent, discharged, folded or moot at base, **except** the two in #1. Condensation defects are in #5.
- SHAKEDOWN7G is deleted; only docs/slim mentions it.

**4. The run6 HALT: HELD.**
- run6.yaml is byte-identical to 69e15329, and both frozen oracles are unchanged.
- The only tree readers of `configs/run6.yaml` are those two frozen files plus the HALT-2 row.
- Nothing needs run7 or run8.
- The filed oracle diff is a narrowing (#7).

**5. R368(f) STATE:** present-tense facts TRUE at the tip:
- no live run;
- run10 armed and not started;
- configs = run10, run6, dev_example, smoke_preflight_armed;
- `dev` = 69e15329 = the merge-base.

Every cited path exists. That includes `tools/probe1.py`, a 15-line entry over the `tools/probe1/` package. Every cited sha exists and is an ancestor, and every R-number resolves. There are no box-local specifics (gate 17 green; the helper grep for paths, hosts, IPs, units, ssh and providers → none). The pointer-table defects are in #2, and the rest in #11.

**6. Register text:** APPEND-ONLY and ACCURATE.
- `git diff 4e8c663a 8eec45a9 -- docs/governance/{RULINGS,LAWS,falsified}.md | grep -c '^-[^-]'` → 0.
- F-43's symbols all exist and do what the annotation says.
- F-04: 3dd20b49 is the commit that removed `aggregate_cluster_values_min` and the `value_pool = "min"` rows (present at `3dd20b49^`, gone at 3dd20b49).
- The coverage note: the given grep prints 322 entries and 321 unique numbers, one below each stated figure. The duplicate is R279(g)-ANNEX, and "Four" names R24, R29, R32, R33 and R267.
- Placement follows the existing annotation slots.

**7. R368(i) run10 safety: HELD.**
- `run10_resolved.py configs/run10.yaml | cmp - run10_resolved_base.json` → rc 0.
- run10, dev_example, smoke_preflight_armed and run6 are byte-identical to 69e15329.
- The src diff is prose only: 3 string edits in armed_aborts.py MANIFEST text (#3) and 1 comment in pipeline.py.
- No schema key or default, stamp format or numerics change.
- The Cargo change deletes an unbuilt profile and edits a comment; the release profile is untouched.
- probe1 `--run-id` is now required (#17).

**8. R367(a) on the changed code: HELD, with residue.**
- There is no new run-named symbol. The `_run5()` helper and two run5-named test names survive in the re-point-only file (#10).
- Production pins are census-parametrized. The one by-name read is HALT 2, and it is recorded.
- There are no shims.
- The added code has no new comment carrying a ruling number. The cite-bearing added lines are pre-existing assertion messages, re-worded with f-strings.
- comment_lint is at its floor on all seven measures.
- One implementation per thing: see #8.

**9. The mechanical docs leg (8db24400..6db4f09a): HELD, except #6, #12, #13 and #15.**
- The CLAUDE.md comment bullet states R368(g) faithfully.
- The "28 bench floors" count is gone. The derivation is `grep -c '^\[floor\.'` (23 today, not transcribed).
- The digest points to docs/governance/LAWS.md, which exists and holds LAW-01..18 with LAW-10 deleted.
- The viewer and analyzer admissions exist in repo_design (R352(g), R363(d)), along with their tools and make targets.
- repo_design §3: `GnnArch`, `GnnArchV2` and `GnnArchV2SoftPolicy` are sibling frozen dataclasses with no base (arch.py), and `build_net` checks SoftPolicy → V2 → V1 as a defensive order. The text is TRUE.
- `[profile.profiling]` has no builder in the Makefile, tools, pyproject, crates or workflows.
- .gitattributes: the manifests.toml rule is gone, and `golden_tests.rs::GOLDEN_BITS` is the right pin.
- rust-toolchain.toml and mise.toml: the pin is `[provenance].rustc` = 1.97.1, matching the channel.

**10. Gates and commits:** see §1. All green, with the count equal to the floor at every commit. There are 45 one-line subjects, with empty bodies and no trailers.

## §4 Checked and holds

- **Floors:** collected == floor at every one of the 45 commits (per-commit census in §1), with every move riding its causing commit. The comment measures are unchanged across the range.
- **LAW-07:** no gate or monitor input lost its producer test.
  - The MANIFEST's status fields and config paths are untouched; only text changed.
  - Gate 12 audits rc 0.
  - The drawrate PZ-1 witnesses are re-pointed with identical predicates.
  - test_drawrate_arming_authority now binds every production config, which extends it.
- **tools/ci_gates/:** touched only in `test_count_floor.txt`. No DISSOLVED_PATHS row was needed, because the STATE rewrite landed before the delete and gate 10 is green.
- **HALT scope:** the only red on a simulated run6 removal is HALT 2, so no other W6 row depends on run6 by name.
- **Hygiene:**
  - The scratch worktree was removed.
  - Per-commit node-id lists are under `$TMPDIR/ids`, and the helper scratch is under `$TMPDIR`.
  - No pushes; no edits to the main checkout or any other worktree.
  - The main checkout's HEAD advanced to 2057c043 during the review, through the dispatcher's own docs commits.

## §5 Disposition (dispatcher)

Pending: fix loop 1 in progress.
