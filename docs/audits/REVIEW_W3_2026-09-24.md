# REVIEW-W3 — SLIM-FIX wave 3 (8b75f984..ecfea44a)

Filed verbatim from the fresh read-only review agent (R367(b)); §5 is the dispatcher's disposition.

## §1 Scope and method

Range: 59 commits, 244 files (+1609/−8041; src/ 17 deleted + 77 modified, 9 test files deleted). Read R368/R367, CLAUDE.md, PROGRESS W3, 00_MAP §3/§4 (PZ-1..PZ-6), REVIEW-W1/W2, WAVE_BRIEF + W3_ADDENDUM, LEDGER §4–§6 and the scout files S-A-CORE-1/2/3, S-L-DUP; every code commit's diff read; docs-only commits (59cd05cb, 7999d8fd, cc70a3ff, ecfea44a) skimmed.

Symbol checks: every deleted symbol/module grepped `-w` over src tools tests crates docs (excl. docs/slim, governance/archive, audits) — remaining hits are frozen archives, contract version-history notes, RULINGS quotes, kept-with-grounds code (handshake_ran/skipped, CORE-1-27 still C), the W5-scheduled `train_step_from_tensors` test stubs (HANDOFF W5 owns "~13 train_step_from_tensors test stubs"), and `_probes`' constant moved into resolvers.py (cd42505e, live).

Run10 safety: `git diff 8b75f984..HEAD -- configs/ tools/config_templates` → **empty**; `src/mantis/train/checkpoints.py` untouched; `src/mantis/eval/snapshot.py` touched only by the sha256 leg (inline 1 MiB chunk loop → `sha256_file`, same algorithm, stamp bytes unchanged); `.venv/bin/python docs/slim/handoff/run10_resolved.py configs/run10.yaml | cmp - docs/slim/handoff/run10_resolved_base.json` → **rc=0, no output (MATCH)**.

Floors: `test_count_floor.txt` = 5041 = collected at HEAD (`pytest --collect-only -q -m ''` → "5041 tests collected"); floor == collected spot-verified in a scratch worktree at the five rise commits (745b591a=5095, b137ecf1=5080, 018d6897=5043, 84a4eef0=5042, e02f0b95=5042 — each rise is that commit's added pin tests, the W1/W2 convention); all four `comment_length_floor.txt` measures monotone non-increasing across all 59 commits; `comment_lint.py --measure` at HEAD equals the floor exactly (3169/0/12268/1378/2486; ungated ruling_cite 381). Net lines ≤ 0 on every slimming commit; only the four correctness commits grow (broad-except ×2 +59/+36, escalation witness +5, import-side-effect witness +20).

Planted breaks: 1, in `.wt/review-w3` (defect 17, §3). Worktree removed; main checkout tracked-clean at start and end.

Python: ~45 targeted test files default-tier → **625 passed, 3 deselected, 0 failed** (hashing witness, import-side-effects, seam conformance + phantom-member, dead-resolver-export/artifact-pin/r8-identity/FFI geometry, inference_local/census/pool-surface/buffer-facade, f816_37 instrument, eval selfplay child parity, deploy_head ×4, regime, bots protocol/strix, consumer twins + resolve encoding/nsims/arrows, event rosters/absence/target-counter/anchor/bc-graph-reroute/ema, worker-sweep authority/determinism, eval child memory, mirror receipts/puller/audit-bootstrap, lifecycle contract/arch-stamp, drain parity, positions-dropped, monitor rules/phantom-rule, manifest contract + producer-reachable, registry handshake + audit CLI, bootstrap-encode, strix net-only/strength-frontier, resolver wiring/fused caps, pool lifecycle/survivability/strict-composition/actor-lag/actor-sync, inference batching/lifecycle events, deploy budget/leaf-batching/vram). Gates run green: 3c (5041≥4862, tier census "76 deselected, all declared"), 7, 8 ("ARMED + PASS; mutation self-test BITES"), 9 (170 modules, no cycles), 10, 11, 12 (`--audit-only` rc 0), 13, 15 (166 justified, 0 stale), 16. pyright (src+tools, basic) **0 errors** / 40 warnings. `ruff check .` → **2 errors** (finding 1).

## §2 Findings

| # | sev | commit | file:symbol | finding | evidence (command → result) | suggested fix |
|---|---|---|---|---|---|---|
| 1 | MUST-FIX | cc70a3ff | docs/slim/handoff/run10_resolved.py | The handoff script committed in-range carries `import json, sys` (E401) and an unsorted import block (I001); gate 14's ruff arm runs `uv run ruff check .` over the whole tree, so it REDS at HEAD — the W3 exit sweep (`make gates` at the tip) has not run yet and would catch this mid-packet. Nothing else in the tree is red. | `.venv/bin/ruff check .` → `E401 ... docs/slim/handoff/run10_resolved.py:2:1` + `I001 ... run10_resolved.py:2:1` (2 errors); `grep -n "run_ruff" tools/ci_gates/lint_gate.sh` → `run_ruff() { uv run ruff check . ; }` | split the import into two lines (both auto-fixable with `ruff --fix`); no exclusion needed |
| 2 | SHOULD-FIX | b2fa4b4f | src/mantis/selfplay/inference_local.py::LocalInferenceEngine.__init__ (`_is_graph`) | Grid-residue flag the wave's own standard deletes: `is_graph_representation` never returns False (raises instead), so line 51's flag is always True and line 62's `if self._is_graph:` (no else arm) is an always-true conditional. The InferenceServer twin got exactly this treatment in ed057b27 (`is_graph_representation(self.encoding_spec)` as a bare closed check); this one was outside CORE-2-04's named subject and survived. R368(d) orders "arms, flags" gone. | `sed -n '51p;62p' src/mantis/selfplay/inference_local.py` → `self._is_graph = is_graph_representation(...)` / `if self._is_graph:`; `grep -n "_is_graph" src/mantis/selfplay/inference_local.py` → only those two lines | call `is_graph_representation(self.encoding_spec)` bare and unindent the block, mirroring ed057b27 |
| 3 | SHOULD-FIX | cf88ec79 | tests/diagnostics/test_worker_sweep_determinism.py (module docstring) | The docstring still grounds the sweep's seeding placement by "importing anything under `mantis.train` pulls eight training modules in" — FALSE since cf88ec79 landed (the C1-11 fallout reworded util/determinism.py's copy of this claim in the same commit, per NEW-2, but missed this second copy). The structural bar itself still holds (worker_sweep is barred from `mantis.train` by test_worker_sweep_reachability/DAG design); only the measurement is stale misinformation a reader trusts. | `.venv/bin/python -c "import sys, mantis.train; print(sorted(m for m in sys.modules if m.startswith('mantis')))"` → `['mantis', 'mantis.train']`; `sed -n '13,14p' tests/diagnostics/test_worker_sweep_determinism.py` → "pulls eight training modules in" | reword to the live ground (the reachability bar), one line, on contact |
| 4 | NOTE | 49ab6cfe | tests/fixtures/search_golden/temperature/gen_temperature_parity_golden.py (docstring) | The generator's docstring still cites "the Python ``get_temperature(mode="training")`` path" — deleted with selfplay/utils.py. Fixture-generator prose; TESTS-9-04 territory (W-later). | `grep -n get_temperature tests/fixtures/search_golden/temperature/gen_temperature_parity_golden.py` → line 4 | drop the Python-path clause when W5/W6 touch the fixture generator |
| 5 | NOTE | 84a4eef0 | tools/audit_bootstrap_corpus.py::sha256_file | A second implementation of the name and behaviour of `mantis.util.hashing.sha256_file` (its own `_HASH_CHUNK` loop). The recorded kept-apart ground holds — the tool is deliberately mantis-free (stdlib-only imports verified) — but it is the one remaining same-named duplicate, worth a line in the AQ-SHA card. | `grep -n "^import\|^from" tools/audit_bootstrap_corpus.py` → argparse/hashlib/json/math/re/statistics/sys/collections/pathlib/typing only; `grep -rn "def sha256_file" src tools` → hashing.py + audit_bootstrap_corpus.py | none now; card it as the mantis-free twin |
| 6 | NOTE | — (PROGRESS W3) | arena/match.py::_trajectory_hash ≡ eval/aggregate.py::_traj_key | The near-identical trajectory-dedupe hash pair survives (both PZ-1: LAW-04 dedupe key / gate pair statistics). PROGRESS records it as a card candidate, not done — correct handling, noted so the card exists. | both bodies read side by side (match.py:67-72, aggregate.py:78-86); PROGRESS "Card candidate: arena/match.py::_trajectory_hash ≡ eval/aggregate.py::_traj_key (PZ-1)" | dispatcher: card it |
| 7 | NOTE | 624b11c9 | tests/_drivable.py + 12 test files (`train_step_from_tensors` stubs) | Stub methods for the deleted phantom member remain in 13 test files — compatibility shims for a state the tree no longer has, but explicitly scheduled (LEDGER "S-A-TESTS-2-15 [after CORE-1-08]"; HANDOFF W5 names them). The phantom member itself is gone from the protocol and pinned deleted (§3). | `git grep -l -w train_step_from_tensors -- tests` → 13 files; `grep -n "W5" docs/slim/HANDOFF.md:73` → "~13 train_step_from_tensors test stubs" | resolve in W5 as scheduled |

No protection breach, no witness narrowed, no run10-safety breach, no LAW-07 break, no census/by-name-pin edit. Finding 1 is the only gate red and the only MUST-FIX.

## §3 Per-row-group verdicts

- **AQ-SHA (L-DUP-11, CORE-1-20/-21, CORE-3-14 sha half, TOOLS-2-12a/b, defect 51)** — HELD. Every file sha256 in src/ and tools/ reads `mantis.util.hashing.sha256_file` (strix, bootstrap_encode, worker_sweep, fusion_calibrate, encoding/__init__ via `bytes.fromhex` — digest bytes identical, gate 8 ARMED+PASS, handshake test 18 passed — audit_sections, snapshot (stamp digest unchanged), step.py, graph_route (`_assert_launch_pin`, the 3 `_CORPUS_SHA_PINS` monkeypatch cases intact), bundle, resume_state, heldout, mirror_receipts, probe1/swa, mirror_pull, select_balanced_book, strix_follower). Kept-apart grounds all verified live: `preflight_mint_parent._sha256` (+`_events_block`) under tools/ci_gates/** (ruling needed), audit_bootstrap_corpus mantis-free by design (imports verified), registry_gate.sh independent shell oracle, books.py hashes the bytes it parses, checkpoint_state_sha256 a net-param hash (PZ-1). Remaining `hashlib.sha256(` sites are payload/state/trajectory hashes, not file hashes. Witness tests/util/test_hashing.py pins the helper to every manifest-pinned sha + a >1 MiB partial-chunk file vs a whole read (passed).
- **CORE-1 (train/model/encoding)** — HELD. 01 (chain loss), 03/04/05 (event-builder params; the `solver_deltas` premise re-pointed to pin the deletion, `corpus_selfplay_frac` had no contract row), 06 (eight StepCoordinator params; 40 test sites followed, seam + dispatch tests green), 07 (try_save_buffer + counter + RecentBufferLike; SEAM_MATRIX persist rows lost their subject), 08/-10 + defect 15 (phantom members gone; `test_the_production_trainer_IS_a_TrainerLike` added; SEAM_MATRIX import + row edited in the SAME commit 624b11c9 — 13 seam tests pass), 09 (registry strings re-pointed), 11..16/NEW-1/NEW-2 (facades: `import mantis.train` loads one module, measured), 17 (EmaModel direct), 18/19 (dense-arm flags → argparse exit 2, pinned per flag over the retired set), 20 (launch pin via sha256_file), 22 anchor half (sidecar through `bundle.atomic_write` — adds fsync/unique temp/`encoding=` to a provenance sidecar, not the checkpoint format; resume_state half stays C on the PZ-1 ground, recorded), 24/-25 (resolver cluster deleted; `_DELETED` census pins all five), 26 (marker key into resolvers.py), 28 (CNN-era anchor content; the (B) guard now refuses ANY missing tensor — stronger), 29 (four TrainHParams fields; schema keys stay live via selfplay), 30 partial (kept params PZ-1/seam/golden-pinned, recorded), 31, 32 (grid wording; audit sidecar reads utf-8). CORE-1-27 stays C (handshake_ran/skipped present, matches PROGRESS).
- **CORE-2 (selfplay/config/data/env/run.py)** — HELD. 01/-02/NEW-1 + defect 17 (corpus pack + mantis.env gone; CLAUDE.md map + repo_design amended in the SAME commit e02f0b95, R9; audit tool strings reworded — `floor_source` no longer cites a dead path; pyproject `analysis = ["scipy"]`, uv.lock −465), 03 + defect 16 (InferenceServer grid arm/`submit_and_wait`/`_is_graph` gone; the closed `is_graph_representation` check stays), 04 (LocalInferenceEngine's four methods; worker.py error strings re-pointed to `infer_batch_ls` — W2 note 6 closed), 05 (utils.py; CARDS B-15 updated by the touching leg), 06/-07/-08 (dense PoolDims arm, `_RAW_FOR`, `runner_encoding`), 10 (latest_replay_path chain, monitor half in adbfdeda), 11 (WIN_AXES; FFI test keeps pinning the live instrumentation table), 12 (encoding resolver keeps the production arm; AbsentEncodingError stays, gate 13 green), 13 name half (sealbot opponent raises by name, pinned; the resolver module itself stays C on the contract-cite ground, recorded), 15 (selfplay facade), 16 run.py half + 17 (wrappers gone; actor-lag/actor-sync/strict-composition tests green), 18, 19/-20, 21 (flat_leaves public, config_diff + preflight test retargeted), 22 (required_env). CORE-2-23 stays C (drain arm = the drain-parity oracle's driven path → W5, recorded; pool.py's `_is_graph` + pool_drain's `"graph" if … else "dense"` belong to that deferral).
- **CORE-3 (eval/arena/bots/diagnostics/monitor/util)** — HELD. 01 (package facades; arena/eval no longer pull torch at package import), 02/-03 (errors.py), 04 (RULED_DETERMINISM_BAND_PCT + its tautology), 05 (coordinate helpers; axial_distance rows kept), 06/-07 (TERMINATIONS, is_receipt), 08 (from_canonical; the regime test now pins the canonical string), 09 (StrixBot unread attrs), 10 + the adapter (SKIP_REASON_MARKERS + sealbot retired with the repo_design §947 amendment and eval_instrument v2 in 5d002da4; `find_vendor_root` moved to bots/strix.py FIRST in 70bebd36; vendor script/pin/patch side = W4 as recorded; RETIRED_STAMP_PATHS keeps `eval.sealbot_model_sims` correctly for run7/run8 stamps), 12 (grid decode arm; `DeployHeadPlayer` takes the one collaborator; the exactly-one test re-pointed to the required-kwarg TypeError; arena tests drive a dense stub through expand_fn), NEW-1 (should_escalate now driven by test_gate_parity), 15 (fmt_gib shared), 16 (forwarder), 17 (docstrings state current facts), 18/-19 KEEP. Residue done as claimed: HISTORY_LEN (scanner stopped naming it; the FFI test's mention is a RETIRED-note), grid IfExps in run.py (270d51cf; the serial-width pin deleted with its arm, the DERIVED-width assert kept), grid prose (9bd62861 — manifest stays v4, verified: sink contract string unchanged, roster test green).
- **Broad-except class** — HELD for the five sampled fixes: eval/child_memory `make_probe` (ImportError only + pin), worker_sweep CLI ×2 (`_LOG.exception`, message does not repeat the exception, + traceback pin parametrized over both refusal paths), train stamp resolver (EncodingRegistryError; planted TypeError surfaces — pinned), parent-death arm ((OSError, AttributeError) + both a swallow-pin and a defect-pin), disk_guard (`logger.exception`), selfplay spearman (ImportError/ValueError, comment states exactly the two). Remaining `except Exception` sites in src all carry `noqa: BLE001` with stated grounds (record-and-surface, dump-on-fire, watchdogs, force-exit reap, re-raise-as-named); no swallowed persistence path found (checkpoints.py:399-400 counts then aborts).
- **Floors** — HELD (§1). Rises are test-adding commits, floor == collected at each spot-check; comment measures strictly non-increasing; per-commit floor moves are in their causing commits (the dispatcher folded them, as the brief directs).

## §4 Checked and holds

- **R368(i) run10 safety**: configs/ and tools/config_templates byte-unchanged; no schema key, minted row or resolver VALUE changed (resolver slimming is value-neutral — proven by the resolved-config MATCH at HEAD over a base generated before the resolver edits); checkpoints.py save/load untouched; snapshot.py stamp digest identical by algorithm; trainer/search/eval numerics untouched (no hot-path refactor landed; the one eval wiring change 270d51cf makes the resolver call unconditional on an always-true branch).
- **R367(a)**: the only added `runN` token in code-bearing paths is none — the single "run10" added token is HANDOFF procedure prose naming the protected run's config; no new run-named symbol/pin/test; census.py untouched; no by-name pin list edited; no compat shim added in src/tools added lines (grep for back-compat/shim/deprecated/vestigial → 0); the only added R-cites are reflows of pre-existing text (R347(e), R45), no new ruling cites.
- **R368(b) protection**: PZ-1 implementing symbols edited only as far as rows required (losses.py clip_and_step untouched; step.py param trims + counter removal with the monitor_gates field's producer gone; checkpoints.py untouched; aggregate.py untouched except the docstring-adjacent nothing — should_escalate kept and now called; anchor.py strengthened). PZ-2 seam: TrainerLike edited under R368(d)'s explicit order with the SEAM_MATRIX + phantom-member witnesses extended in the same commit; `_registry_sha_handshake` body line swapped digest-identically under AQ-SHA (the card names the file+idiom), gate 8 + its mutation self-test green. Witnesses extended/re-pointed, none narrowed without their subject being deleted (solver_deltas premise, anchor B-guard strengthened, exactly-one → required-kwarg, grid-width pin deleted with the grid arm per R368(d)).
- **LAW-07**: producer_manifest.yaml and armed_aborts.py byte-unchanged over the range (`git diff` → empty); gate 12 rc 0; manifest contract + producer-reachable tests 21 passed.
- **R8/gates**: r8_header_gate green (166 justified, none stating a count, 0 stale — graph_route.py at 259 correctly lost its header); encoding_io_gate green; import DAG green (170 modules); pyright 0 errors.
- **Commits**: all single-line subjects, no bodies, no trailers (`git log --format=%B` spot-checked).

## §5 Disposition (dispatcher)

- **F1 (MUST-FIX) — FIXED in ec0ac144**: import block split + sorted (+ blank line ruff's isort wants); `ruff check .` → "All checks passed!".
- **F2 (SHOULD-FIX) — FIXED in cb4a5dd1**: `_is_graph` gone, bare closed check, block unindented (mirror of ed057b27); tests/selfplay/test_inference_local.py + pool lifecycle green (58 passed).
- **F3 (SHOULD-FIX) — FIXED in bf22b4c3**: docstring re-grounded on the reachability bar (test_worker_sweep_reachability); determinism + reachability tests green.
- **F4 (NOTE) — DEFERRED by schedule**: the fixture-generator prose clause goes when W5/W6 touch the generator (TESTS-9-04 row); recorded in PROGRESS W3 residue.
- **F5 (NOTE) — RECORDED**: the mantis-free sha256 twin in tools/audit_bootstrap_corpus.py is a card candidate (AQ-SHA follow-up), recorded in PROGRESS W3 card candidates; W6 adds it to CARDS.md.
- **F6 (NOTE) — RECORDED**: the _trajectory_hash/_traj_key pair stays a card candidate (PZ-1, LAW-04/gate pair statistics); W6 adds it to CARDS.md.
- **F7 (NOTE) — W5 as scheduled** (the 13 `train_step_from_tensors` stubs, LEDGER S-A-TESTS-2-15).
- The O-30 operator question recorded in PROGRESS W3 stays open for the operator (a witness is never narrowed).
