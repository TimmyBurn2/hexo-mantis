# REVIEW-W8 — SLIM-FIX's close (W8), read-only, filed 2026-09-25

Filed verbatim from the fresh read-only review agent (R367(b)); §5 is the dispatcher's disposition.

I am REVIEW-W8, a fresh read-only reviewer; I wrote none of this code. Scope: every commit in `e4043497..25772266` (W8 entry → dissolution), reviewed against `docs/slim/handoff/W8_ADDENDUM.md` and `docs/slim/PROGRESS.md` §W8 as they stood at `19172118` (read in history). Method: per-grant diff inspection (all nine W8-D items), AST field-diff of the armed-abort manifest, re-execution of the re-pointed oracles (default tiers, plus a read-only reproduction of the grant-1(a) plant mechanism by minting twins into `$TMPDIR`), recomputation of the exit record's §1 figures with the record's own commands at both cited commits, cross-checks of every sha/count/model claim in §2/§5/§6/§7 against history, the run10 MATCH, and the repo's gate scripts (7, 10, 12, 13, 14-comment-lint, 15, 17) run read-only. Bash was used read-only throughout; no file was edited.

## §2 Findings

| # | class | sha | where | finding | evidence (command) | fix |
|---|---|---|---|---|---|---|
| 1 | MUST-FIX | 25772266 | `tools/ci_gates/check_tracked_refs.py` | The gate-10 `DISSOLVED_PATHS` row for `docs/slim/` is **not in any commit** — it exists only as an uncommitted working-tree modification. HEAD as committed fails gate 10 (rc 1) on seven stale `docs/slim/` references in `docs/governance/STATE.md` (:14, :47, :54, :78, :84, :92×2). The commit's own message ("gate 10 carries the dissolved-path row") is false at that commit; the exit record's §9 "post-grant gates rc 0" claim does not cover this, but the wave's gate-10-green claim at the final tip does. | `git status --porcelain` → ` M tools/ci_gates/check_tracked_refs.py`; `git diff-tree --no-commit-id --name-only -r 25772266 \| grep -v '^docs/slim/'` → empty; `git show 25772266:tools/ci_gates/check_tracked_refs.py > /tmp/g10.py && .venv/bin/python /tmp/g10.py` → rc 1, 7 `STATE.md` lines (working-tree version: rc 0) | Commit the row — amend 25772266 (branch is local-only, and nothing cites its sha) or land a one-file follow-up; gate 10 must be rc 0 at the committed tip before the sweep |
| 2 | MUST-FIX | 19172118 | `docs/audits/SLIM_FIX_EXIT_2026-09-25.md` §1 | The BEFORE figure is mislabeled. The record says "BEFORE at `1e8d6d6`" with 1 071 files / 419 648 lines (docs 67/40 710); those numbers are the tree at **`69e1532` (dev)**. At `1e8d6d6` (which includes the scout's 27 committed `docs/slim` files) the tree is **1 098 files / 428 980 lines** (docs 94/50 042), so from the stated cut point Δfiles is −8, not +19, and Δlines −152 648, not −143 316. `00_MAP` §1's own landing commit says "size and partition at 69e1532". The mislabel was seeded by the W8 addendum ("§1's BEFORE figure (1 071 files / 419 648 text lines at 1e8d6d6)") and repeated here. AFTER at `2e69d0e2` reproduces exactly (both tables, all rows). | `E=$(git hash-object -t tree /dev/null); git diff --numstat $E 1e8d6d6 \| awk …` → docs 94/50 042, total 1 098/428 980; same at `69e1532` → docs 67/40 710, total 1 071/419 648; `git log --oneline --diff-filter=A -- docs/slim/00_MAP.md` → `e7c1d08c docs(slim): 00_MAP size and partition at 69e1532` | Relabel §1: BEFORE measured at `69e1532` (dev, 00_MAP's own commit); optionally record the `1e8d6d6` figure (1 098/428 980) in one clause. Doc-only, in place |
| 3 | MUST-FIX | 19172118 | exit record §1 status line, §9 bullet 3 | The record asserts the result of a measurement that has not run: §1 "run10 launches from the merged tip only if the full gate set is green at it, slow tier included **(it is, below)**" and §9 "`make gates.exit` … : **ALL GREEN — 20 gates** (result appended below at the sweep's landing)". No `mantis-gates-w8` unit exists and `.wt/gates` has not been re-checkouted past W7 — the sweep has not run. A result line written before the measurement is exactly the asserted-not-derived class the packet polices (LAW-01, SF-7), and it feeds the operator's merge decision. | `systemctl --user status mantis-gates-w8` → "could not be found"; `systemctl --user list-units 'mantis-gates*'` → 0 units; nothing appended after §9 bullet 3 in the file | Reword both spots to pending ("result recorded in §9 at the sweep's landing"); the sweep then appends the true line — green or the recorded-red path |
| 4 | SHOULD-FIX | 19172118 | exit record §2, W6 row | "CARDS 1028→730 lines" is the cards-**leg** figure (true at `029adc0a`); at the W6 exit sha the row itself cites (`0c8b9176`), CARDS is **778** lines (later W6 commits added 48). A stale transcribed number in the permanent record. | `git show 0c8b9176:docs/governance/CARDS.md \| wc -l` → 778; `git show 029adc0a:docs/governance/CARDS.md \| wc -l` → 730 | Repair in place: "CARDS 1028→778 at exit (730 at the cards-leg landing)" |
| 5 | NOTE | 19172118 | exit record §6 table, W3 row | Table says W3 MUST=0; `REVIEW_W3_2026-09-24.md`'s own §2 findings table carries MUST=1 (the ruff red in the handoff helper). The record's §2 narrative discloses the exclusion ("0 must-fix beyond a ruff red"), but the table's 0 does not reproduce from the file without that caveat. | `sed -n '/^## §2/,/^## §3/p' docs/audits/REVIEW_W3_2026-09-24.md \| grep -c '\| MUST-FIX \|'` → 1 | Optional: footnote the W3 cell "(1 = the handoff-helper ruff red, fixed at ec0ac144)" |
| 6 | NOTE | 16c1b4ed | exit record §5 item 3 | "42 passed before/after" — the run set is not reconstructible: the witness file has 11 rows at HEAD and `tests/eval` whole is 352. The substantive claims (witness green, both plants red) are verified/recorded; only the count is unverifiable as stated. | `.venv/bin/python -m pytest tests/eval/test_eval_broken_reason_routes.py -q` → 11 passed; `… tests/eval -q` → 352 passed | Optional: name the run set beside the count, or drop the count |
| 7 | NOTE | 25772266 | `docs/governance/STATE.md` :47–:92 | STATE's historical coordinate rows still name `docs/slim/` paths as pointers. This is sanctioned (W8-F: "Do not rewrite those docs"; the DISSOLVED_PATHS row is the mechanism) — recorded here so a future reader knows it is by order, not drift. | `git grep -n 'docs/slim' -- docs/governance` (STATE.md rows above); W8_ADDENDUM.md W8-F in `git show 19172118:docs/slim/handoff/W8_ADDENDUM.md` | None (whitelist row, once committed per #1, is the record) |
| 8 | NOTE | — | repo root (untracked) | Untracked host dotfiles sit in the repo root (`.bashrc`, `.claude/`, `.gitconfig`, `.idea`, `.vscode`, `.mcp.json`, …) — environment noise, not the wave's. A careless `git add -A` would commit host config. | `git status --porcelain \| grep '^??'` | Leave untracked; never stage; optionally clean before the sweep worktree is made |

## §3 Verification of the wave's own claims

**Grant scope (W8-D, all nine + item 10's carries).** Each grant diffed against its addendum item: 1(a) `ddad53e0` is the EQUALITY-KEEPING shape — `forced_twin_leaves(base_config)` = device leaves ∪ each carried block + its fields, `differing == forced` kept, heldout_gap leaves present only for run10 (the base that carries the block); the filed subset-narrowing shape (`differing <= FORCED_TWIN_LEAVES and "train.device" in differing`) is preserved untouched at `docs/audits/SLIM_FIX_halt_twin_heldout.diff` and was NOT used. Both census members stamp their artifact blocks as block-level `# delta:` lines, so the `startswith("{block}=")` filter is exact. 1(b) `0f4bab11` deletes exactly the one test + the `_RULED_DEPLOY_CONFIG` constant + floor 4928→4927. 1(c) `d03e3d00` reads `_P`/`_SAMPLE_TS`/`_THRESHOLD` off `_modelled_monitor` with the all-members-agree assert at module level; both members agree on (5.0, 15.0, 100) → `_SAMPLE_TS` (0.0, 15.0, 30.0, 45.0), `_THRESHOLD` 100 — behavior-identical to the old literals; invocations use `_CENSUS_BASES[0]` (= run10.yaml, `production_configs` is sorted, deterministic). 2 `0affd3ce`: AST field-diff of all 9 ArmedAbort rows — **only `owner`/`note` moved** (rows 2, 3, 5); every other keyword byte-identical; gate 12 rc 0; `test_armed_abort_manifest` + `test_eval_config_remint` 28 passed; the new RESIDUAL witness exists (`tests/test_run_eval_enabled_authority.py:122`); the corrected prose is true (neither config arms the trough; run10 arms ply-cap {0.5, 600, 3000}). 3 `16c1b4ed`: message drops `detail=%s`, payload unchanged, witness green. 4 `8d77b5da`, 5 `c09823e0`: doc-only, exact; tombstone sha 33e32a16 verified as the deleting commit. 6 `5a905f7b`: deletes exactly the 19-line classmethod; zero remaining callers (all other `load_checkpoint` hits are the checkpoints-module LAW-12 loader); `checkpoints` import still live (core.py:626); resume/checkpoint rows 52 passed, conformance 196 passed + 4 deselected (= 200 rows). 7/8/9: records only — the range's non-docs file list contains nothing beyond the eight grant files. 10 `2e69d0e2`: only the three `format!` strings; conditions, `SYM_TABLE_IDS` and registry.toml untouched (range diff over mantis-encoding = validate.rs only); `cargo test -p mantis-encoding` → axis_pin 7 + registry_census 6 green. All seven leg shas (`e98addd9`, `be1ff899`, `af27c298`, `830bb693`, `c21dae11`, `f421dace`, `42b61f4f`) exist and are content-identical to their integrated commits (empty diffs).

**Oracle tiers re-run.** `tests/tools/test_preflight_mint_process.py` default tier: **169 passed, 12 deselected** (matches PROGRESS); `tests/tools/test_preflight_mint.py`: **30 passed** (matches). Plant mechanism reproduced read-only: minting each base's twin without the `run_id` replacement makes `differing == forced` fail for BOTH members with exactly `run_id` missing (and nothing extra — which also proves the derived forced set is exact for both). Premises verified to hold for both census members: draw-rate abort armed (min_step 25000 → burst 25001), device cuda, representation graph, `gate_interval: 1000` ×1, `terminal_eval_enabled: true`, heartbeat (15.0, 5.0), L=1 000 000 with earliest fire 300 000 inside (0.25·L, 1.0·L].

**Exit record vs sources.** §1 AFTER at `2e69d0e2`: both tables reproduce **exactly** (9 top-dir rows, 51 package rows, binaries 30). §1 BEFORE: see finding 2. §2: every cited sha exists with matching subject (`9ee503d7`, `1fbddf17`, `cadcc367`, `3db6ab5a`, `8297c74d`, `0c8b9176`, `816c003e`, `8b00b4dd`, `cdbc8000`, `09fa8e6b`); W5 exit floor 4927 ✓; W7 measures 1163/3124/466 → 91/2144/228 confirmed from floor-file history ✓; CARDS → finding 4. §3: halt closures verified by the diffs themselves; the halt-diff `git mv` is byte-identical to its pre-move form. §5: all claims re-verified as above. §6: review counts reproduced from each file's §2 table (W1 0/3/8, W2 0/3/9, W4 0/0/5, W5 3/5/9, W6 1/7/10, W7 5/7/6; W3 → finding 5). §7: model-per-leg claims match PROGRESS's W5/W6/W7/W8 dispatch logs verbatim (W7's L1–L8 package table included). §10 appendix script is byte-identical to history's `run10_resolved.py`.

**R367(a)/R368(b)/(c)/(i).** run10 MATCH: silent (`cmp` rc 0) via the pinned procedure. `git grep -n 'run6\.yaml' -- src tools tests` → empty. The only runN tokens in the range's added lines are four prose fragments inside grant-2's MANIFEST notes (historical narrative, sanctioned by the grant); no symbol/test/pin/tool named runN; no protected pinning test narrowed (the two frozen oracles were re-pointed equality-keeping/strengthened; the one deleted pin was the operator-ordered 1(b)); range file list shows no configs/, schema, stamp or numeric path touched.

**Dissolution.** 25772266 removed exactly 41 files, all `docs/slim/`; `test ! -e docs/slim` ✓; no Makefile/README/CLAUDE/docs-contracts reference remains; self-expiring semantics intact (`_check_dissolved` raises if a whitelisted path is tracked again) — but the row itself is uncommitted (finding 1). STATE.md's exit paragraph is true: branch `claude/slim-fix-r368` local-only (no origin ref), `origin/dev` at `69e15329`, zero merge commits in range.

**Gates/loose ends at HEAD.** Gate 7 rc 0; gate 12 rc 0; gate 13 rc 0; gate 15 rc 0 (148 justified, 0 stale); gate 17 `--full-tree` rc 0; comment_lint GREEN at floor with §9's exact seven measures (2143/0/11897/1283/2235/91/228); collected **4 948** = `test_count_floor.txt` ✓; gate 10: working-tree version rc 0, **committed version rc 1** (finding 1). The §9 sweep is pending (expected; but see finding 3 on its pre-asserted result).

## §4 Closure check

Findings 1–8: **open, dispatcher to fix.** None is closed by a later commit in range — finding 1 specifically cannot be, as the row exists in no commit at all.

## §5 Disposition (dispatcher, 2026-09-25 — ONE loop)

- **#1 CLOSED by amend:** the dissolution commit was amended (`25772266` → `bba0456`), staging the
  gate-10 row the first commit silently dropped (the `git add -A docs/slim <file>` failed on the
  removed pathspec with stderr swallowed; the row is now in the commit). Gate 10 re-run from the
  COMMITTED tree (`git show HEAD:tools/ci_gates/check_tracked_refs.py`): rc 0.
- **#2 CLOSED in place:** §1 relabeled — BEFORE at `69e1532` (00_MAP's own landing commit), with
  the `1e8d6d6` figure (1 098 / 428 980) recorded in one clause and the addendum's label named as
  the error. The reviewer's numbers re-derived by the dispatcher before editing (they reproduce).
- **#3 CLOSED in place:** §1's "(it is, below)" and §9's pre-asserted ALL GREEN reworded to
  PENDING; the sweep's true result is appended to §9 when the unit lands.
- **#4 CLOSED in place:** "CARDS 1028→778 lines at exit (730 at the cards-leg landing)".
- **#5 CLOSED in place:** the W3 cell footnotes the handoff-helper ruff red (`ec0ac144`).
- **#6 CLOSED in place:** "The witness file green before and after (11 rows at HEAD; 42 rows in
  the landed leg's logged run set)."
- **#7 RECORDED, no action** (sanctioned by W8-F's own text).
- **#8 RECORDED, no action** (host dotfiles stay untracked; the sweep worktree is a fresh
  checkout of tracked files only).

Closure check: 0 findings remain open. No new MUST/SHOULD was introduced by the fixes (the
exit-record edits are doc-only; gate 10 verified from the committed tip; comment_lint GREEN;
collected 4 948 = floor unchanged).
