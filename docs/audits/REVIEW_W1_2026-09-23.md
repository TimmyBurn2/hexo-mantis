# REVIEW-W1 — SLIM-FIX wave 1 (fa5fa5d1..df3d5fbd)

Filed verbatim from the fresh read-only review agent (R367(b)); §5 is the dispatcher's disposition.

## §1 Scope and method

Range: 35 commits, 128 files. Read R368/R367, CLAUDE.md, 01_DEFECTS, PROGRESS, 00_MAP §3 (PZ-1..PZ-6); read every
code commit's full diff; docs-only commits (6371c355, ff6adda7, c559b6d2) skimmed. Run10 safety: `git diff --stat
fa5fa5d1..df3d5fbd -- configs/ src/mantis/config/schema/ src/mantis/train/checkpoints.py src/mantis/model/` → only
`schema/monitor.py` (2 docstrings). Floors: collected at every one of the 35 commits in a scratch worktree
(`git worktree add --detach $TMPDIR/rv`, `PYTHONPATH=$TMPDIR/rv/src`, main venv, `pytest --collect-only -m ''`) —
count == `test_count_floor.txt` at every commit (5112 → 5149), each move in its causing commit; every
`comment_length_floor.txt` measure monotone non-increasing across the range; `comment_lint.py --measure` at HEAD ==
the floor file exactly (3370/0/13056/1485/2674; the ungated `ruling_cite_comment_lines` 393 → 391). Targeted tests
(main checkout): 26 files (the lever, roster, absence, rules, stub, rule7, registry, tier-census, hardcode, meta,
lifecycle, books, pretrain, ema, conformance, arch, monitor-schema, hygiene rows) → **312 passed** (`-m ''`);
`ruff check` on every touched .py → clean. Rust: `cargo clippy -p mantis-search -p mantis-core --all-targets --
-D clippy::all` → 0 errors; `cargo build --all-targets` on both → no unused/dead/non-snake warnings;
`cargo test -p mantis-search --lib -- golden dirichlet forced` → 15 passed, `--test dirichlet_parity` → 4 passed
(S4 golden included). mantis-bridge not built by me (runtime `.so` built 20:02, after 745bfae5, exposes the four
getters). Planted breaks: 15, each in the scratch worktree, reverted (results in §3). Worktree + cargo target
removed; main checkout has no tracked changes.

## §2 Findings

| # | sev | commit | file:symbol | finding | evidence (command → result) | suggested fix |
|---|---|---|---|---|---|---|
| 1 | SHOULD-FIX | c2c21623 | src/mantis/selfplay/pool_hooks.py::runner_stats; docs/contracts/event_manifest.md `search_levers` row | The contract row says "`runner_stats` reads the four getters with NO default, so an engine without them raises rather than publishing zeros", but no test witnesses that half: every stub-runner test now passes the levers explicitly (`_LEVERS`), so re-introducing `getattr(r, "pcr_full_moves", 0)` stays green — the exact LAW-07 shape the commit claims to close (a missing getter would publish a constant 0 as a measurement). | planted `getattr(r, name, 0)` on all four levers → test_target_law18_counters + test_inference_seam_counter + test_pool_surface + test_event_rosters → **20 passed** | add a row: `runner_stats(_Pool(SimpleNamespace()))` raises `AttributeError` naming a lever; fold the duplicated `_LEVERS` dict (test_target_law18_counters.py, test_inference_seam_counter.py) into one helper |
| 2 | SHOULD-FIX | 7a5c001a | crates/mantis-search/src/mcts/selection.rs::select_leaves_forced | Doc still says "`forced_root_child` is CLEARED on every exit", but the new OutOfRange early return (`?` before any descent) leaves a pre-armed `forced_root_child` untouched; and the test's `assert_eq!(tree.forced_root_child, None, "no forced child left armed")` is vacuous — `setup_expanded_root` never arms one. | code read selection.rs:386-392 vs mcts/mod.rs:206-214; tests.rs fixture :805 | clear `forced_root_child` on the OutOfRange arm (or amend the doc), and arm it (`set_forced_root_child(Some(first))`) before the bad call so the assertion can red |
| 3 | NOTE | 7a5c001a | crates/mantis-selfplay/src/runner/search_drive.rs (Gumbel round loop) | The bridge's caller-side pre-validation loop was deleted, but the runner's identical loop (`set_forced_root_child(Some(child))` per candidate, then `None`) remains; the same range check now runs twice per Gumbel round on the hot path. Negligible (O(m)), but two implementations of one check (R367(a)); removal is dead-code removal R368(h) admits. Error stage label would change `forced_root_child` → `selection`; no test pins the label. | `grep -rn '"forced_root_child"' crates tests src` → only search_drive.rs:431 | drop the runner's loop in a later wave; the at-cause check covers it |
| 4 | SHOULD-FIX | a3cbe956 | crates/mantis-selfplay/tests/audit1_named_errors.rs::an_unknown_encoding_is_an_ERR_naming_the_registered_set | All other non-snake names were renamed; this one got `#[allow(non_snake_case)] // the name is cited verbatim by docs/slim/S-A-RUST-2.md`. docs/slim is a census working set W8 deletes, not authority; the comment is a finding-document citation (R368(g)) and the allow will outlive its reason. | `git grep -n an_unknown_encoding_is_an_ERR -- . ':!docs/slim'` → only the definition | rename to snake case like its siblings and drop the allow (the scout note is non-canonical, repaired in place per R9) |
| 5 | NOTE | 84ae2c5b / c2c21623 | src/mantis/monitor/producer_manifest.yaml:1 | Header still reads "(contract: docs/contracts/event_manifest.md, v1)" after the v2/v3 bumps; event_manifest.md's `status:` line still opens "v1 —". Nothing parses either. | `grep -n "v1" src/mantis/monitor/producer_manifest.yaml` → line 1 | say "v3" or drop the version from the comment |
| 6 | NOTE | 84ae2c5b | tests/train/test_event_rosters_match_the_contract.py | Roster coverage limits: `training_step` checked only on the no-probe path (`early_game_probe` metrics would add keys); `iteration_complete` only under Gumbel (`mcts_root_concentration` None there by design). Production passes no probe, so correct today. | code read events.py:199-242 | none now; note if the probe is ever wired |
| 7 | NOTE | a3cbe956 | crates/mantis-bridge/src/inference.rs::PyInferenceBatcher::new | With the field gone, `feature_len` is an accepted-and-ignored constructor argument (only selects the explicit arm) — the defect-7 class at the FFI. Grid residue, so W2/W3, not W1. | `tests/bridge/test_pyclass_roundtrips.py:66` passes `feature_len=0` | retire with the grid arm in W2/W3 |
| 8 | NOTE | 7eb4f0a8 | crates/mantis-core/tests/miri_cache.rs::miri_nested_shared_borrows | "g1's borrow has ended at the block" is no stronger than `drop(g1)` under NLL (the borrow ends at last use either way); the substantive repair is the new `ptr::eq(g1, g2)`, which pins both borrows to the one cache. | code read | optional: reword the comment to credit the ptr::eq as the proof |
| 9 | NOTE | c77b3ceb | tests/_monitor_config.py::REQUIRED_TEST_VALUES vs tests/config/test_monitor_schema.py::VALID_MONITOR_SCALARS | Two copies of the same test values (R367(a)). | diff read | have test_monitor_schema read the shared factory's values |
| 10 | NOTE | 791b3501 | tests/eval/* `FusedGraphCapsSpec(max_fused_edges=57149441, max_fused_nodes=1785921)` | The caps literal is now transcribed in 8 more eval fixtures (plus existing config-test copies). Pattern predates the wave; the wave widened it. | `git grep -n 57149441 -- tests` → 15+ sites | card for W5: one caps fixture read off dev_example's resolved section |
| 11 | NOTE | 0d7ea040, edd1c299, 014c401a | test_ema_lever_is_reachable.py (flat `glob("*.yaml")`), test_pool_encoding_bridge.py (`_run5_dump` on configs/run6.yaml), test_resolved_config_emit.py (`_run5`) | W1-touched files keep run-named symbols and a flat glob; PROGRESS already defers them to W5/R368(d)/(e). Recorded so W5's entry list includes them. | PROGRESS.md "28's residue" | resolve in W5 |

No MUST-FIX: no correctness bug, no witness that stays green under the defect it names, no run10-safety breach, no
protected-set narrowing. Finding 1 is a LAW-07 witness gap on a counter whose producer and real-runner test are live
— rated SHOULD-FIX because nothing publishes a constant today.

## §3 Per-defect verdict

- 1 (rule7 sha256) — HELD: planted `hashlib.sha1` in content_sha256 → test_rule7_gate 1 failed.
- 2 (first_inference_* producer tests) — HELD: planted `if False:` on served emit → 1 failed; `if True:` (once-only guard broken) → 1 failed.
- 3 (PCR/Gumbel counters reach Python) — HELD for the producer path (planted `search_levers={}` in step.py → test_search_lever_counters 1 failed); WEAK for the "no default" claim (finding 1).
- 4 (buffer_save_errors_total) — HELD: removed from the stream; roster test pins monitor_gates keys both ways.
- 5 (rules canonical/legacy) — HELD: planted old fall-through key + input → test_phantom_rule_is_visible 1 failed.
- 6 (corpus_selfplay_frac, always-None training_step fields) — HELD: planted `value_accuracy` back → roster/absence tests 1 failed.
- 7 (--corpus-npz / --no-compile) — HELD: argparse refuses both; the "every parsed dest is read" AST test covers the class.
- 8 (all-or-none witness) — HELD: planted `if False:` on the refusal → test failed with GraphPretrainError (wrong reason) → red.
- 9 (EMA lever) — HELD by reasoning: exact decay-0.5 mix assertion + both arms through a real trainer step.
- 10 (conformance self-tests) — HELD: neutered check_grave's used-route → `can_FIRE[used]` failed; neutered `defined` → failed; neutered arch-table verdict → 2 failed.
- 11 (soft-policy arch round-trip) — HELD: parametrised over ARCH_KINDS × {checkpoint, snapshot}, through a weights_only load.
- 12 (gate 8 with registry present) — HELD by reasoning: real script run, registry at src/registry.toml, matching and drifted.
- 13 (preflight XDG_STATE_HOME) — HELD by reasoning (stamp path asserted under tmp; integration tier not run here).
- 14 (ban censuses name V2 nets) — HELD: set derived from the registry; test_one_amp keeps the old literals as a floor (not narrowed).
- 18 (select_leaves_forced unchecked child) — HELD: planted removal of the range check → Rust test failed (tree applied a garbage move at (32767, 32767)); doc/vacuity gap is finding 2.
- 19 (sample_dirichlet expect) — HELD: named error, RNG not advanced on refusal, S4 golden green.
- 20 (miri drop of a reference) — HELD via the new ptr::eq (finding 8).
- 21 (engine stubs vs runtime) — HELD by reasoning: set equality both ways, module and class level, against `dir(mantis._engine)`.
- 22 (tier_census temp leak) — HELD: planted NamedTemporaryFile(delete=False) → 1 failed.
- 25 (hardcode_scan n_chain_planes) — HELD: planted dead field back with getattr default → 1 failed.
- 26 (Makefile $(UV)) — HELD by reasoning: the meta regex catches a literal `uv run` in a recipe.
- 27 (MonitorConfig literal defaults) — HELD: planted `alert_entropy_min: float = 1.0` → 1 failed.
- 28 (flat config glob, three sites) — HELD; seven further sites deferred to W5 (PROGRESS).
- 29 (eval heartbeat tmp/sleep) — HELD.
- 30 (parent-death monkeypatch, survivability mkdtemp) — HELD.
- 31 (cwd-relative paths) — HELD.
- 32 (book_v1 vs packaged book + manifest sha) — HELD.
- 33 (harness 11/5 fallback, fg6_04 TypeError) — HELD (TypeError now matched on `fused_graph_caps` alone).
- 34 (v6_live2_ls fixtures, grid stubs) — HELD for tests/eval; residue outside named in PROGRESS.
- 35 (sealbot_wr_abort case) — HELD: no-row branch keeps a witness with a premise assert.
- 36 (lying test names/docstrings) — HELD.
- 37 (skipif on a helper) — REFUTED-AT-CONTACT confirmed: every `@_LINUX_ONLY` in test_arm_exec_trampoline.py sits on a `def test_*`.
- 38 (rustc warnings) — HELD for mantis-search/mantis-core (bridge not rebuilt by me).
- 39 (pyo3 skip_from_py_object) — HELD (compiles; nothing extracts either type by value).
- 42 (STATE box specifics) — HELD: grep of STATE.md for `/workspace`, `vast`, `ssh`, `pid N`, IPv4, instance id → none left.

## §4 Checked and holds

- R368(i) run10 safety: configs/ untouched; schema/monitor.py docstring-only; checkpoints.py, model/ and the stamp
  format untouched; numerics unmoved (select_leaves_forced pre-check is pure; dirichlet refusal precedes any draw,
  valid-alpha draws bit-identical per the S4 golden; events/step changes are payload shape only; MonitorConfig
  values reach production only via the schema resolver, unchanged).
- Seam contract: event_manifest.md `version: v3` == `sink.EVENT_CONTRACT == "event-manifest-v3"`, pinned by
  `test_the_contract_version_is_the_one_the_stream_stamps`; no reader parses the segment contract string; no
  src/tools reader remains for any removed field (grep of all 14 names → only the contract's v3 note).
- Callers: `emit_training_step_event` lost `qfire_delta` and `emit_iteration_complete_event` lost `w_pre`, both
  positional, handled at every caller; the new required `search_levers` kwarg passed at all 8 call sites;
  `RunnerStats`' new required fields supplied by the one production constructor (`runner_stats`) and the two test
  constructions; worker_sweep's `runner_stats(pool)` runs on a real pool with the getters.
- Bridge: `select_leaves_forced` error mapping keeps ValueError (out of range) / SelectionDesync (desync);
  `policy_len` match arms behaviour-equivalent to the old four arms incl. the error case.
- R368(b)/(h): conformance edits confined to defect 10 and not narrowed (the grave census's three routes and roots
  unchanged); protected witnesses touched (test_finite_gradient_guard / test_nonfinite_guard renames,
  test_one_amp_dtype_authority) renamed or extended only, the renamed node-ids cited nowhere outside docs/slim; no
  schema key, minted row or CI workflow change; the only hot-path search edit is defect 18's check.
- R367(a): no new runN symbol in the range's added code; new helpers are shared, not copied (`tests/_monitor_config.py`,
  `tests/model/_registry_nets.py`, `check_grave`, `_incomplete_tables`, `apply_exemptions`, `_direct_net_ctors`) —
  except findings 1, 9, 10.
- Floors: test floor == collected at every commit; every comment floor only falls, each move in its causing commit.
- R368(g) comments: new comments/docstrings are overwhelmingly one line; the only new finding citation in code is
  finding 4; LAW-18/LAW-07 mentions name laws, not rulings.

## §5 Disposition (dispatcher)

- 1 FIXED: `test_a_runner_without_a_lever_getter_is_refused_not_published_as_zero` (per lever); the two stub-runner
  files take the lever set off `step._SEARCH_LEVER_COUNTERS`. Planted `getattr(r, "pcr_quick_moves", 0)` → the
  `[pcr_quick_moves]` row FAILED; reverted → green.
- 2 FIXED: the out-of-range arm clears `forced_root_child`; the test arms one before each bad call. Planted removal of
  the clear → `assertion left == right failed: no forced child left armed`; reverted → green.
- 4 FIXED (renamed, allow dropped); 5 FIXED (producer_manifest header names no version; the status line says v3);
  8 FIXED (the comment credits ptr::eq).
- 3, 7 → W2/W3 (grid/dup residue in the runner and bridge). 9, 10, 11 → W5. 6 stands as a note.
- Loops: one. No MUST-FIX.
