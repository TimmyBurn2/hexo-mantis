# REVIEW-W7 — SLIM-FIX wave 7 (d18dc5bc..97ec5d2b)

Filed verbatim from the fresh read-only review agent (R367(b)); §5 is the dispatcher's disposition.

## §1 Scope and method

**Range:** 45 commits, 39 of them `style(...)` and 6 `docs(slim)`. No commit has a body or a trailer: `git log --format='%h|%b' d18dc5bc..97ec5d2b | grep -v '^[0-9a-f]*|$'` prints nothing. The main checkout's HEAD moved to 5c0e7749 while this review ran. That is one docs-only PROGRESS commit (`git diff --stat 97ec5d2b 5c0e7749` → docs/slim/PROGRESS.md only), so the gates run in the main tree measured 97ec5d2b.

**Read first:** CLAUDE.md; RULINGS `### R368` (all clauses) and `### R367`; WAVE_BRIEF; W7_ADDENDUM; PROGRESS `## W7`; 00_MAP §3 and the PZ file list; CARDS "CARD-* that exist only as in-source markers"; REVIEW-W6, for the format.

**Mechanical checks, all run by me:**
- **Verifier on all 39 style commits, not a sample.**
  - Command: `python3 <scratchpad>/verify_style.py $(git log --format=%H --grep='^style' d18dc5bc..97ec5d2b)`.
  - Output is 536 lines. The only non-`comment-only` row in each commit is `tools/ci_gates/comment_length_floor.txt`, which is the integrate.sh fold.
- **Checks of the verifier's blind spots** (verdict in §4):
  - Rust string literals across all 83 touched `.rs` files, compared byte-exact without whitespace collapse: 0 mismatches.
  - `tomllib` equality of `worker_sweep_plan.toml` and `bench_floors.toml`: both True.
  - Text-format hunks against heredoc and TOML multi-line-string ranges: none touched. Trailing `#` changes: none.
  - Pragma comments (`noqa`, `type: ignore`, `pyright:`, `fmt:`, shebangs, `shellcheck`): none lost. The one `# noqa: BLE001` was kept with its rule id.
  - Doctest ``` blocks: none touched.
  - Every test that reads source text or `__doc__` (160 modules, found by grepping for `__doc__`, `getsource`, and `read_text`/`open` of .py/.rs), run in a scratch worktree at 97ec5d2b in the default tier: **2014 passed, 2 skipped, 27 deselected** (195 s).
- **Package and floor checks:**
  - Package per commit: file→package map of every style commit.
  - Floor movements: `git diff c^ c -- tools/ci_gates/comment_length_floor.txt` per commit.
  - `test_count_floor.txt` is untouched in the range.
- **Out-of-scope paths:** `git diff --name-only d18dc5bc 97ec5d2b -- .github tools/config_templates tests/fixtures tests/model/conformance configs docs/governance/{RULINGS,LAWS,falsified}.md src/mantis/monitor/producer_manifest.yaml` is empty. Outside docs/slim, the only docs file touched is CARDS.md, with the single row re-point in b4559073.
- **MANIFEST literal (PZ-6):** sha256 of the `MANIFEST = …` source segment is identical at d18dc5bc and 97ec5d2b: `f4cb75e954e7cac5…`, 29 280 bytes. The only armed_aborts.py hunks sit above the literal: the R8 header, the module docstring, and three `#:` lines.
- **CARD census:** `git grep -hoE "CARD-[A-Z0-9-]+[A-Z0-9]"` over src, tests, tools, crates and the Makefile, base against tip.
- **Marker counts:** "planted", "mutation", "pinned", "measured" and "LAW-07", base against tip, with every removed "planted" line read.
- **In-code line references (`file:N`):** checked for drift caused by W7.

**Gates at the tip** (main checkout, rc via `$?`):
- 14's comment_lint: GREEN, every measure equal to its floor (2146 / 0 / 11897 / 1283 / 2235 / 91 / 228).
- 15: rc 0 (148 justified, 0 stale).
- 10: rc 0.
- 13: rc 0.
- 12 `--audit-only`: rc 0.
- 17 `--base d18dc5bc`: rc 0 (498 files, local supplement live).
- 3c: `collected 4928` equals the floor 4928.
- **run10 MATCH:** `run10_resolved.py configs/run10.yaml | cmp - run10_resolved_base.json` prints nothing, rc 0.

**Helpers:** two read-only helper agents, using git show and git grep only, read every hunk of the Rust slice (7 commits) and the tests slice (14 commits). I read the src/mantis/{config,train,eval}, run.py, tools/ci_gates, tools/ root and Makefile hunks myself. I re-ran every helper finding filed below from its evidence command before filing it. Helper items I could not reproduce, or that are wording only, are pooled in #13 or left out.

**Workspace:** `.wt/review-w7`, detached at 97ec5d2b, with its own `uv sync` (run unsandboxed). It was tracked-clean and was removed with `git worktree remove --force`. I made no edits to the main checkout.

## §2 Findings

| # | sev | commit | file:location | finding | evidence | recommended fix |
|---|---|---|---|---|---|---|
| 1 | MUST-FIX | e6b56e5d | crates/mantis-graph/tests/dense_index_fallback.rs:138-139, :152-153 (`the_two_arms_build_the_same_graph`) | **The comment is now FALSE about its test.** The base said the arms must agree field for field up to translation, and then "*Rather than translate*, this drives the strongest available form: a position just inside the budget and one just outside". The rewrite turned the rejected alternative into the claim: "the same stones translated … must agree field for field". The body translates nothing and compares no fields. It asserts only dense/not-dense, edge-count parity (`% 2`) and `num_edges() > 0`. The second comment also dropped the qualifier "when they are far enough not to overlap". | `git show e6b56e5d -- crates/mantis-graph/tests/dense_index_fallback.rs`; `git show 97ec5d2b:crates/mantis-graph/tests/dense_index_fallback.rs \| sed -n 136,170p` | Say what the test drives: "one position just inside the budget and one just outside, not a translated pair; the arm changes the LOOKUP, never the edge pairing". Restore the non-overlap qualifier |
| 2 | MUST-FIX | 2ce26192 | crates/mantis-selfplay/src/records.rs:165 (`refuse_zero_visit_export`, PZ-listed file) | **Invariant cut to a wrong stub.** Base: "Deleting the prior fallback instead would swap a prior dump for an all-zero target, which the DENSE recorder does not catch at all. This pin, its seam sibling and the capacity formula are all derived from the CURRENT visit-limited construction and re-derive with it." New: "Refusing rather than falling back matters: an all-zero target slips past the DENSE recorder." Three problems: (a) it merges the two alternatives (falling back gives a prior dump, and deleting the fallback gives an all-zero target); (b) it cites a recorder that is gone (only hexg.rs still mentions it); (c) it drops the co-derivation invariant (pin + seam sibling + capacity formula). | `git show 2ce26192 -- crates/mantis-selfplay/src/records.rs \| sed -n 20,40p`; `git grep -n 'DENSE recorder' 97ec5d2b -- crates` | Restore the co-derivation sentence. State the alternative as "deleting the fallback would export an all-zero target" |
| 3 | MUST-FIX | e0896f13 | tests/diagnostics/test_f816_37_rate_bar.py:1 | **The docstring now names the wrong mechanism.** "Planted breaks for R342(b)(iv)'s rate bar" became "Planted breaks for **the draw-rate collapse bar**". The module under test is the memory-corruption rate bar (collate-dump count plus the out-of-wire channel) and has nothing to do with draw rate. | `git show e0896f13 -- tests/diagnostics/test_f816_37_rate_bar.py`; `git show 97ec5d2b:src/mantis/diagnostics/f816_37_rate_bar.py \| head -3` | "Planted breaks for the memory-corruption rate bar — …" |
| 4 | MUST-FIX | 676934d4 | tests/config/test_disk_guard_keys.py:5, :39 (`_MINTED`) | **Armed-value provenance lost, and the new text is garbled.** (a) The `_MINTED` 60/10/5 comment dropped "the literals were dead, so nothing has ever measured them". That is the provenance of the pinned values (a carve-out class), and without it a reader takes them for measured. (b) "LAW-16's third leg unarmed" became "the disk-guard watchdog's third leg". The disk guard IS the lifecycle rule's third leg; it has no legs of its own. | `git show 676934d4 -- tests/config/test_disk_guard_keys.py` | Restore "unmeasured: the literals were dead before the mint". Write "the disk guard (the lifecycle's third leg) unarmed" |
| 5 | MUST-FIX | 15e73d3a | tests/test_run_launcher.py:~394 | **A planted-break statement deleted.** Removed: "the same `sink=None` revert that kills the periodic assertions kills this one". It was the only statement of which mutation reds the `trainer_step` assertion. Planted-break markers are a carve-out. | `git show 15e73d3a -- tests/test_run_launcher.py \| grep -n -B2 -A2 'sink=None'` | Restore the clause, without a cite |
| 6 | SHOULD-FIX | c7ea737b | tests/eval/test_gate_parity.py:144; tests/eval/test_eval_value_channel.py:28; tests/eval/test_eval_selfplay_child_parity.py:57 (`_FIXTURE_BYTE_BUDGET`) | **Measured grounds behind load-bearing fixtures and constants dropped.** (a) The gate-parity collision measurement "12 at n=11; still colliding at n=67 for the 20260625/999 pair" is gone, leaving only an unmeasured "collides on one quantile". (b) "n_sims=1 yields one distinct root q at 4/4 positions and n_sims=32 yields 2-4" is gone; it is the margin behind `len(distinct) >= 2`. (c) The byte budget's "RE-DERIVED when the per-node cap was raised, not loosened … grew by the same factor" is gone, and the remaining sentence no longer follows. None of these numbers survives elsewhere in the tree. | `git show c7ea737b -- tests/eval/test_gate_parity.py tests/eval/test_eval_value_channel.py tests/eval/test_eval_selfplay_child_parity.py` | Restore each number in one line |
| 7 | SHOULD-FIX | 39fa4868, 2ce26192, f0766683 | crates/mantis-selfplay/tests/ply_cap_game_coverage.rs:1-6; crates/mantis-selfplay/src/queues/wire.rs:5-6; crates/mantis-search/tests/pool_overflow.rs:6-7 | **Measured grounds dropped, and one comment now misleads.** (a) The ply-cap test lost why `RANDOM_OPENING_PLIES` exists ("a full 128-ply game at 50 sims in a DEBUG build did not complete in 600 s") and the definition of "the deep regime" (ply > ~120). (b) wire.rs lost the 33.78 ms/pop at 1.35 GB/s measurement and the `u32→i64 + offset` via `extend` over `TrustedLen` note. That note is the guard against rewriting the widening as a push loop. (c) pool_overflow.rs now says the drive runs "where an uncapped radius-8 legal set overflows", but it uses `Board::new()` at `DEFAULT_LEGAL_MOVE_RADIUS = 5`. It also dropped that the prior mitigation "silently corrupted training targets", which is why the obvious fix is refused. | `git show 39fa4868 -- crates/mantis-selfplay/tests/ply_cap_game_coverage.rs`; `git show 2ce26192 -- crates/mantis-selfplay/src/queues/wire.rs`; `git show f0766683 -- crates/mantis-search/tests/pool_overflow.rs`; `git grep -n 'DEFAULT_LEGAL_MOVE_RADIUS: i32' 97ec5d2b -- crates` | Restore one line each: the 600 s debug measurement, the ms/pop figure plus the TrustedLen guard, and "radius-8 was the old overflow regime; this drives the default board" |
| 8 | SHOULD-FIX | f0766683 | crates/mantis-search/src/mcts/tests.rs:443-444 (CF-6); crates/mantis-search/src/mcts/kind.rs:5-6 against tests/search_kind_conformance.rs:130-131 | **The condition of a pinned invariant was cut, and a referent was deleted.** (a) CF-6 dropped "(at `mr==1` the child is the other player, at `mr==2` the same)". That condition is exactly what the test pins: selection negates only when `moves_remaining == 1`. (b) kind.rs deleted the only list of the four replaced switches (`gumbel_mcts`, `gumbel_variant`, `completed_q_values` ×2). The conformance test's comment still says "the four keys this one replaces must not parse either", and only one of the four is in its `bad` list. | `git show f0766683 -- crates/mantis-search/src/mcts/tests.rs \| sed -n 30,45p`; `git show f0766683 -- crates/mantis-search/src/mcts/kind.rs`; `git show 97ec5d2b:crates/mantis-search/tests/search_kind_conformance.rs \| sed -n 128,140p` | Restore the mr parenthetical. Restore the four names in one line in kind.rs, or re-word the test comment |
| 9 | SHOULD-FIX | 6282b66f | crates/mantis-encoding/src/spec/validate.rs:129-132 | **A stale claim was re-authored as a present-tense fact.** New: "`mantis_selfplay`'s `sym_tables_for` PANICS at runner start … `registry_census.rs` holds the two equal". `sym_tables_for` is gone: sym.rs's own base header records that R346(f) deleted it. registry_census.rs pins only `sym_table_id == "size_19"`. The staleness predates W7, but the pass rewrote the sentence and kept the false part. The error-string literals (:137-152) name the same deleted function; those are code, not W7's to fix. | `git grep -n 'fn sym_tables_for' 97ec5d2b -- crates` → empty; `git show d18dc5bc:crates/mantis-selfplay/src/replay/sym.rs \| sed -n 3,8p`; `git grep -n sym_table 97ec5d2b -- crates/mantis-encoding/tests/registry_census.rs` | Comment: "the registry refuses any other `(sym_table_id, n_chain_planes)` at load; the selfplay-side table it guarded is deleted". Card the stale refusal strings |
| 10 | SHOULD-FIX | 00f04f6a, e00bc282, 463bba16 | src/mantis/train/events.py:128; src/mantis/run.py (trainer-sink bind, `_DeferredSink`); tools/ci_gates/test_count_gate.sh header | **Measured grounds cut from src/tools narrative runs** (the 5 sampled, which is not exhaustive). (a) events.py dropped "the burst fired this on 193 of 193 emissions", the measurement that justified demoting the axis alert. (b) run.py `init_trainer` dropped that `sink=None` meant "every trainer-side emission was authored, unit-tested and DROPPED in every production run": the bind's reason is now implicit. (c) test_count_gate.sh dropped the F-816-33 measured failure (`4414 tests collected, 1 error` read as a PASS). Self-test arm 9 still says "VERBATIM the summary line … this tree printed", so the fact survives in the arm's data. The other src/tools cuts I read (config schema, run.py ladder/seam comments, preflight_mint, lint_gate, run_all) keep each invariant: ordering, the one-authority reason, the rc taxonomy, pipefail scoping. | `git show 00f04f6a -- src/mantis/train/events.py \| grep -n -A4 DEMOTED`; `git show e00bc282 -- src/mantis/run.py \| grep -n -A4 'sink=None'`; `git show 463bba16 -- tools/ci_gates/test_count_gate.sh \| grep -n 4414` | (a) "fired on 193/193 emissions at the minted 0.5". (b) one line: "without the bind every trainer emission drops". (c) optional |
| 11 | SHOULD-FIX | 97ec5d2b, 28cb8660, 15e73d3a, 5d23cdce, c4b3a0fb | tests/selfplay/test_pool_hparams_arms.py:33,162; tests/selfplay/test_graph_collate_parity.py:53; tests/train/test_target_stage45_pins.py:57,~86; tests/train/test_target_counter_events.py:47; tests/test_run_composition.py:33; tests/conftest.py:71; tests/util/test_device.py:65; tests/model/test_bf16_parity_nulldist.py:28,43 (PZ-listed); tests/eval/test_gate_sequential.py:71 | **"Why this fixture value / this assertion" grounds dropped.** The reasons lost: draw_reward -0.4 / ply_cap_value -0.7 are chosen DISTINGUISHABLE from the defaults; PUCT-only diagnostics gate on the kind; a byte capture must not be regenerated; `max_nodes=1_000_000` makes the split edge-driven and `len(parts) >= 2` non-vacuous; three live counters are needed for the crosswire proof; the three step-clock knobs are co-overridden because the validator spans all three; the conftest's lazy imports (the stated exception to the import rule); the measured 15 342 MiB sink figure; the pointer to the live re-derivation test (`test_detection_floor_is_derived_…`, which exists at :222); "the cap PROMOTES" (gate_sequential now keeps only "the bounds decide before it"). Each is a fact the code cannot state, and none is a cite. | `git show <sha> -- <file>` for each; e.g. `git show c7ea737b -- tests/eval/test_gate_sequential.py \| grep -A1 cap`, `git show c4b3a0fb -- tests/model/test_bf16_parity_nulldist.py` | Restore each as one line |
| 12 | SHOULD-FIX | 676934d4, 28cb8660 | tests/config/test_armed_abort_cadence.py:336; tests/config/test_cadence_clock_mutations.py:74; tests/train/test_coordinator_gates.py:287 | **Cite replaced by a WRONG attribution instead of dropped.** "ADJ-D22's own/measured config" became "the module layer's own config" and "this module's measured config". The 1e9 config was the adjudication's measurement, not the module's. "A pre-R362 sidecar's sealbot ring" became "A pre-existing sidecar's", which loses "written before the sealbot rung was removed". | `git show 676934d4 -- tests/config/test_armed_abort_cadence.py tests/config/test_cadence_clock_mutations.py`; `git show 28cb8660 -- tests/train/test_coordinator_gates.py` | "the adjudication's measured config" and "a sidecar from before the sealbot rung's removal" |
| 13 | NOTE | several | Rust/tests wording | **Minor wording and context losses** that change no invariant. Rust: selection.rs:12 overclaims "every caller latches" (`run_simulations_cpu_only` drops the error by design); mod.rs:341 dropped "bit-equivalent"; backup.rs:101 dropped "mcts_mean_depth 3.4 → 2.5"; tt.rs:19 dropped the deferred `#[repr(C)]` packing; search_drive.rs:342 dropped why the root charge is not a config key; graph lib.rs:1038 dropped the `axis_pin.rs` pointer; graph.rs:42 "VERBATIM" has lost its referent; bridge tactics.rs:14 is a fragment; empty_board_legal_pin.rs:35 has a misattached parenthetical. Tests: two headline docstrings emptied ("At the composition root.", "Closure — …"); test_run_one_authority.py:61 "checkably so" lost its grounds; test_gnn_seam_smoke.py:68 re-worded a pre-existing falsehood (the server reads `amp_dtype_for("graph")`, not `train.amp_dtype`); finding-token prefixes are handled inconsistently (B-5/F-nn dropped, while B-2/B-3/B-7/A-n are kept as bare docstring prefixes). | helpers' per-item `git show <sha> -- <file>` | On contact |
| 14 | NOTE | 00f04f6a, 15e73d3a | src/mantis/train/coordinator/dispatch.py:173; tests/test_run_one_authority.py:61 | Two planted-break mentions were dropped from comments, but each plant is still stated where it lives: `test_graph_microbatch_authority.py:257` says "A THIRD read anywhere adds a key and reds", and `test_the_pool_allowlist_BITES_on_a_third_construction_site` carries the planted pair. No planted-break test lost its statement. | `git show 97ec5d2b:tests/train/test_graph_microbatch_authority.py \| sed -n 254,258p`; `git show 97ec5d2b:tests/test_run_one_authority.py \| grep -n BITES` | None |
| 15 | NOTE | 384b127a | tests/encoding/test_r8_identity.py:39 | **One new run-named reference, in a docstring.** "If this reds, R26 is broken" became "…run5's radius pin is broken". This is provenance, not a symbol, so R367(a) is not breached, but it keys the sentence to a run where the base keyed it to a rule. It is the only `runN` token the range adds that the base did not already carry at the same site. | `git show 384b127a -- tests/encoding/test_r8_identity.py \| grep '^+.*run5'` | "the v1 row's radius-6 pin" |
| 16 | NOTE | 5c0e7749 / 6f8d5d3a (record) | docs/slim/PROGRESS.md W7 rows | **PROGRESS names a sha that is not on the branch.** It cites e2c0ca7a for L7's train package. The branch commit is 00f04f6a; e2c0ca7a is the leg's pre-integration commit and has an identical src patch (`diff <(git diff e2c0ca7a^ e2c0ca7a -- src) <(git diff 00f04f6a^ 00f04f6a -- src)` is empty). `git branch -a --contains e2c0ca7a` is empty. | as stated | Re-point to 00f04f6a |
| 17 | NOTE | c4b3a0fb | tests/model/test_net_param_hash_promotion.py, tests/model/test_one_amp_dtype_authority.py | **Cites left unrecorded.** These two files still carry 7 and 8 cite lines (R321(d), R346(f), AUDIT-1 F-31 …), the largest remainder in-scope outside the kept markers. Both are on 00_MAP's PZ list, which is a sound reason to leave them, but PROGRESS records no disposition. | the comment_lint per-file measure at the tip | Record "kept: PZ-listed pinning tests" or trim on contact |
| 18 | NOTE | — | the verifier | **Method limits** (§4 has the checks that close each one here). (a) Python docstrings are stripped wholesale, so an edit to a test-asserted docstring is invisible. (b) `ast.parse` drops every comment, so a removed `# noqa`, `# type: ignore` or `# pyright:` pragma is invisible. (c) Rust comparison collapses whitespace INSIDE string literals, and `///` doctest blocks are stripped as comments. (d) Text formats drop every own-line `#`, including `#` lines inside a shell heredoc (registry_gate.sh's embedded Python), a TOML `"""` string or a YAML block scalar, all of which are data. (e) Files with other extensions (.pyi, .json, .md) only report OTHER. (f) Each commit is compared against `rev^`, so a range's net effect relies on linear history. For this range every limit was checked separately and none hid a change. | read `verify_style.py` | For the W8 exit: add a literal-exact Rust pass, a pragma grep, and a heredoc-range guard |

## §3 Per-package verdicts

| commit | package | one-package | floor fell | verifier | judgment |
|---|---|---|---|---|---|
| 2ce26192, 39fa4868 | crates/mantis-selfplay (src \| tests+benches, split as recorded) | yes | cite 1163→1088, excess 3124→2954 | comment-only | #2, #7(a,b); SAFETY 6/6, CARD 1/1, planted 28/28 kept |
| f0766683 | crates/mantis-search | yes | 1088→1079 / 2954→2841 | comment-only | #7(c), #8; FPU-perspective, virtual-loss and RNG-seed invariants kept |
| 6282b66f | crates/mantis-encoding | yes | 1079→1066 | comment-only | #9 |
| 463bba16 | tools/ci_gates | yes | cite 1066→1008, textfile 466→252 | comment-only (floor fold) | sound. Self-test corpora, pattern registers, heredoc bodies and refusal strings untouched; rc taxonomy, pipefail and ref-order grounds kept; CARD-LINT-GATE kept |
| f03b4d08 / 3c6bf3af / e6b56e5d | core / bridge / graph | yes | each fell | comment-only | #1 (graph); core and bridge sound apart from #13 |
| 00f04f6a | src/mantis/train | yes | 993→922 / 2675→2575 | comment-only | #10(a); CARD-SERVER-OWNED-COPY ×2 kept |
| dd1c8985 | src/mantis/config | yes | 851→812 | comment-only | sound: bound grounds (`ge=1`, `gt=0`, inf, MAX_ARMED_SIMS 4×, eval_interval) kept; MANIFEST byte-identical |
| 7e2545ad | src/mantis/eval | yes | 776→736 | comment-only | sound: seed-offset measurement, once-only latch, pair-unit resampling, one-sample-bootstrap refusal kept |
| e00bc282 | src/mantis root | yes | 736→719 / 2421→2311 | comment-only | #10(b); teardown ladder, pin-at-step-0, first-fire-wins and hoist reasons kept |
| dd08abb8, 741bc95b, 38dc900e, 35e60ed3, 006f3ed5, 5897e055, b36c4c2e | remaining src | yes | each fell | comment-only | sound (cite drops) |
| 275e604e, c43b6717, a9093063, 2adc79ae, ad2f69b8, c4d2fdcd | tools root / dashboard / ladder / probe1 / viewer / Makefile | yes | each fell | comment-only; TOML data equal | sound; worker_sweep_plan keeps its band and ladder grounds; bench_floors cites kept (known) |
| c7ea737b | tests/eval | yes | 922→871 | comment-only | #6, #11 |
| c4b3a0fb | tests/model | yes, conformance untouched | 812→798 | comment-only | #11, #17 |
| 8269dce4, e0896f13, 8663c766, 384b127a, 5d23cdce, daadf9f5, 178c795a | bridge / diagnostics / monitor / encoding / util / arena / data | yes | each fell | comment-only | #3 (diagnostics), #15 (encoding), #11 (util); others sound |
| 28cb8660 | tests/train | yes | 669→534 | comment-only | #11, #12; R8 header correctly dropped (301→299) |
| 15e73d3a | tests root | yes | 534→508 | comment-only | #5, #11, #14; CARD-CS2 kept |
| 676934d4 | tests/config | yes | 463→374 | comment-only | #4, #12; `adjudication A-3` kept (string); CARD-MINT-RESOLVE-PARENT-CONJUNCT kept |
| a45414cd | tests/tools | yes | 257→181 | comment-only | sound; the rendered-string cites kept (known) |
| 97ec5d2b | tests/selfplay | yes | 181→91 | comment-only | #11; CARD-POOL-ENCODING-BRIDGE and the R8 header kept |

## §4 Checked and holds

- **Code and behaviour: no change.** All 39 style commits are comment-only by the verifier. Each blind spot in #18 was closed by an independent check:
  - Rust literals: 0 mismatches over 83 files.
  - Doctests and pragmas: none touched or lost.
  - Heredoc, TOML-string and trailing-`#` hunks: none.
  - Docstring and source readers: 160 modules, 2014 passed / 2 skipped.
  - Test node ids cannot move, because the ASTs are equal.
  - Collected count is 4928 at base and tip, and `test_count_floor.txt` is untouched.
- **One package per commit; floors only fall.** Every style commit touches exactly one addendum package; crates/mantis-selfplay is split in two, as recorded. Every commit lowers at least one measure, and no measure rises anywhere. At the tip every measure equals its floor.
- **Out of scope untouched.** `.github`, config_templates, fixtures, conformance, configs, the three register files and producer_manifest.yaml are all unchanged. The MANIFEST literal is byte-identical to d18dc5bc. Outside docs/slim, docs changes are only the CARDS row re-point (b4559073), and its new target is correct (`git grep -n CARD-STAMP-FLOOR 97ec5d2b -- tools` → preflight_mint_parent.py:60).
- **CARD census holds.** Every in-source-only marker CARDS lists is present at the tip, including the CLOSED/LANDED "still cited" rows. The tokens that were dropped are LADDER-RUNG, STOP-DRAIN-VS-GRACE, PYRIGHT-STRICT and one each of OC7-OVERRUN, RUN5-GPU-OOM and STAMP-FLOOR; each has its own CARDS row. PYRIGHT-STRICT is still live in pyproject.toml:112.
- **Carve-outs held in the areas checked.** Rust SAFETY comments (6/6), the atomic.rs and queue_fuse_pin planted markers, and every gate-15 header on a file still over 300 lines were kept. The two headers removed (schema/train.py at 289 lines, test_drawrate_gate_branch_flipset.py at 299) were required removals. Gate 15 is rc 0 with 0 stale.
- **Tokens and strings that tests read are unchanged.** Every string literal and assertion message is unchanged.
- **Standards and safety.**
  - R367(a): no run-named symbol, test or pin was introduced (#15 is prose).
  - R368(b)/(i): PZ files were touched in comments only; run10 MATCH.
  - LAW-07: no producer-test or planted-break statement lost its meaning (#14), except #5, where the one statement of a mutation was deleted.
  - Budget, determinism and seam contracts: no code change.
- **Commit hygiene.** Every subject is one line with no body and no trailers. Several subjects run past 150 characters (00f04f6a at 169); this is not a rule breach.
- **Known dispositions** (the bench_floors cites, the three dropped CARD tokens, cites in strings/parametrize/TOML, the tests/tools rendered-string cites, the stash incident): I found none of them wrong.

## §5 Disposition (dispatcher)

All five MUST-FIX and seven SHOULD-FIX rows were fixed in ONE loop, by two fresh fix legs (**opus**, one for Python
and one for Rust). Their 15 commits are b6a8a843..81c1b6bd, one package per commit and comment-only by the
verifier. After the loop no measure rose: comment_excess 2146→2144, the rest at floor. Other results: run10
MATCH, collected 4928, gates 15/17 rc 0. The reviewer's closure check found every routed row CLOSED, 0 new
MUST/SHOULD and 1 note.

| # | sev | disposition |
|---|---|---|
| 1 | MUST-FIX | FIXED 1d490bec: the comment says what the test drives; the non-overlap qualifier restored |
| 2 | MUST-FIX | FIXED 8fec7dc7: the two alternatives stated separately, the co-derivation invariant restored, the DENSE-recorder reference gone |
| 3 | MUST-FIX | FIXED a535b82a: "the memory-corruption rate bar" |
| 4 | MUST-FIX | FIXED 27f85384: "Unmeasured: the literals were dead before the mint"; the disk guard is the lifecycle rule's third leg |
| 5 | MUST-FIX | FIXED 6300b5d6: the `sink=None` planted-break clause restored |
| 6 | SHOULD-FIX | FIXED 08419e49: the three measured grounds, one line each |
| 7 | SHOULD-FIX | FIXED 8fec7dc7 (a, b), a8211ad7 (c) |
| 8 | SHOULD-FIX | FIXED a8211ad7: CF-6's `mr==1` condition and kind.rs's four replaced switches |
| 9 | SHOULD-FIX | FIXED 81c1b6bd (the comment). The three refusal STRING literals in validate.rs still name the deleted `sym_tables_for`; they are code, outside W7's classes, and go to W8's exit record as an on-contact item |
| 10 | SHOULD-FIX | FIXED (a) c49dd61f, (b) 3f7344fe. (c) left: the fact survives in self-test arm 9's data (the reviewer agrees) |
| 11 | SHOULD-FIX | FIXED 6300b5d6, 08419e49, 45776568, 3f6aeadb, 61ca6b5b, 17d624af |
| 12 | SHOULD-FIX | FIXED 27f85384, 3f6aeadb |
| 13 | NOTE | on contact |
| 14 | NOTE | none needed |
| 15 | NOTE | FIXED b4bdaddd: "the v1 row's radius-6 pin" |
| 16 | NOTE | FIXED: PROGRESS re-points e2c0ca7a to 00f04f6a (this commit) |
| 17 | NOTE | recorded in PROGRESS: tests/model's two PZ-listed pinning tests keep their cites, since W7 edits a PZ file's comments only where the pass required |
| 18 | NOTE | closed for this range by the §4 checks. The verifier upgrade (a literal-exact Rust pass, a pragma grep, a heredoc-range guard) goes to W8's exit record |
| closure N1 | NOTE | not a deviation. 81c1b6bd's own validate.rs edit is the −2 (the Rust leg's per-file delta: 0 everywhere else), so the fold sits in the commit that caused the fall |
