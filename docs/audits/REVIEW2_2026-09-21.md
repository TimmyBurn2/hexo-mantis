# REVIEW-2 — the R367 fix leg (19e8351d..7b33b38e) against REVIEW-1 and R367(a)

> **Provenance.** Taken 2026-09-21 by a fresh read-only review agent under R367(b), with no history
> of either leg. The worktree it was given was checked out at `19e8351d` (the BASE), not the head;
> HEAD was NOT moved. Every "at HEAD" observation below is over `7b33b38e` read through
> `git show 7b33b38e:<path>`, `git grep <pattern> 7b33b38e -- …`, `git diff 19e8351d..7b33b38e`,
> and a `git archive 7b33b38e | tar -x` copy in the scratchpad (`scratchpad/head/`, with
> `scratchpad/base/` for `19e8351d`) for `grep`/`wc`/`awk`. Read in full: `CLAUDE.md` (rules 10/11),
> `docs/audits/REVIEW_2026-09-21.md` (all 34 findings), every `src/` and `tools/` hunk in the
> range, the governance hunks (RULINGS R367 + the R365 A1 annotation, CARDS, repo_design, prereg),
> and at HEAD: `census.py`, `lr_schedule.py`, `losses.py`, `trainer/core.py` (the changed regions
> plus the aux/scheduler/head paths), `checkpoints.py` (the changed regions), `pool.py`, `warmstart.py`,
> `anchor.py`, `arch.py`, `gnn.py`/`gnn_v2.py` (the head methods), `ema.py`, `graph_collate.py`
> (the segment helpers), `preflight_mint.py` (the census read), `check_tracked_refs.py`,
> `tests/_drivable.py`, `tests/train/_coordinator_pool.py`, `test_lr_schedule.py`,
> `test_config_census.py`, `test_gnn_widths_resolver.py`, `test_server_owned_copy.py`,
> `test_actor_sync_composition.py`, `test_arch_selector_makes_v2_selectable.py`, the
> `test_preflight_mint_process.py` diff in full, `test_graph_collate_parity.py`. Nothing was run —
> no `uv`, `cargo`, `pytest`, `make`, no gate; every number is `grep -c` / `wc -l` / `awk length` /
> `git diff --numstat`, labelled **measured**. Wall costs are reasoned, labelled so. §4 lists what
> was not read.

Range: 8 commits, 88 files, **measured** `+1 516 / −1 550` (docs +637/−12, configs +0/−195,
tests +552/−984, src+tools +327/−359). No Rust file is in the range.

---

# 0. Verdict

**DONE on REVIEW-1: all ten must-fix findings are discharged and verified at HEAD** (F1.1–F1.4,
F4.2, F4.3, F5.1–F5.4) — the census is the one authority and gate 12 reads it, the LR is floored by
construction with the parent's state format and swept over every cosine config's whole budget, the
actor wiring is pinned with distinct sentinels through `compose_run` (a revert reds), the model-less
shim is gone, and a pool never serves its argument. 27 of 34 findings discharged, 5 partial (all
minor residue), 1 not done (F5.6, a minor with no stated push-back), 1 pushed back with a reason
that holds (F6.3's flat head-width loop — the conformance suite still feeds it).

**NOT YET DONE as a leg in its own right under R367(a)** — by eight should-fix items, none of
them a run mechanism, all of them one short commit: a fourth copy of the sparse tail the leg left
in a file it touched (G2.1); two comments/headers the leg made false (G3.1, G3.4); the trainer
still holds two authorities for "this net has an aux head" — the kinds TABLE gates the LAW-18 rows
while the NET's declaration gates the loss, so a mismatch would emit a zero that reads as a measured
head (G4.4); the census's only remaining escape (an exempt row with a reason) is unpinned after the
by-name test was deleted (G5.1); the new LR test drives ≈ 6 000 000 `scheduler.step()` calls in the
DEFAULT tier (G5.2, reasoned); five census consumers reduce paths to basenames and re-join them
under a flat `configs/`, which the census itself does not promise (G4.3); and the prereg's §1a/§4
still describe the periodic tail and cite the deleted test (G7.1), with STATE.md's current phase
still naming `configs/run9.yaml` as minted and the size conditional as open (G7.2).

Shortest path: ONE commit (`fix(...)`: REVIEW-2) over the eight should-fix rows — ≈ 40 lines of
code/tests plus two doc repairs in place — then the exit sweep (`make gates.exit`). No third review
is owed for the minors; the dispatcher may fold them into the same commit or card them.

| REVIEW-1 class | discharged | partial | not done | pushed back | notes |
|---|---|---|---|---|---|
| F1 run-keyed (6) | 5 | 1 (F1.6) | 0 | 0 | the sweep card not opened; the in-range message fixed |
| F2 duplication (4) | 3 | 1 (F2.2) | 0 | 0 | 20 → 16 typed `model.gnn` sites (**measured**); the 8 residue are one line each inside pre-existing whole-config literals |
| F3 comments (5) | 4 | 1 (F3.1) | 0 | 0 | src/tools lines > 200 chars 76 → 67 (**measured**); longest 668 → 335 |
| F4 extensibility (7) | 7 | 0 | 0 | 0 | see G4.4 for the residual seam |
| F5 correctness (7) | 6 | 0 | 1 (F5.6) | 0 | F5.6 minor, no push-back stated |
| F6 bloat (5) | 2 | 2 (F6.4, F6.5) | 0 | 1 (F6.3 half) | net −34 lines over the range; −613 excluding docs |
| **total (34)** | **27** | **5** | **1** | **1** | |

| G class | must-fix | should-fix | minor |
|---|---|---|---|
| G1 run-keyed | 0 | 0 | 2 |
| G2 duplication | 0 | 1 | 1 |
| G3 comments | 0 | 1 | 4 |
| G4 extensibility | 0 | 2 | 1 |
| G5 correctness | 0 | 2 | 3 |
| G6 bloat | 0 | 0 | 1 |
| G7 governance | 0 | 2 | 1 |
| **total** | **0** | **8** | **13** |

Findings: **21** — must-fix 0, should-fix 8, minor 13. Checked-holds rows: 14.

---

# 1. Half A — the 34 findings

Each row: verdict · what closed it (path:symbol at `7b33b38e`) · what remains.

**F1.1 — run-named LR test** · **DISCHARGED** · `tests/train/test_lr_schedule_run10.py` deleted;
`tests/train/test_lr_schedule.py::test_every_cosine_config_reads_its_own_rows_and_never_rises_inside_its_budget`
parametrised over `[p for p in discover_configs(configs) if load_config(p).train.lr_schedule == "cosine"]`
(**measured** 6 configs at HEAD: run6/7/8/10, dev_example, smoke), reads `lr`/`eta_min`/`scheduler_t_max`/
`total_steps`/`max_train_steps` off each config through `TrainHParams.from_config`, asserts the three
closed-form points and monotone non-increasing over `[0, max(max_train_steps, horizon)]`; no `108_000`,
no `1_500`, no run name (**measured**: `grep -c run10 tests/train/test_lr_schedule.py` = 0). The deleted
`test_the_horizon_is_the_mint_row_and_not_total_steps` survives implicitly: `sweep[horizon] == eta_min`
with `horizon = scheduler_t_max` reds if the trainer preferred `total_steps`. Cost: see G5.2.

**F1.2 — by-name production pins** · **DISCHARGED** · `src/mantis/config/census.py::production_configs`
= `discovered_config_paths − exempt_config_paths`, `EXEMPT_CONFIGS` the one by-name list with reasons;
`PRODUCTION_CONFIGS` is gone from `armed_aborts.py` (`__all__` too). Every former pin site reads the
census (**measured**, `git grep production_configs 7b33b38e`): `test_drawrate_schema_range.py:242`,
`test_fused_graph_caps_authority.py:47`, `test_eval_posture_inert.py:105`,
`test_graph_microbatch_authority.py:154,379,413`, `test_gate_interval_decoupling.py:357` (the count
became a non-empty guard), `test_armed_abort_manifest.py:205,571`, `test_monitor_liveness_arming.py:221`,
`test_server_owned_copy.py:198`, `test_preflight_mint_process.py` (12 sites). No `"run6.yaml".*"run7.yaml"`
tuple remains anywhere under src/tests/tools (**measured**: 0 hits). Gate 12 reads it:
`tools/ci_gates/preflight_mint.py:243 _resolve_production_configs` → `census.production_configs(REPO_ROOT)`,
`ConfigCensusError` mapped to `PreflightManifestError` (rc 31), the report's `exempt_configs` from
`census.exempt_config_paths()`. The old `#: Membership is audited BY NAME` comment and
`test_run5_is_bound_BY_NAME…` are gone; the ruling record names the reversal as a class (R367 entry,
"Amends": "the R366 leg's by-name production pins … are the class (a) forbids and become a census") —
F-P2B/N-1 are not named there, which is acceptable (they are pre-R346 archive rows). The deleted
test's SECOND half is the subject of G5.1.

**F1.3 — `_MINTED_ARCH_KIND_ROW`** · **DISCHARGED** · table deleted;
`test_every_shipped_config_selects_the_kind_its_own_row_declares` expects
`ARCH_KINDS[config.identity.arch_kind or INCUMBENT_ARCH_KIND[rep]]` per discovered file;
`test_the_selector_row_is_the_ONE_config_key…` asserts the carriers are exactly the census. The
"an unruled move reds" content now lives in the mint header round-trip
(`tests/config/test_config_diff_from_header.py`, pre-existing, not read here).

**F1.4 — plant stem `"run11"`** · **DISCHARGED** · `tests/tools/test_preflight_mint_process.py:1607`
`_F1_PLANT_STEM = "planted_by_the_rig"`; `test_the_F1_plant_stem_is_on_no_exempt_row_and_on_no_file`
asserts it collides with neither the exempt rows nor any discovered file. The subdirectory test also
plants under the stem instead of `prod/run6.yaml`.

**F1.5 — `configs/run9.yaml` as a fixture** · **DISCHARGED** · `test_gnn_widths_resolver.py` loads
`dev_example.yaml` for the refusal rows and parametrises the re-mint row over the census (`:27,:40`);
`test_aux_soft_policy.py:123` and `test_server_owned_copy.py:196-199` likewise (the latter now loops
every production config). `test_gnn_widths_resolver.py:23`'s own glob is gone; `discover_configs` is
the enumeration (**measured**: `glob("*.yaml")` sites in tests 18 → 14, the four removed are this
leg's). `dev_example.yaml` by name is the exempt template, not a run — acceptable.

**F1.6 — the standing inventory (card) + the in-range message** · **PARTIAL** · the message at
`test_armed_abort_manifest.py:135` no longer says "run5" over a `run6.yaml` load (fixed). No card was
opened for the sweep (**measured** at HEAD: `RUN5 = …/run6.yaml` at 5 sites incl. `_RUN5` in
`tests/eval/test_deploy_matched_hparam_coincidence.py:22`, which REVIEW-1's pattern missed; 12
`run9` synthetic run-ids in `tests/tools/test_analyzer_*`; both pre-existing). Fix: one CARD row
(the class, the count, the rule) · **minor**.

**F2.1 — 14 stub trainers** · **DISCHARGED** · `tests/_drivable.py::DrivableTrainerStub` (46 lines),
`actor_sd = {"w": "ACTOR-SENTINEL"}` ≠ `inference_sd = {"w": "DEPLOY-SENTINEL"}`, `on_step`/`saves`
hooks. **Measured** 15 importers (`git grep -l "from _drivable import" 7b33b38e -- tests`): the 14
files plus `test_heldout_gap.py`; zero private stub trainers with their own `actor_state_dict` remain
in those files. The three subclasses are real behaviour, not copies: `test_run_root_lifecycle._Trainer`
(a REAL `checkpoint_dir` and a `save_checkpoint` returning a real path — the signal-save sidecar leg),
`test_terminal_eval_rc._Trainer` (a real `GnnArch` + net, `resolve_anchor` reads `trainer.arch`),
`test_heldout_gap._Trainer` (a rising `policy_loss` and an `eval_step_from_graph_batch`). R5: no
`sys.path` write anywhere (**measured**: every `sys.path` hit under src/tests/tools is prose or a
census that bans it). Import mechanism, reasoned: `tests/` has no `__init__.py`, pyproject sets no
`--import-mode`, so pytest's default prepend mode inserts each conftest's basedir into `sys.path`
when it loads it; `tests/conftest.py` is an ancestor of every test file and is loaded whenever any
single file is run (confcutdir = rootdir = the repo root), so `tests/` is importable from
`tests/config/` and `tests/train/` alone. `_drivable` is a unique module name (the `conftest`
collision `tests/config/conftest.py:22` warns about does not apply) and the underscore keeps it out
of collection. Holds.

**F2.2 — the `model.gnn` block typed 24 times** · **PARTIAL** · `mantis.model.arch.gnn_widths_block`
exported and used by `checkpoints._SYNTH_ARCH_SCOPED`, `tests/tools/conftest.py`,
`test_ladder_backends.py`, `_microbatch_harness.py`, `_warmstart_config.py`, `test_periodic_checkpoint.py`
(**measured**). Typed sites 20 → 16 (**measured**, the `"gnn": {"hidden"` / `["model"]["gnn"] =` patterns):
the residue is 8 full-config dict literals each carrying one `"model": {"gnn": {…}, "aux_soft_policy": None}`
line (`test_actor_sync_schema.py:128`, `test_eval_schema_bounds.py:114`, `test_schema_strict.py:102`,
`test_train_policy_value_target_consistency.py:103`, `tests/train/conftest.py:217`,
`test_checkpoint_conformance.py:648`, `test_launch_path_smoke.py:147`, `test_resume_wiring_integration.py:129`),
plus foreign-shape literals that ARE the subject (`test_gnn_widths_resolver.py` ×6) and two patches
(`test_arch_stamp_authority.py:43`, `test_resume_owned_paths.py:34`) that could read `gnn_widths_block`.
The 8 are a pre-existing whole-config-literal class (REVIEW-1 §2 said so); hoisting the one line
without the literal is not worth a commit. Fix: the two patch sites → `gnn_widths_block(arch)`; the
literal class → a card · **minor**.

**F2.3 — two `TrainHParams` builders** · **DISCHARGED** · `tests/train/conftest.py::make_full_train_hparams`
delegates to `_microbatch_harness.graph_hparams`; the two 12-key defaults were byte-identical at base
(**verified** side by side), so no test's hparams moved.

**F2.4 — `test_heldout_gap._Pool`/`_Trainer`** · **DISCHARGED** · `tests/train/_coordinator_pool.py::CoordinatorPoolStub`
(flags optional, `search_kind` kwarg) shared by `test_ply_cap_gate.py` and `test_heldout_gap.py`; the
ply-cap file dropped under the cap (316 → 271 **measured**) and its R8 header went with it.

**F3.1 — 23 lines > 200 chars** · **PARTIAL** · src+tools lines > 200 chars 76 → 67 (**measured**
tree-wide); the leg's own added src/tools lines over 200: 14 (**measured**), longest `losses.py`
`soft_policy_target` 335 (was 668); the measurement narrative moved to the prereg cite. Still folded
paragraphs at 14 sites; the ratchet direction held (`comment_length_floor.txt` 3403 → 3386 /
13105 → 13083 / 1506 → 1489, all down-only, three commits). Residue is the same class at half the
size · **minor** (see also G3.3 — the leg ADDED four in tests).

**F3.2 — comments restating code** · **DISCHARGED** · all nine sites deleted, verified in the
diffs: `run.py` (the DEPLOY-net and LEARNER-weights lines), `checkpoints.py:349`, `coordinator/step.py:191`,
`trainer/core.py:184,522`, `losses.py` section labels, `pool.py:110-111`, `orchestrator.py:149-150`.

**F3.3 — transcribed `:NNN` cites** · **DISCHARGED** · `test_armed_abort_manifest.py:108,119` no
longer carry `:200` / `:203`.

**F3.4 — stale docstrings** · **DISCHARGED** · `build.py:35` "not a kind this build has";
`dispatch.py:324` names both consumers; the selector test says "more than one kind".

**F3.5 — the `actor_lag` note** · **DISCHARGED** · `armed_aborts.py:500-503` is the mechanism in
three lines, no run names, the history pointed at the register.

**F4.1 — `SOFT_POLICY_ARCH_KINDS` twice** · **DISCHARGED** · defined once at
`src/mantis/config/schema/core.py:35`; `mantis.model.arch` no longer defines or exports it; readers
are the schema validator (`core.py:480`) and `trainer/core.py:187`; the equality test is gone.
(But the comment above the one table is false — G3.1.)

**F4.2 — `file_sha256` copy** · **DISCHARGED** · `heldout.py` imports `mantis.util.hashing.sha256_file`;
`probe1/swa.py` too (its two `hashlib.sha256(read_bytes())` sites also folded in); both consumer
registries' `ring_sha256` rows re-worded.

**F4.3 — the sparse tail ×3** · **DISCHARGED** · `losses.sparse_tail` + `rebuild_sparse_target` are
the ONE construction; `segment_ids`/`segment_sum`/`segment_softmax` live in `selfplay.graph_collate`
(the pre-existing `segment_softmax` now built on the two new helpers) and `losses` imports them
(`train → selfplay` is a design edge that pre-existed at `trainer/core.py:39`, `dispatch.py:171`).
**Measured** at HEAD: `repeat_interleave` ×1 (`graph_collate.segment_ids`), `scatter_add_` ×1
(`graph_collate.segment_sum`) across the three files; `_hard_target`/`_segment_probs` deleted; the
trainer's KL row calls `rebuild_sparse_target` over the same `prior` the soft target uses. Numeric
path of the CE read before/after: `policy_logits.to(float32)` first (unchanged), the same
scatter_reduce/exp/scatter_add/divide in the same dtype, `policy_target.reshape(-1)` a no-op on the
flat `[Lg]` row, `(1 - mask)` == `(~mask.bool()).float()` on a 0/1 mask, `output_size=` a no-sync
hint only — **unchanged**. `test_sparse_gumbel_row_target.py` keeps its own hand-rolled reference
(deliberately, its docstring says why). One copy the leg missed in a file it touched — G2.1.

**F4.4 — two `model.gnn` parsers** · **DISCHARGED** · `arch.declared_gnn_widths` wraps
`config.resolve.gnn_widths.resolve_gnn_widths` (returns `{}` only with no `model` key, re-raises
`MissingGnnWidthsError` as `RepresentationMismatch`); `orchestrator.py` still calls the resolver once
for its log line and `arch_from_spec_and_config` parses again through the wrapper — two calls, one
implementation, which is what the standard asks. The new `ArchScopedKeyOutsideItsArchError` arm is
named in the docstring; `refuse_outside_its_arch` is a no-op when no representation is declared, so
the identity-less stamp path (F5.7) is safe.

**F4.5 — deploy readers** · **DISCHARGED** · `anchor._build_anchor_model:289` and
`tools/analyzer/engines.py:164` read `deploy_state(ck)[0]`; `bench_server.py:155`, `probe1/nets.py:46`,
`probe1/swa.py:37` state in one line that they read the LEARNER; `warmstart.apply_bc_warm_start:121`
reads `deploy_state` for both the identity hash and the transfer, and
`test_server_owned_copy.py::test_every_deploy_reader_rebuilds_the_shadow_from_an_ema_stamp` drives
anchor + warm start against an EMA stamp (the learner's hash is REFUSED, the shadow's lands). The
run10 mint's hash: see G5.8 (holds). One pre-existing straggler outside the range — G5.6.

**F4.6 — the trainer's kind branches** · **DISCHARGED** (with a residual seam, G4.4) · every net
exposes `forward_batch_heads` (`gnn.py:159`, returns `None` in the aux slot) and `policy_heads()`
(`gnn.py:174` one name, `gnn_v2.py:240` two); the trainer calls ONE method (`core.py:394`), takes the
aux term on `aux_logits is not None and hp.aux_soft_policy is not None` (`:418`), and iterates
`base.policy_heads()` for grad norms, computed when `len(...) > 1` (`:456`). `self.soft_policy` (the
TABLE) survives at two sites: the construction-time config↔arch pairing refusal (`:187-195`, a
legitimate cross-check) and `_aux_soft_policy_block:561`. The split is defensible for the refusal;
it is the same branch under another name at the rows site — G4.4.

**F4.7 — `.arch` planted on the EMA view** · **DISCHARGED** · `EmaModel.arch = getattr(base, "arch", None)`
at construction (`ema.py:48`), `_EmaModuleView.arch = owner.arch` (`:90`); `deploy_module` is one
expression. Reading the planted handle is not the banned structural sniff (`tests/model/test_arch_ban.py`
patterns are isinstance/type-name/hasattr-attr/dense-default — none match); a module without the
handle yields `None`, which `eval/snapshot.py:51-54` refuses by name. Mechanism differs from the
suggested kwarg; holds.

**F5.1 — the STOP LAW was prose** · **DISCHARGED** · `src/mantis/train/lr_schedule.py::FlooredCosineAnnealingLR`
overrides `get_lr` (the recursive path the trainer's `scheduler.step()` takes — `core.py:467`, no
epoch argument anywhere in `src/`, **measured**) and `_get_closed_form_lr` (the `step(epoch)` path),
both returning `eta_min` for `last_epoch >= T_max`; no new state attribute, so the parent's
`state_dict` shape restores unchanged (`test_the_floor_survives_a_resume_from_the_parents_state`
drives a parent-written state past T_max into the floored class). Torch's own recursive form gives
exactly `eta_min` AT `T_max` (cos π = −1) and the restart arm fires at `T_max + 1` — the floor
overrides precisely there; verified by reading torch's `CosineAnnealingLR.get_lr` arms. The trainer's
ONE scheduler construction (`core.py:260`) is the floored class; **measured**: no other
`lr_scheduler` construction in `src/` or `tools/` (the pretrain CLI's "CosineAnnealingLR" is a help
string over the same `eta_min` row). A resume rebuilds through `_build_scheduler` then loads state,
so no path re-admits the periodic tail. Every cosine config is swept over its whole budget
(**measured**: `max_train_steps` 1 000 000 on five of six, 200 on the smoke). Caveat, pre-existing
torch behaviour not introduced here: after the floor a T-CK-17 re-horizon cannot lift the LR,
because the recursive form multiplies `(lr − eta_min) = 0` — identical to stock torch at `T_max`.

**F5.2 — actor wiring unpinned** · **DISCHARGED** · `tests/test_actor_sync_composition.py:133-136`
drives the REAL `compose_run` with `DrivableTrainerStub` and asserts every payload `is trainer.actor_sd`
and none `is trainer.inference_sd`; `run.py:686` is `state_dict_fn=trainer.actor_state_dict`. A revert
to `inference_state_dict` makes every payload the DEPLOY sentinel → both assertions red. The
`test_run_composition.py` sibling row (`sd is trainer.actor_sd`) reds the same way.

**F5.3 — the model-less shim** · **DISCHARGED** · `TrainHParams.from_config:120` calls
`resolve_aux_soft_policy(cfg)` unconditionally; the resolver refuses an absent key by name. Test
mappings without a `model` block now fail loudly — the exit sweep is the check.

**F5.4 — `WorkerPool(arch=None)` served the learner** · **DISCHARGED** · `pool.served_copy:81-82`
deep-copies the base module when `arch is None`; `WorkerPool.__init__:113` calls it unconditionally;
`test_a_pool_with_no_declared_arch_still_never_serves_the_module_it_was_handed` mutates the served
net and proves the learner's hash unmoved. **Measured**: no `pool.model is model` / `is pool.model`
identity reliance remains (the two hits are the new negative assertions). A non-Module `model` at a
test site deep-copies fine (`object()` is copyable); a Mock would be the one shape that could bite,
and the exit sweep would say so.

**F5.5 — three engine imports** · **DISCHARGED** · `run.py:31` one top-level
`from mantis._engine import HexgBuffer, derived_hexg_visit_capacity`; both lazy sites and their
comments gone (**measured**: one `_engine` line in `run.py`); `repo_design.md:137-139` amended in the
same range with the reason. The DAG gate models Python modules only, so `_engine` is not an edge it
scores.

**F5.6 — the frozen slice at the training ring's capacity** · **NOT DONE** · `HeldoutSlice.open`
still takes `capacity` (`heldout.py:29`) and `run.py` still passes the coordinator's; no commit
message pushes it back. REVIEW-1 rated it minor and said why (virtual, not free). Stays **minor**;
the shortest fix is the one REVIEW-1 wrote (read the HEXG header's `size`).

**F5.7 — width check behind the identity early return** · **DISCHARGED** · `_refuse_identity_drift`
computes the identity rows only when both blocks exist and ALWAYS reaches `_width_drift` when no
identity row drifted; `test_the_shape_refusal_does_not_wait_on_an_identity_block` drives a
`{}` baked config. Behavioural note in §3 (a double drift now names one class).

**F6.1** · **DISCHARGED** (F4.2). **F6.2** · **DISCHARGED** (F4.3; `_segment_probs`, `_hard_target`
and the hand-rolled scatter gone — `core.py` 82 lines changed, net −24). **F6.3** · `_width_drift`
written once and read by both refusals — **DISCHARGED**; `_GRAPH_CONFIG_KEYS` loop — **PUSHED BACK**,
commit 548343f6 says "the conformance suite still feeds it"; **verified**:
`test_arch_selector_makes_v2_selectable.py:61 _HEAD_WIDTHS = {"gnn_policy_hidden": 8, "gnn_value_hidden": 8}`
feeds the flat keys, so the reason holds. **F6.4** · **PARTIAL** — stubs and heldout done; widths
sites 20 → 16; `test_lr_schedule.py` is 77 lines against the deleted 58 (it now carries the class
tests and the resume test, so this is not bloat). **F6.5** · **PARTIAL** — the note reduced; 14 folded
docstrings remain over 200 chars (F3.1).

---

# 2. Half B — the fix leg's own findings

## G1 — run-keyed strings the leg added

**G1.1** · `tools/ci_gates/check_tracked_refs.py:40-41` · `DISSOLVED_PATHS["configs/run9.yaml"]` ·
R367(a) "no runN in a … tool" vs its own exempt-set carve-out · This is a dissolved-path exemption
with grounds (gate 10's existing mechanism, self-expiring if the path is tracked again) and it is
NEEDED — `docs/governance/STATE.md` cites the path three times (**measured**) and RULINGS is
scan-exempt; the class R367(a) forbids is a pin edited per mint, which this is not. What is wrong is
the reason string's "STATE's items 16-18" — a transcribed item-number cite of the derive-or-delete
class (G-DFIX-4) · Fix: "STATE names it as history" · **minor**.

**G1.2** · the 15 remaining `run9|run10|run11` hits under src/tests/tools (**measured**, 40 at base)
are all pre-existing (12 analyzer synthetic run-ids, `test_every_key_has_consumer.py:68` a consumer
description, `test_checkpoint_conformance.py:216` prose) plus G1.1; the leg added no other. Holds —
the card is F1.6's · **minor** (same row).

## G2 — duplication the leg added or left on contact

**G2.1** · `tools/probe1/nets.py:81-96` · `_per_graph_sum`, the `seg = repeat_interleave(...)`
line and the `tail_prior / tail_denom / scale / t = policy_target + tail_prior * scale[seg]`
block · R367(a) one implementation per thing; the leg edited this file (3d93447c renamed
`_segment_softmax` → `segment_softmax` at `:21,:93`) and left the fourth copy of the sparse tail
beside it · Fix: `t = rebuild_sparse_target(inputs.policy_target, p, inputs.explicit_mask, inputs.tail_mass, lo)`,
`seg = segment_ids(lo)`, `_per_graph_sum` → `segment_sum`; the probe's WHOLE-SET `soft` construction
(`torch.exp(logt / T) * (t > 0)`) stays — it is the artefact form PROBE-1 measured and must keep
reading it · **should-fix**.

**G2.2** · `tests/config/test_config_census.py:23,29,43` · `exempt = tuple(Path(rel).relative_to("configs").as_posix() for rel in census.exempt_config_paths())`
typed three times · Fix: one `_exempt_names()` helper · **minor**.

## G3 — comments the leg added or made false

**G3.1** · `src/mantis/config/schema/core.py:33-34` · `#: … \`mantis.model.arch\` imports this, the
one table.` · R316(e) "a comment states what code cannot" — and this one states a falsehood:
**measured** readers of `SOFT_POLICY_ARCH_KINDS` at HEAD are `schema/core.py:480,491`,
`trainer/core.py:28,187`, `schema/__init__.py` and one test; `mantis.model` does not import it.
`arch.py:6-8`'s module docstring ("the `model.gnn` parser and the soft-policy kinds table are the
config package's (the `model -> config` edge …)") implies the same read · Fix: "the schema validator
and the trainer's pairing refusal read it" · **minor**.

**G3.2** · `src/mantis/model/gnn_v2.py:243` · `def forward_batch_heads(  # type: ignore[override] — the aux slot is a Tensor here, never None`
over a return annotation the same commit widened to `tuple[Tensor, Tensor, Tensor, Tensor | None]`
(`:253`) · the ignore is dead (the signature now matches the base) and its comment contradicts the
annotation beneath it · Fix: narrow the return to `tuple[Tensor, Tensor, Tensor, Tensor]` (a
covariant override needs no ignore) and drop the comment · **minor**.

**G3.3** · four NEW one-line docstrings over 200 chars in tests (**measured**, added lines):
`tests/_drivable.py:8` (286), `tests/train/test_lr_schedule.py:1` (252), `tests/test_run_root_lifecycle.py`
`_Trainer` (249), `tests/train/_coordinator_pool.py:1` (232); tests lines > 200 chars 41 → 44
tree-wide · the F3.1 class re-added by the leg that was trimming it (a paragraph folded so
`docstring_excess_lines` does not move) · Fix: each to its invariant in one line · **minor**.

**G3.4** · `tests/test_run_strict_composition.py:8-9` · the R8 header ">300 justify (R8): splitting
the behavioural half out would fork a fourth copy of the drivable pool/trainer/buffer fakes,
cross-test imports being barred." · R8: a justification "states a reason"; this reason is now false
on both halves — the trainer fake is `from _drivable import DrivableTrainerStub` (`:14`, this very
file) and cross-test imports are evidently not barred. A stale justification is the misinformation
gate 15 exists to stop (SF-7) · Fix: reword to the reason that still holds (the pool/buffer fakes
and the AST census share one harness) · **should-fix**.

**G3.5** · `tests/config/test_drawrate_schema_range.py:239-241` · `# … The pin is on the MINTED
file, … gate 12 audits it by name.` over a loop the leg re-pointed at the census · stale rule text
· Fix: "gate 12 audits every census member" · **minor**.

## G4 — extensibility of what the leg built

**G4.1 — `census.py` is reusable: checked, holds.** Pure functions parametrised by `repo_root`, the
exempt set as data with reasons, both failure modes named (`STALE`, `EMPTY`), zero filesystem calls
at import (`armed_aborts.py` keeps its layer boundary), consumed identically by gate 12 and 14 test
files. Nits: `production_configs`'s docstring says "sorted absolute paths" — true only when
`repo_root` is absolute; sort is lexicographic (`run10` < `run6`), harmless.

**G4.2 — `FlooredCosineAnnealingLR` is the right seam: checked, holds.** A subclass over both LR
paths keeps the parent's state format (a `LambdaLR` would have broken T-CK-17's restore of every
existing stamp), one consumer, 19 lines. Caveat: `_get_closed_form_lr` is a torch-private name; the
trainer never takes that path, so a rename would cost nothing today · no fix owed · **minor** note.

**G4.3** · five census consumers reduce the census to basenames and re-join under a FLAT
`configs/`: `test_drawrate_schema_range.py:242`, `test_fused_graph_caps_authority.py:47→60,71,107,136`,
`test_eval_posture_inert.py:105→141`, `test_graph_microbatch_authority.py:154→170,218,228`,
`test_armed_abort_manifest.py:571-572` (`production_configs(...)` → `.name` → `REPO_ROOT / "configs" / name`)
· the census the leg built discovers nested configs and its own tests prove it
(`test_config_census.py:25` `nested/c.yml`; gate 12's `prod/<stem>.yaml` rows) — so the leg's
consumers do not honour the leg's producer. Today `configs/` is flat, so it is latent, and the
failure mode is a loud `FileNotFoundError`, not a silent pass. It is also the same idiom typed five
ways (a G2 row in disguise) · Fix: parametrise over the `Path` (`ids=[p.name …]`) and load the path
· **should-fix** (small, mechanical).

**G4.4** · `src/mantis/train/trainer/core.py:187` (`self.soft_policy = type(self.arch).__name__ in SOFT_POLICY_ARCH_KINDS`)
against `:394,:418,:456` (the NET's `forward_batch_heads` / `policy_heads()` declaration) and
`:561` (`_aux_soft_policy_block` gated on `self.soft_policy`) · the seam contract F4.6 was meant to
close: "this net has an aux head" now has TWO authorities — the config-side kinds table and the
net-side declaration — and nothing pins them equal. The loss and the grad norms follow the net; the
LAW-18 rows follow the table. A table kind whose net lacked the override (a third head added on one
side only) would train NO aux term (`aux_logits is None`, `aux_total` stays 0.0 at `:383/:446`) and
still emit `aux_soft_policy_loss: 0.0` — "a zero that reads as a measured head", the exact hazard
`_aux_soft_policy_block`'s own docstring forbids (LAW-18 / LAW-07 class). Unreachable at HEAD (one
kind, correctly overriding) but it is the kind of split REVIEW-1's F4.6 asked the leg not to leave ·
Fix (one line + one test): at construction, refuse by name when
`self.soft_policy != (len(self._base_model().policy_heads()) > 1)`, so the table is a cross-check
and the net the authority; then `_aux_soft_policy_block` can drop its `not self.soft_policy or`
conjunct (redundant under the constructor invariant either way) · **should-fix**.

## G5 — correctness

**G5.1** · `src/mantis/config/census.py:14-36` `EXEMPT_CONFIGS` and `tests/config/test_config_census.py`
· the deleted `test_run5_is_bound_BY_NAME_and_is_not_freely_exemptable` had two halves: the by-name
binding (reversed by R367(a), correctly deleted) and a DRIVEN escape — "move a production config to
EXEMPT with a written reason: every structural check stays green while the run ships unaudited".
Under the census that swap is the ONLY remaining way to un-audit a production config, and at HEAD
nothing reds on it: the census tests check the exempt rows exist on disk and carry a non-empty
reason, never WHICH rows they are · this is protective content that vanished, not moved · Fix:
`test_config_census.py` pins `census.exempt_config_paths() == frozenset({"configs/dev_example.yaml", "configs/smoke_preflight_armed.yaml"})`
with the message "an exemption is a ruling event; move this pin with the ruling cited". This is
consistent with R367(a): the exempt set IS the one by-name list the census clause permits and it is
not edited per mint · **should-fix**.

**G5.2** · `tests/train/test_lr_schedule.py:56-77` · the per-config sweep runs
`_lr_sweep(trainer.scheduler, max(budget, horizon))` = 1 000 000 `scheduler.step()` calls for each
of the six cosine configs (**measured**: `total_steps` and `max_train_steps` are 1 000 000 on five;
the smoke's budget is 200 but its horizon is 1 000 000, so `max(...)` sweeps 1 000 000 there too) —
≈ 6 000 000 torch scheduler steps in the DEFAULT tier, each a Python-level `get_lr` with two `cos`
and a param-group write; reasoned ≈ 5–10 µs each → ≈ 30–60 s per default-tier run (NOT measured;
the default tier is ≈ 5 min, R330(g)). A `slow` marker is the wrong fix (R333(b): deselected from
both tiers) · Fix: keep the 200-step recursive sweep in the class test (it is the planted break);
per config, assert the three closed-form points and the floor at `budget` by stepping to
`horizon` only where `horizon ≤ 200` and otherwise reading `get_lr` at the sampled `last_epoch`
values (set `last_epoch` directly, the closed form is exact), so the config test costs milliseconds
· **should-fix** (tier cost). Also `:77` `assert sweep[budget] == pytest.approx(hp.eta_min if budget >= horizon else sweep[budget])`
compares a value with itself when `budget < horizon` (the smoke) — a tautological arm · **minor**.

**G5.3** · `tests/train/test_lr_schedule.py:64` · `kind(in_dim=11, edge_dim=5, …)` types the
encoding's feature dims where every sibling test reads `lookup(encoding).node_feat_dim/edge_feat_dim`
· a transcribed constant that goes stale with the registry · Fix: read them off `lookup(config.identity.encoding)`
· **minor**.

**G5.4 — `_refuse_identity_drift`: checked, holds (with a note).** `checkpoints.py:1141-1143`:
widths are compared only when NO identity leaf drifted (`if not drift:`), with the stated reason
that a moved representation would make `resolve_gnn_widths` raise `ArchScopedKeyOutsideItsArchError`
instead of the identity error — true (`refuse_outside_its_arch` runs first). Consequence: a launch
that moves both `identity.arch_kind` and `model.gnn` is refused naming only the identity rows (base
named both). Still a refusal, still by name; no test asserts the double message · no fix owed.

**G5.5 — gate 3c (collected count ≥ floor): not derivable here.** The range deletes 14 `def test_`
and adds 19 (**measured**), but `configs/run9.yaml`'s deletion removes one case from every
all-configs parametrisation (14 `glob("*.yaml")` sites + the census sites) and the census itself
shrinks 5 → 4 members; `tools/ci_gates/test_count_floor.txt` (4862) is untouched in the range. The
exit sweep decides; if it reds, the floor is lowered with the deletion cited, not the tests widened.

**G5.6** · `src/mantis/diagnostics/acceptance_witness.py:237` · `load_checkpoint(arm.checkpoint, …).model_state`
loaded into an arm's net · the F4.5 class ("deploy_state is the one deploy reader") REVIEW-1 missed;
outside the range, pre-existing; the BC acceptance arms are pretrain stamps (EMA off), so inert
today · Fix: `deploy_state(ck)[0]`, one line, or a card row beside F1.6's · **minor**.

**G5.7 — EMA `arch` handle: checked, holds.** See F4.7.

**G5.8 — the warm-start reader change vs the run10 mint: checked, holds, conditionally.**
`warmstart.py:121` now takes the parent's DEPLOY state; run10's `identity.warm_start` is
`checkpoints/run8/run8_00045000_3bdedf76.ckpt` at `net_hash 3aef7883…` (`configs/run10.yaml:56-58`).
`configs/run8.yaml:104-107` has `train.ema.enabled: false` (**measured** at HEAD), so a stamp written
by run8 carries no `ema_state`, `deploy_state(ck)` returns `(model_state, "learner")`, and the hash
is over the same tensors as before the change. `tools/strength_frontier.py:73` reads the same
`deploy_state`, so the frontier record the hash was typed from and the warm start's check are over
the same net by construction — for an EMA-ON parent both would move together, which is the rule the
comment at `warmstart.py:119-120` states. What this review cannot verify: the stamp itself (no
checkpoint in the tree) — the box preflight's `WarmStartIdentityError` is the live check, and it
fires by name.

## G6 — bloat

**G6.1** · `tests/selfplay/test_graph_collate_parity.py:10-13,138-157` · `_normalized_body`
(20 lines) plus `import ast`, `import inspect`, `import textwrap`, `Callable` — dead since the
identity pin replaced the AST-equality legs (**measured**: no remaining caller) · Fix: delete ·
**minor** (≈ 24 lines).

**G6.2 — stat: no other addition is net bloat.** Over the range **measured** `+1 516 / −1 550`;
excluding docs (+637/−12) the code+tests+configs net is −613. The 69-line `census.py` replaces 41
lines in `armed_aborts.py` plus 40 in `preflight_mint.py`; `_drivable.py` (46) + `_coordinator_pool.py`
(48) replace ≈ 430 lines of stubs (tests +552/−984); `lr_schedule.py` (19) is the mechanism F5.1
demanded.

## G7 — governance text

**G7.1** · `docs/design/measurements/RUN10_PREREG_2026-09-21.md:43` (the `scheduler_t_max` row) and
`:166-167` (the STOP LAW) · the row still says "Pinned through the trainer's own scheduler:
`tests/train/test_lr_schedule_run10.py` reads … DISCLOSED: torch's `CosineAnnealingLR` is PERIODIC
past T_max — … the LR climbs by < 1e-6 (pinned), and at 2 × T_max it is back at the peak; the STOP
LAW (§4) is the guard, not the scheduler", and §4 says "The LR's periodic tail (§1a) makes the
108 000-step point a hard edge for the trainer too"; `:176` says "`configs/run10.yaml` in
`PRODUCTION_CONFIGS`" · at HEAD the test is `test_lr_schedule.py`, the scheduler is floored, the LR
never climbs, and `PRODUCTION_CONFIGS` does not exist — the leg changed the mechanism the run's
pre-registration discloses and edited this same file in the same range (§2, §6) without repairing
these lines · R9 / R311(c): a working doc that disagrees with verified repo state is repaired in
place by whoever finds it, and the STOP LAW's stated second guard is operationally load-bearing for
the box session · Fix: three in-place repairs, one line each, dated · **should-fix**.

**G7.2** · `docs/governance/STATE.md:10` (Current phase) and `:583` · untouched in the range
(**measured**: no diff); still says "the size conditional 6×192 … waits on the operator's parent
ruling (CARD-RUN10-SIZE-PARENT)" (closed by R367(c)), "`configs/run9.yaml` stays as minted" (deleted
by R367 §0(4)), and no item 19 for R367 / REVIEW-1 / the fix leg · CLAUDE.md's map: STATE.md is
"repaired in place by whoever finds it stale"; the leg's own `check_tracked_refs.py` row cites
STATE's items as the reason the deleted path is history, while STATE's head still calls it minted ·
Fix: item 19 (R367: the standard, the withdrawal, REVIEW-1 landed, the fix leg's eight commits, run10
mint green pending the exit sweep) and the two stale clauses struck in place · **should-fix**.

**G7.3 — the register and CLAUDE.md: checked, holds (with one note).** The R367 entry's quoted
packet text is internally consistent with its own header line and its §0 enactment list (six
items, each traceable to a change in the range: A1 annotation present under R365's foot; CARD-NET-EXPAND
opened and CARD-RUN10-SIZE-PARENT closed into it; prereg §2/§6 repaired; run9.yaml deleted with its
pins; REVIEW-1 landed at the named path; R366's Status amended). "Next R368", the header count
line, and the R366 Status line agree. CLAUDE.md rule 10 states R367(a)/(b) without adding a clause;
rule 11 states (d); the gate-12 line names the census module. Note: rule 10's Reason says "seven
by-name pin sets" — the packet's figure, cited as such; REVIEW-1 §1 measured 8 pin lines in 7 files
plus a count plus the tuple. Seven FILES is defensible; no edit owed · **minor**.

---

# 3. Checked, holds

1. **Every REVIEW-1 must-fix (10) is discharged and verified at HEAD**, not taken from commit
   messages: F1.1–F1.4 (grep + read), F4.2 (grep `file_sha256` = 0 in src), F4.3 (one
   `repeat_interleave`, one `scatter_add_` across losses/core/graph_collate), F5.1 (one scheduler
   construction; both LR paths floored; the resume test), F5.2 (`compose_run` driven, distinct
   sentinels), F5.3 (unconditional resolver), F5.4 (`served_copy` deep-copies on `None`).
2. **The CE's numeric path is unchanged** (F4.3): same float32 cast first, same ops, same dtypes,
   `reshape(-1)` a no-op on the flat row, `output_size=` a hint.
3. **The kinds-table consolidation keeps the DAG one-way**: `mantis.config` imports nothing from
   `mantis.model` (**measured** 0 hits); `model → config` has two edges (`amp.py:19`, `arch.py:17`),
   both on the design's `model → encoding, config` row.
4. **`train → selfplay`** (`losses.py:14`) pre-existed at `trainer/core.py:39` and `dispatch.py:171`
   and is on the design's `train → all above except eval` row.
5. **R8 headers**: every touched file over 300 lines carries a justification (**measured**, 34
   files); no touched file at or under the cap carries a marker — `losses.py` (293) and
   `test_ply_cap_gate.py` (271) dropped theirs correctly. G3.4 is a header whose REASON went stale,
   not a size claim.
6. **The comment ratchet moved down only** (3403 → 3386, 13105 → 13083, 1506 → 1489, `banner 0`,
   `rust 2676` unchanged), three commits, each lowering in the same commit as its measure.
7. **`_drivable` imports are R5-clean** and work file-alone (the prepend-mode mechanism, §1 F2.1).
8. **The three `DrivableTrainerStub` subclasses are real** (§1 F2.1).
9. **`make_full_train_hparams` → `graph_hparams`**: the two default dicts were byte-identical.
10. **`test_train_reads_the_selfplay_segment_softmax_and_never_a_copy`** replaces AST equality + a
    mutation self-test with object identity (`is`), a strictly stronger invariant that needs no
    self-test; the numeric battery is kept.
11. **The deleted preflight tests' protective content moved, with one exception (G5.1)**:
    "undeclared → rc 31" became "undeclared → production → audited → rc 30 when disarmed" at every
    suffix and depth; "IN BOTH TUPLES" is impossible by construction; the STALE arm survives as
    `ConfigCensusError("STALE")` → rc 31; the vacuity arm as `("EMPTY")` → rc 31, driven through
    `TOOL.REPO_ROOT`.
12. **`_refuse_identity_drift`** (G5.4) and **`EmaModel.arch`** (G5.7) — see the rows.
13. **The warm-start reader vs the run10 mint** — holds for an EMA-off parent (G5.8).
14. **The governance edits** (G7.3) — the entry, the annotation, the cards, rule 10/11 and the
    repo_design amendment state the rule without inventing a fact.

---

# 4. What this review did not read

- Nothing was executed: no tier, no gate, no bench, no `pytest --collect-only` (gate 3c's count,
  G5.5, is the exit sweep's); the LR test's wall (G5.2) and the `_drivable` import (F2.1) are
  reasoned from pytest's documented prepend mode, not observed.
- The REVIEW-1 report was read as landed; its verbatim-ness against the review agent's original
  file was not checkable from here.
- The R367 packet text was checked only for internal consistency (header ↔ quoted block ↔ §0
  list ↔ the range); the operator's original packet was not available.
- `tests/config/test_config_diff_from_header.py` (the mint-header round-trip F1.3 now leans on),
  `tools/ladder/backends.py`, `tools/strix_follower.py` and the dashboard readers were grepped for
  `deploy_state`/`model_state`, not read.
- Rust: no file in range; not read.
- `tests/tools/test_preflight_mint_process.py` was read as a diff (305 lines) plus the census
  sites, not the whole 2 985-line file; `test_arch_ban.py` only for its pattern set.
- The run8@45k stamp itself (G5.8) is not in the tree; the reasoning rests on `configs/run8.yaml`'s
  EMA row and `deploy_state`'s `ema_state is None` arm.
