# REVIEW-W2 — SLIM-FIX wave 2 (96f34181..3db6ab5a)

Filed verbatim from the fresh read-only review agent (R367(b)); §5 is the dispatcher's disposition.

## §1 Scope and method

Range: 35 commits, 133 files (+1174/−4921 overall; code/tests/tools +1106/−4905). Read R368/R367, CLAUDE.md,
PROGRESS W2, 00_MAP §3 (PZ-1..PZ-6), REVIEW_W1, the census rows S-A-RUST-1/2/3 and LEDGER; read every code
commit's full diff; docs-only commits b77fc06d/2d23c2bd/3db6ab5a read.

Symbol checks: every deleted symbol grepped over `src tools tests crates benches docs` (excl. docs/slim and
archives) — remaining hits are only the still-used search-crate internals (`last_search_stats`,
`run_simulations_cpu_only`, `MCTSTree::omitted_prior_stats`, `root_raw_value`), core `threat_moves` /
`terminal_value_to_move` (used by the tactics solver), and core `set_legal_move_radius` (test/bench only now).

Run10 safety: `git diff --stat 96f34181..3db6ab5a -- configs/ tools/config_templates src/mantis/config` → empty.
Golden/parity files byte-unchanged over the range: mantis-search `mcts/golden_tests.rs`, `mcts/parity_tests.rs`,
`tests/{mctx_parity,temperature_parity_golden,search_kind_conformance}.rs`, `seq_halving.rs`, `completed_q.rs`;
`mantis-graph/tests/graph_parity.rs`; `mantis-core/tests/golden_replay.rs`; `tests/fixtures/**`. Changed only by
helper hoists (read line by line): `graph_child_parity.rs`, `target_export_parity.rs`, `worker_output_pin.rs`,
`served_sims_exact.rs`; `queue_fuse_pin.rs` lost two `is_available` asserts; `replay_hexg.rs` lost two tests;
`r153_leg2` rewritten. `replay_persist.md`: only the O-31 note changed; the HEXG record layout and the `weights`
column are unchanged.

Floors: collected in a scratch worktree (`.wt/review-w2`, main venv, `PYTHONPATH=<wt>/src`, `pytest
--collect-only -m ''`) at the base and every commit touching Python tests: 96f34181=5153, 9f790c4d=5149,
941f84f5=5144, 5b04be57=5149, d54e9eb4=5144, 43354f1c=5147, ed1df3e5=5147, 3db6ab5a=5147 — each equals
`test_count_floor.txt` at that commit, each move in its causing commit. `comment_length_floor.txt` falls
monotonically across all 35 commits; `comment_lint.py --measure` equals the floor exactly at the six commits that
left it untouched and at HEAD (3229/0/13024/1484/2487); the ungated `ruling_cite_comment_lines` 389 → 386. Net lines
≤ 0 on every slimming commit; the permitted exceptions are 43354f1c (+160, wiring) and 9c08555b (+7, unwrap fix);
6400b0dd is 0.

Rust (main checkout): `cargo test --locked` passed for mantis-core, mantis-encoding and mantis-graph (graph_parity
5/5, fixture_selftest 5/5, golden_replay 2/2). mantis-search full: lib 114 (goldens and parity included),
search_kind_conformance 11, mctx_parity 3, temperature_parity_golden 1, r153_leg2 1 (685 s debug),
gumbel_sparse_row_kl, r8_game_target_mass, dirichlet_parity. mantis-selfplay full: 35 binaries, 0 failures, no
rustc warnings. `mantis-bridge --lib` 45. `cargo clippy --workspace --all-targets -- -D clippy::all` rc 0 with no
unused/dead warnings. `cargo check -p mantis-graph --target wasm32-unknown-unknown` green.

Python: 16 targeted files with `-m ''` → 109 passed (the stub/runtime check, positions_dropped, pool surface, the
LAW-18 counters, event rosters, the pyclass round-trip, the MCTS round-trip, the buffer facade, the bridge-default
census, drain-row shape, cluster wiring, the inference server, target counter events, the manifest contract, the
surface test). `ruff check` on touched .py clean; gates 10/15/16 green. pyright (src + tools,
`--pythonpath .venv/bin/python`) 0 errors at HEAD; its 45 `reportMissingModuleSource` warnings identical at the
base. A scratch pyright probe confirms the stub is read through `stubPath`: `registry_sha_hex` resolves to
`() -> str` and `x: int = registry_sha_hex()` reds. `lint_gate.sh` itself not run (its `uv run` could not lock
the read-only uv cache inside the sandbox).

Planted breaks: 2, in the scratch worktree, reverted (results in §3). Worktree removed; the main checkout has no
tracked changes.

## §2 Findings

| # | sev | commit | file:symbol | finding | evidence (command → result) | suggested fix |
|---|---|---|---|---|---|---|
| 1 | SHOULD-FIX | 43354f1c | docs/contracts/event_manifest.md "Event stream conventions" | The version was bumped to v4 and `sink.EVENT_CONTRACT` reads `"event-manifest-v4"`, but the segment-header convention still states the literal `contract: "event-manifest-v3"`; the `status:` line still opens "v3 —" (W1 note 5's class). | `sed -n 400,403p docs/contracts/event_manifest.md` → `contract: "event-manifest-v3"` beside `(mantis.monitor.sink.EVENT_CONTRACT)` | say v4 or cite the constant without a literal; fix the status line in the same edit |
| 2 | SHOULD-FIX | 0c3ec191..c3231390 (search group) | docs/slim/PROGRESS.md W2 | R368(k): every other lane-C row is re-laned under (b) at its wave's entry. PROGRESS W2 lists the RUST-2/RUST-3 rows only; the search group's lane-C rows (S-A-RUST-1-09 R153 leg 1, ruling-named; -10/-11/-12 on the PZ test glob and a seam member; the other RUST-1 rows landed in 11ccc814/ed1df3e5) have no done/re-laned/kept record, and the resume point stops at "the search agent resumes". | `awk '/^## W2/,/^## Ledger/' docs/slim/PROGRESS.md \| grep -c RUST-1` → 0 | add the RUST-1 disposition to PROGRESS W2 incl. 1-09's re-lane grounds (R155/R157 label leg 1 non-production and direct no file) and that 1-10/1-11 landed partially |
| 3 | SHOULD-FIX | 6c30e948 | crates/mantis-selfplay/src/par.rs::map_in_order (build_leaf_graphs_batch, hexg sample build_and_align_batch) | Merges the thread fan-out around the graph build (leaf graphs on the eval path; replay rebuild on the trainer's `sample_ring`). R368(h) puts hot-path refactors in graph build and collate out of this phase; the leg's own CARD-SEARCH-HOT-DUP defers the search dups on exactly that ground, but this one landed with no bench and no recorded ground that it is outside (h). Behaviour-exact by construction (same chunking, same join-to-named-error, same flatten); hexg_sample_parallel_parity and leaf_graph_parallel_parity green. | diff read; `grep -c . crates/mantis-selfplay/src/par.rs` → 42; CARDS "CARD-SEARCH-HOT-DUP … R368(h)" | record in PROGRESS why the fan-out is not an (h) hot path, or attach a LAW-09 before/after bench of `sample_ring` and `build_leaf_graphs_batch` at n_threads > 1 |
| 4 | NOTE | 1fb31555 | crates/mantis-core/src/board/state/core.rs::Board.cluster_threshold, ::cluster_threshold(), BoardGeometry.cluster_threshold, moves.rs DEFAULT_CLUSTER_THRESHOLD | Its only reader `get_clusters` is deleted, so it is write-only, plumbed from the registry (bridge board.rs, selfplay game.rs) into a value nothing consumes; its doc still reads "two stones share a cluster iff … Default 5 (v6 wire format)". Residue under R367(a). | `grep -rn cluster_threshold crates/mantis-core/src` → set, cloned, getter, test only | card it with the golden_replay fixture field (removal touches BoardGeometry); at minimum correct the doc |
| 5 | NOTE | 3a1d38bf | crates/mantis-selfplay/src/runner/search_drive.rs::select_move | "Gated on ply RELATIVE to game start" survives `game_start_ply`'s deletion; the gate is now absolute (`explore_gate_open(ply, explore_moves)`). | `grep -n "RELATIVE to game start" search_drive.rs` → line 704 | one-line reword on contact |
| 6 | NOTE | 0c3ec191 | src/mantis/eval/worker.py::_assert_policy_pool_implemented | An error message says "the Rust expand_and_backup_ls exists but is not wired"; that function is now deleted. W3 territory (grid arm). | `sed -n 76p src/mantis/eval/worker.py` | fix with the W3 grid-arm deletion |
| 7 | NOTE | 43354f1c | src/mantis/monitor/producer_manifest.yaml (target_integrity note) | New comment text "positions_dropped (R368) is the fifth" — a ruling number in a new comment (R368(g)), not a carve-out marker. | added-line grep for `\bR[0-9]{2,3}\b` → producer_manifest.yaml | drop "(R368)" |
| 8 | NOTE | a1d5776a / 1b5d93a8 | mantis-core `board::zobrist::splitmix64_next` (new `pub` in the production lib); mantis-selfplay `tests/common::splitmix64` | A test-only stream helper joined mantis-core's public production surface, and a second copy remains in selfplay's tests/common: one per crate, not one (R367(a)). Behaviour-exact. | `git grep -n 0x9E37_79B9_7F4A_7C15 -- crates` → core zobrist + selfplay tests/common (+ tactics internal) | gate it behind mantis-core's `test-fixtures` feature and have selfplay's tests import it |
| 9 | NOTE | c543e667 | crates/mantis-selfplay/tests/queue_fuse_reserve_parity.rs (deleted) | R368(k) orders the deletion (no breach), but the census row's "partly unique coverage" (degenerate-array and dst-half offset-walk cases) was neither re-homed into queue_fuse_pin nor recorded as dropped; the shipped fuse is now pinned by the 3-graph frozen input + reconstruction. | S-A-RUST-2.md §2-13 "deliberate?: partly unique coverage"; `grep "fn " queue_fuse_pin.rs` → no degenerate case | record the dropped coverage in PROGRESS, or add one degenerate case to queue_fuse_pin |
| 10 | NOTE | d54e9eb4 | crates/mantis-selfplay/src/replay/schedule.rs::WeightSchedule | After the bracket removal: a one-field struct holding a constant with a zero-arg `weight_for()` — a wrapper around f16 1.0 bits. Behaviour-exact. | diff read | collapse to a `const UNIFORM_WEIGHT_BITS` on contact |
| 11 | NOTE | 0c3ec191 | crates/mantis-search/tests/r153_leg2_ls_target_mass.rs | Now runs aborts 1/3/4 over both encodings through `expand_and_backup_ls_at` — correct and stronger for r8; takes 685 s in a debug `cargo test` (gate 2). | `cargo test -p mantis-search` → `finished in 685.48s` | none; worth knowing when sizing gate 2 |
| 12 | NOTE | 11ccc814 | crates/mantis-encoding/Cargo.toml (`toml` comment) | Still says "registry.toml + manifests.toml parse"; 7c540b22 removed the same stale claim from the bridge. | `grep -n manifests.toml crates/mantis-encoding/Cargo.toml` → line 11 | one-word fix on contact |

No MUST-FIX: no correctness bug, no numerics/stamp/format change, no config change, no protected invariant narrowed
(every deleted test's claimed replacement checked and holds), no LAW-07 break (the new counter's producer test reds
under both planted breaks).

## §3 Per-row-group verdicts

- RUST-3-16 (one `_engine.pyi`) — HELD: the runtime test pins the path (maturin `python-source` + `module-name`)
  and pyright's `stubPath`, and asserts no `_engine.pyi` under `src/`; the pyright probe resolves the stub's types;
  `known-first-party = ["mantis"]` is sound (mantis._engine lives outside src/); gate 14's pyright count 0 errors /
  45 warnings at base and HEAD — the gate means what it did.
- RUST-3-01/02/03/04/05/06/07/09/12 (dead bridge/core exports, cluster BFS, Turn) — HELD: no Python or Rust caller
  remains; repo_design and the CLAUDE.md map amended in the same commit; residue is finding 4.
- RUST-3-08 (encoding plane-slot accessors) — HELD: no callers; registry data and sha unchanged (axis_pin,
  registry_census green).
- RUST-3-10 (CRATE_NAME) — HELD: every retained dep edge is used by src.
- RUST-3-11/17 (graph `parallelism_hint`/`native`; sha2 dev-dep) — HELD: lib stays dep-free, wasm32 green; every
  manifest sha row still checked (fixture_selftest 5/5); the FIPS self-test goes with the hash it tested.
- RUST-3-13 (InferenceBatcher requires `encoding_spec`) — HELD: only InferenceServer's unreachable non-graph arm
  changed call shape; every caller passes a spec; the `graph_params` expects are named startup invariants.
- RUST-3-14/18 (prose; shared bench/test helpers) — HELD (finding 8 is placement only).
- unwrap/expect class (9c08555b, b9415f7f) — HELD: `present()` is a pure `Option`→named-`Err` substitution in the
  same field order; `Cursor::array::<N>` exact for all six widths; `version_range` and the opening draw
  behaviour-exact (`choose` on an empty slice draws no RNG); hot-loop sites and the poison stance carded.
- RUST-2-01..13, 15 (selfplay slimming) — HELD, finding 3 on -15. `GraphQueue::new` is exactly
  `with_contract_version_and_supply(1, 0)`; the dense training queue / `WorkerResultRow` had no pusher and the
  drain-shape test pins `GameResultRow`; the HexgBuffer grid branch and `is_available` removals exact; the shared
  `spawn_uniform_producer` checked against all eleven removed copies (5 ms pop wait, uniform `1/n.max(1)`, value 0.0,
  production `assemble_ls_from_gnn_probs`, each caller's `pop_max` preserved); flat-fixture readers differ only in
  panic text.
- RUST-2-12/-13 (S-PREFUSE harness, pre-A1 oracle) — HELD per R368(k): falsified.md names 69e15329, gate 10
  whitelists the deleted path; coverage note is finding 9.
- REVIEW-W1 note 3 (runner forced-child pre-check) — HELD: `select_leaves_forced` range-checks every round child
  and clears `forced_root_child` before any descent (W1 finding 2's fix present); the new runner-level test
  `a_foreign_round_child_is_a_named_selection_failure` passes; red-ability argued, not planted (with the check
  removed, `u32::MAX` would index the pool out of bounds); only the error stage label changed and nothing pins it.
- REVIEW-W1 note 7 (`feature_len` ctor arm) — HELD (gone with RUST-3-13).
- positions_dropped (RUST-2-02, contract v4) — HELD for the wiring, finding 1 on the doc: planted
  `getattr(r, "positions_dropped", 0)` → `test_a_runner_without_the_getter_is_refused_not_published_as_zero` reds;
  planted removal from `_TARGET_INTEGRITY_COUNTERS` → `test_the_count_reaches_iteration_complete` reds (KeyError);
  the real-runner drive (cap 1, no drain) asserts dropped > 0 and dropped + retained ≤ generated;
  `sink.EVENT_CONTRACT == "event-manifest-v4"` pinned by the roster test against `version: v4`; no rule or gate
  consumes `target_integrity`, so the new key arms nothing.
- Search group (0c3ec191, 11ccc814, 6400b0dd, 55d4db8c, 066b17b7, b1e0bf74, ed1df3e5, c3231390) — HELD, finding 2 on
  the paperwork. R153 leg 1 drove only the non-production dense expand; leg 2 now runs both encodings through the
  production `_ls_at` with aborts 1/3/4 each (R155's production-path clause more strictly than before). The seven
  deleted tests are restated: search_kind_conformance holds the round-trip, the superset refusal list and
  `completed_q_target`; dirichlet_parity covers sum and non-negativity at n ∈ {1, 2, 5, 24, 50}; the temperature
  golden CSV has thr=15/tmin=0.05 rows at ply 0 (τ=1.0) and ply 30 (cm 15 → 0.05). Dead-code removals in
  `selection.rs`/`backup.rs` remove only writes nothing read. Tactics ordering with `pol` ≡ 0 gives the identical
  key; TT `Bound` was never read; `get_policy` has no caller; the shared Gumbel drive equals both removed copies.
  Goldens and parity unchanged and green → numerics bit-identical.

## §4 Checked and holds

- R368(i): no config, schema, resolver or checkpoint/stamp code changed; search, self-play and trainer numerics
  identical by the diffs above; goldens and parity tests untouched and green; the HEXG format unchanged.
- R368(b): no PZ-1 implementing symbol or pinning test edited; PZ-2 goldens touched only by helper hoists that keep
  every assertion; graph lib.rs's "frozen" header restated to the mechanism that freezes it (graph_parity.rs), as
  (b) directs; search_kind_conformance.rs unchanged.
- R367(a): no runN symbol and no shim/compat token in added lines; new ruling numbers in comments: finding 7 only;
  the DISSOLVED_PATHS string is data with grounds, like its sibling rows.
- R8: `r8_header_gate.py` → 171 over the cap, all justified, none stating a count, 0 stale.
- rustfmt on touched files only (explains the reflow noise in core `state/core.rs` and `board/mod.rs`), per CLAUDE.md.
- Commits single-line, no trailers.

## §5 Disposition (dispatcher)

- 1 FIXED (status line v4; the segment-header convention cites `mantis.monitor.sink.EVENT_CONTRACT` without a
  literal). 2 FIXED (PROGRESS W2 carries the search group's rows and re-lane grounds). 3 GROUND RECORDED in PROGRESS
  and disclosed as a deviation from the letter of R368(h): kept, not reverted — a dedup of the scheduling skeleton,
  behaviour-exact, both parallel-parity tests green; no LAW-09 bench taken.
- 4 CARDED (CARD-CLUSTER-THRESHOLD-RESIDUE) and its doc corrected; 5, 7, 12 FIXED; 9 recorded in PROGRESS;
  6 → W3 (grid arm); 8, 10 recorded as on-contact residue in PROGRESS; 11 stands as a note.
- Loops: one. No MUST-FIX.
