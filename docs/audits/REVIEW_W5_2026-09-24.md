# REVIEW-W5 — SLIM-FIX wave 5 (9839becd..80ba1f6e)

Filed verbatim from the fresh read-only review agent (R367(b)); §5 is the dispatcher's disposition.

## §1 Scope and method

**Range:** 99 commits and 305 files (+3 073/−138 124; the bulk is fixture bytes and goldens). No commit in the range has a body or a trailer: `git log --format='%h|%b' 9839becd..80ba1f6e | grep -v '^[0-9a-f]*|$'` prints nothing.

**Read first:** CLAUDE.md; RULINGS `### R368` (all clauses) and `### R367`; PROGRESS `## W5`; WAVE_BRIEF and W5_ADDENDUM; 00_MAP §3 (PZ-1..PZ-6); the scout rows for S-A-TESTS-1-14 / 5-17 / 6-06 / 7-10 / 7-11; REVIEW-W4, whose format this report follows.

**Workspace:**
- Scratch worktree `.wt/review-w5` at 80ba1f6e with its own venv (`uv sync`, run unsandboxed), removed at the end.
- The main checkout was tracked-clean at the start and at the end; HEAD is still 80ba1f6e.
- Side effect to disclose: `git worktree prune` also dropped three stale admin entries, `w5-drain`, `w5-misc` and `w5-fakes`. Their directories were already gone.

**Collection census (A, F):**
- I checked out every commit in the range, plus the base, and ran `pytest --collect-only -q -m ''` (tip-built `.so`; the extension's Python surface does not change in the range).
- I saved node ids per commit in `$TMPDIR/rv/ids/*.txt` and compared the count with `test_count_floor.txt` at each commit.
- Base to tip: 189 node ids removed, 72 added. By directory: config 51, selfplay 37, train 33, encoding 14, model 12, tools 9, eval 8, bridge 6, monitor 5, root run files 11, arena 1, diagnostics 1.

**Deletion claims (A):**
- 150+ deleted tests and removed asserts were checked against their named survivors at 80ba1f6e, line by line on the asserted predicate.
- I did the drain, instrumentation and signal-posture checks myself. Three read-only helper agents did the selfplay/bridge/arena, train/root/monitor and config/encoding/eval/model/tools slices using `git show` / `git grep 80ba1f6e` only.
- I independently re-verified every FAIL they reported before filing it here.
- Example I verified myself: the nine `test_instrumentation_arms` G-06..G-15b rows. Each vector (two_stones_far, six_in_a_row_p1, seven_collinear_p1 [6, 6/7], two_cluster_p1 [ct5→2, ct19→1], empty, stride5_chain_of_4 [4, 4], three_adjacent_r0 [1, 3]) is present with identical values in `pure_function_battery.json`, which `test_pure_function_goldens` asserts. **HOLDS.**

**Planted breaks (B, C, H)** — each planted in the scratch worktree, run, then reverted with `git checkout src`:

| plant (src) | suite | result |
|---|---|---|
| run.py `_stop_pool_if_start_attempted`: `pool.stop()` → `pass` | 78 hoisted suite files, 836 tests | RED ×4 (`test_a_pool_that_comes_up_halfway_and_then_raises_is_still_stopped`, root_lifecycle ×2, strict_composition) |
| same helper: `if start_attempted:` → `if True:` (stop an unstarted pool) | same | RED ×2 (`test_close_out_with_never_started_pool_does_not_raise`, partial_composition). The shared DrivablePoolStub's strict `stop()` is doing its job |
| step.py `counts_fn(spec.window_games + 1)` | same | RED ×2 (`test_ply_cap_gate.py`) |
| run.py `state_dict_fn=trainer.inference_state_dict` | same | RED ×2 (`test_compose_run_syncs_actor_on_cadence_without_eval`, strict drive [smoke]) |
| heartbeat_watchdog `age >= deadline * 2` (FakeClock suites) | same | RED ×3 (`test_heartbeat_watchdog.py`) |
| pool_push `tail_mass=0.0` on push | drain suites, 31 tests | RED (`test_graph_drain_push_rows`, `test_the_graph_row_carries_its_tail_mass…`) |
| pool_push drops `self_play_positions_pushed += n` | drain suites | RED (`test_counters_mirror_runner[graph]`, `[graph_5s_not_crossed]`) |
| pool_drain `game_length = plies // 2` | drain suites | RED ×3 (`test_counters_mirror_runner[*]`) |
| amp.py map `"bf16": torch.float16` | bf16 suites | nulldist, `one_amp_dtype_authority` and both guards stay GREEN (35 passed). RED `tests/model/test_amp_dtype.py::test_graph_is_bf16_unconditionally` (see §3 AQ-BF16) |
| `tests/_determinism.py` `use_deterministic_algorithms(False)` | graph_microbatch, forward_parity | GREEN, 77 passed. Pre-existing CPU blind spot; see N9 |
| supervise.py `_install_stop_handlers()` at module scope (evades the AST check) | signal posture probe | RED "import installed a handler" |
| supervise.py helper sets SIGTERM `SIG_IGN` at import | same | RED "import installed a handler" |
| supervise.py direct module-scope `signal.signal` | same | RED at the static check (:416) |
| supervise.py `main` stops installing handlers | same probe | GREEN (it never asserted main's install); 7 sibling rows in the file RED |

**Gates at the tip** (worktree, `rc` via `$?`, no pipe):
- 9 `check_import_dag`: rc 0, 171 modules.
- 10 `check_tracked_refs`: rc 0.
- 12 `preflight_mint --audit-only`: rc 0.
- 13 `contract_doc_gate`: rc 0.
- 14 `lint_gate.sh` (ruff, pyright 248 files 0 errors, comment ratchet at floor on all seven measures): GREEN.
- 15 `r8_header_gate`: rc 0 (150 justified, 0 stale).
- 16 `encoding_io_gate`: rc 0 (734 files).
- 3c `test_count_gate.sh`: `collected=4918 … tree_floor=4918`, 73 deselected, all declared.
- `ruff check .`: clean.

**Full default tier (J), once:** `4860 passed, 15 skipped, 43 deselected` in 340 s, 0 failed. Seven of the skips are host-only: the strix vendor is not fetched in a fresh worktree (`-rs` → "external/hexo-strix is not fetched").

**Rust:**
- `cargo check -p mantis-core --release --locked` builds without `from_stones`.
- `cargo test -p mantis-core --lib`: 58 passed.
- `cargo test -p mantis-selfplay --lib atomic`: 4 passed; the `$TMPDIR/mantis-atomic-*` count is 24 before and 24 after, so there is no new leak.

## §2 Findings

| # | sev | commit | file:symbol | finding | evidence (command → result) | suggested fix |
|---|---|---|---|---|---|---|
| 1 | MUST-FIX | 5e485132 | tests/selfplay/test_buffer_facade.py::test_graph_arm_missing_getter_propagates; tests/selfplay/test_pool_surface.py::test_graph_pool_buffer_composition_is_nan_and_that_is_parity | **False subsumption ×2.** The subject says "the battery, the goldens and the surviving twins already pin every assertion". Nothing at the tip asserts that `ReplayFacade.outcome_in_range_count` lets the AttributeError out, or that the graph buffer lacks the getter. Nothing asserts the NaN `draw_target_fraction` / `corpus_fraction == 1.0` / `buffer_size == 0` arm of `pool_push.buffer_composition`. The surviving `test_buffer_composition_inherits_the_under_count` checks only `n_games_observed` and the four terminal fractions. The file docstring (test_buffer_facade.py:6–8) still claims "the missing attribute must PROPAGATE". | `git grep -n outcome_in_range_count 80ba1f6e -- tests` → only test_buffer_facade.py:7 (docstring). `git grep -n "draw_target_fraction\|corpus_fraction" 80ba1f6e -- tests` → no assertion. `git show 5e485132 -- tests/selfplay/test_buffer_facade.py tests/selfplay/test_pool_surface.py` → the two deleted bodies | Restore both rows. Or, if `buffer_composition` is judged caller-less (`git grep "buffer_composition\b" 80ba1f6e -- src` → only pool.py, pool_push.py and the `WorkerPoolLike` protocol member), card the src deletion and delete the tests with it — never the tests alone |
| 2 | MUST-FIX | 4a724be3 | tests/test_run_eval_enabled_authority.py::test_every_minted_config_declares_the_key_explicitly (×6) | **Only value pin deleted with no owner.** The scout row S-A-TESTS-7-11 recorded "witness: NONE for the value … Design choice: keep a census-based one or drop". The leg dropped it with no ruling. At base it was the only test asserting `eval_enabled is True` on committed configs. `armed_aborts.py`'s terminal_eval_broken row discloses as its RESIDUAL that "the other half [eval_enabled] is held by tests/config/test_minted_config_remint.py::…". That file does not exist at base or tip (pre-existing stale cite), so the deleted row was the real holder. A production config minted with `eval_enabled: false` now passes every test, and gate 12 stays green (the row arms `train.terminal_eval_enabled` only). | `git grep -nE "eval_enabled\"?\]? is True" 80ba1f6e -- tests` → only test_actor_sync_real_config.py:52 (synthetic payload) and test_preflight_mint_process.py:2925 (`terminal_eval_enabled`). `sed -n 712,720p src/mantis/config/armed_aborts.py` → the residual cite. `git ls-tree 9839becd tests/config/test_minted_config_remint.py` → empty | Re-add one census-parametrized row over `production_configs(REPO_ROOT)` asserting `eval_enabled is True`. Route the stale armed_aborts citation (a PZ-6 MANIFEST row's text) to the operator for a ruling-named re-point |
| 3 | MUST-FIX | 876f8353 | tests/arena/test_book_geometry_pairing.py::_config_files; tests/train/test_bc_graph_reroute.py::test_no_shipped_config_names_the_pretrain_route; tests/train/test_ema_lever_is_reachable.py::_CONFIGS; tests/train/test_arch_stamp_authority.py::_graph_config_dump | **Witness narrowed.** Defect 28 was blindness to subdirectories. The repair swapped the flat glob (6 configs) for `production_configs` (4 configs), which also drops the two EXEMPT configs. Those are the only `gnn_axis_v1` configs, and both name `book_v1_s20260625_p4`, so the LAW-15 book/encoding pairing sweep no longer sees the v1×book pair. The smoke profile is the one the box's preflight burst runs. The PZ-6-named absence-oracle file's pretrain-route sweep and the EMA sweep lost the same two files (−4 node ids). arch_stamp's base config silently moved from dev_example to run10, with two keys patched to None. `census.discovered_config_paths` (gate 7's own enumeration) fixes defect 28 with no narrowing. Note: W5_ADDENDUM leg 7 literally ordered `production_configs`; that order conflicts with R368(c). | `diff <(grep :: ids/9d520676.txt\|sort) <(grep :: ids/876f8353.txt\|sort)` → −4 ids `test_ema_lever_is_reachable…[dev_example.yaml]` / `[smoke_preflight_armed.yaml]` ×2. `grep -n "book\|encoding:" configs/dev_example.yaml configs/smoke_preflight_armed.yaml` → gnn_axis_v1 plus book_v1 in both. `sed -n 47,53p src/mantis/config/census.py` → `discovered_config_paths` | Re-point the four sweeps (at least book pairing and the pretrain-route absence) to `discovered_config_paths(root)`. Keep `production_configs` only where the law is production-only. Floor +4 in the same commit |
| 4 | SHOULD-FIX | 876f8353 | tests/train/test_ema_lever_is_reachable.py::test_there_are_configs_to_check | **Latent W6 red created by this wave.** `assert len(_CONFIGS) >= 3` is now over the production census, which becomes {run10} when W6 deletes run6–8. The same floor over the old flat glob would have kept 3 (run10, dev_example, smoke). This is a hard count a prune must edit — the class R367(a) forbids. | W6 simulated in the scratch worktree: `rm configs/run6.yaml configs/run7.yaml configs/run8.yaml`, then pytest over the six W5-touched census suites → `FAILED …test_ema_lever_is_reachable.py::test_there_are_configs_to_check`. The other failure, `test_book_geometry_pairing.py::test_run6_is_not_one_of_them`, is a pre-existing run-named row, W6's | Fixed by #3's re-point, or replace the count with the census's own non-empty guarantee (`production_configs` raises on empty) |
| 5 | SHOULD-FIX | 3a254e67 | docs/contracts/run_config_schema.md:175 | **Stale contract row created by the AQ-P2 retirement.** It still cites `tests/config/test_regime_parity.py` for "the radius knob's ABSENCE from every production config". That assertion lived only in regime_parity_p2's O12, which was deleted. test_regime_parity.py's own docstring (:6–10) says O12 is RETIRED and points to test_radius_removed.py. Gate 13 checks keys and symbols, not row-to-test truth, so it stays green. | `sed -n 175p docs/contracts/run_config_schema.md`; `grep -n radius tests/config/test_regime_parity.py` → docstring only | Re-point the clause to tests/config/test_radius_removed.py or trim it |
| 6 | SHOULD-FIX | 5e485132, 4dd5f752 | tests/selfplay/test_collate_geometry_is_declared.py::test_the_graph_row_roster_is_not_empty_and_includes_the_r8_row; docstrings of test_buffer_facade.py:6–8, test_check17_vectorized_parity.py:12–13, test_gil_release_on_native_calls.py (overlap row), tests/monitor/test_sink.py:10 | **Residue the deletions left behind.** The vacuity control guards a parametrized row that no longer exists (`git grep -n GRAPH_ROWS 80ba1f6e -- tests` → the definition plus this control only). Four docstrings still claim coverage their deleted rows carried: facade propagation, "ZERO legal nodes", "the refusal guard above" (now inside the same test), and the sink write-failure row (now in test_persist_fatal). | grep commands as stated → the lines named | Drop or re-aim the control; reword the four docstrings (comment floors only fall) |
| 7 | SHOULD-FIX | 3b752fed | tests/tools/test_strix_ruler_r6_cell.py (`from test_strix_net_only_cell import _load`) | **Test-module-to-test-module import of a private one-line wrapper.** It is not a collection hazard: r6 alone → 5 passed; both files in either order → 12 passed; `--import-mode=importlib` breaks every bare harness import equally (`ModuleNotFoundError: _toolpath`). It is a convention breach: helpers live in `_`-prefixed harness modules, and `_load` is `load_module_by_path(name, _REPO / rel)` over the already-shared `_toolpath`. The dedupe traded a one-liner for coupling two test files. The tree carries 4 older and 2 other W5 test-to-test imports (fc10d8a4 RUN5/_revalidated, d2a27ae3 record_runner_config_factory); those move substantive shared helpers. | the pytest runs above; `grep -rn "^from test_\|^import test_" tests` → 8 sites | `from _toolpath import load_module_by_path` in the r6 cell (or move `_load` into `_toolpath`) |
| 8 | SHOULD-FIX | 7360de68 | tests/monitor/conftest.py::fake_clock; tests/train/conftest.py::fake_clock | **The hoist kept a needless duplicate.** "The fake monotonic clock is ONE class" holds, but two directory conftests still define body-identical `fake_clock` fixtures (`return FakeClock()`). | `grep -n -A2 "def fake_clock" tests/*/conftest.py` → the two copies | One `fake_clock` fixture in tests/conftest.py |
| 9 | NOTE | e4018c6a / f4c23467 | tools/ci_gates/test_count_floor.txt | **A floor move did not ride its causing commit.** e4018c6a (census parametrization) raised collected 4911→4923 with no floor move; f4c23467, a fixture-only deletion, carried the +12. The gate is unaffected because the floor lagged upward. It is a record defect only; every other move matches its commit exactly. | `$TMPDIR/rv/counts.txt` → `e4018c6a 4923 floor=4911`, `f4c23467 4923 floor=4923` | Record in the W5 ledger; no history rewrite |
| 10 | NOTE | 15912645 | tests/fixtures/selfplay/drain/drain_goldens.json | **Re-capture script not committed.** Judged ACCEPTABLE under R368(e)/LAW-07: no generator existed to keep, since the dense goldens had none. "Generators of live goldens stay" forbids deleting one; it does not mandate authoring one. The golden's `_method` records the drive, and the equality oracle is verified (§3 drain). | `git grep -n drain_goldens -- tools` → none; `_method` string at the tip | Record the capture procedure in PROGRESS; optional card for a committed generator |
| 11 | NOTE | 76fee167, 5d35f887, 3a254e67, 7d0f6d36, 4dd5f752, 5e485132 | various | **Small narrowings, each HOLD with a residue:**<br>• `keep_all` DiskGuard tombstone gone. o16 proves `extra="forbid"` but not that key's absence.<br>• Sorted-tuple equality became set equality (duplicates impossible by construction).<br>• regime_p2 O12's `not hasattr` (ClassVar/property) became model_fields-only.<br>• 7d0f6d36 dropped two doc-text oracles that were real, not tautological. Policy.<br>• 4dd5f752's `hasattr(events,…)` tombstones and the "config OK" string absence have no owner (shim-free posture, low).<br>• `_engine.__doc__` is now asserted only by opt-in gate 1 (`--with-fresh-sync`).<br>• RegimeKey's positive `==` leg is not re-asserted; production compares `.canonical()` strings. | helper-agent tables, spot-checked: `git grep -n "keep_all" 80ba1f6e -- tests` → none; `grep -n "__doc__" tools/ci_gates/gate_01_fresh_sync.sh` → :28 | Record; optionally list `disk_guard.keep_all` under run_config_schema.md "Deliberately absent" (gate 13 then owns it) |
| 12 | NOTE | 55c82def, 2b891a08, 5e485132, d7b86b3b | subjects | **Subject-line inaccuracies (the claims hold):**<br>• 55c82def says "eight rows" but deletes 9 (collected −9).<br>• 2b891a08 names train_import_dag, which covers train/** only; the monitor half is owned by `test_monitor_census.py::test_monitor_mantis_imports_are_within_the_allowed_set`.<br>• 5e485132 cites R368(e) for test deletions; (e) is the decided-deletions list and the authority is (d) / the ledger.<br>• d7b86b3b's "pipeline uses the terminal cap for the terminal round" was untested before and after (the live-consumer row is a source-substring check). | `$TMPDIR/rv/counts.txt` (4958→4949); `git grep -n "def test_monitor_mantis_imports" 80ba1f6e -- tests` | Record in the ledger |
| 13 | NOTE | c1216770 | tests/_determinism.py | **AQ-BF16 as merged is behaviour-identical, but the determinism context has no CPU witness.** Disabling it reds nothing on CPU; its only consumers that matter are the CUDA legs, skipped here. The production dtype pin reds on a planted fp16 swap, but through `test_amp_dtype.py` (unedited), not the nulldist file. The nulldist file's fp16 injection is its own mutation arm and is green at the tip. Pre-existing, not narrowed by the merge. | the plant table in §1 | Record; a CPU leak/restore row for the context is optional |
| 14 | NOTE | 96adf76c, 2900da45 | tests/monitor/test_arm_exec_trampoline.py, tests/test_run_pdeathsig.py | The suites now measure parentage with the production `signals._ppid_of`, the code whose neighbour they test. It is byte-equivalent to the deleted copy, including the None-pid path (opening `/proc/None/status` raises OSError, which returns None). The circularity is low-risk (a `/proc` read) and was ordered by L-DUP-10. | `grep -n -A10 "def _ppid_of" src/mantis/train/lifecycle/signals.py` vs `git show 2900da45` | Record |
| 15 | NOTE | 3b752fed (KEEP ground) | tests/diagnostics/test_mirror_receipts.py::_receipt_everything ⇄ tests/tools/test_preflight_start_halts.py::_receipt_run_dir | The KEEP ground "differs by a default arg" is weak. The bodies are identical except the `run_id="synth"` default and a `D.` prefix on `complete_bundles`; a default does not block sharing. The `_model_samples` C ground holds: a ruling granted a frozen edit naming test_preflight_mint_process.py (RULINGS.md:4209), which is a freeze under R368(b). | `sed -n 18,30p` / `sed -n 35,45p` of the two files | Re-lane the receipt pair to a later hoist or record it as KEEP-by-choice |
| 16 | NOTE | cdbc8000 | tests/monitor/test_supervisor_signal_posture.py::_reset_stop_dispositions | Its comment is two lines (R368(g): one). `preexec_fn` carries CPython's fork-with-threads caveat, but the run is clean under `-W error` on 3.13. The alternative, resetting to SIG_DFL inside the probe script before the import, is equivalent and thread-safe. | `-W error` run → 1 passed | Optional; see §3 H for the substantive verdict |
| 17 | NOTE | 25b21333 | tests/train/_graph_drive.py::GRAPH_FULL_CONFIG | One module-level mutable dict is now shared by 27 call sites (it was per-file). No mutation was found in tests or in `src/mantis/train/coordinator`. | `git grep -n GRAPH_FULL_CONFIG 80ba1f6e -- tests` → pass-through only | Optional `MappingProxyType` |

## §3 Per-row verdicts

**Drain re-base (15912645, a670e1dc, 85cb2e9f, 0f7787f2, fd6b28fc): HELD.**
- Oracle equality: I compared the old `dense_5s_crossed` (base) with the new `graph` variant at the tip. clock_sequence, clock_calls_consumed, sleeps, event_order, events (incl. the corrected event 3/5 nulls), game_id_format_ok, instrumentation_after and recorder_calls are EQUAL. counters_after differs only on push-count fields: positions 4→3 (3 graph rows vs 4 dense rows), plus the new `graph_rows_pushed` / `next_game_id_calls`.
- `graph_5s_not_crossed` and `graph_zero_rows` match their dense twins on every arm-independent field except `events`. There the new capture carries the corrected nulls; the old not_crossed and zero_rows variants were never re-captured after the fabrication fix and were unasserted.
- `log_calls` (dropped) had no reader at base (`git grep log_calls 9839becd -- tests` → the fixture only).
- My three new plants RED (§1).
- The deleted tests were dense-only. `is_graph_representation` returns True or raises, so the `else` arm was structurally unreachable (R368(i) "unreachable by structure").
- `git grep -nE "push_dense|_is_graph|PoolDims|_feat_len|_chain_len|_pol_len|recent_buffer|_board_size|_trunk_size" 80ba1f6e -- src crates tools` → zero readers. The only hits are the unrelated `graph_trunk_size` in inference.rs and `registry_board_sizes` in sym.rs.
- The `first_record_drained.representation` payload is still `"graph"`.
- `build_runner_config` now returns the config alone; its one caller (pool.py) is updated.
- Manifest re-pins carry dated grant comments. The wpa_positions row names sha `18ad4a26…`, size 966 939 B and the commits it lived in (fe9d3973..69e15329).
- The line-endings floor moved 40→41 in b27a49d7 (tightened to the count), then 41→39 in a670e1dc (two npz rows). The test is green.

**AQ-CARD-FAKES (905cf259, e5436147, 209451ee, a13d0cc6, 7360de68): HELD** (except #8).
- `DrivablePoolStub` keeps the strictest `stop()` (plant F1b reds two rows) and both game semantics.
- The `cluster_*` removal is compensated by `_ClusterCarryingStats`, so cluster-absence is still the builder's doing.
- `with_deltas` equals the old `dataclasses.replace(_PRODUCTION_BUILDER(**kwargs), …)` for keyword calls.
- `SpyEventSink` is instance-state only, and its strict `named()` subscript is stricter than the old `.get`.
- There is no shared mutable module state in `_drivable` / `_spy` / `_proc_harness` (class attributes are immutable scalars); `fake_clock` fixtures are function-scoped.
- Five src plants red the hoisted suites (§1).

**AQ-LIFECYCLE (2900da45, 96adf76c): HELD.** `alive` ×3 and `_events` ×2 are byte-equivalent; the only delta is None handling, which is added, not removed. Spawn/reap stay local as K20 carves.

**AQ-BF16 (c1216770, 4bd890d6): HELD.**
- The two `deterministic_algorithms` bodies and the two `_graph_step` bodies were identical (diff read).
- `bp.deterministic_algorithms` stays importable for the PZ nulldist file.
- `git diff 9839becd 80ba1f6e -- tests/model/test_bf16_parity_nulldist.py tests/model/conformance` → empty.
- The PZ-1 guard files (test_finite_gradient_guard / test_nonfinite_guard) changed only the helper re-point; there is no assertion edit. See #13.

**AQ-P2 (e6974f43, 3a254e67): HELD** (except #5). Helper-agent AST check at e6974f43: 160 = 160 keys, empty symmetric difference. Every p2 row has a same-body survivor in test_every_key_has_consumer.py / test_regime_parity.py / the census-parametrized test_resolved_config_emit.py. The witness was retired with its sole subject.

**AQ-POOL-HPARAMS (d2a27ae3): HELD.** The survivor `test_killed_and_relocated_fields_never_reach_the_runner[full_config, minimal_config]` plus J-03 `test_j03_no_killed_tokens` cover the killed knobs. R38 was annotated in R368.

**Train slice (17593498..d7b86b3b, 8ed994ac, 2b891a08, d1d6357d): HELD.**
- All rows in these commits have named same-or-stronger owners at the tip (helper table).
- 363bb8bd's survivor is census-parametrized (`_PRODUCTION_CAPPED` over `production_configs`), and `max_edges` / `max_nodes` are `Field(ge=1)` (schema/train.py:120–121).
- 8ed994ac: every pdeathsig and inference-seam row has an owner. The three tier_declaration rows removed are exactly the deleted skipif tests.

**Root / monitor (12bbb9c5, 4a724be3, 4dd5f752, f4c23467, 0a15ad81, 011e782f, 5459e474, 344c4413): HELD** except #2 (4a724be3's value pin) and #11's tombstones. The fixture deletions are behaviour-identical to the root autouse `_restore_signal_dispositions`.

**Config / encoding / eval / model / tools (76fee167 … 108279cc): HELD** except the #11 residues and #5.
- 76fee167: o16's census reaches all 8 StrictModels (`test_o16_census_reaches_every_schema_block`).
- 108279cc: HexTacToeNet is in conformance GRAVES (unedited).
- 85b3fd12's survivor calls the guard itself (stronger).

**Selfplay / bridge / arena (5e485132, ff3554b0): HELD** except #1 and #6. The count matches: 30 functions plus 1 param = 31.

**Residue:**
- 4300b691 and e4018c6a: census-parametrized, no presence pins; W6 shrinks them to run10 with no red (simulated).
- 41e4271f: TempDir guard, no leak.
- 2f002d9d: `from_stones` gate `any(test, feature="test-fixtures")`; release is unaffected (cargo check `--release`). It differs from the old `fwm_board` only in `has_stones` for an empty slice, and no `fwm_board` caller passes one.
- d02e1e83: owned TemporaryDirectory.
- e5d236dc: the 20 cap literals now read dev_example's resolved section; values are identical at the tip (57149441 / 1785921). These are constructor inputs in PZ-1 files, not assertions.

**H — KNOWN-RED (cdbc8000): NOT NARROWED.**
- Old file under an inherited SIG_IGN (`trap '' TERM INT`) → **1 failed**. New file → **1 passed**. The flake mechanism is reproduced and cured.
- Import-time install (direct, indirect, or SIG_IGN via helper) still reds.
- "Installed by main" was never this row's assertion (plant H2 leaves it green at base and tip alike); 7 siblings own it.

**J spot-checks: HOLD.**
- TESTS-5-17: `test_j01_census_covers_every_frozen_row` is kept; the other five members were deleted in 5e485132 with owners.
- TESTS-1-14 REFUTED: `test_a_PARTIAL_stopping_rule_is_refused` now drives `pretrain(...)` under `pytest.raises(SystemExit)` (6c28c1c9).
- TESTS-6-06 REFUTED: `_ENV_KEYS == {"strix": …}`; the sealbot arm is gone and the strix row has no subsumer.
- L-DUP-28: see #15.
- The 13 `train_step_from_tensors` stubs: `DrivableTrainerStub` retains the method; the addendum's REPROBE ground stands (the protocol member is gone from src; `git grep train_step_from_tensors 80ba1f6e -- src` → none).

## §4 Checked and holds

- **R368(i) run10 safety:**
  - `git diff 9839becd..80ba1f6e -- configs/ tools/config_templates tools/mint_config.py | wc -c` → 0.
  - `run10_resolved.py configs/run10.yaml | cmp - run10_resolved_base.json` → rc 0, silent.
  - The src/crates diff is 7 files, +46/−124: the unreachable dense drain arm and its attributes, docstrings, a test-only cfg widening, and test-module Rust.
  - No stamp, checkpoint-format or numerics change.
- **R368(b) protection:**
  - tools/ci_gates touched only in `tier_declaration.txt` (3 row removals for 8ed994ac's deleted skipif tests), `test_count_floor.txt` and `comment_length_floor.txt`.
  - conformance/** and test_bf16_parity_nulldist.py are unedited.
  - PZ-1 pinning files were touched only by import / helper / cap-literal re-points and the census re-point (#3); there is no assertion edit in them.
  - The protected selfplay/bridge files are untouched (`git log 9839becd..80ba1f6e -- tests/selfplay/test_edge_geometry_checker_thread.py tests/selfplay/test_graph_collate_adv.py tests/bridge/test_runner_derived_means.py tests/selfplay/test_drawrate_pooled_statistic.py` → empty).
- **R367(a):**
  - Added lines grepped for `run[0-9]` → 3 hits, none a new run-named symbol or pin: the `RUN5` import is a dedupe of a pre-existing symbol, the `run3` cite is a pre-existing test name in a docstring, and the `run7.yaml` line was moved by aa408765.
  - The census is the discovery authority at every re-pointed site. Pre-existing run6/run7 presence pins remain for W6: test_run_strict_composition.py:431, test_eval_posture_inert.py:179, test_book_geometry_pairing.py::test_run6_is_not_one_of_them, test_strength_frontier.py, test_armed_abort_cadence RUN5.
  - No compatibility shim was added.
- **Floors:** collected 4918 = floor at the tip. Every per-commit collected count equals that commit's floor except e4018c6a (#9). The comment measures only fell: 3169→3124, 12239→11910, 1378→1283, 1210→1164, the rest unchanged; each move rides its commit.
- **LAW-07:** no gate or monitor input lost its producer test. The drain goldens have a live equality oracle with red plants. The armed-abort residual in #2 is the one disclosure whose holder is gone.
- **Commits:** 99 one-line subjects, empty bodies, no trailers.
- **Hygiene:** the scratch worktree was removed; `$TMPDIR/rv` holds the per-commit id lists and golden dumps; no pushes, no edits to the main checkout.

## §5 Disposition (dispatcher)

Fix loop 1 (80ba1f6e..3a94133d, one opus implementer) landed every MUST-FIX and SHOULD-FIX. The same
reviewer then verified closure with its own plants in a fresh worktree at 3a94133d: all eight CLOSED, no new
finding. Node ids were exactly +10/−1, collected 4927 = floor, and the comment floors only fell.
- **#1 FIXED** 4e18a0c8: both deleted rows are restored with their assertions. Planted swallow/0.0/0.9 all RED.
- **#2 FIXED** c79b4e79: a census-parametrized `eval_enabled` value pin (declared, raw True, loaded True).
  Planted false/absent RED. The armed_aborts `terminal_eval_broken` RESIDUAL still cites the non-existent
  tests/config/test_minted_config_remint.py. That text is a PZ-6 manifest row, so it is ROUTED TO THE
  OPERATOR for a ruling-named re-point to test_run_eval_enabled_authority.py's census row.
- **#3 + #4 FIXED** 15c2b6e8:
  - The book pairing, pretrain-route and EMA sweeps walk `discovered_config_paths`; arch_stamp is back on
    dev_example.
  - The hard count became `production_configs ⊆ _CONFIGS`.
  - W6 deletion simulated: only the pre-existing run-named `test_run6_is_not_one_of_them` reds (W6's row).
  - The addendum's leg-7 wording ("production_configs") conflicted with R368(c). The W6 addendum states the
    rule: a sweep over every committed config reads `discovered_config_paths`; only a production-only law
    reads the census.
- **#5 FIXED** 6d02eaa5 (row trimmed; test_radius_removed's own row owns it; no version bump, matching
  3a254e67's precedent).
- **#6 FIXED** 06cd974e.
- **#7 FIXED** 7a5173b0.
- **#8 FIXED** 3a94133d.
- **#9–#17 NOTES**, recorded:
  - #9 is in the W5 ledger (the e4018c6a floor lag); no history rewrite.
  - #10: the drain capture procedure is in PROGRESS; a committed generator is carded via the W6 addendum's
    residue list.
  - #11/#12 are recorded as is.
  - #13: the determinism context's missing CPU witness is carded.
  - #14 recorded.
  - #15: the receipt pair re-laned to KEEP-by-choice, open to a later hoist.
  - #16/#17 optional, not taken.
