One process per registered bot (`tools/ladder_bot.py`): hold the stream, answer every move request through the backend — the book prefix first (R363(c): `book_v1_s20260625_p4` paired, opening index = match index, both bots counting the same games; `--match-offset` names where a resumed series left off) — write a receipt per game under `receipts/<net_hash8>/`; `--preset play` is the 64-sim row for playing, labelled on the receipt and never a unit reading; `BUILD.md` is the CPU recipe and `ladder-bot@.service` the unit.
